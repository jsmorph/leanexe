# Proof Journal

The unchanged candidate passed `PROOF_IMPORT_CHECK.js`.  The required Lean build then failed at `Behavior.lean:18:116` with the starter postcondition still open.  The context contains the `RuntimeReady` decomposition, array representation, length read and bound, input address, heap top, empty free-list, and allocation-counter facts; the residual goal is the complete `TerminatesWith` execution theorem for function 0.

Knowledge retrieval started with `KNOWLEDGE/forest.json`.  It selects the single versioned package `leanexe-core` at `KNOWLEDGE/packages/leanexe-core`.  The first catalog searches will use the residual `TerminatesWith` goal together with the artifact's array search, early-exit loop, erase, allocation, copy, and shift features.

I searched the `leanexe-core` arrays, loops, allocation, proof-construction, compiler-motifs, and memory indexes with `rg -n -i 'leanexe\.array\.length-dispatch\.v1|wp_fixed_array_length_le_dispatch_from|TerminatesWith|findIdx|eraseIdx|early-exit|copy|shift|search|constantCapacityRegion'`.  I narrowed the results with `rg -n '"id":"fixed-array-(length-dispatch|search[^\"]*|allocation|capacity|result)"'` over the same indexes.  The current residual and the checked `leanexe.array.length-dispatch.v1` annotation select `leanexe-core/fixed-array-length-dispatch`; I inspected its `entry.json` and `README.md`, while the allocation, capacity, and result entries remain deferred until a branch goal requires them.

The `fixed-array-length-dispatch` tactic record matches the planned body-level goal, requires the existing represented-array hypothesis, and names the checked annotation kind present in `PROGRAM_ANNOTATIONS.json`.  `PROOF_RECIPES.json` gives the exact invocation `wp_fixed_array_length_le_dispatch_from hArray at 8, 8`, with `leProgram_spec` as the fallback declaration.  I will attempt that command before reducing the dispatch instructions, and progress means replacing the whole-function `TerminatesWith` goal with separate bounded and oversized branch obligations.

The import check passed after adding `Wasm.TerminatesWith.of_wp_entry_for` and the exact recipe tactic.  The Lean build failed with five elementary dispatch premises and two semantic branch goals: the bounded branch begins with the early-exit search loop, while the oversized branch begins with constant capacity, allocation, and empty-result stores.  The tactic therefore matched its recorded goal shape and removed the public execution and length-dispatch boundary, so `leanexe-core/fixed-array-length-dispatch` is used.

For the new frame and branch residuals, I searched `PROOF_RECIPES.json` and the `leanexe-core` proof-construction, compiler-motifs, arrays, and loops indexes with `rg -n -i 'Function\.toLocals|toLocals|entry frame|params|locals length|local-frame|FixedArraySearch|search chain|search tree|early-exit|copy-and-shift|copy shift|erase'`.  The recipe and index expose frame projections only for named capacity, allocation, fold, and generic `withValues` frames; none applies to the initial `func0Def.toLocals` parameters, value stack, or local-list length.  Those five closed frame and numeric premises therefore require definitional reduction, while the search and erase branch has no indexed theorem or tactic with the required annotation or goal shape.

The oversized branch matches the checked constant-capacity and shifted-allocator features, so I inspected `leanexe-core/fixed-array-capacity`, `leanexe-core/fixed-array-allocation`, and `leanexe-core/fixed-array-result` through each canonical `entry.json` and `README.md`.  The first two entries provide a complete constant-capacity-plus-allocator boundary, and the result entry provides `emptyStore_at` for the final zero-length array.  The result entry's `uint64_array_singleton` and `uint64_array_pair` tactics do not match the empty-array goal, so the applicable fallback is the named `emptyStore_at` declaration rather than either structured tactic.

I inspected the allowed `FixedArraySearch`, `FixedArraySearchChain`, and `FixedArraySearchTree` ProofKit sources because their names matched the bounded search residual.  Their theorems implement exact-size key lookup that returns a two-element status pair, with unrolled equality nodes and a 24-local frame.  The artifact performs a dynamic first-zero search followed by in-place copy and shift into a variable-size result, so these declarations do not match its program, result, or frame and are rejected for this proof.

The import check passed after assigning the five elementary dispatch premises to definitional proofs.  The Lean build discharged four of them and rejected `decide` for `hInputLocal` because its expected type still contains `inputPtr` through `func0Def.toLocals`.  The next edit will unfold `func0Def` with `simp` for that premise; the only remaining semantic goals should then be the bounded and oversized branches.

The next import check passed, but the Lean build showed that `simp [func0Def]` leaves `Wasm.Function.toLocals` folded.  A workspace search `rg -n 'toLocals|numLocals' PROOF_KIT_SOURCE LeanExeGen -g '*.lean'` found the exact reduction declarations `Wasm.Function.toLocals`, `Wasm.Function.numParams`, and `Wasm.Function.numLocals` in the checked ProofKit simplification sets.  I will use those named declarations to close the local-bound premise instead of expanding unrelated frame operations.

The import check passed, and the Lean build discharged the final local-bound premise after adding `Wasm.Function.toLocals` and `Wasm.Function.numParams` to its simplifier.  The diagnostic now contains only `hValid` and `hInvalid`, confirming that the dispatch boundary and entry-frame obligations are complete.  The linter reports `Wasm.Function.numLocals` as unused, so the next candidate edit will remove that argument while adding the first branch theorem.

I inspected `leanexe-core/fixed-array-filter-lt` because its indexed features include bounded input, allocation, a dynamic result length, conditional stores, and a loop.  Its README restricts `wrapperProgram_spec` to a whole-function `leanexe.array.filter-lt.v1` annotation, while this artifact has only `leanexe.array.length-dispatch.v1` and different search-and-shift instructions.  The entry is rejected as a theorem application, although its checked ProofKit source confirms the loop, allocator, framing, and result-proof boundaries suitable for the new local lemmas.

The import check accepted the new `FixedArrayResult` import and the named oversized-program decomposition.  The Lean build rejected a combined `change`, so at least one of the program, entry frame, or public-postcondition presentations is not definitionally equal to the residual target.  I will separate those three conversions; the first successful boundaries will identify whether the mismatch is list association, initial-frame construction, or the target's unreduced `take` and `drop` expression.

The separated conversion check passed for both `entryFrame` and `oversizedProgram`; only the raw public postcondition differs from `FixedArrayPairResult.publicPost` by reduction of `func0Def`, `List.take`, `List.drop`, and append.  I will retain the raw outer postcondition while applying `constantCapacityRegion_spec_withTail` at offset 3 with six trailing locals.  Its continuation will use `allocFrame_get_root`, `lengthStore_spec`, `emptyStore_at`, and `finishProgram_spec`; progress means closing `hInvalid` without reducing allocator instructions.

The import check passed, and the Lean build accepted the complete capacity-plus-allocator theorem application.  The remaining oversized-branch diagnostics are presentation gaps: two base-frame premises need the `capacityFrame_params` and `capacityFrame_locals_length` projections, the generated word-address theorem includes a reducible zero offset, and the final bounds need `BumpFacts.rootToNat`; I also named a nonexistent `Wasm.Function.numResults` declaration.  I will replace each reduction with the exact checked projection or bump fact and reduce the raw postcondition with `simp [func0Def]`.

The next import check passed, and the Lean build reduced `hInvalid` to one representation conversion.  `FixedArrayResult.emptyStore_at` proves `Project.ProofKit.UInt64Array.At`, while the raw public postcondition still names the definitionally equal `FormalSpec.UInt64ArrayAt`.  I will state that final goal as `UInt64Array.At`; all capacity, allocation, root transfer, memory bounds, and empty-array facts have otherwise closed.

The import check passed, and the Lean build now reports only `hValid`, so the oversized branch is complete.  The proof used the exact combined capacity and allocator theorem, the capacity and allocation frame projections, `BumpFacts.wordAddress_toNat` and `rootToNat`, `lengthStore_spec`, `finishProgram_spec`, and `emptyStore_at`.  No allocator instruction was reduced in the artifact theorem.

The bounded branch needs a semantic invariant for the early-exit loop and an equality connecting its sentinel index with `Array.findIdx?`.  Before defining that invariant, I will use a temporary `#print Array.findIdx?` diagnostic in the checked workspace to obtain the exact recursive definition available in this toolchain.  The next proof boundary will establish the search result once, then use separate no-match and found-index continuations without enumerating array elements.

The temporary diagnostic passed the import check and printed `Array.findIdx? p as = Array.findIdx?.loop p as 0`.  The Lean build otherwise retained only the bounded branch, as expected.  I will print the named loop declaration next, then remove the diagnostic declarations before adding the semantic search lemma.

The loop diagnostic shows an irreducible well-founded recursion: at index `j`, it returns `some j` when the predicate holds, recurs at `j + 1` otherwise, and returns `none` at the array bound.  I will check the generated equation theorem `Array.findIdx?.loop.eq_def`, which is the appropriate rewrite boundary for an irreducible well-founded definition.  A valid equation theorem will support induction on `input.size - j` without exposing the internal fixpoint.

The checked equation theorem has the required form and exposes one search step without unfolding the fixpoint.  The search invariant will carry `Array.findIdx?.loop zeroPred input index = input.findIdx? zeroPred`, the input bound, unchanged store, and an exact generated frame.  A false predicate rewrites this equality to the successor index, while a true predicate or the array bound yields the final `some index` or `none` result.

The import check accepted the search frame, invariant, measure, and removal of the temporary diagnostics.  The Lean build stopped during the bounded setup because `wp_run` retained the folded `entryFrame`, leaving local getters and setters unreduced.  I will simplify only `entryFrame`, the input pointer address, and the input length read at this boundary, then select the checked load-bound branch before applying the block and loop rules.

The import check passed, but the Lean build rejected the setup edit because it referred to `hInput`, while this theorem names the represented-array hypothesis `hArray`.  No proof term elaborated beyond those unknown identifiers.  I will use the existing `hArray.pointerAddress_eq`, `hArray.lengthRead`, and `hArray.generatedLengthBound` projections.

The corrected setup passed the import check and reduced the bounded goal to the block-wrapped search loop with the exact initialized local frame.  The next edit will apply `wp_block_cons` and `wp_loop_cons`, prove the zero-index invariant by reduction, and execute only the loop guard before splitting on `index = input.size`.  Progress means a body goal whose live data are the semantic index, last loaded word, search-loop equality, and generated guard.

The import check passed, and Lean accepted the block rule, loop rule, invariant initialization, and invariant decomposition.  The broad `simp [searchFrame, entryFrame]` at loop entry also simplified the weakest-precondition term, so the following `wp_run` found no instruction-level simplification to perform.  I will restrict that step to `simp only [searchFrame, entryFrame]`, preserving the program for `wp_run` while exposing the concrete frame.

The restricted frame reduction passed the import check, and `wp_run` reached the generated unsigned guard.  The residual contains the expected two cases: `index = input.size` exits the loop and block, while `index < input.size` requires the represented element's load bound and read equation.  I will split those cases, normalize `UInt64.ofNat` with the bounded index, and use `hArray.generatedElement` before inspecting the zero-test continuation.

The guard split and indexed read passed the import check.  Lean now exposes the exhausted branch with a nonzero `br_if` operand and the active branch with the exact logical element on the operand stack.  I will derive `findIdx? = none` or `findIdx? = some index` by rewriting `Array.findIdx?.loop.eq_def`; the nonzero active case will re-establish the invariant at `index + 1`, while the zero case will enter the removal continuation.

The import check passed, and the build confirms the search-result reasoning but found two presentation errors.  `FormalSpec.expected` and the recursion equation use `input[index]`, while the machine branch hypotheses use `input[index]!`; the indexed bound supplies `getElem!_pos` for this conversion.  The generated Boolean normalizations also remain as two explicit `iff` instructions, so I will select each with `wp_iff_cons` before applying the no-match result or reconstructing the successor invariant.

The repeated import check passed, and the build exposed four local proof boundaries before the removal program.  The exhausted branch still has its final `localGet 7`; the found-result predicate needs a definitional `change`; and each generated equality branch asks directly for `hZero` or its negation.  The successor measure proof also exceeded the simplifier step limit, so I will unfold the measure and reduce only the concrete frame projection before applying arithmetic.

The import check passed, and the build accepted the predicate conversion and both direct zero-test selections.  `wp_run` executes the exhausted branch's final getter completely, so its following `simp` is redundant and fails by making no progress.  The remaining no-match errors come from simplifying a concrete 23-local frame too broadly; I will use definitional equality after rewriting the increment and state the measure comparison in its reduced arithmetic form.

The next import check passed, but the build showed that `wp_run` cannot select the final getter under the raw public postcondition.  I searched `PROOF_RECIPES.json`, every selected `leanexe-core` category index, and the allowed ProofKit mirror with `rg -n -i 'localGet|local getter|get_local|Locals\.get|wp_local'`; no recipe or catalog entry provides this one-instruction boundary, while the checked sources consistently use `Wasm.wp_localGet_cons` and `Wasm.Locals.get`.  The same diagnostic shows that the successor frame contains `UInt64.ofNat index + 1` on the left and `UInt64.ofNat (index + 1)` only after unfolding the right, so the rewrite must follow `hIndexSucc` rather than its converse.

The import check passed, and the build closed the successor invariant and measure.  The exhausted branch is already a `FixedArrayEqNode.branchPost` applied to `Fallthrough`, so I must reduce that wrapper before applying `Wasm.wp_localGet_cons`.  The found branch is now the sole semantic continuation and begins with the checked index arithmetic before capacity, allocator, length store, prefix copy, and shifted suffix copy.

For this new residual class, I searched `PROOF_RECIPES.json` and the selected package indexes with `rg -n -i 'eraseIdx|copy.and.shift|copy-and-shift|shift|prefix|suffix|dynamic result|conditional store|array copy|fixed-array-map|allocation.*loop|loop.*allocation'`.  I inspected `leanexe-core/fixed-array-map-add` through its canonical `entry.json` and `README.md`, then read its named allowed ProofKit source; its whole-function theorem requires `leanexe.array.map-add.v1` and cannot apply to this artifact, but its proof gives the applicable allocator, input-preservation, payload-store, prefix-invariant, and address calculations.  I will use that checked pattern with this artifact's two copy loops, while the already inspected `fixed-array-allocation` entry supplies the direct shifted allocator theorem.

The import check passed, and the build reduced the exhausted path through its final getter.  An unrestricted simplification then unfolded `UInt64Array.At` into four fields, so I will preserve the representation goal with an explicit `change` after reducing only `Wasm.Locals.get`.  In the found path, the diagnostic now shows the two sentinel-normalization `iff` instructions that precede removal; a shared bounded increment fact will select them without reopening the search invariant.

The import check passed, and the build accepted both sentinel selections, leaving the removal body's initial getter and index-validation code.  `simp only [Wasm.Locals.get]` did not normalize the concrete local index because its arithmetic decision lemmas were excluded; the earlier unrestricted simplification had already reduced the exhausted path to the exact fields of `UInt64Array.At`.  I will use that reduction and close it directly with `hArray`, then normalize the concrete removal frame before selecting its first bounds branch.

The import check passed and the exhausted search path now closes with `hArray`; the bounded found path is the only remaining goal.  Its first removal test checks that the one-based search sentinel is nonzero, followed by a guarded subtraction that converts the sentinel to the zero-based erase index.  I will select both branches from the existing `hSentinelNe` fact and a `UInt64` comparison proved through `toNat`.

The import check passed, and the build accepted the first removal test.  During reduction, Lean normalized the compiler's `sentinel < 1` guard to `sentinel = 0`, so the attempted order-shaped rewrite did not occur in the goal.  I will select the subtraction branch using `hSentinelNe` in the normalized equality form and remove the unused order fact.

The import check passed, and Lean accepted the normalized guarded-subtraction branch.  The residual is now a concrete `localGet 8`, `localGet 9`, and subtraction match over the 23-local frame; `wp_run` stops because those structural getters have not reduced.  I will simplify that concrete frame once, expecting the next goal to be the represented input's header load and erase-index bounds check.

The import check passed, but the build stopped on two consecutive identical `simp` commands at the same concrete-frame boundary: the first made the required reduction, and the second had no work.  No later removal step elaborated because the redundant tactic failed.  I will delete the duplicate and retain the already written validity and subtraction branch selections.

The import check passed, and the build now reaches the concrete subtraction after selecting both removal guards.  The residual remains the unreduced local-get match because the intended simplification was attached to the earlier sentinel guard rather than this later subtraction guard.  I will place the reduction after the second guard's `wp_run`, where it can expose the input header load.

The import check passed, and the subtraction reduction produced the exact zero-based index in local 4.  The residual is the first input-header load and comparison `index < input.size`; the removal implementation repeats that check once inside the array primitive before computing the new length and copy counts.  I will use `hArray.generatedLengthBound`, `hArray.lengthRead`, and one `UInt64` form of `hIndexLt` for both checked branches.

The import check passed, and the first load-bound rewrite succeeded.  The read address remains in generated form `UInt32.ofNat (inputPtr.toNat % 2^32)`, while `hArray.lengthRead` uses `inputPtr.toUInt32`; the existing `hInputAddress` is the missing accessor conversion.  I will apply it before each of the two header-read equations.

The import check passed, and both header reads now use the represented length.  The second `wp_iff_cons` has reduced its data comparison to true but leaves the host-level selector `if 1 ≠ 0`; this constant selector is distinct from another Wasm instruction boundary.  I will choose it directly, execute the new-length and capacity prefix, and inspect the resulting allocator frame before applying `region_spec_withTail`.

The import check passed, and the bounded branch has reached the capacity floor `iff` immediately before the shifted allocator at offset 9.  The executed prefix has established the new length, prefix count, suffix count, and normalized capacity in locals 13, 11, 12, and 18, respectively.  I will expose the selector through `wp_iff_cons`; its normalized condition will determine the exact capacity fact needed for the direct allocator theorem.

The import check passed, and the selector exposes capacity `(8 + (UInt64.ofNat input.size - 1) * 8 + 7) / 8 * 8`.  Because a found index proves `0 < input.size`, this word has natural value `8 * input.size`, is at least eight bytes, and matches the found-case heap reservation.  I will use these facts to choose the no-floor-adjustment branch and apply `FixedArrayAllocatorWindow.region_spec_withTail` at offset 9 with no trailing locals.

The import check passed, and `region_spec_withTail` accepted the exact offset-9 allocator, leaving only the length store and two copy loops.  The diagnostic also found two minor presentation issues: generic `not_lt.mpr` does not infer the `UInt64` order instance from the rewritten capacity expression, and the heap-reserve premise differs only by addition association.  I will prove the capacity negation through `UInt64.lt_iff_toNat_lt`, use `omega` for the association, and execute the header store with `Allocation.bumpFacts` before defining the loop invariants.

The import check passed, and the dynamic header store executed at the allocator root; the residual begins with the first copy block.  The capacity selector itself tests whether the comparison result word is nonzero, so its proof must simplify the nested Boolean expression rather than change it directly to the word comparison.  I will name the exact pre-allocation frame and a reusable local-15 counter frame, then state the prefix and shifted-suffix invariants over those named frames.

The import check accepted the named removal frame at the allocator boundary.  Two reductions remain incomplete: the capacity inequality still contains `(8 : UInt64).toNat`, and the header-store getter remains folded through `removalBaseFrame`.  I will state the first as the natural inequality `¬8 * input.size < 8` and unfold the named base frame when simplifying the allocator frame.

The import check passed after those two presentation fixes, and the Lean build accepted the capacity branch, shifted allocation, and dynamic result-length store.  Its sole residual starts with the prefix-copy block and includes the later shifted-suffix block and result handoff.  I will add a prefix invariant that preserves the input and header while recording copied words; closing the first block with those facts will constitute progress before extending the proof to the shifted suffix.

The import check accepted the prefix invariant, and the build checked its complete store case and decreasing measure.  The initialization needs a separately named root-address projection because the goal has already normalized the zero word offset, while the completed-loop case retains the `br_if` value match after `wp_run`.  I will add the exact root `toNat` equality and reduce that match, after which the residual should begin at the shifted-suffix loop with the prefix facts available.

The import check passed, and the build now checks the prefix loop in full.  The residual starts at the shifted-suffix block with the represented input, result header, and every prefix payload word available as hypotheses.  I will use a second counter invariant whose source index is `index + counter + 1` and whose destination word is `index + counter + 1`; its completion should leave only the final `eraseIdx!` representation and result-pointer handoff.

The import check accepted the suffix invariant, and the build reached its guard and store case.  The remaining errors are exact normalizations: the wrapped subtraction proof needs its final arithmetic step, the guard comparison was stated in the reverse order, and the generated `br_if` zero path presents its two memory checks as conjunctions rather than nested `if` terms.  I will correct those forms, rewrite the allocator-root modulus before rearranging arithmetic, and convert the source `getElem` to `getElem!` with the known source bound.

The import check passed, and the build accepted the corrected subtraction, guard order, output address, source read, and conjunction-shaped store proof.  Two local mismatches remain: the completed suffix guard has already reduced its branch wrapper, and the generated source bound associates `index + 1 + shift + 1` differently from `generatedElement`.  I will remove the redundant wrapper simplification and pass the bound through arithmetic normalization.

The import check passed, and the build now checks the entire suffix store case and decreasing measure.  The sole failure is `wp_run` reporting no progress after the completed suffix guard because the generated continuation has already performed the result-pointer handoff and reduced to its semantic postcondition.  The prior knowledge search found no `eraseIdx!` accessor entry, so I will print the local toolchain definitions for `Array.eraseIdx!` and `Array.eraseIdx`, remove the diagnostic immediately afterward, and prove the final representation from the two loop invariants.

The diagnostic import check passed.  The build printed `Array.eraseIdx!` as the in-bounds test followed by `Array.eraseIdx input index hIndexLt`; `Array.eraseIdx` is the irreducible checked primitive, whose standard size and getter simplification lemmas remain available through the imported array library.  I will remove both prints, reduce the completed `br_if` match directly, and test those simplification lemmas against the result size and the prefix-versus-shifted getter split.

The import check passed, and the build accepted the completed guard reduction and rewrote the expected result to `input.eraseIdx! index`.  The residual still contains the explicit existential result pointer and its one-word return equality, so the attempted direct representation conversion was premature.  I will provide `heapTop + 48` and its return equality first, then continue with the already written size, length, and element proof.

The import check passed, and the build accepted the result pointer, `eraseIdx!` size lemma, encoded new length, and both semantic getter lemmas.  The first fit proof tried to rewrite a capacity variable no longer present in the goal, while each payload target uses dependent `getElem` and each invariant uses `getElem!`.  I will retain the capacity equality only in the bump-fit hypothesis and rewrite each target through `getElem!_pos` before applying its prefix or suffix memory fact.

The import check passed, and the build accepted all final representation fields except the two proposed `hEraseGet` proofs.  Simplifying `Array.eraseIdx!` selects the valid branch but leaves `Array.eraseIdx` irreducible at its getter, so the generic simplifier lacks enough information without its named getter theorem.  The knowledge indexes contain no erase accessor, so I will query a small set of exact `Array.getElem` declaration names in this toolchain and use only a declaration whose printed signature matches the prefix or suffix residual.

The diagnostic import check passed.  The build found `Array.getElem_eraseIdx_of_lt` and `Array.getElem_eraseIdx_of_ge` with exactly the required prefix and shifted-suffix postconditions; `Array.getElem_eraseIdx` and `Array.getElem?_eraseIdx` are broader alternatives, while `Array.eraseIdx_getElem` does not exist and is rejected.  I will remove all diagnostic checks and apply the two specialized declarations after selecting the valid `eraseIdx!` branch, using `getElem!_pos` only to match the loop invariants' element notation.

The final import check passed, and the required `LeanExeGen.GeneratedRb9ad29e25c8033e5.ArtifactResult` build completed successfully.  The proof uses the length-dispatch tactic, constant and shifted allocator theorems, allocator bump facts, allocation input preservation, result-store declarations, two semantic loop invariants, and `Array.getElem_eraseIdx_of_lt` and `Array.getElem_eraseIdx_of_ge`.  Both public branches establish the exact expected representation, and no residual goal remains.
