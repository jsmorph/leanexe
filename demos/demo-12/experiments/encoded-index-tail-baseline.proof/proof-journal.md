# Proof Journal

## 2026-08-26

The required initial build reached the theorem starter and failed at the final `TerminatesWith` goal.  The `RuntimeReady` decomposition produced the represented input, allocator globals, address and length facts, output bounds, heap-reserve bounds, and page bound without errors.  The remaining goal is execution of function 0 from `[Value.i64 inputPtr]` to one pointer representing `FormalSpec.expected input`.

I will inspect the selected `leanexe-core` indexes using the annotation kinds `leanexe.array.length-dispatch.v1`, `leanexe.array.find-idx-eq.v1`, `leanexe.option.encoded-index.v1`, and `leanexe.array.erase-copy.v1`, together with the declarations named by `PROOF_RECIPES.json`.  I will attempt the exact length-dispatch tactic first.  Progress means that the public execution goal splits into the valid and invalid length branches described by the checked annotation.

The first catalog query was `leanexe\.array\.(length-dispatch|find-idx-eq|erase-copy)\.v1|leanexe\.option\.encoded-index\.v1|wp_fixed_array_length_le_dispatch_from|FixedArray(LengthDispatch|FindIdxEq|Copy)|EncodedIndexDecoder|constantCapacityRegion_spec_withTail` across `leanexe-core` category indexes.  It selected `fixed-array-length-dispatch`, `fixed-array-find-idx-eq`, `encoded-index-decoder`, `fixed-array-erase-copy`, `fixed-array-capacity`, and `fixed-array-allocation`.  I inspected each selected entry's `entry.json` and `README.md`.  The first four match exact checked artifact regions.  The capacity and allocation entries match the invalid branch's constant empty-array construction named by the length-dispatch recipe.

The second catalog query was `FormalSpec\.expected|eraseIdx!_of_reads|UInt64Array\.At\.eraseIdx|resultSuffix|fixed-array-result|fixed-array-erase-reconstruction|allocator-window|constant result|return pointer|output array`.  I inspected `fixed-array-erase-reconstruction` because the copy theorem produces the read facts required to represent `Array.eraseIdx!`.  I also inspected `array-memory-framing` because both the search and copy phases require source reads to survive writes above the input region.  `fixed-array-result` appeared in the query, but its singleton and pair result stores do not match this artifact's variable-length erased array, so I did not inspect its entry files.

`PROOF_RECIPES.json` has four direct recipes and no complete composition.  I will use the length-dispatch command before its fallback declaration, as required by its structured tactic record.  The other three regions have direct theorems but no tactics.

The first edited candidate passed the import check.  The build failed before the dispatch command because `of_wp_entry_for` left the target program as `func0Def.body`, so rewriting with `function_0_length_dispatch_0_function_eq` found no occurrence of `func0`.  This is an instruction adapter gap.  The length-dispatch entry states that the proof should change the body-level target to `func0` before applying the checked function equality.  I will add that exact `change`; progress remains separate valid and invalid branch goals.

After the body `change`, the import check passed and the build confirmed that `wp_fixed_array_length_le_dispatch_from hArray at 8, 8` matches the goal.  It produced five entry-frame and numeric premises, followed by the valid and invalid branch obligations.  This is the expected postcondition of the direct recipe.  The tactic reduced the search and bounded-length load rather than exposing either loop.

Before reducing entry-frame projections, I searched the `leanexe-core` proof-construction, compiler-motif, and array indexes for `toLocals|branchFrame|params|locals.length|values|local-frame|frame projection|validIndex`.  The search selected `local-frame-projection`; I inspected its `entry.json` and `README.md`.  Its declarations address equality and projections after operand-stack replacement, but these five goals concern the concrete initial `func0Def.toLocals` frame.  Direct reduction with `simp [func0Def, Wasm.Function.toLocals]` is the exact accessor for this concrete frame.  The entry remains relevant for the search, decoder, allocator, and copy frames later in the valid branch.

I will discharge the five concrete premises and introduce `hSize` in each branch.  Progress on the valid branch means exposing the annotated `FixedArrayFindIdxEq.program 8 0` prefix while keeping its early-exit loop folded.  Progress on the invalid branch means exposing `FixedArrayCapacity.constantProgram 0 1 12` before applying its checked capacity and allocator composition.

The next import check passed.  The build showed that `decide` cannot close the open entry-frame local bound until `func0Def.toLocals` is reduced.  After replacing that one premise with focused normalization, the direct `FixedArrayFindIdxEq.program_spec` recipe matched with `scratch := 8` and `tail := 12`.  Its structural premises reduced, leaving the none and some search continuations.  The theorem now supplies the complete early-exit loop proof and the exact first-match equations.

The attempted broad simplification of the none continuation stopped because the entry frame remained expanded through `func0Def.toLocals`; it also showed that the formal predicate requires an explicit function equality before the search equation rewrites `FormalSpec.expected`.  I will normalize `func0Def`'s parameter, result, and initial-local projections once at the entry boundary, and derive the formal none equation with `simpa [FixedArrayFindIdxEq.predicate] using hFind`.  This reduces later frame access without adding a local frame theorem.  The prior `local-frame-projection` search found no none-frame getter declaration; direct normalization applies after that exact-accessor search.

The import check passed.  The explicit predicate conversion now proves `FormalSpec.expected input = input` in the none branch.  The entry normalization omitted `List.length`, so the parameter `take`, result `drop`, and concrete local bound remained folded.  The none continuation reduced to a single concrete `List.getElem?` at index zero.  I will add the exact list-length and head-access reductions named by the existing ProofKit simplifier macros.  Progress means that the none path returns `inputPtr` and reuses `hArray` without entering the removal branch.

The next import check passed.  The build reduced the none path through its first structured `iff`; the second `wp_iff_cons` needs a simplified operand-stack equality rather than `rfl`.  The entry frame is now the compact record with one parameter and 23 replicated zero locals.  Its remaining dispatch bound closes by arithmetic normalization.  I will use `by simp` for the second branch frame's operand stack and retain the two explicit control steps.

The import check passed.  My preceding edit placed `by simp` on the first `wp_iff_cons`, where it left metavariables for the stack tail; that first stack is definitionally `[.i32 0]` and needs `rfl`.  The second stack contains the result of the equality instruction and needs simplification.  I will restore `rfl` on the first control step, use `by simp` on the second, and state the concrete dispatch bound as `8 < 24` before `decide`.

The import check passed.  The concrete dispatch bound now closes.  The second none-path stack equality still resists `simp` because the two atomic instructions retain an updated frame term.  I will use the standard `wp_run` atomic-instruction tactic for those two instructions, after which `wp_iff_cons rfl` applies to the normalized stack.

Before beginning the successful search continuation, I searched the relevant indexes for `someFrame.*get|encodedLocal|encoded result operand|resultFrame_decoded|localSet.*encoded|encodedIndex`.  The query returned the already inspected `fixed-array-find-idx-eq` and `encoded-index-decoder` entries.  `someFrame_values` supplies the encoded result operand, and `resultFrame_decoded` supplies the decoder output; neither entry provides a getter for local 2 after the emitted `localSet`.  I will use `someFrame_values` before the local update and focused reduction for the resulting local-2 getter.  The direct decoder recipe will handle the encoded-index instruction region once the outer option branch selects it.

The import check passed.  `wp_run` normalized the second none-path stack, and the branch now reaches exactly `FormalSpec.UInt64ArrayAt initial inputPtr input`; changing that alias back to `UInt64Array.At` closes it with `hArray`.  No loop or removal instruction remains in the none path.

I will prove the successful encoded result is nonzero from `index < input.size ≤ 8`, execute the two option-selection `iff` instructions, and stop after copying the input pointer into local 3.  The residual program should then match `EncodedIndexDecoder.program 2 8 4 ++ rest`; applying its direct recipe is the next progress boundary.

The import check passed.  The successful option selector chose the removal branch using `encodedIndex_ne_zero`.  The focused simplifier stopped inside the two local instructions because their concrete parameter and local lists require `List.length` reduction; the residual instruction list begins inside the decoder, so the direct decoder recipe cannot match yet.  I will add `List.length` and list-append reduction to this focused adapter.  Progress remains the exact decoder program boundary.

The next import check passed.  List-length reduction exposed concrete natural-number additions in the combined-local bounds.  The adapter still stops before the decoder because `0 + 1` and the 23-step local-list length remain unreduced.  I will add `Nat.reduceAdd`, matching the focused local-window simplifiers in `FixedArrayAllocatorWindow`.  No semantic premise has changed.

The import check passed.  Natural-number reduction converted the local bounds to `True` and `False`, but `simp only` retained the corresponding conditional expressions.  I will add `if_true` and `if_false` to finish the two-instruction adapter.  The decoder instruction list remains intact.

The import check passed.  The recursive simplifier began reducing the decoder's first `localGet` after it finished the two intended prefix instructions; this destroys the exact decoder boundary.  The diagnostic also left the equality `2 = 1` from that unwanted third instruction.  I will replace the recursive simplifier with one `rw [wp_localGet_cons]` and one `rw [wp_localSet_cons]`, simplifying only each resulting frame expression.  This preserves the complete decoder region required by the direct recipe.

The import check passed after separating the first `localGet`, but the broad simplifier used for its frame expression invoked globally registered `wp` simplification lemmas.  It consumed the following `localSet`, the decoder's first getter, and its equality instruction before the explicit `wp_localSet_cons` rewrite.  I will restrict each frame reduction with `simp only`, excluding every `wp` lemma.  The residual goal that counts as progress is the untouched `EncodedIndexDecoder.program 2 8 4` prefix.

The import check passed.  The restricted simplifier preserved the instruction list, but its first frame expression still contains `0 + 1` in the parameter bound.  `Nat.reduceLT` cannot evaluate that comparison before addition reduces.  I will add `Nat.reduceAdd` to this single reduction; the later `localSet` remains outside the simplifier.

The import check passed, and the exact decoder theorem now applies.  Its residual premises are one concrete getter, two lower bounds, two valid-index bounds, and the continuation from `EncodedIndexDecoder.resultFrame`.  The first five close by definitional computation.  The inspected decoder entry names `resultFrame_decoded` for local 4 in the continuation.  I will close the structural premises and run only the atomic prefix after decoding.  Progress means reaching one of the two explicit bounds-test `iff` instructions without expanding the allocator or copy loops.

The import check passed.  `rfl` closed the concrete encoded getter, while `decide` rejected each length proposition because its expected type retained irrelevant symbolic frame values.  I will normalize these propositions with `simp [Wasm.Locals.validIndex]`.  `wp_run` confirmed that the next instruction is the decoded local-4 getter, but expanded its definition instead of selecting the cataloged accessor.  I will rewrite that instruction with `EncodedIndexDecoder.resultFrame_decoded`, use `encodedIndex_sub_one`, and then resume atomic execution.

At the invalid-branch checkpoint I searched the allocation, arrays, compiler-motif, memory, and proof-construction indexes for `fixed-array-result|emptyStore_at|finishProgram_spec`.  I inspected the `fixed-array-result` entry and README because `emptyStore_at` matches the oversized-input empty representation, while `finishProgram_spec` matches the four final local transfers shared by both result branches.  The entry's singleton and pair tactics remain inapplicable.  I will import its checked module when constructing the invalid result.

The import check passed.  Focused simplification closed all four decoder length premises, and `resultFrame_decoded` reduced the first continuation getter to `UInt64.ofNat index`.  Atomic execution then stopped at the source-pointer getter in combined local 3.  The earlier accessor search found no theorem for a local that the decoder preserves.  I will unfold only `resultFrame` and list getters after an explicit `wp_localGet_cons`; no `wp` simplification lemma will occur in that reduction.  Progress means preserving the remaining bounds-check instruction sequence with `inputPtr` on its operand stack.

The import check passed.  The decoded getter rewrite produced `match some (Value.i64 (UInt64.ofNat index)) with ...`; the next instruction remains correctly folded inside its successful branch.  My source-pointer rewrite ran before reducing this match and therefore found no top-level `wp`.  I will perform iota reduction with an empty `simp only` between the two getter steps.

The import check passed.  Iota reduction exposed the source getter, and unfolding the decoder frame reduced it to three concrete list-set index comparisons: `3 = 2`, `8 = 2`, and `7 = 2`.  The inspected allocator-window simplifier set names `Nat.reduceEqDiff` for this exact residual class.  I will add that declaration to the focused getter reduction, preserving the subsequent `localSet 8` and bounds-check instructions.

The import check passed.  `Nat.reduceEqDiff` proved that the decoder preserved local 3 and reduced its getter to `inputPtr`.  Atomic execution next stopped in the `localSet 8` frame update.  The already inspected allocation entry provides `wp_alloc_to_store_lists`, whose `simp only` set handles local getter/setter bounds and list updates without including memory loads or control instructions.  I will use that command as a frame adapter.  Progress is a residual beginning at the input length load.

The import check passed, and `wp_alloc_to_store_lists` reached the first input length load with the stack `[inputPtr.toUInt32, UInt64.ofNat index]`.  At this new bounds-check goal I searched the relevant indexes and local ProofKit mirror for `UInt64.ofNat.*<|ofNat_lt|lt_iff_toNat_lt|array.*index.*bound`.  The indexes returned only the already inspected erase reconstruction entry; it addresses final memory reads rather than scalar comparison.  `FixedArrayInput.program_spec` proves the same comparison but its program includes initialization instructions absent from this residual, so it does not apply.  Its proof uses `UInt64.lt_iff_toNat_lt` with `UInt64.toNat_ofNat_of_lt'`, which fits the residual.  I will prove the encoded comparison, discharge the length load with the exact `RuntimeReady` read and bound facts, and select both compiler-emitted in-bounds branches.  Progress means entering the length, prefix, suffix, and capacity computations preceding allocation.

The import check passed.  The first load's address and bounds reduced, but its read remained at `inputPtr.toUInt32 + 0`, so the exact `hLengthRead` rewrite did not match.  The local ProofKit copy proof names `UInt32.add_zero` explicitly for this residual; generic `add_zero` did not select the UInt32 operation during the earlier reduction.  I will add `UInt32.add_zero` and `Nat.add_zero` at both length loads.

The import check passed.  Both address additions now normalize, and rewriting the out-of-bounds condition and `hLengthRead` performs the required match reduction itself.  The following empty `simp only` therefore reports no progress.  I will remove those two redundant calls; the next explicit rules are `wp_ltUI64_cons` and the two in-bounds `iff` selections.

The import check passed and selected both in-bounds branches.  The residual now begins with the scalar computation of result length `input.size - 1`, prefix length `index`, suffix length `input.size - 1 - index`, and the dynamic normalized capacity in local 18.  `FixedArrayCapacity.constantProgram_spec` does not match because this artifact reads the computed length from local 13.  The allocation entry's `wp_alloc_window_lists` tactic has matching concrete-frame premises and covers these scalar instructions.  I will attempt it before reconstructing individual local updates.  Progress means reaching the exact `FixedArrayAllocatorWindow.region 9 1` boundary with the capacity stored in local 18.

The import check passed, but my edit inserted `wp_alloc_window_lists` after the first bounds branch rather than after the second.  It advanced into the second load, where the following local-only adapter made no progress.  I will move the command to the scalar residual after the second `if_pos`; the proven load sequence remains unchanged.

The import check passed.  The allocation simplifier computed result, prefix, suffix, and unnormalized capacity locals and stopped at the capacity-minimum `iff`, as intended.  For `1 ≤ input.size ≤ 8`, the computed capacity has natural value `8 * input.size` and is never below eight.  No catalog entry supplies a theorem for the local-length capacity prefix.  I will establish these two arithmetic facts over the bounded length, select the false minimum branch, and apply `region_spec_withTail` with offset 9 and tail 0.  The region premises should reduce to the RuntimeReady allocator facts and `hHeapFitMemory` after rewriting `heapReserveBytes` with the successful search equation.

The import check passed.  Bounded arithmetic proved both capacity facts, but the compound `norm_num ... at hIndex ⊢` reported `No goals to be solved` after closing some cases; I will state positivity once and normalize only each arithmetic target.  The heap-reserve simplification needs the formal predicate equality stated separately because `hFind` uses the ProofKit predicate wrapper.  Finally, reducing the capacity comparison leaves the emitted branch selector `if 0 ≠ 0`; I will select its false branch before changing to the allocator region.

The import check passed.  `interval_cases` narrowed the length to 1 through 8, but `norm_num` does not evaluate the closed UInt64 arithmetic expressions.  I will use `native_decide` on each closed case.  The ProofKit predicate wrapper is definitionally equal to the formal lambda, so the formal search equation should use `change` followed by `exact hFind` rather than simplification.

The import check passed, and `native_decide` closed every capacity case.  The heap fit differs only by associativity of natural addition.  Selecting the empty capacity-minimum branch also leaves the standard `wp module []` continuation adapter.  I will normalize addition in the fit proof and apply `wp_nil` before changing the remaining program to `region 9 1`.

The import check passed, and `region_spec_withTail` matched the complete checked allocator region.  Its parameter- and local-length premises retain symbolic input and index values, so `decide` rejected them because the expected types contain free variables; focused simplification is the appropriate structural proof.  The continuation begins at getter 14 of `allocFrame 9`, the allocated root.  The prior catalog query for `fixed-array-result|emptyStore_at|finishProgram_spec` selected `fixed-array-result`; its `lengthStoreLocal_spec` also matches the successful branch's four-instruction length-header store, so that entry is now used for both result branches.

Before reducing the allocator continuation, I searched the inspected allocation and result material for `allocFrame.*get|lengthStoreLocal|lengthLocal|preserv.*local|allocator.*root.*getter`.  `FixedArrayAllocatorWindow.allocFrame_get_root` is the exact root accessor.  No checked allocator accessor states preservation of local 13, which contains the result length.  I will use `allocFrame_get_root`, reduce only the local-13 projection, and apply `FixedArrayResult.lengthStoreLocal_spec`.  Progress means reaching the exact `FixedArrayCopy.program 1 8 14 11 12 15` boundary with a checked target header and unchanged source representation.

The import check passed.  Focused simplification now closes the allocator frame's parameter and local-length premises, and `allocFrame_get_root` reduces the first continuation instruction to the root value.  The residual begins with the remaining three instructions of `lengthStoreLocalProgram 14 13`, followed by the exact two-loop erase-copy program.  I will restore the four-instruction boundary and apply `lengthStoreLocal_spec`.  `Allocation.bumpFacts` supplies the root address, while the inspected `array-memory-framing` entry supplies `FixedArrayPairResult.input_preserved_by_alloc` and `UInt64Array.At.write64After` for the copy theorem's source representation.

The import check passed.  `lengthStoreLocal_spec` discharged the root getter, preserved length-local getter, store bound, and exact memory update.  The residual is `FixedArrayCopy.program 1 8 14 11 12 15 ++ [localGet 14]`, which matches the direct erase-copy recipe exactly.  I will apply `eraseIdxProgram_spec`, using allocator framing for the source, the written header for the target length, and the RuntimeReady disjoint bump boundary.  Progress means that both copy loops produce `UInt64Array.At final (heapTop + 48) (input.eraseIdx! index)` and leave only the final root transfers and function suffix.

The import check passed, and `eraseIdxProgram_spec` matched.  The source-preservation, source and prefix getters, target header, disjointness, and scalar allocator facts closed.  Three proof-boundary adapters need refinement: the target `UInt64.ofNat` conversion should use a separately typed size-bound fact rather than an unrestricted rewrite; positional bullets obscured the frame-value premise; and unfolding `counterFrame` in the final transfer exposed its proof argument.  I will pass every copy premise by name, retain the checked counter frame, and use `finishProgram_spec` for the final local transfers.  No copy-loop invariant remains to construct.

The import check passed.  The separately typed suffix conversion now closes.  Lean could not infer the five numeric local parameters from the folded copy-program equality while elaborating named premises, so their metavariables propagated into the frame getters.  The target header also requires the scalar equality `UInt64.ofNat input.size - 1 = UInt64.ofNat (input.size - 1)`.  I will supply all five locals explicitly and state that equality from the already proved `toNat` fact.  This is an elaboration adapter at the direct theorem boundary, not a new semantic obligation.

The import check passed, and the header scalar equality closed.  Supplying the local numbers inside a single `refine` term still elaborates the dependent frame premises before Lean unifies the theorem conclusion with the current `wp` goal; the frame remains a metavariable at those premises.  I will use tactic-level `apply` with only the five local parameters, then discharge the generated premises in order after conclusion unification.  This restores the direct-recipe application shape that succeeded at the preceding checkpoint.

The import check passed.  Tactic-level `apply` fixed every dependent frame metavariable.  It generated three explicit local-distinctness goals because Lean discharged one during application; my four `decide` bullets shifted the remaining copy premises by one.  The resulting diagnostics confirm that the prepared source, target, prefix, suffix, bounds, header, and disjointness facts have the required types.  I will remove the extra distinctness bullet.  The next residual should occur inside the final transfer theorem or close the successful branch.

The import check passed.  Every semantic premise of `eraseIdxProgram_spec` now closes, so both annotated copy loops and reconstruction of `input.eraseIdx! index` are proved.  The direct copy region intentionally ends before `localGet 14`; the following `localSet 5`, `localGet 5`, and `localSet 7` live in the enclosing option-branch postcondition.  Therefore `finishProgram_spec` cannot match this boundary.  I will execute the single root getter with `counterFrame_get_ne`, then reduce the three enclosing transfers and the one-instruction function suffix.  The copy counter-validity premise also needs an explicit concrete `change` because generic simplification leaves the deeply nested frame visible.

The import check passed.  `apply` had already discharged the copy counter-validity premise, so the first remaining goal is `8 ≠ 15`; I will restore `decide` there.  The result-root getter and three enclosing transfers reduce to a `branchPost` on a concrete fallthrough frame whose locals 5 and 7 contain the root.  The previous combined simplification stopped at this newly exposed assertion.  I will unfold `branchPost` in a separate step, execute the suffix getter, and close its expected-array fact with the copy theorem's `hResult`.

The import check passed.  The successful branch now reduces through `branchPost` and the function suffix.  Its sole residual is three nested `List.getElem?` matches at literal index zero, followed by the existential result pointer and the already proved erased-array representation.  The standard `List.getElem?_cons_zero` reduction exposes that existential directly; I will supply `heapTop + 48` and `hResult`.

The import check passed.  The existential result pointer and representation close.  The only successful-branch residual is the side condition `14 ≠ 15` generated by the earlier unrestricted rewrite with `counterFrame_get_ne`; its main rewrite goal is already complete.  I will pass this decidable side condition as an explicit theorem argument so the rewrite creates no deferred goal.

The import check passed.  Passing `14 ≠ 15` explicitly removed that side condition, while the placeholder for `counterFrame_get_ne`'s preceding counter-validity argument created the remaining goal.  The concrete frame still has one parameter and 23 locals, so I will pass `15 < 1 + 23` explicitly as well.  The successful branch otherwise has no residual goal.

The import check passed.  Supplying the base frame and its counter-validity proof explicitly prevented the rewrite theorem from matching the occurrence because the occurrence determines a dependent frame term beneath the copy theorem.  The theorem can infer that frame and validity proof from the occurrence.  I will specify only `hNe := by decide`, leaving the dependent arguments to occurrence matching.  Progress means closing the successful branch without a deferred local-validity goal.

The import check passed.  The build confirms that the occurrence-specific `counterFrame_get_ne` rewrite closes the successful branch.  The sole residual is the dispatch theorem's invalid branch under `hSize : ¬ input.size ≤ 8`.  I will use the direct constant-capacity allocator composition named by the allocation recipe, then the inspected `fixed-array-result` length-store and finish declarations.  Progress means obtaining `UInt64Array.At` for the empty allocation without expanding the allocator region or its memory writes.

The import check passed, but I inserted the invalid-branch construction at the first textual `intro hSize`, which belongs to the valid branch.  The diagnostics show the resulting contradiction: the empty expected result and 56-byte heap reserve were requested under `input.size ≤ 8`, while the genuine invalid branch remained untouched.  I moved the same construction to the dispatch theorem's second branch.  This changes no proof argument; it corrects the tactic scope before testing the allocator composition.

The import check passed.  The constant-capacity allocator composition matched the entire invalid allocation region, and the length-store and finish theorems matched their exact residual programs.  Four adapters remain: `wordAddress_toNat` needs the checked normalized-capacity equality to prove its one-word premise; the allocator root accessor and local-validity facts need the named frame-length preservation theorems; and the suffix getter leaves an iota-reducible `match some` before `wp_nil`.  These are the exact accessors found in the prior allocation, local-frame, and result searches.  I will use their preservation equalities and reduce the option match between the getter rewrite and `wp_nil`.

The import check passed.  The allocator and suffix obligations now close.  The final goal compares `Project.ProofKit.UInt64Array.At`, produced by the inspected `fixed-array-result` entry, with the frozen `FormalSpec.UInt64ArrayAt`.  I searched the selected package indexes with `FormalSpec\\.UInt64ArrayAt|UInt64Array\\.At|representation.*convert|array representation|emptyStore_at`.  The query returned only the already inspected `array-memory-framing`, `fixed-array-erase-reconstruction`, and `fixed-array-result` entries.  None names a conversion theorem.  Inspection of the permitted `Project.ProofKit.Array` mirror confirms that `UInt64Array.At` has the same four conjuncts as the frozen predicate.  I will unfold these two predicates at this boundary; no array read or bound is re-proved.

The import check passed.  Simplifying both representation definitions in a `using` expression caused Lean to normalize through unresolved `wp` frame metavariables from the enclosing proof term, producing a large implicit-argument diagnostic rather than a local representation goal.  I will destruct the checked `emptyStore_at` result into its four conjuncts and construct the frozen predicate from those same conjuncts.  This confines reduction to the representation boundary and preserves every checked bound and memory-read proof.

The import check passed.  Constructing the frozen predicate from the four checked conjuncts closes the last semantic goal, after which Lean reports a latent metavariable in the successful copy continuation's `rw [wp_localGet_cons]`.  `eraseIdxProgram_spec` fixes the continuation frame to `counterFrame frame 15 (input.size - 1 - index) hCounter`, but `rw` elaborated the WP lemma before occurrence matching and left its frame implicit.  I will use `simp only [wp_localGet_cons]`, whose simplifier matches the complete top-level WP occurrence before instantiating the lemma.  The following cataloged `counterFrame_get_ne` rewrite remains unchanged.

The import check passed.  The simplifier instantiated the copy continuation frame, exposing the next latent placeholder in the bounded UInt64 suffix-length equality.  The equality ranges over `input.size ≤ 8` and `index < input.size`; the existing symbolic `rw` proof left one theorem instantiation unresolved.  I will prove the same closed UInt64 equality by finite interval cases and `native_decide`, using the two established bounds.  This proof covers only the bounded scalar computation and leaves the copy theorem unchanged.

The import check passed, and `hSuffixScalar` appears in the diagnostic context, so the finite proof closed.  My preceding attribution was wrong: the unresolved placeholder belongs to the following `change wp _ (...) _ _ _ _`, whose program pattern matched but whose assertion, state, frame, or environment placeholder remained synthetic.  The displayed goal starts definitionally with `lengthStoreLocalProgram 14 13`, and its continuation starts definitionally with the erase-copy program.  I will remove both `change` adapters and apply the two checked theorems directly, allowing conclusion unification to determine every dependent argument.

The import check passed, but removing both WP adapters did not move the first diagnostic: Lean still attributes the synthetic metavariable to the local `hSuffixScalar` declaration.  I will isolate the bounded UInt64 equality in a private theorem with explicit `size`, `index`, and bound arguments.  This creates a separate elaboration boundary for the finite proof.  The artifact theorem will use the resulting declaration by exact application, so no arithmetic tactic metavariable can remain attached to its WP proof.

The import check passed, and the private bounded-suffix theorem itself checks, but the diagnostic still points to the local alias that applies it.  I will remove that alias and pass the private theorem directly to the suffix-local simplifier.  This will determine whether the synthetic placeholder belongs to the alias or to the immediately following fixed-array result theorem application without changing either fact.

The import check passed.  Removing the alias moves the diagnostic to `lengthStoreLocal_spec`, confirming that `apply` left a dependent argument synthetic while matching the large WP postcondition.  I will use `refine` with explicit `module_`, `env`, root, length, and local indices, plus one goal hole for each proof premise.  Expected-type elaboration can then determine the store, frame, assertion, and residual program before Lean creates the four proof goals.

The import check passed and gave the theorem's partially applied type.  `Q` and `rest` are explicit arguments between the memory-bound proof and the continuation proof, so the first `refine` supplied too few positional holes.  I will supply holes for all six remaining arguments.  Expected-type unification should assign the `Q` and `rest` holes from the target, leaving the existing root getter, length getter, bound, and continuation bullets.

The import check passed and showed that elaboration stopped after assigning `Q`, before applying `rest` and the continuation.  The residual program is exactly `FixedArrayCopy.program 1 8 14 11 12 15 ++ [.localGet 14]`.  I will pass this `rest` explicitly and return to tactic-level `apply`.  With the program split fixed, conclusion unification determines the store, frame, and assertion and generates only the four theorem premises.

The import check passed.  The explicit copy-program tail moved elaboration into `lengthStoreLocal_spec`, where the diagnostic displayed the singleton instruction as `?m.1620` despite the source spelling `.localGet 14`.  The remaining metavariable is the overloaded instruction constructor, rather than a store, frame, or assertion parameter.  I will qualify `Wasm.Instruction.localGet` in both explicit continuation tails.  The second qualification fixes the same boundary in `eraseIdxProgram_spec` before Lean reaches it.

The import check passed, and the qualified instruction tails elaborate.  Lean now reports the first synthetic placeholder at the local `hLengthScalar` declaration.  This is the same bounded UInt64 arithmetic class as the suffix equality already isolated above.  I searched `PROOF_RECIPES.json` and the selected package indexes with `ofNat.*- 1|length.*scalar|toNat_sub_of_le`.  The only result was the unrelated, uninspected `fixed-array-singleton-wrapper`; no exact length accessor or arithmetic theorem was cataloged.  I will isolate the length equality as a private finite theorem over sizes one through eight and apply it directly at the checked copy postcondition.

The import check passed, and `bounded_length_scalar` checks.  The first remaining placeholder moved to the local `hBefore` separation fact.  The inspected `fixed-array-allocation` entry supplies `Allocation.BumpFacts.rootToNat`, but neither its declarations nor the direct recipe supplies the derived input-before-output inequality.  I will isolate the one-step consequence of `RuntimeReady`'s `hInputBelow` and `rootToNat` in a private theorem, then apply it directly to `eraseIdxProgram_spec` without a local alias.

The import check passed, and `input_before_bump_root` checks.  The first placeholder moved to `hTargetFitMemory`; its displayed residual remains the unchanged copy WP.  The `fixed-array-capacity`, `fixed-array-allocation`, and `fixed-array-erase-copy` entries identify the three premises: positive input length, normalized capacity, and bump-root arithmetic.  I will replace both local target-fit aliases with one private arithmetic theorem parameterized by the final bound.  Its two direct applications prove the 32-bit and memory-page copy bounds; the memory application first uses the checked page-preservation declarations.

The import check passed.  `bounded_target_fit` has an ordinary rewrite-location error: `hRoot` occurs in the goal, while `hCapacity` occurs in `hFit`; the combined `rw ... at hFit ⊢` required each rewrite to occur in both.  I will perform the two rewrites at their exact locations.  The diagnostic also identifies the next latent boundary at `hInputAfterLength`, but I will first check the helper correction so its result is available to that continuation.

The import check passed, and `bounded_target_fit` now checks.  The first remaining placeholder is `hInputAfterLength`.  The inspected `array-memory-framing` and `fixed-array-allocation` entries provide the complete composition: `input_preserved_by_alloc` gives the represented source after allocation, and `UInt64Array.At.write64After` preserves it across the disjoint output-header write.  I will pass this composition directly as `eraseIdxProgram_spec`'s `hSource` premise, eliminating both local representation aliases.

The import check passed.  The direct representation composition closes, and the next placeholder is the local `hCapacityFit32`.  The same section also contains local aliases for `wordAddress_toNat` and its derived root bound.  The direct recipe and inspected `fixed-array-allocation` entry name the exact accessor and page-preservation declaration.  I will apply those declarations at the store and copy premises and derive the 32-bit capacity fit inside `bounded_target_fit`'s final argument.

The import check passed.  The direct root and 32-bit capacity proofs close.  Lean now locates the first placeholder at the local `hBumpFacts` alias, whose only consumers are `wordAddress_toNat` and `rootToNat` projections.  I will apply the checked `Allocation.bumpFacts` constructor directly at each projection.  This removes the alias without expanding the allocator facts or changing their premises.

The import check passed.  All direct `bumpFacts` projections elaborate.  Lean now attributes the remaining placeholder to `wp_iff_cons`'s `hBody` at the capacity conditional.  The residual after simplifying that conditional already has the exact `region_spec_withTail` program.  The intervening `change wp _ (region ... ++ _) _ _ _ _` introduced five synthetic program and state placeholders.  I will remove that adapter and let the checked region theorem's conclusion unify with the concrete WP goal.

The import check passed, but removing the `change` adapter leaves the same `wp_iff_cons.hBody` placeholder.  The checked region application still has no reported premise or conclusion mismatch.  The positional `refine wp_iff_cons rfl ?_` therefore failed to register its opaque placeholder as the continuation tactic goal.  I will use `apply wp_iff_cons` and discharge its equality premise in an explicit bullet, forcing Lean to create the body as the remaining goal.

The import check passed.  The explicit `apply wp_iff_cons` closes `hBody`, and the diagnostic now bubbles to the preceding `hCapacityFit` wrapper while displaying the allocator-and-copy continuation.  Inspection of the remaining successful-branch syntax found one explicit synthetic frame argument in `allocFrame_get_root 9 0 _ ...`.  The inspected allocation entry's accessor has `base` determined by its conclusion.  I will use tactic-level `apply` with only `offset` and `tail`, then prove the two frame-length premises; occurrence matching will determine `base`, `heapTop`, and `capacity`.

The import check passed.  Occurrence-driven `allocFrame_get_root` elaborates, but the continuation placeholder remains at the same wrapper.  The only explicit synthetic syntax left after that wrapper is `Wasm.Mem.read64_write64_same ..` in the target-header premise.  I will use tactic-level `apply` so the exact post-write memory, address, and value are determined from the premise goal instead of anonymous term placeholders.

The import check passed.  Tactic-level `read64_write64_same` also elaborates, leaving the same allocator continuation placeholder.  The remaining program split inferred by a checked theorem is `region_spec_withTail`'s `rest`.  I will supply the exact tail: `lengthStoreLocalProgram 14 13`, the erase-copy program, and the final `localGet 14`.  This fixes the outer list-append boundary before the already explicit store and copy tails.

The import check passed, but the exact region tail leaves the same continuation placeholder.  No explicit term hole remains after `hCapacityFit`.  The final copy continuation still uses occurrence matching for `counterFrame_get_ne`, whose `hCounter` proof appears in the dependent frame term.  The next simplifier already unfolds `counterFrame`, `allocFrame`, and `Locals.get`; I will remove the rewrite and let that concrete simplification reduce the root getter, avoiding a dependent theorem instantiation.

The import check passed.  Removing `counterFrame_get_ne` exposed an ordinary residual instead of the latent continuation metavariable: `eraseIdxProgram_spec` needs `(allocFrame 9 ... heapTop capacity).validIndex 15`.  I searched `PROOF_RECIPES.json`, the selected package indexes, and `PROOF_KIT_SOURCE` with `allocFrame.*validIndex|validIndex.*allocFrame|counter.*validIndex`.  The inspected `fixed-array-erase-copy` entry names `counterFrame_validIndex`, but that theorem preserves validity after constructing a counter frame and assumes the base-frame validity now required.  No recipe, index entry, or permitted ProofKit declaration provides an exact `allocFrame` validity accessor.  I will pass `hCounter` explicitly to `eraseIdxProgram_spec`, reducing `Wasm.Locals.validIndex` and `FixedArrayAllocatorWindow.allocFrame` only after this accessor search.  I will then restore `counterFrame_get_ne`; its occurrence will contain the explicit proof rather than a synthetic argument.  Progress means closing the successful branch and reaching no residual theorem goal.

The import check passed.  The explicit `hCounter` proof eliminates the synthetic metavariable.  Lean now exposes the `apply` tactic's generated premise order: it places the four local-distinctness and frame-projection goals before `hErase` and `hSource`, which it postpones because later premises depend on them.  The existing bullets still followed declaration order, so the first two facts were checked against `8 ≠ 15` and `14 ≠ 15`; all later errors were positional consequences.  I will reorder the same checked facts to Lean's displayed goal order, without changing any premise proof.  Progress means reaching `hDone` with the explicit counter proof available to `counterFrame_get_ne`.

The import check passed.  All four distinctness goals, frame projections, target bounds, target header, and separation premise now close.  Lean schedules `hDone` before the postponed proof-only `hErase` and `hSource` goals, so the last three positional bullets remain shifted.  I will pass `hErase` and `hSource` by name in the theorem application and remove their postponed bullets.  This leaves `hDone` as the sole continuation goal and fixes the remaining ordering ambiguity.

The import check passed.  Naming `hSource` made theorem inference unfold `FixedArrayResult.writeLength` into its record update before generating `hTargetFitMemory` and `hTargetLength`.  The checked `writeLength_pages` theorem and the focused unfolding step therefore had no syntactic occurrence to match.  I will keep only `hErase` named, leave `hSource` as the final postponed premise, and discharge it after `hDone`.  Conclusion unification then retains the checked `writeLength` expression throughout the target-bound and header premises.

The import check and the `LeanExeGen.GeneratedRb9ad29e25c8033e5.ArtifactResult` build both succeeded.  The proof uses the shared length-dispatch tactic, `FixedArrayFindIdxEq.program_spec`, `EncodedIndexDecoder.program_spec`, the allocator-window region theorems, `FixedArrayResult.lengthStoreLocal_spec`, `FixedArrayResult.lengthStore_spec`, `FixedArrayResult.finishProgram_spec`, `FixedArrayCopy.eraseIdxProgram_spec`, the allocation framing declarations, and `FixedArrayResult.emptyStore_at`.  The bounded branch proves the annotated early-exit search and both checked erase-copy loops; the oversized branch proves the empty allocation.  No residual goal remains.
