# Proof Journal

The unchanged candidate failed its first Lean check at the intended starter boundary.  After `RuntimeReady` supplied the represented input, globals, allocation bounds, memory bounds, and page bound, Lean left one `TerminatesWith` goal for function 0.  The next step is to inspect the exact recipes and the `leanexe-core` knowledge indexes for a theorem or tactic matching that execution goal before expanding any instruction region.

Knowledge retrieval started at `KNOWLEDGE/forest.json`, which selects the versioned `leanexe-core` package.  I searched its arrays, compiler-motifs, proof-construction, loops, allocation, and memory indexes with `rg -n -i 'length-dispatch|find-idx-eq|erase-copy|TerminatesWith|wp_fixed_array_length_le_dispatch_from|FixedArrayLengthDispatch'`.  The results identify four relevant entries: `fixed-array-length-dispatch` for the current entry goal, followed by `fixed-array-find-idx-eq`, `fixed-array-capacity`, and `fixed-array-erase-copy` for the bounded branch described by the recipes and annotations.

I inspected `entry.json` and `README.md` for the four entries in the `leanexe-core` catalog.  `fixed-array-length-dispatch` matches the current public-to-body conversion and has a structured tactic whose goal shape, represented-array premise, and `leanexe.array.length-dispatch.v1` annotation match once `Wasm.TerminatesWith.of_wp_entry_for` converts the goal.  The other three entries match the checked search, allocation-capacity, and copy regions, so I retain them for the branch continuations rather than rejecting them before their premises are visible.

I searched the permitted workspace with `rg -n 'of_wp_entry_for|wp_fixed_array_length_le_dispatch_from|function_eq' . --glob '*.lean' --glob '*.md' --glob '*.json'` to find an exact local use of the entry conversion and tactic.  The search found the selected knowledge entry, recipe, tactic definition, and generated body equality, but no artifact proof or worked use.  I will therefore follow the selected entry directly: apply `of_wp_entry_for`, rewrite with `function_0_length_dispatch_0_function_eq`, and invoke the structured length-dispatch tactic before handling its premises.

The import check accepted the first proof edit, and the Lean check reached a body-level `wp` goal.  `Wasm.TerminatesWith.of_wp_entry_for` applied, but rewriting the generated body equality failed because the target still displayed `func0Def.body` instead of `func0`; the structured tactic did not run.  The `fixed-array-length-dispatch` README names this exact presentation issue and calls for an explicit `change` to `Wasm.wp _ func0 _ _ _ _`, so the next edit will add that reduction boundary and preserve the direct recipe.

Before reducing any frame shape, I searched the relevant `leanexe-core` indexes with `rg -n -i 'toLocals|frame projection|params|locals length|branchFrame_params|branchFrame_locals_length|branchFrame_values|local-frame-projection'`.  The results confirm the branch-specific projections already named by the recipes and also identify `fixed-array-allocation`, whose exact shifted-window allocator and post-allocation root projections will apply after the constant-capacity prefix; I inspected that entry's `entry.json` and `README.md`.  The generic `local-frame-projection` index record does not address the initial `Function.toLocals` reduction or this annotation kind, so I did not inspect its entry and will use definitional reduction only if the dispatch theorem leaves those initial frame premises.

The second import check succeeded, and the exact length-dispatch command applied after the explicit body `change`.  It left five reducible premises for the initial frame and numeric bounds, plus valid and invalid branch `wp` goals under `FixedArrayEqNode.branchPost`; this is the expected postcondition from the recipe.  The direct command therefore counts as used, and the next retrieval checkpoint concerns the two branch programs rather than any failure of the dispatch boundary.

For the branch checkpoint, I searched the arrays, allocation, memory, proof-construction, and compiler-motifs indexes with `rg -n -i 'fixed-array-result|result construction|empty array|empty result|erase reconstruction|eraseIdx!|input preserved|branchPost|finishProgram|lengthStore'`.  I inspected `fixed-array-result` because its `emptyStore_at`, store-region, and root-transfer theorems match the invalid branch and common suffix, and I inspected `fixed-array-erase-reconstruction` because it states the final representation needed after the checked copy loops.  Both entries remain selected: the result theorem handles emitted stores and return-local transfer, while the erase theorem is consumed directly by `FixedArrayCopy.eraseIdxProgram_spec` if that stronger direct boundary matches.

The third import check succeeded.  The Lean check discharged the parameter, operand-stack, positivity, and maximum-size premises, but `decide` did not reduce the local-list length through `func0Def.toLocals`; the following blanket `intro` then failed on that non-function goal, while the invalid branch acquired its size hypothesis.  The selected dispatch README names `func0Def`, `Function.toLocals`, `Function.numParams`, and `ValueType.zero` for this exact local-index residual, so I will use that focused simplification and give each branch an explicit proof block.

The fourth import check succeeded, and the invalid branch matched `FixedArrayAllocatorWindow.constantCapacityRegion_spec_withTail`, then reached the checked length-store and finish-region theorems.  Its remaining errors are frame-presentation mismatches: the root projection needs premises for the allocator's base frame, the destination bounds need explicit rewrites by the named frame lengths, and the final suffix sees the finish frame with its operand stack cleared by `branchPost`.  I will name the capacity and allocation frames, derive each projection once, and transport `finishFrame_return_get` across the operand-stack replacement through `Wasm.Locals.get`.

The fifth import check succeeded.  The named-frame revision resolved the allocator root, frame bounds, and cleared-stack getter, and the invalid branch reached the final output existential; a second `simp only [wp_simp]` then failed because the first focused simplification had already performed that reduction.  I will remove that redundant command, leaving the invalid branch composed from the checked capacity, allocator, length-store, finish, and empty-array declarations.

The sixth import check succeeded, and `FixedArrayFindIdxEq.program_spec` matched the valid branch's exact first twelve instructions.  It replaced the early-exit loop with the indexed `noneFrame` and `someFrame` continuations promised by the recipe, while the invalid branch retained one page-bound goal because `emptyStore_at` asks about the allocator store before the length write.  That bound uses `FixedArrayAllocator.allocStore_pages` alone; the attempted `writeLength_pages` rewrite had no matching term and will be removed.

For the new search-continuation goals, I queried the relevant indexes with `rg -n -i 'noneFrame|someFrame|encodedIndex_sub_one|encodedIndex_ne_zero|findIdx\?|no-match|first matching index'`.  The query returned only the inspected `fixed-array-find-idx-eq` entry, whose structured frame projections and encoded-index declarations match the successful continuation exactly.  I will use its `encodedIndex_ne_zero` and `encodedIndex_sub_one` declarations before reducing the surrounding option branches, and the no-match continuation will use the folded `noneFrame` with focused straight-line execution.

The seventh import check succeeded, and removing the unrelated page rewrite closed the invalid branch.  Focused reduction of the no-match continuation stopped at the concrete local-zero lookup before branch selection, while the successful continuation remained folded at `someFrame` as intended.  The dispatch proof kit's focused simplifier includes `List.getElem?_cons_zero` for this exact lookup, so I will add that declaration to the no-match reduction rather than unfold the unused successful branch.

The eighth import check succeeded.  Adding `List.getElem?_cons_zero` reduced the no-match continuation through the option-word comparison and left the first structured `iff` with a known zero condition; the invalid branch now has no residual goal.  I will apply `wp_iff_cons` twice, select the zero-producing option-normalization branch and then the no-match branch, and use the represented input to close the final suffix.

The ninth import check succeeded.  Both structured selections in the no-match path reduced, and its sole residual is the definitional change from `FormalSpec.UInt64ArrayAt` back to `UInt64Array.At`, which the existing `hArray` proves.  For the successful path, I will retain `someFrame`, apply its three named frame projections, and use `encodedIndex_ne_zero` to select the option branch before decoding the index with `encodedIndex_sub_one`.

The tenth import check succeeded and closed the no-match path after the explicit representation change.  In the successful path, the three `someFrame` projections and `encodedIndex_ne_zero` reduced the option word setup to the first `iff` with condition one, without unfolding the search frame.  I will select that branch, execute its constant result, select the following condition-zero successful branch, and reduce the next straight-line segment to the index-decoding `iff`.

The eleventh import check succeeded.  The two option selections reduced as expected, and the next focused execution stopped at the parameter-zero getter while copying `inputPtr` into the generated source local.  This is the same concrete head lookup as the no-match path, so `List.getElem?_cons_zero` applies before the index-decoding `iff`.

I searched the permitted proof-kit mirror with `rg -n 'lt_one|< 1|one.*iff' PROOF_KIT_SOURCE/Project/ProofKit LeanExeGen/GeneratedRb9ad29e25c8033e5 --glob '*.lean'` for an exact declaration matching the encoded-word comparison against one.  The search found no relevant accessor or arithmetic theorem in the allowed mirror, while the selected search entry supplies `encodedIndex_eq_ofNat_succ`.  I will rewrite the encoded word with that theorem, compare `UInt64.toNat` values, and use the result to select the subtraction branch.

The twelfth import check succeeded and reached the nested index-decoding `iff` with condition zero.  The proof of the comparison failed only because `omega` retained the opaque-looking term `(1 : UInt64).toNat`; its definitional equality to one resolves that presentation.  After recording that equality, the next step is to select the subtraction branch, apply `encodedIndex_sub_one`, and execute the generated source-length comparison with `hArray`'s checked read and bound facts.

The thirteenth import check succeeded.  The encoded-index comparison and subtraction now prove, and focused execution reached the generated input-length load after storing the decoded index.  The load boundary presents the out-of-bounds test as a strict inequality, so I will select its safe branch with `Nat.not_lt.mpr hLengthBound`, rewrite the checked pointer and length read, and select the in-bounds index branch with `hIndexEncodedLt`.

The fourteenth import check succeeded.  After selecting the safe load branch, the target already contained the outer `iff` with condition one because the preceding focused execution had used `hInputAddress` and `hLengthRead`; the explicit rewrite therefore found no occurrence.  I will remove only that redundant rewrite, retain the checked bound selection, and continue with the `hIndexEncodedLt` branch.

The fifteenth import check succeeded, and the first in-bounds selection exposed the generated second read of the source length at the start of the removal path.  Its residual goal has the same checked address condition and read value as the preceding load, followed by the subtraction, capacity, allocator, length-store, and copy regions.  I will select the safe memory branch, rewrite it with `hInputAddress` and `hLengthRead`, and select the repeated index-bound branch before constructing the allocator boundary.

The sixteenth import check succeeded.  The repeated source-length read and index-bound branch reduced to the exact arithmetic prefix that computes the result length, prefix length, suffix length, and aligned allocation capacity before the allocator window.  The next edit will execute only this finite prefix and stop at its capacity comparison; progress means that Lean exposes the allocator region with capacity `UInt64.ofNat (8 * input.size)` and the derived frame remains available for the checked allocator theorem.

At this residual-goal checkpoint, I searched the selected package indexes and permitted proof-kit mirror with `rg -n -i 'result length|prefix length|suffix length|erase.*pre|prepare.*erase|localSet 13|localSet 11|localSet 12|eraseIdx.*capacity|dynamic.*capacity'`.  The indexes returned only the already inspected erase-copy and erase-reconstruction entries, and the proof-kit mirror contains no declaration for the four arithmetic local assignments.  The checked capacity theorem does accept a proof-level runtime length, so I will reduce the assignments and refold the following generated instructions as `FixedArrayCapacity.constantProgram` instead of inspecting another entry.

The seventeenth import check succeeded.  Focused execution normalized all arithmetic assignments and the capacity computation, then stopped at the minimum-eight-byte `iff`; the allocator program remains folded as the following instruction region.  Since the successful search implies a positive input length, the aligned capacity is at least eight, so I will select the empty branch and apply the exact shifted allocator theorem to the residual program.

The eighteenth import check succeeded, but my branch proof targeted the inner unsigned comparison while `wp_iff_cons` asks for the generated Boolean word's nonzero test.  Lean reported the exact outer condition and left the continuation through an empty program.  I will first prove a named natural-value equation for the aligned capacity using the permitted `Memory.toNat_sub_of_le` and `UInt64.toNat_*` declarations, use that equation to simplify the complete Boolean condition, and then reduce the empty continuation.

The nineteenth import check succeeded but exposed two arithmetic presentation errors.  `Memory.toNat_sub_of_le` concludes with the operands' `toNat` expressions, so the result-length proof needs a `calc` step before rewriting those expressions; the capacity equation also requires proving that the small operations do not wrap, for which the established bound `1 ≤ input.size ≤ 8` permits finite interval normalization.  The Boolean branch itself selected once its named premises were admitted by the failed subgoals, and its continuation confirms that the exact allocator begins immediately after the empty branch, so the next edit will change directly to that checked region instead of simplifying allocator instructions.

The twentieth import check succeeded.  The `calc` shape now matches `Memory.toNat_sub_of_le`, but `omega` still sees `(1 : UInt64).toNat` as opaque, and interval normalization leaves the analogous `toNat` forms for seven and eight plus the named subtraction equation unused.  I will state the three numeral projections as definitional equalities and include them with `hResultLengthNat` in each finite normalization branch; these changes preserve the exact allocator application already present after the arithmetic facts.

The twenty-first import check succeeded, and all arithmetic facts now close.  The only diagnostic occurs before the allocator application: the empty selected `iff` branch remains represented as `wp module []` around its fallthrough continuation, so a direct `change` cannot skip that semantic wrapper.  The permitted proof-kit implementations use `rw [wp_nil]` followed by normalization of `List.take_zero`, `List.drop_zero`, and `List.nil_append` at this boundary; I will apply that exact reduction before changing to the allocator region.

The twenty-second import check succeeded, and the shifted allocator theorem discharged its frame, capacity, page, memory-mode, and global premises.  Its memory-fit premise did not rewrite `hFind` because the theorem hypothesis uses `FixedArrayFindIdxEq.predicate 0` while the specification contains the extensionally equal lambda, so I will name that exact converted search equation and reuse it in both reserve and expected-result reductions.  The allocator continuation now starts with a result-length store whose value comes from local 13; `FixedArrayResult.lengthStore_spec` requires an immediate constant and therefore does not match this residual program.

I searched the arrays, memory, compiler-motifs, proof-construction, and allocation indexes, the recipes, and the permitted proof-kit mirror with `rg -n -i 'dynamic length|length local|localGet.*length|result length store|lengthStore|store.*header|header.*local'`.  The search returned only the already inspected `fixed-array-result`, `fixed-array-allocation`, and `fixed-array-erase-copy` entries, and no declaration executes a header store from a local getter.  I will add a small checked `lengthStoreLocal_spec` lemma that follows the existing `FixedArrayResult.lengthStore_spec` proof with a length-local premise, then preserve the post-allocation frame as a named boundary for the erase-copy theorem.

The twenty-third import check succeeded.  The new local-length lemma reaches its second getter, but that getter sees a frame whose operand stack contains the wrapped root, so the original `hLength` does not rewrite syntactically even though `Locals.get` ignores the operand stack.  I will add the same stack-insensitive getter transport used by `FixedArrayResult.payloadStore_spec`, then resume the exact search-equation conversion and named allocator-frame construction.

The twenty-fourth import check succeeded, including the complete `lengthStoreLocal_spec` lemma.  The attempted `simpa` conversion of `hFind` still retained the partially applied `predicate 0`, because simplification did not eta-expand that definition to the specification lambda.  I will copy the equation and use an explicit definitional `change` on its predicate, which preserves the compiler theorem's original `hFind` while producing the exact equation needed by `heapReserveBytes` and `expected`.

The twenty-fifth import check succeeded.  The explicit predicate `change` produced `hFindSpec`, and the expected-result equation reduced, but the allocator fit premise still cited the original `hFind` in its simplifier list.  I will replace that one argument with `hFindSpec`; the remaining continuation and all other allocator premises are unchanged.

The twenty-sixth import check succeeded.  `hFindSpec` reduced the reserve branch, leaving only the associativity difference between `heapTop + (48 + 8 * size)` and `(heapTop + 48) + 8 * size`; `Nat.add_assoc` resolves it.  Before applying the new local-length store theorem, I will now name the computed capacity, exact pre-allocation local frame, allocator store, and allocator frame so that root, source, length, prefix, suffix, and counter projections remain stated against stable objects.

The twenty-seventh import check succeeded, and the Lean check discharged every allocator premise after the associativity rewrite.  The sole residual goal starts with the four-instruction dynamic length store, followed by `FixedArrayCopy.program 1 8 14 11 12 15` and the final root getter.  I inspected the exact permitted declarations `FixedArrayCopy.eraseIdxProgram_spec`, `FixedArrayCopy.counterFrame_get_ne`, `FixedArrayPairResult.input_preserved_by_alloc`, `UInt64Array.At.write64After`, and `Allocation.BumpFacts.wordAddress_toNat`; they supply the stable boundary for the next composition without instruction-level loop reasoning.

The twenty-eighth import check succeeded.  The dynamic length-store theorem applied and left exactly `FixedArrayCopy.program 1 8 14 11 12 15 ++ [.localGet 14]`; the only earlier arithmetic failure was that `omega` did not unfold the constant value of `UInt64.size` while proving the result length fits in 64 bits.  I will state that constant bound explicitly, preserve the represented input across allocation and the target-header write, and apply `eraseIdxProgram_spec` with the four generated local projections.

The twenty-ninth import check succeeded, while the Lean check stopped at the explicit record-update presentation used to restate `copySt`; the parser treated `allocSt.mem.write64` as the complete `mem` field before its arguments.  The checked `UInt64Array.At.write64After` theorem otherwise matched and introduced `hSourceCopy`, and no copy-theorem premise was elaborated past the syntax error.  I will unfold `copySt` and `FixedArrayResult.writeLength` at the representation goal instead of writing the record update explicitly.

The thirtieth import check succeeded, and unfolding the two definitions proved that the represented input survives the target-header write.  `apply eraseIdxProgram_spec` inferred the valid-counter proof from the local context, so my following explicit `hCounter` bullet was assigned to the first distinct-local premise and shifted every later bullet; elaboration then timed out while attempting to decide a frame equation.  I will pass `hCounter := hCounter` in the theorem application and remove the redundant bullet, leaving the four numeric distinctness premises to `decide`.

The thirty-first import check succeeded, and `eraseIdxProgram_spec` accepted every source, frame, length, fit, and disjointness premise.  Its final `hDone` callback receives the completed `UInt64Array.At` representation, but the broad simplifier timed out while expanding the generated suffix continuation and the deeply defined allocation frame together.  I will divide this boundary by executing only the final root getter with `wp_localGet_cons` and `wp_nil`; the resulting concrete suffix goal will determine which stable frame projections need names before any further reduction.

The thirty-second import check succeeded, but `simp only` still invoked simplifier normalization across the deeply nested continuation before completing the single getter rewrite.  The timeout remains at the final callback, while every copy premise and the reconstructed result representation remain established.  I will use ordered `rw` steps for `wp_localGet_cons`, `hRootAfter`, and `wp_nil`, which rewrite the three exact heads without running the simplifier over the continuation.

The thirty-third import check succeeded.  Ordered rewriting completed `wp_localGet_cons` and `hRootAfter`, exposing a `match some` whose selected arm contains `wp module []`; the same `rw` call then could not find `wp_nil` because rewriting does not reduce that match before searching.  I will separate the operations: first rewrite the getter, then use focused simplification with `wp_nil`, allowing iota reduction to expose the empty program without revisiting the deep getter frame.

The thirty-fourth import check succeeded, but even the isolated `simp only [wp_nil]` normalized the surrounding generated continuation and exhausted the heartbeat limit.  The residual continuation is the three remaining instructions of `FixedArrayResult.finishProgram 14 5 7`, followed by the checked length-dispatch suffix `[.localGet 7]`; the invalid branch already proves the same finish-and-suffix shape with `finishProgram_spec` and `finishFrame_return_get`.  I will refold the callback as that finish program, name the post-copy counter frame, and use the same checked finish boundary before transferring `hResult` through `hExpected`.

The thirty-fifth import check succeeded, and the named post-copy frame projections elaborated without reducing the allocation frame.  Refolding as `finishProgram` failed because the enclosing WebAssembly `if` first normalizes the branch operand stack with `List.take 1 (List.take 1 values)`, so the callback is not definitionally the ordinary four-instruction program even though both execute the same local assignments.  I will retain `doneFrame`, rewrite the root getter, explicitly change the selected arm to `wp module []` over the one-value frame, and only then reduce `wp_nil`; this isolates the branch-result normalization from the copy proof.

The thirty-sixth import check succeeded, and the staged getter and empty-program rewrites exposed the concrete fallthrough arm without a timeout.  Its remaining computation takes the one result word, stores it in local 5, reads it, stores it in local 7, clears the branch stack, executes the annotated suffix getter, and checks the public result assertion.  The named parameter and local lengths now keep all index tests independent of the allocation-frame definition, so focused simplification with those projections, `Locals.get`, `Locals.set?`, the suffix equation, `hExpected`, and `hResult` should close this final callback.

The thirty-seventh import check succeeded, and focused simplification executed the entire finish and suffix path within the heartbeat limit.  The only residual goal is the definitional presentation change from `FormalSpec.UInt64ArrayAt final (heapTop + 48) (input.eraseIdx! index)` to the proof kit's `UInt64Array.At` proposition already supplied by `hResult`.  I will make that explicit `change` and close with `exact hResult`; no execution or reconstruction goal remains after it.

The thirty-eighth import check succeeded, and the independent `LeanExeGen.GeneratedRb9ad29e25c8033e5.ArtifactResult` build accepted the proof.  The proof uses the annotated length dispatch, the early-exit `FixedArrayFindIdxEq.program_spec`, shifted allocator-window theorems, `FixedArrayPairResult.input_preserved_by_alloc`, the local checked dynamic length-store lemma, `FixedArrayCopy.eraseIdxProgram_spec`, counter-frame projections, and the fixed-array result declarations for the invalid branch.  The final theorem has no residual goal, and the candidate remained unchanged after this successful import-and-build pair.
