# Proof Journal

## 2026-08-26: Initial check

The required initial build failed in `Behavior.lean` at the theorem starter's final goal.  After the `RuntimeReady` decomposition and the supplied array and global facts, the residual goal is `TerminatesWith env module 0 initial [Value.i64 inputPtr]` with one returned pointer representing `FormalSpec.expected input`.  The check finished in 20.6 seconds and produced a Lean diagnostic.  No candidate file had been edited.

I started retrieval with `KNOWLEDGE/forest.json`.  It selects the versioned `leanexe-core` package at `KNOWLEDGE/packages/leanexe-core`.  `PROOF_RECIPES.json` names exact regions for the length dispatch, equality search, encoded-index decoder, and erase copy.  I will attempt the length-dispatch command `wp_fixed_array_length_le_dispatch_from hArray at 8, 8` before reducing that region.  Progress means replacing the public execution goal with the valid and invalid branch continuations specified by the recipe.

The first index query was `length-dispatch|find-idx-eq|encoded-index|erase-copy|TerminatesWith|RuntimeReady|fixed array|array copy|early-exit` over the `leanexe-core` category `tools.jsonl` files.  It returned the four annotation entries named by the recipes, plus capacity and allocation-related entries.  I inspected `leanexe-core/fixed-array-length-dispatch`.  Its structured tactic matches the expected body-level `Wasm.wp` goal after `Wasm.TerminatesWith.of_wp_entry_for`; I selected the entry for use.  Its fallback declaration is `Project.ProofKit.FixedArrayLengthDispatch.leProgram_spec`.

## 2026-08-26: Length dispatch

The import check succeeded.  The next build failed after 4.4 seconds with seven classes of premises.  The direct command matched the annotated region and produced initial frame parameter, value-stack, local-bound, and constant-bound facts, followed by bounded and over-length branch goals.  This is the expected improvement, so `leanexe-core/fixed-array-length-dispatch` remains used.

Before reducing the initial frame projections, I searched `toLocals|local frame|params|locals length|operand values|frame projection|validIndex` in the `proof-construction`, `compiler-motifs`, and `arrays` indexes.  I inspected `leanexe-core/local-frame-projection`.  Its declarations concern an existing frame's field equality, value-stack replacement, and combined-to-internal getters.  They do not state the projections of `func0Def.toLocals`, so I rejected this entry for the present initial-frame goals.  The projections and numeric bounds must reduce from the generated `func0Def` definition.

I inspected `leanexe-core/fixed-array-find-idx-eq` and `leanexe-core/encoded-index-decoder`, both returned by the first query and named by exact recipes.  The bounded branch starts with `FixedArrayFindIdxEq.program 8 0`; its theorem accepts the current represented array and 12 trailing locals, and exposes callbacks for `findIdx? = none` and `findIdx? = some index`.  I selected it for use.  The successful callback's artifact tail contains the checked decoder `EncodedIndexDecoder.program 2 8 4`; the decoder entry supplies the exact frame and decoded-index getter, so I selected that entry for use after rewriting with `function_0_encoded_index_0_tail_eq` as required by the recipe.

The next edit will reduce only the five generated entry-frame and constant premises, then apply `FixedArrayFindIdxEq.program_spec` to the bounded branch.  Progress means the bounded branch becomes separate `findIdx?` none and some continuations while the over-length branch remains unchanged.

The import check succeeded.  The build failed after 4.5 seconds because `decide` rejected the local-count goal while `inputPtr` remained free.  Reflexivity discharged the parameter and value-stack projections, and `decide` discharged the positive-index fact.  The length-dispatch entry README names `func0Def`, `Function.toLocals`, `Function.numParams`, and `ValueType.zero` for this generated local-bound normalization.  I will use that focused reduction on the local-count goal; the `local-frame-projection` declarations still do not apply.

The focused reduction succeeded.  The next import check succeeded, and the build failed after 2.8 seconds only at the bounded and over-length semantic branches.

Applying `FixedArrayFindIdxEq.program_spec` to the bounded branch succeeded.  Its exact `branchFrame_params`, `branchFrame_locals_length`, and `branchFrame_values` accessors, followed by focused reduction of the generated initial local list, discharged every producer-frame premise.  The build failed after 3.0 seconds at the expected `findIdx? = none` and `findIdx? = some index` callbacks, plus the unchanged over-length branch.

At this residual checkpoint I searched `branchPost|localGet 7|result suffix|result pointer|represented result|UInt64ArrayAt|unchanged input|identity result`, then `fixed-array-result|result-construction|resultProgram|singleton result|array result|root transfer` in the array, proof-construction, memory, and compiler-motif indexes.  The first query returned no entry.  I inspected `leanexe-core/fixed-array-result` from the second query.  Its `finishProgram_spec` covers a four-instruction root transfer, which does not match the no-match branch and one-instruction public suffix.  I rejected that declaration for the no-match continuation.  I selected the entry's `emptyStore_at` declaration for the over-length branch, whose artifact allocates and writes an empty array.

For the no-match callback, I will execute the short scalar prefix one instruction at a time, select the branch determined by the encoded zero, set return local 7 to `inputPtr`, and close the public suffix with the unchanged `hArray`.  Progress means eliminating the no-match callback without opening the removal branch.

The import check succeeded.  A single `wp_run` reduced the first `localSet`, `localGet`, constant, equality, and zero-test together, but it unfolded `noneFrame` only far enough to leave nested local-set bounds and getters as one large match goal.  The build failed after 4.2 seconds at that presentation boundary.  The command did not reach a semantic residual goal, so I rejected this use of `wp_run`.  The relevant knowledge indexes expose no `noneFrame` projection declarations.  I will use a focused `wp_simp` reduction with the checked `noneFrame`, `branchFrame`, and generated local-list definitions, restricted to this short no-match continuation.

The import check succeeded.  The focused reduction normalized the complete producer frame and reduced the first getter to a concrete list lookup, then the build failed after 4.7 seconds at `[Value.i64 0, ...][0]?`.  This is a list-access normalization gap.  I will add the exact `List.getElem?_cons_zero` simplification and name the predicate-normalized `findIdx? = none` fact for the expected-array branch.

The list lookup normalized.  The build failed after 3.7 seconds at the first structured `iff`, with the frame and expected array reduced to concrete values.  I will use `wp_iff_cons` twice: encoded zero selects the decoder's zero arm, and the resulting Boolean selects the unchanged-input arm.  The remaining scalar root transfer and public suffix will use `FixedArrayEqNode.branchPost` and `hArray`.

Both structured branches reduced as expected.  The build failed after 4.5 seconds at the single residual proposition `FormalSpec.UInt64ArrayAt initial inputPtr input`.  The supplied `hArray` has the definitionally equal ProofKit form because the starter changed its type at entry.  I will close this final proposition with `exact hArray` instead of asking the simplifier to use it as a rewrite.

That exact use closed the no-match callback.  The import check succeeded, and the build failed after 4.1 seconds only at the first-match callback and the over-length branch.

For the first-match callback, the inspected search entry and exact recipe name `someFrame_params`, `someFrame_locals_length`, and `someFrame_values`; I will use those accessors rather than unfold the producer frame.  `encodedIndex_ne_zero` will select the removal arm.  After the arm's source-pointer assignment, I will change the residual program to the generated resolved tail, rewrite with `function_0_encoded_index_0_tail_eq`, and apply `EncodedIndexDecoder.program_spec` before reducing any decoder instruction.  Progress means reaching the decoder theorem's semantic frame or its explicit premises.

The import check succeeded.  The accessor-based reduction and `encodedIndex_ne_zero` produced a concrete `i32 1` at the first structured branch.  The build failed after 4.6 seconds at that branch.  I will select its nonzero arm, reduce only the scalar comparison between the two structured branches, and select the removal arm.  I will stop before the decoder prefix.

The import check succeeded.  Both branch selections worked, and the build failed after 4.2 seconds with the removal arm starting at `localGet 0; localSet 3`, followed by the untouched decoder region.  I will execute those two source-pointer instructions with their atomic rules only.  I will then change the program to the generated resolved tail, rewrite it with `function_0_encoded_index_0_tail_eq`, and apply the decoder recipe theorem.  A successful application should expose only the decoder input getter, local bounds, and continuation from `resultFrame`.

The import check succeeded.  The two atomic source-pointer instructions reduced without entering the decoder.  The change to the resolved artifact tail and rewrite with `function_0_encoded_index_0_tail_eq` succeeded.  `EncodedIndexDecoder.program_spec` matched and produced its encoded-word getter, scratch and destination bounds, and continuation from `resultFrame`.  The build failed after 5.4 seconds at those expected premises.  I will use `someFrame` parameter and local-length accessors, `Wasm.Locals.get`, `Wasm.Locals.validIndex`, and the two known local updates to discharge the five frame premises.  No more decoder instructions need reduction.

Four frame bounds discharged.  The build failed after 5.3 seconds because the encoded getter simplification left two bounds on the length-dispatch frame; `FixedArrayLengthDispatch.branchFrame_locals_length` states both.  The continuation now begins at the folded decoder `resultFrame` and the artifact tail after index 8.

Before projecting the preserved source local from `resultFrame`, I searched `EncodedIndexDecoder.*resultFrame.*get|resultFrame_get_of_ne|preserved getter|source local|resultFrame_decoded` in `PROOF_RECIPES.json` and the proof-construction and compiler-motif indexes.  The exact decoded getter and the three frame-shape accessors are present.  No decoder theorem projects an unrelated preserved getter.  The index also returned a fold-specific accessor entry whose annotation kind and frame definition do not match this decoder, so I did not inspect or use it.  I will name the decoder input frame from the two checked local updates.  This gives the decoder accessors a stable frame argument and confines the preserved source getter reduction to that small frame.

The named decoder input matched the artifact state, but the build failed after 6.6 seconds because the local `searchFrame` abbreviation remained folded in all five decoder premises.  The residuals are the expected parameter length, local length, and two update-index comparisons.  I will establish `searchFrame.params = [.i64 inputPtr]` and `searchFrame.locals.length = 23` once with the exact search and length-dispatch accessors, then use those facts in all decoder premises.

The import check succeeded.  The build failed after 6.2 seconds while establishing the two named search-frame facts: the simplifier used the search accessors but stopped at `branchFrame.params` and `branchFrame.locals.length`.  I omitted the exact length-dispatch accessors from those two focused proof lists.  I will add `branchFrame_params` and `branchFrame_locals_length`; the downstream decoder premises already reduce from the named facts.

The added length-dispatch accessors discharged every decoder premise.  The import check succeeded, and the build failed only at the decoder continuation and the over-length branch.  The first residual is the exact artifact tail after decoder instruction 8, with the frame `EncodedIndexDecoder.resultFrame decoderInput 8 4 (FixedArrayFindIdxEq.encodedIndex index)`.

At this checkpoint I searched `theorem .*spec|def .*Frame|_params|_values|_locals|constantCapacityRegion|allocator` in the mirrored allocator-window source after opening `leanexe-core/fixed-array-allocation`.  I also inspected `leanexe-core/fixed-array-capacity`, `leanexe-core/fixed-array-erase-copy`, and `leanexe-core/fixed-array-erase-reconstruction`, all returned by the original annotation query or named by recipes.  The valid branch computes capacity from a runtime length, so the constant-capacity composition does not match it.  I rejected `fixed-array-capacity` for that branch, while retaining its constant-capacity composition for the over-length branch.  `FixedArrayAllocatorWindow.region_spec` matches the shifted allocator at offset 9.  `FixedArrayCopy.eraseIdxProgram_spec` matches the checked copy region and reconstructs `input.eraseIdx! index`; I selected the allocation, erase-copy, and erase-reconstruction entries for use.

Before the result-length store and source-preservation obligations, I searched `theorem .*allocStore|write64.*root|emptyStore_at|store.*length|input_preserved_by_alloc` in the mirrored proof kit and the allocation, array, memory, and proof-construction indexes.  I inspected `leanexe-core/array-memory-framing`.  Its `FixedArrayPairResult.input_preserved_by_alloc` and `UInt64Array.At.write64After` match the allocator and subsequent result-length write.  I selected this entry.  I revisited `leanexe-core/fixed-array-result`: `lengthStoreLocal_spec` matches the valid result-length store, and `emptyStore_at` matches the over-length result, so the entry will be used in both branches.  Its singleton and pair tactics do not match either residual goal.

The next edit will reduce the successful decoded index through the two checked in-bounds tests and the scalar length, prefix, suffix, and capacity calculations.  Progress means reaching `FixedArrayAllocatorWindow.region 9 1` with named frame getters for source pointer, erased index, result length, prefix length, suffix length, and capacity.  I will then apply `region_spec`, `lengthStoreLocal_spec`, and the exact erase-copy theorem instead of reducing those regions.

The import check succeeded.  The build failed after 23 seconds while proving the preserved source getter.  The decoder and search updates reduced as intended, but the getter proof unfolded the search frame and stopped at the length-dispatch frame's parameter and local bounds.  That expansion is unnecessary: `decoderInput` writes `inputPtr` directly to the internal local corresponding to combined local 3.  I will prove this internal getter first from `hSearchLocals`, then use the decoder frame's three distinct updates.  The continuation also remains syntactically under `Annotation.resolve` and `List.drop`, so I will expose only its first three instructions with a definitional `change` before applying atomic weakest-precondition rules.

The import check succeeded.  The revised source proof reached the precise conversion from `decoderInput.locals[2]?` to `decoderInput.locals[2]`; the optional getter already equals `some (.i64 inputPtr)`.  I will convert it with `List.getElem?_eq_getElem` and use that value in the decoder-update simplification.  The definitional `change` exposed the intended three instructions.  Their list prefix remains expressed as an append, so the atomic reduction also needs `List.cons_append` and `List.nil_append`.

The import check succeeded.  The source getter now closes, and the three-instruction prefix reduces to the checked `localSet 8` match.  The residual frame retains one parameter and 23 locals, so combined local 8 is internal local 7.  I will use the decoder frame's parameter and local-length accessors to reduce this update and its following getter, then run the straight-line wrap, length load, and unsigned comparison only to the first structured branch.

The import check succeeded.  The atomic run exposed the source-length load and comparison, but the restricted simplifier left the surrounding decidable `if` expressions.  The remaining facts are exact: local 8 contains `inputPtr`, `hLengthBound` discharges the load, `hLengthRead` supplies `UInt64.ofNat input.size`, and `hIndex` makes the unsigned comparison true under `hInputSizeBound`.  I will normalize this bounded expression with those named facts and stop at the structured branch.

The import check succeeded.  The frame and length read normalized.  The residual contains only the negated load-bound test and the encoded unsigned comparison `index % UInt64.size < input.size % UInt64.size`.  I will close the load test from `hLengthBound`, rewrite both moduli using `hIndex` and `hInputSizeBound`, and select the checked in-bounds arm with `wp_iff_cons`.

The import check succeeded.  The first in-bounds arm is selected.  Its frame is the decoder frame with source local 8 restored to `inputPtr` and an empty operand stack.  I will name this frame, derive its source and decoded-index getters from the decoder accessors, and reduce the second length read and index comparison to the nested structured branch.  No allocator or copy instruction lies before that branch.

The import check succeeded.  The named outer frame is definitionally correct.  Both getter proofs reached the same optional-to-total list conversion seen at the decoder source boundary: the exact values are already present at internal indices 2 and 3.  Because these two unfinished facts prevented the later scalar simplifier from using the named getters, it expanded the frame instead.  I will convert both optional getters with `List.getElem?_eq_getElem` before repeating the focused scalar reduction.

The import check succeeded.  Both named getters now close.  The scalar prefix reached the second load-bound test followed by two exact `List.set` self-lookups at internal locals 8 and 9.  I will close the load bound from `hLengthBound` and normalize only those two lookups.  The resulting structured branch should again be selected from `hIndex` without reducing its body.

The import check succeeded.  The load bound and both list lookups close.  The restricted simplifier left only two `if True` wrappers around the known values.  I will remove those wrappers, normalize the UInt64 moduli as in the first bounds check, and select the nested successful arm.  Its first instructions compute the result length, prefix count, suffix count, and allocation capacity.

The import check succeeded.  Removing the `if True` wrappers also normalized the comparison from explicit moduli to `(UInt64.ofNat index).toNat < (UInt64.ofNat input.size).toNat`.  The attempted modulus rewrite therefore had no target.  I will use `UInt64.toNat_ofNat_of_lt'` with the existing index and input-size bounds, then select the arm.

The import check succeeded.  Both bounds checks now select their successful arms.  The first residual instruction computes `input.size - 1`, followed by the prefix length, suffix length, rounded allocation capacity, and `FixedArrayAllocatorWindow.region 9 1`.  This is the scalar setup boundary expected by the allocation recipe.

At this residual checkpoint I searched `UInt64.*sub|ofNat.*sub|toNat_sub|scalar arithmetic|frame update|List.set_set|localSet.*frame|arithmetic` in `PROOF_RECIPES.json`, the mirrored ProofKit modules, and the `proof-construction`, `compiler-motifs`, and `arrays` indexes.  The package has no `arithmetic` category index.  The existing find-index, allocation, and array-framing entries reappeared.  The query also returned counter-transition and map-add entries whose scalar post-test and map annotation shapes do not match this erase setup, so I did not inspect them.  `PROOF_RECIPES.json` names no accessor for these scalar locals.  The mirrored memory module provides `UInt64.toNat_sub`, and the standard list facts include `List.set_set`; no new catalog entry applies.

I will name the scalar input frame and the four artifact-computed words: result length, prefix length, suffix length, and rounded capacity.  A focused weakest-precondition reduction should produce the allocator input frame without expanding the allocator.  Progress means changing the residual program to `FixedArrayAllocatorWindow.region 9 1 ++ rest` with the computed capacity at internal local 17.

The import check succeeded.  The focused scalar reduction completed in 15.7 seconds and stopped at the one structured capacity-floor branch immediately before the allocator.  The residual begins with `iff 0 0 [constI64 8; localSet 18] []`; all preceding result-length, prefix, suffix, rounded-capacity calculations have become frame updates.  I will expose this `iff` with `wp_iff_cons` before proving that the rounded capacity is at least eight.  Progress means selecting its empty arm while preserving the computed allocator frame.

The import check succeeded.  `wp_iff_cons` exposed the precise condition: `(8 + (UInt64.ofNat input.size - 1) * 8 + 7) / 8 * 8 < 8`.  The mirrored fixed-array map and filter proofs establish the same rounded-capacity fact by rewriting `UInt64.toNat_mul`, `toNat_div`, `toNat_add`, and `toNat_ofNat_of_lt'`; their capacities use the original length, while this artifact uses `input.size - 1`.  I will prove that this capacity has natural value `8 * input.size`, select the empty normalization arm, name the resulting allocation frame, and apply `FixedArrayAllocatorWindow.region_spec` at offset 9.  Progress means replacing the allocator implementation with its `allocStore` continuation.

The import check succeeded.  The build failed after 16.9 seconds at three focused boundaries.  `Memory.toNat_sub_of_le` needs an explicit `calc` because its conclusion retains both operand projections before normalization.  The rounded-capacity proof itself then succeeds and establishes `capacity.toNat = 8 * input.size`.  The first `if_neg` already reduces the normalization program to `wp module []`; the second `if_neg` was therefore redundant and searched inside later terms.  I will replace it with `wp_nil`, after which the named allocation frame should match `region 9 1`.

The import check succeeded.  The artifact now matches `region 9 1`, and `region_spec` discharged every allocator premise except the capacity local's final `List.set` self-lookup.  Its continuation starts exactly with `FixedArrayResult.lengthStoreLocalProgram 14 13`, followed by the two-loop erase-copy program and `localGet 14`.  Two earlier local facts also need refinement: the subtraction premise will use the named nonempty-input inequality instead of `decide`, and the expected predicate will use a definitional `change` instead of simplification.  I will close these three local boundaries before constructing the post-allocation memory facts.

The import check succeeded.  All allocator premises now close, and the only valid-branch residual is its semantic continuation.  I will name the allocated store and frame, apply `lengthStoreLocal_spec`, preserve the represented input first across allocation with `input_preserved_by_alloc` and then across the result header write with `UInt64Array.At.write64After`, and apply `FixedArrayCopy.eraseIdxProgram_spec` to locals 8, 14, 11, 12, and 15.  The required target bounds follow from `Allocation.bumpFacts` and `capacity.toNat = 8 * input.size`.  Progress means replacing both copy loops with a callback that supplies `UInt64Array.At final (heapTop + 48) (input.eraseIdx! index)`.

The import check succeeded.  The build produced diagnostics after 37 seconds but hit the deterministic heartbeat limit while normalizing the post-allocation frame shape.  The first failure shows that simplification did not use `allocFrame_params` through the local abbreviation; the subsequent local-length and root facts then consumed the remaining heartbeats.  Per the timeout boundary, I will add a small generic `allocFrame_shape` lemma that packages the exact parameter, local-length, and value-stack accessors, and use its equalities without unfolding `allocFrame`.  This divides the elaboration boundary before the memory and erase-copy proof.  The over-length branch remains open and will be handled after the valid branch compiles.

The import check succeeded.  The generic frame-shape lemma removed the heartbeat failure, and the build reached the erase-copy application in 18.3 seconds.  Three focused corrections remain.  I expanded `writeLength` too early when preserving the input, which changed the address presentation to `heapTop.toUInt32 + 48`; I will use an explicit `change` and the already proved root-address equality.  I attempted to rewrite the structure-valued `hFacts`; I will copy its `fit32` field and rewrite that inequality.  Finally, `apply eraseIdxProgram_spec` inferred the counter-validity proof, shifting the bullets by one; I will pass `hCounter := hCounter` explicitly and keep four disequality premises.

The import check succeeded.  `eraseIdxProgram_spec` now closes the two copy loops and supplies the represented erased array.  The remaining valid-branch goal is the post-copy `localGet 14` and the enclosing result-local transfers.  My broad simplification left the first root getter as an unreduced match because `wp_localGet_cons` had not been applied before simplifying the continuation.

For this getter checkpoint I searched `counterFrame_get_ne|counterFrame.*root|post-copy frame|erase copy.*continuation|localGet 14|root getter` in `PROOF_RECIPES.json`, the `arrays`, `proof-construction`, and `compiler-motifs` indexes, and the mirrored copy module.  The already inspected `leanexe-core/fixed-array-erase-copy` entry names `counterFrame_get_ne`, `counterFrame_params_length`, `counterFrame_locals_length`, and `counterFrame_values`; no new entry applies.  I will use those exact accessors, apply `wp_localGet_cons` with the preserved root, and then reduce only the three result-local transfers.  Progress means closing the valid branch with the represented erased array.

The import check succeeded.  The exact counter-frame accessors and atomic root getter closed the valid branch.  The sole residual goal is the over-length callback `¬ input.size ≤ 8 → wp ...`.

At this residual checkpoint I revisited the original `length-dispatch|invalid-capacity|constantProgram|emptyStore|allocator` query results.  `PROOF_RECIPES.json` names `function_0_length_dispatch_0_invalid_capacity_eq`, `FixedArrayCapacity.constantProgram_spec`, and the normalized-capacity frame accessors.  The inspected `leanexe-core/fixed-array-capacity`, `fixed-array-allocation`, and `fixed-array-result` entries match this branch.  The complete checked composition `FixedArrayAllocatorWindow.constantCapacityRegion_spec_withTail` combines the exact constant-capacity program with allocator region offset 3 and six trailing locals, so I will attempt it before reducing either region.  Its continuation should start with `lengthStoreProgram 8 0`, followed by `finishProgram 8 6 7`; `emptyStore_at` will establish the required empty representation.  Progress means eliminating the allocator and reaching the final root-transfer continuation.

The import check succeeded.  The constant-capacity composition matched and eliminated both the normalization and allocator regions.  The build failed after 42 seconds at three local issues before the final store: the memory-fit fact still contains the expanded `normalizedCapacity 0 1` rather than the local `capacity` abbreviation; the root bound needs the `rootToNat` rewrite on the supplied fact; and the fixed-length store theorem is named `lengthStore_spec`, not `lengthStoreProgram_spec`.  I will name the capacity-form memory inequality, apply these two exact rewrites, and use the correct result-store declaration.

The import check succeeded.  The corrected capacity and root rewrites and `FixedArrayResult.lengthStore_spec` close the invalid branch through `finishProgram_spec`.  The build failed after 49 seconds only at the public suffix `localGet 7`: `FixedArrayEqNode.branchPost` clears the value stack before executing that getter, so the broad simplifier did not use `finishFrame_return_get` on the updated frame.

For this residual getter I searched `finishFrame_return_get|finishFrame.*get 7|result suffix|localGet 7` in `PROOF_RECIPES.json`, the `leanexe-core` category indexes, and the mirrored ProofKit.  The search returned the already inspected `leanexe-core/fixed-array-result` entry and its exact `finishFrame_return_get` declaration; no new entry applies.  I will transfer that getter across the value-stack update using `Wasm.Locals.get`, apply `wp_localGet_cons`, and provide the result pointer and `hEmpty` directly.  Progress means closing the last residual goal without expanding the final frame.

The import check succeeded.  The build failed after 43 seconds because `wp_localGet_cons` produced the expected root on the value stack but left `wp module []` around the public postcondition.  The getter accessor and frame transfer both succeeded.  I will apply `wp_nil` at this final instruction boundary before constructing the existential result.

The import check succeeded.  The build completed all 3,079 targets in 47 seconds.  `wp_nil` exposed the public postcondition, and the invalid branch now returns the represented empty array at the allocated root.  The candidate has no residual goal.  I will record the inspected knowledge entries and repeat the required import and artifact-result checks without editing the candidate.

The final import check succeeded, and the independent artifact-result build replayed `Behavior` and completed all 3,079 targets.  Acceptance uses `FixedArrayLengthDispatch.leProgram_spec`, `FixedArrayFindIdxEq.program_spec`, `EncodedIndexDecoder.program_spec`, `FixedArrayAllocatorWindow.region_spec`, `FixedArrayAllocatorWindow.constantCapacityRegion_spec_withTail`, `FixedArrayResult.lengthStoreLocal_spec`, `FixedArrayResult.lengthStore_spec`, `FixedArrayCopy.eraseIdxProgram_spec`, `FixedArrayResult.finishProgram_spec`, and the array-memory framing declarations recorded above.  `artifact_behavior` has no residual goal.
