# Proof Journal

## 2026-08-26: Initial candidate check

I ran the prescribed `ArtifactResult` build before editing `Behavior.lean`.  The build reached `artifact_behavior` and left the full `TerminatesWith` postcondition after the starter decomposed `RuntimeReady`.  The context contains the input representation, exact length read, pointer bound and address identity, allocator globals, result-region bounds, heap-reserve bounds, and page bound.  The next step is to inspect the exact recipes and selected knowledge entries before changing the proof.

## 2026-08-26: Initial knowledge retrieval

`KNOWLEDGE/forest.json` selected package `leanexe-core`.  I searched its category indexes with `rg` for `leanexe.array.length-dispatch`, `leanexe.array.find-idx-eq`, `leanexe.array.erase-copy`, `leanexe.option.encoded-index`, `FixedArrayLengthDispatch`, `FixedArrayFindIdxEq`, `FixedArrayCopy`, `EncodedIndexDecoder`, and `TerminatesWith`.  The relevant entries are `fixed-array-length-dispatch`, `fixed-array-find-idx-eq`, `fixed-array-erase-copy`, `encoded-index-decoder`, and `fixed-array-capacity`.

I then searched the allocation, arrays, compiler-motifs, and proof-construction indexes for `FixedArrayAllocatorWindow`, `fixed-array-allocation`, `empty free list`, `bumpFacts`, and `constantCapacityRegion_spec_withTail`.  This added `fixed-array-allocation`.  These six entries correspond to the four checked compiler regions and the allocation phase named by the length-dispatch recipe.  I will inspect their canonical metadata and guidance before attempting the direct length-dispatch tactic.

I inspected package `leanexe-core` entries `fixed-array-length-dispatch`, `fixed-array-find-idx-eq`, `encoded-index-decoder`, `fixed-array-erase-copy`, `fixed-array-capacity`, and `fixed-array-allocation`.  Each entry matches a recipe, an annotation kind, or the allocator immediately following a checked capacity prefix.  No entry has been rejected at this stage.

## 2026-08-26: Direct length-dispatch attempt

The current goal is a public `TerminatesWith`, while the structured tactic requires a body-level `Wasm.wp` goal beginning with the bounded dispatch.  I will apply `Wasm.TerminatesWith.of_wp_entry_for` with `func0Def`, then invoke the exact recipe command `wp_fixed_array_length_le_dispatch_from hArray at 8, 8`.  Progress means separate valid-size and oversized branch goals whose programs start after the checked length load and comparison.

The import check succeeded.  The Lean build failed with seven residual goals.  Five are frame and numeric premises: initial parameters, empty operand stack, `0 < 8`, local index 8 within the 23 generated locals, and `8 < UInt64.size`.  The other two are the expected valid-size and oversized branch `wp` goals.  The direct tactic therefore matched the goal shape and reduced the full entry proof to the checked dispatch boundary.

I will normalize the five generated-frame premises with `func0Def`, `Function.toLocals`, `Function.numParams`, and `ValueType.zero`.  Progress means only the valid and invalid branch programs remain.  The invalid branch begins with the checked constant-capacity program for an empty result.  The valid branch begins with the checked first-match search region.

The import check succeeded.  The Lean build failed because `all_goals simp` also acted on the branch obligations.  It reduced the generated frame premises, then crossed into the oversized branch's capacity instructions and exposed a large instruction-level `wp` term.  This is an elaboration-boundary error in the proof script.  I will replace the broad application with five explicit frame-premise bullets and leave the valid and invalid programs folded for their selected theorems.

The import check succeeded after that edit.  The Lean build now leaves only `hValid` and `hInvalid`; all five dispatch premises close with focused normalization.  The valid residual starts at the exact twelve-instruction first-match region.  Before reducing it, I will change the residual program to the resolved valid-branch tail, rewrite it with `function_0_find_idx_eq_0_tail_eq`, and apply `FixedArrayFindIdxEq.program_spec` with scratch 8 and twelve trailing locals.  Progress means none and some callbacks carrying the mathematical `findIdx?` equations and the named producer frames.

For this residual checkpoint, I searched the arrays, loops, and proof-construction indexes for `noneFrame`, `someFrame`, `encodedIndex`, `branchPost`, and `findIdx?`.  Only the already inspected `fixed-array-find-idx-eq` entry matched.  Its structured tactics list is empty, so the direct declaration is the selected command.

The import check succeeded.  The direct search theorem matched after the tail rewrite and produced the expected none and some callbacks.  One frame premise remained as `inputPtr = ?inputPtr` because the theorem application did not infer its explicit `inputPtr` argument before simplifying the frame.  I will supply `inputPtr` and `input` in the application.

Before reducing the none-frame getters, I searched `PROOF_RECIPES.json` and the arrays and proof-construction indexes for `noneFrame`, `someFrame`, `resultFrame`, `Locals.get`, `getter`, and `projection`.  The search recipe provides only the three `someFrame` shape projections; it provides no `noneFrame` getter.  The search also returned package `leanexe-core` entry `local-frame-projection`, which I inspected.  Its declarations convert an existing combined getter or preserve getters across operand-stack replacement.  They do not derive the encoded-result getter from `noneFrame`, so focused reduction of that named frame is required for the no-match path.

The import check succeeded.  Supplying the input arguments closed the search theorem's frame premises.  The no-match attempt left two local issues.  First, `hFind` uses `FixedArrayFindIdxEq.predicate 0`, while `FormalSpec.expected` contains the eta-expanded equality predicate; I will derive an exact `hFind'` by unfolding `predicate`.  Second, the post-search program remains folded as `drop 12 (Annotation.resolve ...).getD`; the focused simplifier cannot see its first instruction.  I will unfold only `Annotation.resolve`, `Annotation.descend`, and `func0` in this straight-line no-match callback.  Progress means the callback returns `inputPtr` with the unchanged input representation.

The import check succeeded, but `simp only [FixedArrayFindIdxEq.predicate]` did not rewrite the predicate beneath `Array.findIdx?`.  I will use `change` on `hFind`, which asks Lean to check the definitional equality directly.  Unfolding `resolve` stopped at a list lookup.  The recipe already names `function_0_length_dispatch_0_valid_branch_program`; I will change the callback program to that named branch's `drop 12` and reduce the named branch rather than expand `func0` and annotation resolution.

The import check succeeded.  The predicate `change`, expected-result equality, and named branch boundary all succeeded.  The no-match callback reduced through the result-local assignment and stopped at a concrete local-list lookup at index zero.  The exact frame accessor search found no theorem for this `noneFrame` projection, so I will add the standard concrete-list successor and zero lookup rules to the same focused reduction.

The import check succeeded.  The concrete lookup rules reached the first structured `if` with condition `i32 0`.  I will apply `wp_iff_cons`, choose its false branch, execute the scalar normalization to the outer `if`, choose that true branch, and finish the two local operations and public suffix from the unchanged-array fact.

The import check succeeded.  Both structured branches selected the intended no-match path, and the remaining goal is exactly `FormalSpec.UInt64ArrayAt initial inputPtr input`.  I will change that goal to the shared `UInt64Array.At` predicate and close it with `hArray`; no execution goal remains in this callback.

The import check succeeded, and the no-match callback is closed.  The new residual class is the successful search callback with `index < input.size`, a `findIdx? = some index` equation, and `someFrame` containing `encodedIndex index`.  The recipe and inspected `fixed-array-find-idx-eq` entry provide `someFrame_params`, `someFrame_locals_length`, `someFrame_values`, and `encodedIndex_ne_zero`; these match the residual frame and tag test.  I will use those accessors to select the successful option branch while keeping `someFrame` folded.  Progress means the residual program begins with the two pointer-save instructions before the checked encoded-index decoder.

The import check succeeded.  The `someFrame` accessors and `encodedIndex_ne_zero` selected both tag-test branches, and the residual begins with `localGet 0; localSet 3` followed by the annotated decoder.  This is the encoded-index retrieval checkpoint.  The already inspected `encoded-index-decoder` entry has no structured tactic and names `EncodedIndexDecoder.program_spec` plus its result-frame accessors.  I will execute only the two pointer-save instructions, change the remaining program to the resolved decoder tail at offset 2, rewrite `function_0_encoded_index_0_tail_eq`, and apply the direct decoder theorem before reducing any decoder instruction.

The import check succeeded.  The two pointer-save instructions reduced separately, the decoder tail rewrite matched, and the direct decoder theorem now asks for its encoded getter and four frame bounds.  Inference left the encoded word implicit.  I will instantiate it with `FixedArrayFindIdxEq.encodedIndex index`, then close the getter and bounds by focused local-set and frame-shape reduction.  The recipe has no accessor for the producer frame after these two pointer-save updates; `someFrame` shape projections and the standard local-set lookup rule are the exact available facts.

The import check succeeded.  All decoder premises close, leaving its `hNext` continuation at `EncodedIndexDecoder.resultFrame` and the artifact tail at offset 8.  Before reading decoded local 4 and preserved pointer local 3, the accessor search identified `resultFrame_decoded` and `resultFrame_get_ne` in the inspected decoder entry; both match.  I will use them, plus `encodedIndex_sub_one`, to execute the emitted bounds check.  I will prove `UInt64.ofNat index < UInt64.ofNat input.size` from `hIndex` and the represented-array size bound.  Progress means the in-bounds branch is selected without reducing the allocation or copy regions.

The import check succeeded.  The index comparison fact elaborated, but the broad simplifier unfolded `Wasm.Locals.get` before it could apply either `EncodedIndexDecoder.resultFrame` accessor.  The residual is a nested match on raw list projections for combined locals 4 and 3.  I will expose one `localGet` at a time, rewrite the combined getter with the selected accessor, and reduce the underlying producer frame only after the accessor removes the decoder scratch updates.

The import check succeeded.  Restricting simplification too far left the annotation resolver as an unreduced `Option` computation beneath `List.drop`, so `wp_localGet_cons` and `resultFrame_decoded` found no occurrence.  I will normalize the annotation computation without `wp_simp`, then expose the leading getter in a separate step.  This keeps the result frame folded while revealing the instruction list.

The import check succeeded.  General annotation normalization triggered Lean's tactic-instance checker on an already elaborated numeric decision and made no progress.  `AnnotationMatches.lean` confirms that the resolved tail at offset 8 starts with `localGet 4`.  I will add a small `rfl` tail decomposition beside the proof, rewrite by it, and leave the resolver opaque during the continuation proof.  Progress means `resultFrame_decoded` rewrites the exposed combined getter.

The import check succeeded, and the tail decomposition rewrote the resolver.  Its right side used singleton append, while `wp_localGet_cons` matches list cons without reducing that append under the current transparency boundary.  I will state the decomposition with `localGet 4` as a cons cell.  This changes no artifact fact and presents the instruction in the form expected by the `wp` lemma.

The import check succeeded.  `resultFrame_decoded` rewrote the getter and generated exactly its two documented premises: the decoded local lies after the parameter prefix and within the combined frame.  The main goal now contains the decoded word on the operand stack.  I will close those shape premises, reduce the nonzero decoder result to `UInt64.ofNat index`, then expose the pointer getter and use `resultFrame_get_ne`.  A second small `rfl` decomposition will cover the straight-line pointer-save and length-read prefix through the comparison.

The import check succeeded.  The decoder shape premises and decoded-word reduction close.  The pointer getter is taken from a frame whose parameters and locals equal `resultFrame` but whose operand stack now contains the decoded word.  Consequently `resultFrame_get_ne` does not match syntactically.  I searched the proof-construction index and `PROOF_KIT_SOURCE` for `withValues`, `operand-stack`, and getter preservation.  The previously inspected `local-frame-projection` entry provides `Frame.withValues_get`, whose goal shape exactly matches this residual.  I will apply it before `resultFrame_get_ne`; this entry is now used rather than rejected.

The import check succeeded.  `Frame.withValues_get` and `resultFrame_get_ne` rewrote the pointer projection, all five accessor premises closed, and the straight-line prefix computed the comparison result `i32 1`.  The only remaining condition before the structured bounds branch is the memory-read bound, with the exact negation supplied by `hLengthBound`.  I will discharge that condition, apply `wp_iff_cons` to the resolved instruction at offset 15, and select its true branch.  Progress means the successful removal path starts at its second length check.

The import check succeeded.  The memory bound and outer branch close, leaving the concrete eleven-instruction prefix that reloads the input length and checks the decoded index a second time.  The successful body contains the capacity, allocator, and erase-copy regions and must remain folded.  I will run `wp_run` on this scalar prefix; it stops at structured control in the checked ProofKit proofs.  Progress means a single `wp_iff_cons` goal with the condition derived from `hIndex`.

The import check succeeded.  `wp_run` stopped at the inner `iff`, but it expanded the scalar local operations into raw `get`, `set?`, and memory-read matches because the decoder and producer frames remain symbolic.  The branch body itself remains inside one `wp` instruction.  I will normalize these raw frame projections and the length read without `wp_simp`; progress is the folded `iff` with an `i32 1` operand.

The import check succeeded.  The normalization reduced the combined-get definitions but stopped on the conditional representation of `EncodedIndexDecoder.resultFrame`.  I omitted `hEncodedNe` from that normalization, so Lean retained both decoder branches and could not establish the list lengths or projections.  I will add the known nonzero equation and the decoder subtraction identity.  The residual remains before the allocator boundary.

The import check succeeded.  Adding `hEncodedNe` closed every frame projection, local update, length read, and comparison.  The residual is the second memory-read bound followed by one folded `iff`; its true body starts with result-length, prefix-count, and suffix-count calculations.  I searched the arrays, proof-construction, compiler-motifs, and allocation indexes for `erase setup`, `result length`, `prefix suffix`, and `normalizedCapacity`.  No new setup entry exists.  The inspected `fixed-array-capacity` theorem accepts a constant-length instruction, while this body reads the dynamic result length from local 13, so that theorem does not match this region.  `FixedArrayAllocatorWindow.region_spec_withTail` remains applicable after the capacity prefix.

The same search identified package `leanexe-core` entry `fixed-array-erase-reconstruction`, and I inspected its canonical metadata and guidance.  `UInt64Array.At.eraseIdx!_of_reads` matches the final representation goal; the stronger selected adapter `FixedArrayCopy.eraseIdxProgram_spec` composes that reconstruction with both checked copy loops, so I will use the adapter when the annotated copy tail is reached.  I will now close the memory bound and select the in-bounds body without reducing its allocator or loops.

The import check succeeded, and the Lean build selected the inner in-bounds branch.  The residual starts with fourteen scalar instructions that compute the result length, prefix count, and suffix count, followed by a dynamic aligned-capacity computation, the standard shifted allocator, the result-length store, and the annotated erase-copy region.  I searched the allocation, arrays, compiler-motifs, memory, and proof-construction indexes for `lengthStoreLocalProgram`, `lengthStoreLocal_spec`, `emptyStore_at`, and `finishProgram_spec`.  I inspected package `leanexe-core` entry `fixed-array-result`; its local-length store and final-root transfer match the successful path, while its constant-length store and empty-array constructor match the oversized path.  The structured singleton and pair tactics have incompatible goal shapes and annotation kinds, so I will use the entry's direct declarations.

Before the next elaboration attempt, I will name the exact scalar setup and dynamic-capacity instruction sequences and prove small frame-preserving specifications for them.  The residual that counts as progress begins at `FixedArrayAllocatorWindow.region 9 1` with a named frame containing result length in local 13, prefix count in local 11, suffix count in local 12, and normalized capacity in local 18.  This separates the unannotated scalar arithmetic from the checked allocator and copy regions without reducing either region.

The import check succeeded.  The Lean build accepted all four exact tail decompositions and reduced the main theorem to `successfulRemovalBody.drop 32`, so the intended allocator boundary is correct.  Two private scalar specifications failed internally.  `eraseSetupProgram_spec` unfolded a combined getter into raw internal locals before using its getter premise, leaving the three arithmetic operand matches unresolved.  `localCapacityProgram_spec` had the same ordering problem for its dynamic length getter, so `wp_iff_cons` saw no proved `i32` stack head.  I will derive exact internal-list projections for the hard-coded setup frame and rewrite the dynamic getter before the general instruction simplification.  The artifact boundary and main proof application require no change.

The import check and Lean build now accept both scalar specifications.  The only successful-path residual begins at offset 32 in the named removal body, with the capacity frame established.  Before discharging allocator-frame getters, I searched `PROOF_RECIPES.json`, the selected allocation, arrays, memory, and proof-construction indexes, and the mirrored `FixedArrayAllocatorWindow` and `FixedArrayResult` modules for `allocFrame_get`, `lengthLocal`, `sourceLocal`, `prefixLocal`, and `suffixLocal`.  The selected `fixed-array-allocation` entry provides `allocFrame_get_root`, shape preservation, valid-index preservation, and `input_preserved_by_alloc`; it provides no general getter-preservation theorem for untouched locals.  I will use the root accessor and reduce the named `allocFrame` definition only for locals 8, 11, 12, and 13.  The selected `fixed-array-result` entry provides the exact local-length store theorem once those two getters are available.

The import check succeeded.  `FixedArrayAllocatorWindow.region_spec_withTail` matched offset 32 and advanced its continuation to offset 49.  The frame-shape, capacity-local, minimum-capacity, page, module, and global premises closed.  The capacity equality split input sizes 1 through 8 as intended, but `norm_num` did not evaluate the closed UInt64 operations.  I will use kernel evaluation through `native_decide` for these eight closed word equalities.  The heap-fit premise also needs the concrete value of `UInt64.size` for the `ofNat` conversion and reassociation of `heapTop + (48 + 8 * size)`; these are local arithmetic changes.  No allocator instruction requires reduction.

The import check succeeded.  The impossible zero-length capacity case now closes from `index < input.size`, and `native_decide` proves the eight remaining closed UInt64 equalities.  The allocator and `FixedArrayResult.lengthStoreLocal_spec` both check, advancing the residual to `successfulRemovalBody.drop 53`, exactly the checked erase-copy tail.  Its root and result-length getters were discharged through `allocFrame_get_root` and focused reduction of untouched local 13.  I will now attempt the direct recipe declaration `FixedArrayCopy.eraseIdxProgram_spec`.  Progress means the two copy loops produce `UInt64Array.At final (heapTop + 48) (input.eraseIdx! index)` and leave only the returned-root instruction at offset 59.

The import check succeeded.  The direct erase-copy declaration matched the annotated program and exposed only its documented premises.  Input preservation through allocation and the later result-length write checks.  Several small premises failed because the general subtraction proof retained unevaluated constant projections, and direct simplification of the nested allocator/capacity/setup frame reached the one-million-heartbeat boundary.  I will replace both word equalities with bounded closed checks and add reusable checked accessors for the resulting removal frame's values, counter validity, source pointer, prefix count, and suffix count.  This divides the failed elaboration boundary; the next check will not repeat the unchanged proof term.

The import check succeeded.  All new removal-frame declarations compile, and both bounded word equalities close.  During `eraseIdxProgram_spec` application, Lean discharged `frame.validIndex 15` while unifying the named removal frame, so my explicit counter-validity bullet acted on the following `8 ≠ 15` premise and shifted every later proof by one.  This accounts for the apparent values and getter failures in the diagnostic.  I will remove that redundant bullet; the four numeric inequalities then align before the values and getter accessors.

The import check succeeded.  The erase-copy adapter now accepts the source representation, all frame premises, and both local count words.  The remaining adapter premises are the target 32-bit bound, target memory bound, and target length word.  The first two need the already proved capacity equality converted through `UInt64.toNat`; the third is exactly `hResultLengthEq`.  The adapter's continuation is the intended `hDone` callback at offset 59.  I will add a result-root accessor for the named removal frame, normalize those three premises, and execute the single returned-root instruction before handling the oversized branch.

The import check succeeded.  The target bounds and stored result length reduce to the intended arithmetic facts, but `hCapacityNat` was scoped inside the preceding length-store bound rather than the erase-copy continuation, so the two copy-bound bullets cannot refer to it.  The returned-root accessor also succeeds, after which a broad simplifier reaches the one-million-heartbeat limit while expanding the enclosing branch continuations.  I will move the capacity conversion fact into the copy scope and replace that broad continuation simplification with `wp_nil` followed by focused branch-post and scalar execution.  Progress means the successful callback closes and exposes only the oversized branch.

The import check succeeded, and the capacity conversion now closes both copy bounds.  The returned-root getter rewrites to `some (i64 (heapTop + 48))`, but the getter instruction still appears as a reducible match around `wp []`; therefore `rw [wp_nil]` cannot yet see its left-hand side.  I will reduce that single match before applying `wp_nil`.  This preserves the enclosing continuation while exposing it one layer at a time.

The import check succeeded.  Reducing the getter match exposes `wp []`, and the first `wp_nil` rewrite succeeds.  Recursive simplification of the two surrounding result-bearing `iff` continuations still reaches the heartbeat limit because it expands every continuation branch and the full nested copy frame.  I will unfold each successful fallthrough boundary through a `change` to its next `wp` goal, rewrite each empty program separately, and inspect the resulting three-instruction result transfer.  This divides the timed-out continuation rather than repeating its proof term.

The import check succeeded.  The first explicit fallthrough conversion and second `wp_nil` rewrite succeed.  Converting the next continuation to the three-instruction result transfer times out while weak-head reducing the concrete `counterFrame` over the allocator, capacity, decoder, and search frames.  I will generalize that completed copy frame before entering either continuation.  The root getter remains justified by rewriting back through the generalization equation, while later continuation reduction sees one abstract frame variable.  Progress means the second conversion reaches `localSet 5; localGet 5; localSet 7` within the heartbeat limit.

The import check succeeded, but the generalization did not reduce the continuation because rewriting the root getter with `← hCopyFrame` rewrote every occurrence of `copyFrame` in the goal and restored the concrete nested frame.  I will prove the root getter as a separate fact from `hCopyFrame`, then rewrite only that getter in the instruction.  The continuation will retain the abstract frame variable.

The import check succeeded.  The separate root fact keeps `copyFrame` abstract, and the check completes in seven seconds without a heartbeat failure.  The combined `simp only [wp_localGet_cons, hCopyRoot]` already reduces the getter match, so the following empty `simp only` reports that it made no progress.  I will remove that redundant command; the prepared `wp_nil` and `change` steps can then expose the scalar transfer.

The import check succeeded.  Both result-bearing `iff` continuations now reduce without a timeout.  The second continuation has already inlined `localSet 5; localGet 5; localSet 7`, so changing it back to a `wp` program is not a definitional equality.  Its diagnostic shows that only `copyFrame.values`, `copyFrame.params.length`, and `copyFrame.locals.length` prevent reduction.  I will derive those three shape facts from the generalization equation, then simplify the inlined scalar transfer and the public `localGet 7` suffix from `hResult`.

The import check failed before the scalar simplification because the `counterFrame` generalization used an underscore for its proof-valued `hCounter` argument.  Proof irrelevance lets the pattern match the target without assigning that placeholder, leaving an unsolved validity proof.  I will instead generalize the underlying `removalFrame`, whose arguments are data, and retain the checked `counterFrame` construction over that abstract frame.  Its existing shape theorems quantify over the validity proof and can rewrite the continuation without naming that proof.

The import check succeeded.  A `generalize` pattern containing ordinary underscores also leaves those terms as metavariables rather than matching the concrete removal frame, so it neither rewrites the continuation nor proves the derived frame shapes.  I will remove this invalid abstraction.  The direct continuation reduction previously reached the exact scalar result transfer and exhausted the theorem's one-million-heartbeat setting.  I will retain the focused root accessor and give the public theorem the same four-million-heartbeat boundary used by `FixedArrayCopy.eraseIdxProgram_spec`, then reduce the small continuation with the counter-frame shape declarations included.  This changes the elaboration boundary and preserves the direct artifact proof.

The import check succeeded, but the direct continuation simplifier also exhausts four million heartbeats after 140 seconds.  I will not increase or repeat this boundary.  The diagnostic exposes the exact pre-removal frame as the search `someFrame`, the result-local and pointer updates, the decoder's three updates, and the final pointer, index, and length updates.  I will name that frame as a checked definition and add shape lemmas for it, the removal frame, and the copy counter frame.  The continuation can then rewrite parameters, local length, values, and root without unfolding the search, decoder, allocator, and copy constructions.

The import check succeeded.  The Lean build rejected the new specialized frame before reaching the intended continuation: its nested `List.set` expression lacks a closing delimiter, so parser recovery inserted synthetic holes.  The root theorem also precedes `removalFrame_target`, and the specialized copy-shape proofs do not yet factor through named removal-frame shape lemmas.  The failed root rewrite in the main proof follows from the malformed definition and does not test whether the named frame matches the concrete residual.  I will construct the local list through sequential `let` bindings, add parameter- and local-length preservation lemmas for `removalFrame`, and move the root and copy-shape specializations after the general root accessor.  Progress means the specialized definitions elaborate and the root rewrite either matches the exact residual or reports a genuine frame mismatch.

The import check and all specialized frame declarations succeeded.  The Lean build reaches the intended callback in nine seconds, then reports that `rw [successfulRemovalFrame_root]` does not fold the concrete allocator frame into `successfulRemovalFrame`.  The diagnostic's frame has the same eight local updates in the same order as `successfulBaseFrame`, followed by the same setup, capacity, and allocator constructors.  I will fold `successfulBaseFrame` and `successfulRemovalFrame` at this exact callback before applying the accessor.  Progress means the accessor removes the root getter and the specialized copy-shape lemmas reduce the remaining scalar transfer.

The import check succeeded.  This proof environment has no `fold` tactic, so that command stopped the callback and produced cascading unresolved premises from its enclosing theorem applications.  I will specialize `successfulRemovalFrame_root` as a local fact and simplify only that fact through the two named frame definitions and the fixed entry-frame definitions.  Its resulting left side is the concrete getter printed in the residual, allowing `rw` to avoid any target-side folding.

The import check succeeded.  The local root fact unfolded the specialized names, but `simp only` retained the entry frame as `toLocals`, `List.map`, and `List.take`; the residual contains the evaluated 23-local entry frame.  I will define `successfulBaseFrame` from that concrete frame, which Lean printed after checking the preceding execution.  The definition still names `FixedArrayFindIdxEq.someFrame` and the eight exact local updates; it removes only deterministic entry-frame evaluation from this proof boundary.  The root fact will then unfold `removalFrame` as well as the two specialized names.

The import check succeeded.  The specialized base now matches every local in the residual.  Its parameter field still projects `someFrame.params`, while the residual has the already evaluated singleton parameter list.  I will set that field directly to `[.i64 inputPtr]`; `someFrame_params` proves this equality and the search frame remains unchanged in the local-list expression.

The import check succeeded, and the concrete root getter now rewrites.  The final broad simplifier then exhausted four million heartbeats after 128 seconds while normalizing the completed copy frame inside the scalar result transfer.  I will not rerun that proof term.  I searched the selected package indexes and mirrored ProofKit for `result transfer`, `return local`, `localSet localGet`, `branchPost`, `frame params`, and `counterFrame`.  The existing `fixed-array-erase-copy` entry provides direct `counterFrame` values and shape projections.  The existing `fixed-array-result` entry provides `finishProgram_spec`, but its goal begins with all four finish instructions; this residual has already consumed the root getter across a checked branch continuation, so that theorem does not match.  No new entry provides the three-instruction stack-to-result transfer.  I will first rewrite the concrete frame's values, parameter length, and local length through the direct copy, allocator, capacity, and search-frame projections.  The remaining simplifier will see only closed bounds and scalar local operations.

The import check succeeded, but the focused frame-shape rewrite itself exhausted four million heartbeats after 131 seconds because it still traversed the full continuation with a concrete `counterFrame`.  I will not rerun that boundary.  I will add a checked `successfulCopyFrame` definition with the already proved validity witness, then change the callback frame to this exact name before exposing the returned-root instruction.  Proof irrelevance identifies the adapter's validity witness with the definition's witness.  Root, values, and shape lemmas will rewrite without traversing or unfolding the search, decoder, allocator, and copy frames.

The import check succeeded.  Naming the completed copy frame reduced the callback in 9.7 seconds.  The final scalar simplification leaves exactly `FormalSpec.UInt64ArrayAt final (heapTop + 48) (input.eraseIdx! index)`, identical to `hResult`; the simplifier did not use that hypothesis because the proposition uses the specification alias.  I will close it with `exact hResult`.  No successful-path goal remains after this fact.

The import check succeeded in 9.4 seconds, and only the oversized-input branch remains.  I inspected the exact invalid program, `function_0_length_dispatch_0_invalid_capacity_eq`, `FixedArrayAllocatorWindow.constantCapacityRegion_spec_withTail`, `FixedArrayResult.lengthStore_spec`, `emptyStore_at`, and `finishProgram_spec`.  The invalid branch consists of the checked 18-instruction constant-capacity prefix, the allocator at offset 3 through offset 35, a four-instruction zero-length store, and `finishProgram 8 6 7`.  I will add `rfl` tail decompositions at offsets 0, 35, and 39, name the post-allocation frame, and apply that complete composition before reducing an instruction.  Progress means the composition constructs `UInt64Array.At` for `#[]` and leaves only the public returned-root postcondition.

The import check succeeded.  The complete oversized composition checks through constant capacity, allocation, the zero-length store, empty-array reconstruction, and the finish program.  Its only residual is local 7 read from `{ finishFrame ... with values := [] }`, because the enclosing branch continuation clears the operand stack.  The selected `local-frame-projection` entry's `Frame.withValues_get` has this exact goal shape.  I will apply it before `FixedArrayResult.finishFrame_return_get`; no reduction of the finish frame is needed.

The import check succeeded.  `Frame.withValues_get` and `FixedArrayResult.finishFrame_return_get` expose `some (Value.i64 (heapTop + 48))`.  The remaining goal is the reducible match on that value around `wp module []`; therefore the following `rw [wp_nil]` cannot yet match.  I will reduce this single `Option` match with `simp only`, then apply `wp_nil` and the already proved empty-array representation.  Progress means the oversized callback closes and no theorem goal remains.

The import check and Lean artifact build succeeded in 9.6 seconds after reducing the final `Option` match.  The theorem has no residual goal.  I will leave `Behavior.lean` unchanged, write the required sorted knowledge-use records, and run the prescribed import check and artifact build once more for final acceptance.

Final acceptance succeeded: the import check returned success, and the independent `ArtifactResult` rebuild completed all 3079 jobs.  The proof uses the shared declarations `FixedArrayFindIdxEq.program_spec`, `EncodedIndexDecoder.program_spec`, `FixedArrayAllocatorWindow.region_spec_withTail`, `FixedArrayAllocatorWindow.constantCapacityRegion_spec_withTail`, `FixedArrayResult.lengthStoreLocal_spec`, `FixedArrayResult.lengthStore_spec`, `FixedArrayResult.emptyStore_at`, `FixedArrayResult.finishProgram_spec`, `FixedArrayCopy.eraseIdxProgram_spec`, and `Frame.withValues_get`, together with their frame and memory projections.  No residual goal remains.
