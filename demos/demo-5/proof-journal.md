# Proof Journal

The initial prescribed build on 2026-08-07 failed in `Behavior.lean` at the expected whole-function `TerminatesWith` goal.  The deterministic starter successfully decomposed `RuntimeReady` into the represented input array, allocator globals, input/output memory bounds, heap-reserve bounds, and page limit.  The first applicable checked recipe is the annotated unsigned length-at-most-eight dispatch, `wp_fixed_array_length_le_dispatch 5, 8`.

The decoded valid branch reserves one word for each input element, uses the shifted allocator at offset six with one trailing local, and then runs a block-wrapped filter loop.  Local 7 is the input index and local 11 is the value-dependent output count.  I will use an invariant representing `(input.extract 0 index).filter (· < 100)` in the allocated payload; the accepted branch appends one element and increments the output count, while the rejected branch changes only the input index.  The invalid-size branch uses the offset-zero allocator with seven trailing locals and writes an empty-array length.

The first edited build reached three early definition-shape issues in 5.1 seconds.  The two prefix lemmas must apply `Array.filter_push_of_pos` or `Array.filter_push_of_neg` with the generated stop argument, because unfolding `Array.filter` exposes that argument before the extract rewrite.  The public proof also needs separate frame normalization and a named `func0 = wrapperProgram` equality so a failure distinguishes the entry-frame reduction from the exact program decomposition.

The second edited build confirmed that the entry frame normalizes and isolated the exact-program equality as the remaining structural mismatch.  It also showed that the valid prefix requires a second symbolic-execution phase after each of its two length loads, and that the heap-reserve inequality differs from the allocator premise only by addition association.  I will compare the two decoded branch lists with temporary `#eval` diagnostics through the prescribed build, then remove those diagnostics after correcting the decomposition.

The third edited build exposed two diagnostic-definition mistakes before evaluation: the instruction constructor must be qualified as `Wasm.Instruction.iff`, and the extracted branch needs an explicit `Option Wasm.Instruction` type.  The positive filter lemma now has only the trivial `index ≤ input.size` stop-bound goal.  The negative lemma's eager bang-normalization had no target occurrence, so I will normalize only the predicate premise and supply both stop bounds with `omega`.

The fourth edited build proved all prefix-array helper lemmas and reported the structural comparison: the invalid branch matches all 43 top-level instructions, while the generated valid branch has 69 instructions, the proposed branch has 71, and their first mismatch follows 52 matching instructions.  I will print the short slices around that position to correct the valid suffix.  The valid-prefix execution remains at the capacity conditional with an operand-stack value that must be normalized before applying `wp_iff_cons`.

The fifth edited build took 35 seconds and reached the final line of the specialized allocator theorem.  Its only allocator mismatch is in my theorem statement: the exact valid allocator hard-codes stride one, while I left the postcondition parameterized by an arbitrary stride.  Specializing the postcondition to stride one makes the checked six-header memory transformer identical.  The whole-function proof then reaches the short initialization before the filter loop, where one redundant `simp` call needs removal or replacement with the specific frame normalization.

The sixth edited build completed the specialized allocator theorem and exact wrapper decomposition.  Before the loop, symbolic execution has evaluated the minimum comparison to the `i32` zero condition and left the result-selecting `if` as the next instruction.  Selecting its false branch loads the input length into local 9, after which the block-loop invariant applies.

The seventh edited build entered both loop cases.  The remaining initialization equality needs simplification of the empty filtered prefix.  At loop exit, generated store bounds use the modulo address before the checked root-address equality, so the proof must select that bound and then rewrite the store address.  In the active case, reducing the already-evaluated `br_if` match exposes the input-load bound; the accepted output-store address should be rewritten before applying its logical address bound.

The eighth edited build reduced the proof to three sequencing errors.  The empty-payload contradiction needs `keptPrefix` unfolded, the exit branch already reaches the public postcondition after rewriting the length store, and the input load occurs before the generated index increment.  I will remove the extra execution step and delay the `hIndexSucc` rewrite until the loop-tail local update.

The ninth edited build completed initialization and exposed the two nested predicate conditionals.  The first conditional normalizes the unsigned comparison to `i32` zero or one; the second converts that value into the decision to store.  Each needs an explicit `wp_iff_cons` step.  Two exit arithmetic goals also require using `hBump.fit32` as a field rather than rewriting the `BumpFacts` structure, plus a separately stated root-address-to-Nat equality after simplification removes `+ UInt64.ofNat 0`.

The tenth edited build completed the exit representation and both predicate decisions.  The accepted store check remains in the emitted modulo-Nat form even after the memory-write address rewrites to the checked word address, so it needs its own bound derived from `BumpFacts.fit32` and the payload word fit.  The rejected-frame equality should use `simp only` with the prefix and index successor equations; broad simplification expands the filter implementation and exceeds its step limit.

The eleventh edited build leaves two local proof-script corrections.  After selecting the accepted store bound, the loop rule has already produced the invariant-and-measure goal, so no further symbolic execution applies.  I also changed the accepted-frame simplifier in the previous edit; the same restricted simplifier belongs on the rejected-frame equality.

The twelfth edited build reached the semantic invariant updates.  The accepted frame needs an explicit no-wrap equation for incrementing the filtered-prefix size, parallel to the input-index successor equation.  Its payload proof must avoid dependent rewriting of the array under `getElem`; I will prove the read equation for the pushed prefix first and then transport the complete equation with `simpa`.  The rejected frame retains a literal `List.set` from the generated local update, which a targeted `Locals.set?` and `List.set` simplification closes.

The thirteenth edited build reduced the accepted payload proof to the strict bound and the old-element lookup for `Array.push`.  The pushed-array bound yields `i ≤ old.size`; combining it with the unequal-index branch yields the required strict bound.  I will apply `Array.getElem_push_lt` explicitly, avoiding simplifier dependence on its proof argument.  The rejected frame now closes, and its diagnostic shows that `List.set` alone is the required normalization.

The fourteenth edited build succeeded.  The theorem proves the exact decoded wrapper: the valid branch allocates capacity for the bounded input, iterates through each element, stores only values below 100, and records the value-dependent result length; the invalid branch allocates and returns an empty array.  The remaining output consists of two non-failing simplifier linter suggestions.  I will run the required final rebuild without changing the candidate.
