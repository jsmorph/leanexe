# Proof Journal

## Initial check

I ran `node PROOF_IMPORT_CHECK.js LeanExeGen/GeneratedR23fa7efc3fb0298b/Behavior.lean` against the untouched candidate, and it succeeded.  I then ran the required `ArtifactResult` build through `/home/somebody/src/leanexe/tools/leanrun` with the 60-minute timeout.  The build reached `Behavior.lean` and reported one unsolved goal at line 17: after the starter decomposed `RuntimeReady` and derived the common array and global facts, the goal was `TerminatesWith env module 0 initial [Value.i64 inputPtr]` with the expected represented output postcondition.

## LTG retrieval

I began retrieval by opening `LTG/categories.json`, as required.  The categories relevant to the residual goal are `proof-construction`, `arrays`, `loops`, `allocation`, `memory`, and `compiler-motifs`.  I have not selected an entry yet because the category indexes still need searches keyed by the exact annotations and loop structure.

I searched the relevant category indexes with `rg` for `leanexe\.array\.fold\.v1|foldPrefix|ArrayFold`, then for `length-dispatch|FixedArrayLengthDispatch|leProgram_spec|wp_fixed_array_length_le_dispatch`, and then for `wp_block_loop|wp_entry_to_loop|loop-carried|accumulator`.  The first query found the canonical `array-fold-prefix` entry, and the second found `fixed-array-length-dispatch`; both match checked annotations in `PROGRAM_ANNOTATIONS.json` and declarations in `PROOF_RECIPES.json`.  The third query also found `scalar-post-test-loop` and `demo8-counter-transfer-example`, which I rejected at the index stage because their required `leanexe.loop.scalar-post-test.v1` annotation does not occur in this artifact.

I inspected only `entry.json` and `README.md` for `array-fold-prefix` and `fixed-array-length-dispatch`.  I selected both entries: the dispatch entry supplies the direct `leProgram_spec` recipe at the public wrapper boundary, while the fold entry supplies the semantic invariant `ArrayFold.foldPrefix input UInt64.add 0 index` and its successor and completion equations.  Neither entry covers allocation or result construction, so the remaining lookup must target the post-fold singleton result and the invalid empty-array result.

I inspected the frozen `FormalSpec`, `Program`, and `AnnotationMatches` modules.  Function 0 has an initial unsigned `size ≤ 8` branch; each branch inlines the empty-free-list allocator, the valid branch then traverses the represented input with one block-wrapped loop and stores the accumulator as the singleton payload, and the invalid branch stores zero as the output length.  `AnnotationMatches` contains no additional region theorem beyond its imports, so the checked recipes for length dispatch and fold remain the available exact annotations.

I searched the relevant indexes for `singleton|empty-array|empty array|resultSuffix|result construction|FixedArraySingleton|wrapperProgram_spec`, for `FixedArrayAllocator|allocator|empty free|bump|region_spec_withTail|result-region`, and for `UInt64ArrayAt|write64|array-memory|FreshFixedArrayAt|result storage`.  The allocator and memory queries found `fixed-array-allocation` and `array-memory-framing`, whose declarations match the inlined allocator and its need to preserve the input representation before the fold.  I rejected `fixed-array-singleton-wrapper`, its two worked-example entries, and the map and filter entries at the index stage because their whole-function annotations and direct-call or transformation layouts do not match function 0.

I inspected only `entry.json` and `README.md` for `fixed-array-allocation` and `array-memory-framing`, and I selected both.  The allocation entry directs this proof to `FixedArrayAllocatorWindow.region_spec_withTail`, since function 0 places the allocator scratch window after application locals and retains trailing locals.  The framing entry selects `FixedArrayPairResult.input_preserved_by_alloc` or `UInt64Array.At.frameBefore` to retain the represented input through the valid branch’s allocation before its traversal loop.

## Direct dispatch attempt

I confirmed the signatures of the declarations named by the selected entries and recipes in their permitted dependency modules.  I added `TerminatesWith.of_wp_entry_for (f := func0Def) rfl` followed by the exact recipe invocation `wp_fixed_array_length_le_dispatch 7, 8`, without reducing the dispatch instructions.  The next check will show whether inference supplies the represented input and how the valid and invalid branch obligations expose the allocator regions.

The import check succeeded, and the Lean build showed that the direct tactic matched the exact decoded prefix but left its input pointer and input array as metavariables.  That inference failure also produced the routine frame, local-bound, and maximum-size premises alongside the two intended branch obligations, so it worsened the proof boundary without adding semantic information.  I replaced it with the catalogued `_from hArray` form and supplied the five routine premises explicitly; the next check will isolate the valid and invalid branch goals.

The `_from hArray` check succeeded through represented-input inference and reduced the dispatch to its valid and invalid branch programs.  Four routine premises closed; the local-bound premise retained `Function.toLocals`, so I added that named definition to its normalization.  I am using `wp_run` only for each branch’s short capacity calculation to expose the conditional immediately before the allocator, where the selected allocator theorem should replace instruction-level execution.

The import check again succeeded.  The build closed the dispatch premises, but `wp_run` stopped with unreduced `branchFrame`, `Function.toLocals`, and generated function fields inside local accesses, before reaching the capacity conditional.  This is a frame-presentation gap, so I added focused normalization for those three named definitions before retrying the short straight-line prefix.

Focused normalization reduced the generated frame but left `List.set` and its `min`-normalized local coordinate opaque to ordinary `wp_run`.  The diagnostic still precedes the allocator and contains no semantic obligation.  I switched the short prefix to `wp_length_dispatch`, the checked focused simplifier from the selected dispatch module, while retaining only the same three frame definitions as extra facts.

The checked simplifier reduced the capacity arithmetic to 16 in the valid branch and 8 in the invalid branch.  Its residual goal retained the projections `Function.numParams` and `ValueType.zero`, which prevented the local access from reducing and therefore kept the capacity conditional folded into a match.  I added those two named generated-frame definitions to the focused fact list; no invariant or memory reasoning has begun yet.

The next check reached the concrete capacity conditional in both branches with valid frames and an `i32 0` condition: local 11 contains 16 for the singleton result and 8 for the empty result.  I now discharge the false capacity-adjustment branch with `wp_iff_cons`, normalize its empty instruction list, and use an exact `change` to refold the remaining instructions as `FixedArrayAllocatorWindow.region 2 1`.  This tests the allocator-region equality before applying its semantic theorem, as required by the selected LTG entry.

The import check succeeded, and both conditionals reduced.  The subsequent `change` used the theorem parameter name `module_`, which is absent from the public proof context; the generated module constant is `«module»`.  I corrected that identifier without altering the proposed region equality.

The corrected `change` succeeded for both exact instruction lists.  I began the invalid branch with capacity 8, deriving its existing-memory premise by reducing `FormalSpec.expected input` to the empty array under `hSize`.  After `region_spec_withTail`, I derive the returned root’s concrete `UInt32` address from `Allocation.bumpFacts` and use the allocator-window simplifier for the short zero-length store and public return; the next diagnostic will identify any remaining representation fact.

The allocator application matched, but the generic tactic chain left five named premises because `UInt64.toNat 8` did not normalize under `norm_num`; subsequent tail tactics therefore ran against the capacity premise.  I replaced the chain with named cases, convert the capacity premise to `8 ≤ 8`, and pass the page and global facts explicitly.  The root-address argument now uses the same explicit natural-number conversion before the tail simplifier runs only in `hNext`.

All allocator premises now close.  The root-address theorem required simplification after, rather than during, application because its conclusion contains `+ UInt64.ofNat (8 * 0)`; I now instantiate it with a separate word-capacity fact and simplify the result.  The tail diagnostic also showed that `allocFrame` remained folded and that the store needs the generated modulo-form bound, so I added `allocFrame` to the focused simplifier and derived that bound from `BumpFacts.rootToNat`, `fit32`, and the existing-memory premise.

The build reduced the invalid tail through the store and local transfers.  Its residual expression consists of `getElem?` matches over concrete local lists before the final empty-array representation.  I selected the documented `wp_alloc_window_lists` variant, which adds only the head and successor lookup rules needed to normalize these list matches.

The list-focused tactic now leaves one store-bound conjunction with the empty-array ABI predicate.  I discharge the store bound through `allocStore_pages`, construct the output representation from its two region bounds and zero length read, and identify the generated store address with the root pointer through `Memory.toUInt32_eq_ofNat`.  `Mem.read64_write64_same` supplies the zero length word, while the empty array makes the indexed-element premise vacuous.

The invalid representation proof reduced to `0 = UInt64.ofNat 0`.  I had named a nonexistent simplification theorem for that equation, although the equality holds by reduction.  I removed the name and close the equation with `rfl` after the read-over-write rewrite.

The invalid branch now checks completely, leaving only the valid allocator boundary.  I applied the same exact shifted-window theorem with capacity 16 and derived its memory premise by reducing the expected array to a singleton under `hSize`.  The next check isolates `hNext`, which begins with the singleton length store and then the annotated traversal loop.

The valid allocator premises close and expose the expected suffix.  I derive the allocation root address and modulo-form store bound, preserve the represented input through the allocator with `input_preserved_by_alloc`, and preserve it again through the result-length store with `UInt64Array.At.write64After`.  The focused list simplifier receives the post-store input length read and bound so it can execute initialization only up to the generated minimum-selection branch before the fold loop.

The valid suffix simplifier reached the expected minimum-selection branch but retained three preceding memory bounds and two input-length reads.  The result store uses `heapTop.toUInt32 + 48`, while the representation fact uses `(heapTop + 48).toUInt32`; I added their equality via `hRootPointer` and `UInt32.toNat.inj`.  I also supplied `BumpFacts.rootToNat` and the original generated input bound so the focused simplifier can normalize the displayed address and page forms.

That broad focused simplification exhausted the theorem’s one-million-heartbeat limit after 44 seconds without producing a local semantic goal.  This is the elaboration-boundary failure described by the selected diagnostics guidance, so I am dividing the suffix at its first result store instead of increasing the limit.  `wp_alloc_to_store_lists` now executes only the three instructions that prepare the length store; the post-store initialization and loop will use a separate proof boundary.

The store-boundary tactic returns in six seconds with the result length and root address on the operand stack.  I now run a second focused simplifier from that exact store boundary through the two input-length loads, supplying only the store address, store bound, preserved length read, and input bound.  This smaller call should stop at the minimum-selection `iff`, before any loop instruction or invariant elaboration.

The second call also exhausted the heartbeat limit after 48 seconds, so the large generated frame remained the source of elaboration cost even after the store split.  I introduced `validBaseFrame`, an exact compact name for the concrete valid-branch frame, and refold the allocator goal to use it before applying `region_spec_withTail`.  At `hNext`, focused getter and empty-stack facts let `wp_alloc_to_store_lists` prepare the store while keeping the allocator frame folded; I removed the timed-out second call.

The exact frame refolding and allocator application succeed, but the store-preparation tactic unfolds `Locals.get` before it can use the folded-frame getter fact.  I now rewrite the leading `localGet 7` directly with `wp_localGet_cons`, `hAllocRoot`, and `hAllocValues`.  The remaining `wrapI64` and constant instructions require no local-frame reduction, so `wp_alloc_to_store_lists []` can leave the frame folded at the store.

The direct `localGet` rewrite works: the current frame remains `allocFrame 2 (validBaseFrame inputPtr) heapTop 16`, and only the two store operands appear on its value stack.  I now apply `wp_store64_cons` directly.  The next goal will expose one memory-bound decision and then the post-store state without expanding the frame.

`wp_store64_cons` exposes the expected match over the concrete two-value stack, with the allocator frame still folded.  I added `simp only [FixedArrayAllocator.allocStore_pages]`; its definitional reduction handles the stack match, record projections, zero store offset, and page equality.  The resulting conditional should use the already named root bound.

The store goal now has one explicit bounds conditional and a post-store frame represented only by the folded allocator frame’s parameters and locals.  I select the safe branch from `hRootBound`, rewrite the generated `UInt32.ofNat` address to the root pointer, and refold the resulting store and frame as `validLengthStore` and `validAllocatedFrame`.  These names establish a small boundary for input initialization and the loop proof.

The safe store branch selected successfully.  A subsequent `simp only` reported that it made no progress, which means the `if` reduction had already normalized the zero offset and address into the form required by the named store.  I removed that redundant simplifier and retained the exact `change` to test the store and frame refolding directly.

The exact `change` showed the remaining representational difference: the interpreter retains `UInt32.ofNat ((heapTop + 48).toNat % 2 ^ 32) + 0`, while `validLengthStore` uses `(heapTop + 48).toUInt32`.  I now prove that address equality explicitly by evaluating `2 ^ 32`, using `BumpFacts.rootToNat`, and applying `hRootPointer`.  Rewriting this equality precedes the store and frame refolding.

The generated address proof used the generic `add_zero` name, which did not rewrite the machine-word addition.  The permitted `Init` source confirms the specific theorem `UInt32.add_zero`, so I use that declaration.  The main goal already refolds successfully as `validLengthStore` and `validAllocatedFrame` once the address equality is available.

The machine-word address proof and both refoldings now check.  From the compact post-store boundary, I use the dispatch module’s focused simplifier for the straight-line initialization through the two represented length loads.  Its additional facts unfold only the two named frames and supply the post-store length read, bound, and pointer equality; the tactic must stop at the minimum-selection control instruction.

The focused initialization check completes in seven seconds and stops before the minimum-selection `iff`.  It leaves two identical out-of-bounds guards, each comparing the post-store page capacity against the generated input-length address.  `hLengthBoundAfter` proves the negation of both guards directly, so I discharge them explicitly and retain the displayed loop-entry frame for the next proof boundary.

Both explicit guards reduced, but `omega` could not identify `validLengthStore.mem.pages` with the record expression occurring in `hLengthBoundAfter`.  The failed arithmetic search left the intended main goal unchanged and introduced no new semantic premise.  I now derive a typed bound by `simpa [validLengthStore]` and use `Nat.not_lt_of_ge` for each guard.

The typed post-store bound closes both guards and leaves the exact decoded suffix beginning with `iff 0 1 [localGet 14] [localGet 12]`.  Its condition is the concrete `i32 0`, so the interpreter must take the second branch, copy the input length into effective-stop local 15, and reach the block-wrapped traversal.  I use `wp_iff_cons` and the checked straight-line `wp_run` tactic only for this control-free boundary; the following loop still requires the recipe’s invariant and measure.

`wp_iff_cons` selects the intended branch, while `wp_run` stops on finite arithmetic inside `Locals.get` and `Locals.set?`: comparisons such as `12 < 0 + 1 + ⋯` have not normalized.  The displayed residual contains the block as the next program instruction and has not entered the loop.  I add `norm_num` to evaluate those concrete indexes and list operations before naming the loop-entry frame.

Numeric normalization reaches the exact block boundary with accumulator and index zero, effective stop equal to the represented input length, and an empty operand stack.  I name the full 20-local frame so a loop iteration can normalize without retaining a chain of `Locals.set?` records; the invariant existentially quantifies the scratch locals while fixing the index and accumulator to `ArrayFold.foldPrefix`.  I now attempt the recipe’s complete `wp_block_loop` composition with measure `input.size - index.toNat`, read from annotated local 13, before reducing any instruction inside the loop.

The exact frame refolding succeeds, but the invariant parser rejects the Unicode implication glyph in anonymous functions and match branches; Lean requires `=>` in those positions.  Consequently, the diagnostic contains a parser-inserted placeholder and the loop theorem has not run.  I correct only those tokens and retry the same complete composition without changing its invariant or measure.

The corrected complete composition matches the exact block and loop, producing only `hInit` and `hStep`.  This is the intended proof boundary: initialization states the fold prefix at zero, and the step receives the full decoded loop body under the invariant.  I keep the composition, close initialization from the definition of `foldPrefix`, unpack the invariant, and split the step between `index = input.size` and `index < input.size`.

Before reducing the step’s element load, I searched `LTG/categories/{arrays,loops,memory,proof-construction,compiler-motifs}/tools.jsonl` with `rg` for `FixedArrayTraversalInput|generatedElement|traversal[- ]input|input traversal|load.*element|element.*load`.  The task-specific indexes returned no entry, so there is no additional LTG item to inspect or select for this residual.  The already selected `array-memory-framing` entry and imported `UInt64Array.At.generatedElement` fact cover the load bound and value, while `array-fold-prefix` covers the accumulator transition.

The invariant initialization closes from `ArrayFold.foldPrefix` at index zero, and unpacking the step leaves the two intended cases.  I first substitute `index = input.size`, unfold the named loop frame, and let `wp_run` execute the true guard and the straight-line singleton-result suffix.  Numeric normalization remains confined to the resulting concrete local indexes and instruction constants; the represented-output proof will follow the store boundary exposed by this check.

The exit reduction reaches the final result path but leaves `match [Value.i32 1]` unreduced around both the unreachable traversal path and the intended singleton payload store.  Numeric normalization cannot select that constructor match while its alternatives remain embedded in the continuation.  I add a definitional `simp only` step to perform iota reduction, followed by a second numeric normalization for the selected store address.

The iota-only simplifier reports no progress because the `br_if` continuation uses the numeric subpattern `Value.i32 0`; selecting the `Value.i32 1` discriminator needs the ordinary numeric simp rules.  The absence of a separate exit diagnostic confirms that this failed tactic remains at the constructor decision rather than the represented-output predicate.  I replace it with ordinary `simp`, retaining numeric normalization after the selected branch.

Ordinary simplification selects the nonzero `br_if` path, and the exit goal becomes one store-bounds conditional for result word 1 at `heapTop + 48 + 8`.  Capacity 16 covers both the length word and payload word, so `BumpFacts.wordAddress_toNat 1` identifies the generated address and `hFitMemory` proves the memory bound.  After selecting the safe store branch, I unfold only `branchPost` and execute its final `localGet 6`; the next residual should be the singleton ABI predicate.

The store-bound proof succeeds, and the public return reduces to the exact `FormalSpec.UInt64ArrayAt` predicate for the singleton result.  The address proof failed before that point because rewriting does not treat the literal machine word `8` as syntactically identical to `UInt64.ofNat (8 * 1)`.  I name the normalized `toNat` result from `wordAddress_toNat`, derive both machine-address presentations from it, and prove the representation: allocation bounds cover 16 bytes, the payload write is disjoint from the length read, and the payload read sees the fold sum at the same address.

The allocation and memory bounds, singleton length read, and result-value normalization now close.  `omega` cannot normalize the `UInt64.toNat 16` field inside `BumpFacts.fit32` while proving the strict payload-address bound, so I extract that field and run `norm_num` before arithmetic.  I also name the base and root bounds for `UInt32.toNat_add`; the final element-read goal differs only by `UInt32.ofNat 8` versus the numeral `8`, which an exact `change` converts to `Mem.read64_write64_same`.

The exit branch closes completely, including the `Array.foldl` postcondition through `foldPrefix_size`.  In the strict branch, the preserved `UInt64Array.At` predicate proves that both encoded sizes retain their natural values, so the unsigned `index ≥ size` guard is false.  I obtain the generated load bound and value from `At.generatedElement`, normalize its multiplication order once, and reduce the straight-line body through the back-edge; the next residual should consist of invariant restoration and measure decrease.

The strict branch reaches exactly the two obligations promised by `wp_loop_cons`: its accumulator local is `foldPrefix index + input[index]`, and its index local is `UInt64.ofNat index + 1`.  The first attempt to prove the current encoded index bound with `omega` left `UInt64.size` opaque, although `hCurrentInput.size_lt` already supplies the required upper bound.  I use transitivity through `index < input.size`, restore the invariant at `index + 1` with `foldPrefix_succ`, and normalize the successor encoding before proving `input.size - (index + 1) < input.size - index`.

Invariant restoration now closes.  The remaining measure expression has already expanded the two machine-word `toNat` operations to `(index + 1) % 2^64` and `index % 2^64`, so the named encoded-index equalities no longer match syntactically.  I restate `hIndexSuccLt` with the concrete `2^64` bound, eliminate both moduli with `Nat.mod_eq_of_lt`, and leave the strict natural subtraction to `omega`.

The import check succeeds, and the complete `ArtifactResult` build succeeds in 12 seconds.  Lean reports three linter warnings for `norm_num` calls that make no progress after neighboring simplification has already normalized their goals.  I remove only those no-op tactics; the loop invariant, allocation proof, memory proof, and public postcondition remain unchanged before the required final validation pair.

After removing the no-op tactics, the import check and complete `ArtifactResult` build both succeed without warnings.  A final text scan finds no `sorry`, axiom declaration, or `Source` import in `Behavior.lean`; the workspace has no Git worktree metadata available for `git diff --check`.  I will now run the mandated import check and resource-limited build once more, without further edits or intervening commands.
