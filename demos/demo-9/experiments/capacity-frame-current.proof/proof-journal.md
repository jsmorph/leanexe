# Proof Journal

## Initial check

I ran the required import check against the unedited candidate.  It succeeded with status 0, so every existing import lies within the permitted boundary.  I then ran the required `ArtifactResult` build through the specified Lean runner.

The build failed at `Behavior.lean:20:116` with the deterministic starter goal.  The context contains the decomposed `RuntimeReady` facts for globals, input representation, address bounds, heap bounds, and page bounds.  The remaining goal is `TerminatesWith env module 0 initial [Value.i64 inputPtr]` with a postcondition that returns one `i64` pointer whose memory represents `FormalSpec.expected input`.

## Task-local guidance

I started LTG retrieval at `LTG/categories.json`, as required.  The catalog divides relevant material among arrays, compiler motifs, loops, allocation, memory, and proof construction.  The frozen feature report identifies one 489-instruction function with three loops, memory operations, arithmetic, and allocation.

The annotation report identifies an exact unsigned length dispatch and a forward single-slot array fold.  The recipe file provides a direct length-dispatch tactic, checked capacity-prefix equalities, fold setup and result theorems, an exact fold-region equality, traversal support, and fold-prefix lemmas.  No complete composition appears in `PROOF_RECIPES.json`, so the first construction attempt must combine the direct recipes rather than apply a preassembled artifact theorem.

I will query only the relevant category indexes for the exact annotation kinds and recipe declarations.  The first searches target `leanexe.array.length-dispatch.v1`, `FixedArrayLengthDispatch`, `leanexe.array.fold.v1`, `FixedArrayFold`, `foldPrefix`, `RuntimeReady`, and `TerminatesWith`.  An entry will count as relevant only if its index metadata describes the exact dispatch, fold, allocator/result, or public-proof boundary present in the current goal.

## LTG retrieval

I searched the arrays, compiler-motifs, and proof-construction indexes with `leanexe\.array\.length-dispatch\.v1|FixedArrayLengthDispatch|leProgram_spec`.  I inspected `fixed-array-length-dispatch` and `fixed-array-capacity`; both match the exact dispatch recipe and its branch prefixes, so I will use them.  The dispatch entry requires conversion from public `TerminatesWith` to body-level `wp`, and the capacity entry executes each branch's first eighteen instructions.

I searched the arrays, compiler-motifs, and loops indexes with `leanexe\.array\.fold\.v1|FixedArrayFold|foldPrefix|forwardSetupProgram_spec|resultProgram_spec`.  I inspected `array-fold-prefix`, `fixed-array-fold-structure`, and `fixed-array-traversal-input`; all three match the checked forward one-word fold and will supply its invariant, setup/result frames, and continuing element load.  I also inspected `fixed-array-result` because the same query returned its singleton representation support, and I expect to use its length-store, payload-store, and representation theorems after loop completion.

I searched allocation, arrays, memory, and proof-construction with `RuntimeReady|TerminatesWith|artifact_behavior|singleton|resultSuffix|region_result_spec`.  This returned unrelated singleton-wrapper entries whose annotation kinds require a direct scalar call, which this function lacks, so I rejected `fixed-array-singleton-wrapper`, `demo7-counter-transfer-example`, `demo8-counter-transfer-example`, and `residual-specification-normalization` without opening them.  `fixed-array-result` remained relevant because both result branches write a fixed array directly.

I then searched allocation, arrays, compiler motifs, and proof construction with `FixedArrayEqNode|region_result_spec|FixedArrayAllocator|allocator region|bump region|empty free list`.  I inspected `fixed-array-allocation`, whose shifted-window theorem matches the emitted allocator shape conceptually, although no allocator annotation or generated equality exists for this artifact.  I will defer that entry until the direct dispatch exposes the exact branch frame and program boundary; its `region_spec_withTail` theorem is usable only if the branch reduces to the named offset region.

## Direct dispatch attempt

The selected direct recipe says to convert the public goal with `Wasm.TerminatesWith.of_wp_entry_for`, then invoke `wp_fixed_array_length_le_dispatch_from hArray at 7, 8`.  I will use the checked `wp_entry` wrapper with `func0Def`, unfold only `func0`, and invoke that tactic unchanged.  Progress means replacing the public termination goal with separate valid and invalid branch `wp` obligations; a shape failure before those branches would identify a mismatch in the recipe boundary.

The import check succeeded after this edit.  The Lean build failed at the explicit `unfold func0`, because `wp_entry` left the program syntactically as `func0Def.body`; the diagnostic confirms that public-to-body conversion succeeded.  This is an instruction-boundary presentation issue rather than a semantic failure, so I will remove the unnecessary unfolding line and retry the exact dispatch tactic against the definitionally reducible body.

The second import check also succeeded.  The dispatch tactic recognized the entire generated `leProgram 7 8` shape, including both branch programs and the final `[.localGet 6]` suffix, but its application failed because `wp_entry` introduced a fresh store argument instead of specializing the body proof to `initial`.  The LTG entry prescribes `Wasm.TerminatesWith.of_wp_entry_for (f := func0Def) rfl` for a store-specific public proof, so I will replace the generic macro with that exact conversion and keep the dispatch invocation unchanged.

The third import check succeeded, and the store-specific conversion allowed the direct dispatch recipe to apply.  Lean now reports the five expected entry-frame premises followed by separate `hValid` and `hInvalid` branch obligations.  The exact direct recipe has therefore improved the proof boundary from public termination of 489 instructions to two branch programs, each beginning with the checked constant-capacity motif.

I will solve the parameter, operand-stack, local-index, and `UInt64.size` premises by reduction and arithmetic.  In each branch I will introduce the size hypothesis, refold the first eighteen instructions as the annotation-matched capacity program, and apply `FixedArrayCapacity.constantProgram_spec`.  Progress means reaching the post-capacity named frame at the allocator prefix without reducing the capacity arithmetic or either branch body.

The fourth import check succeeded.  Both capacity programs refolded and both applications of `constantProgram_spec` succeeded, leaving three routine frame premises and one post-capacity continuation per branch.  The valid continuation begins with `constI64 0; localSet 16; constI64 0; localSet 12; globalGet 1; localSet 13`, which is `FixedArrayAllocatorWindow.region 2 2`; its frame has twenty internal locals, so `region_spec_withTail` applies with offset 2 and tail 4.

I will add the two LTG-authorized allocator imports and `FixedArrayResult`, solve the capacity-frame premises by reduction, and apply the shifted allocator theorem with capacities 16 and 8 in the valid and invalid branches.  The respective `RuntimeReady` heap reservations reduce to `48 + 16` and `48 + 8`, so `hHeapFitMemory` supplies the theorem's fit premise.  Progress means replacing each allocator loop and its header writes with the named `allocStore` and `allocFrame` continuation.

The fifth import check accepted all three LTG-authorized imports.  The build solved every capacity-frame premise but rejected the allocator refolding because I passed stride 2.  Inspection of the checked allocator definition distinguishes the element-type header constant 2 at offset 24 from the stride header at offset 16; this artifact stores stride 1, so the exact region is `FixedArrayAllocatorWindow.region 2 1` in both branches.

The sixth import check succeeded.  With stride 1, `region_spec_withTail` applied in both branches and replaced each allocator search loop, bump path, header store sequence, and allocator-global update with its checked `allocStore` and `allocFrame`.  The only unsolved allocator premises are concrete reductions showing capacities 16 and 8 in the capacity local and their `UInt64.toNat` lower bounds.

I searched the arrays, memory, and allocation indexes with `array-memory-framing|preserv.*input|UInt64Array\.At|allocStore|outside-region`.  I inspected and will use `array-memory-framing`, which names `FixedArrayPairResult.input_preserved_by_alloc` for the exact allocator store and `UInt64Array.At.write64After` for later result stores.  This support is required only in the valid branch, where the fold reads the input after allocating and initializing its output.

I will replace the failed generic `norm_num` calls with the allocator window's focused simplifier for the concrete capacity-local getter and direct decision procedures for `UInt64.toNat`.  Before each allocator application I will name its fit proof and `Allocation.bumpFacts`; the later length and payload stores need their root-address and page facts.  I will then apply `FixedArrayResult.lengthStore_spec` to the first post-allocation store in each branch, leaving the valid fold setup and invalid finish sequence as the next proof boundaries.

The seventh import check accepted the explicit framing import.  The focused allocator simplifier reduced each capacity-local premise to a concrete head lookup, so `rfl` will finish those goals; the direct decisions closed both capacity lower bounds.  Both length-store programs refolded, but Lean could not infer their existential root values from the later getter premise, so I will instantiate each root explicitly as `heapTop + 48` and use the shared bump facts for its address bound.

The eighth import check succeeded.  Explicit roots allowed both length-store theorems to apply, and their store-address bounds closed from `BumpFacts.wordAddress_toNat`; only the reduced root-local head lookups need `rfl`.  The branch continuations now begin at the exact fold setup in the valid case and the four-instruction result-local transfer in the invalid case.

I will close the invalid branch by constructing `emptyStore_at` for the post-length-store memory and reducing its final local transfers and public return.  In the valid branch I will preserve the input first through `allocStore` with `input_preserved_by_alloc`, then through the output-length store with `UInt64Array.At.write64After`, and apply the exact `forwardSetupProgram_spec`.  Progress means leaving only the annotated fold loop and its result-placement/store suffix in the valid branch.

The ninth import check succeeded.  The invalid output representation was constructed, but an explicit `unfold finishProgram` failed because the residual program had already reduced to its four instructions; I will run the focused instruction simplifier on that list directly.  In the valid branch, both framing theorems succeeded and `forwardSetupProgram_spec` applied, leaving only its parameter, writable-local, distinctness, and loop-continuation premises.

I will discharge the setup-frame premises with the same focused local-frame reduction used for allocation.  The writable set is the concrete list `[11, 12, 13, 16, 14, 1, 18, 15]` in a twenty-local function, and its distinctness is decidable.  The next diagnostic should therefore expose the loop-rule boundary alone.

The tenth import check succeeded.  Every fold-setup premise closed, leaving the exact block-wrapped annotated loop and its result/store suffix.  The invalid branch reduced to a final existential whose witness and representation are already contained in `hResult`, so `simpa` should close it.

Before constructing the loop proof, I will add a named invariant that fixes the post-length-store state and carries an index bounded by `input.size`, the twenty-local frame shape, input pointer local 11, index local 13, effective-stop local 15, and accumulator local 1.  The accumulator equals `ArrayFold.foldPrefix input (fun sum element => sum + element) 0 index`.  A named measure reads local 13 and computes `input.size - index.toNat`; applying `wp_block_loop` should expose the exact initialization, body, decrease, and exit obligations without expanding the loop rule.

The eleventh import check succeeded.  The block/loop rule accepted the invariant and measure, producing exactly an initialization goal and a universally quantified body-step goal.  The invalid branch's remaining mismatch is only between the definitionally identical task-local and ProofKit array predicates, so I will convert `hResult` to `FormalSpec.UInt64ArrayAt` explicitly before the final simplification.

I will initialize the invariant at index zero by reducing the checked setup frame.  For the body, I will unpack the invariant and split on `index = input.size`; the equal case takes the guard's break edge into the result suffix, while the strict case applies `continuingProgram_spec` and returns at `index + 1`.  Exposing these two smaller residual goals counts as progress because each corresponds to one control-flow edge of the annotated loop.

The twelfth import check succeeded.  Initialization reduced to four concrete head lookups and the fold over an empty prefix, all closed by `rfl` after the focused simplifier.  The body split produced the expected completed-index and continuing-index cases; the completed case retains the accumulator getter at `foldPrefix input … input.size`, and the continuing case retains all four invariant getters.

I will reduce only the completed case's four-instruction guard to reach the result suffix.  In the strict case I will apply `FixedArrayTraversalInput.continuingProgram_spec` to the exact generated prefix with index local 13, stop local 15, array local 11, and item local 2.  This keeps the dynamic address calculation and load behind its checked theorem and should leave only the accumulator update, invariant reconstruction, and measure decrease.

The thirteenth import check succeeded, and the strict case matched `continuingProgram_spec`.  The completed-case use of the broad allocator simplifier crossed the break edge into the full result continuation and exhausted the theorem's heartbeat limit after 78 seconds.  This is an elaboration-boundary failure: the four-instruction guard must remain separate from the suffix.

I will prove a continuation-generic local theorem for the true branch of `FixedArrayTraversalInput.continuingProgram`, using only the empty value stack and equal index/stop getters.  Its conclusion returns `Break 1` to an arbitrary assertion, preventing simplification of the caller's suffix.  I will also replace the initialization `rfl` with a small `simp`, which directly evaluates the four concrete optional head lookups and the fold over the empty prefix.

The fourteenth import check succeeded and completed invariant initialization.  The new exit helper reduced its program in isolation, revealing that `br_if` normalizes the returned frame's value stack to empty; I will state that normalized frame in the helper's continuation premise.  The continuing theorem's semantic premises all closed except for elaboration ambiguity around `hItem`, so I will supply `hItem`, `hInput`, and `hIndex` as named arguments rather than positional bullets.

The fifteenth import check succeeded, and the exit helper now closes.  Its caller receives the expected `Break 1` continuation, which reduces to the fold result suffix after normalizing the empty value stack.  The continuing theorem still assigned the remaining positional bullets ambiguously, so I will name every semantic premise, including the encoded comparison, and leave only its continuation as a generated goal.

The result suffix stores through output root local 7, which the current invariant does not record.  I will strengthen `sumInvariant` with `frame.get 7 = some (.i64 outputPtr)` and pass `heapTop + 48` as that parameter.  Initialization can prove this getter from `allocFrame`, and the loop body never changes local 7; the completed branch can then compose `resultProgram_spec` with the payload store theorem.

I re-read the candidate after context compaction and checked the exact signatures of `FixedArrayFold.resultProgram_spec` and `FixedArrayResult.payloadStore_spec` in the permitted ProofKit mirror.  The fold-result theorem requires the empty value stack, accumulator getter, result-local lower bound, and result-local validity before exposing its continuation.  I will add those four premises while strengthening the invariant, and I will treat a residual payload-store continuation as progress for this check.

The sixteenth import check succeeded.  The build accepted the strengthened invariant, its initialization, and all four explicit fold-result premises, but the completed edge retained the loop wrapper's normalized value-stack expression rather than the syntactic `frame` expected by my `change`; the continuing edge now begins immediately after `continuingProgram_spec`, as intended.  I will normalize the completed-edge frame to `{ frame with values := [] }`, then reduce only the continuing edge's local arithmetic and back-branch with `foldPrefix_succ`, an encoded successor equality, and the invariant facts.

The seventeenth import check succeeded.  The completed edge now reaches the exact payload-store program with the expected `resultFrame`, while the continuing simplifier reached its arithmetic but exhausted the heartbeat budget, and the separate successor-size proof lacked an explicit use of the represented input's size bound.  I searched the relevant LTG category indexes and the inspected `fixed-array-result` entry with `finishProgram|payloadStoreProgram|payloadStore_spec`; the catalog names `payloadStore_spec` and representation constructors, but it contains no theorem for the final four local transfers, so no additional entry applies.

I will isolate the continuing arithmetic in a continuation-generic theorem, following the successful exit-helper boundary.  A named final frame will record the six local writes, and the theorem will stop at `Break 0`; its caller can prove the invariant and decrease without simplifying the result suffix.  On the completed edge I will apply `payloadStore_spec` and use `BumpFacts.wordAddress_toNat` for the payload address, leaving only the final local transfers and public representation as the residual goal.

The eighteenth import check succeeded.  The build refolded and applied both new program theorems, but their instruction simplifiers require concrete `frame.locals[i]` equalities rather than the invariant's `frame.get` equalities; this is the conversion described by the inspected traversal LTG entry.  The payload and singleton bound proofs also retained wrapped-address expressions, while the public `RuntimeReady` output bounds already state the required singleton bounds after reducing `expected` under `hSize`.

I will derive each required internal-local value with `List.getElem?_eq_getElem`, preserving the exact getter evidence from the invariant.  The completed branch will use the derived output-root element for both the payload theorem and the final existential, and it will use the public output bounds for the singleton representation.  The continuing helper will then stop at a concrete `Break 0`, where `sumContinueFrame` should establish the successor invariant and strict measure decrease.

The nineteenth import check succeeded.  All getter-to-list conversions now elaborate, the payload theorem accepts its explicit address bound, and the final result continuation reduces to the intended output-pointer existential.  Three local issues remain: the generic continuation needs one final frame normalization against `hNext`, the singleton constructor needs `BumpFacts.rootToNat` before using the public bounds, and simplifying all eight successor-invariant fields in one call exceeds the heartbeat budget.

I will normalize the helper conclusion explicitly, rewrite the singleton root before arithmetic, and prove the successor invariant one field at a time.  Each field depends on a fixed local that the loop body either preserves or writes, so separate focused reductions avoid traversing the full conjunction repeatedly.  A remaining measure-only goal would isolate the last semantic obligation of the input-traversal loop.

The twentieth import check succeeded.  The completed branch now closes through the singleton representation and public result, and the generic continuation theorem closes after its explicit frame normalization.  The only diagnostics come from the continuing back edge: the loop rule wraps the final frame with `take 0` and `drop 0`, the value-stack field needs that wrapper normalized by `hValues`, and reductions for the two written semantic locals still exceed the global heartbeat budget.

I will normalize the zero-arity loop frame before unpacking the invariant obligation.  I will also add small generic getter lemmas for `sumContinueFrame` at local 13 and local 1, because those locals contain the next index and next accumulator by construction.  These lemmas reduce the fixed six-write frame once, outside the artifact theorem's large context, and their caller will use `hIndexSucc` and `hFoldSucc` for the semantic equalities.

The twenty-first import check succeeded.  The loop-frame normalization and every preserved invariant field now close, and both generic getter lemmas elaborate; the accumulator field also closes with `foldPrefix_succ`.  Simplifying `hIndexSucc` creates too many rewrite steps because it overlaps the standard `UInt64.ofNat_add` orientation, and the measure proof still expands the entire nested frame instead of using the new index getter.

I will rewrite `hIndexSucc` into the generic getter fact once, without adding it to the simplifier.  For the measure, I will normalize the zero-arity frame, change it to the named `sumContinueFrame`, rewrite both index getters directly, convert their bounded encodings to natural numbers, and finish the strict subtraction inequality with `omega`.  This construction addresses the remaining loop-carried decrease without reopening instruction semantics.

The twenty-second import check succeeded.  Every proof obligation except the measure inequality now closes, and that goal contains exactly two reducible matches over known `i64` index values.  The attempted combined rewrite reached those matches but did not reduce them before looking for the encoded `toNat` subterms, so I will separate getter rewriting from a definitional `change` to the two natural-number subtractions.

The twenty-third import check succeeded.  The `ArtifactResult` build also succeeded after the separated match reduction, bounded `UInt64.toNat` rewrites, and final `omega`; Lean checked the public theorem, both length branches, the allocation and result representations, and the loop-carried fold invariant.  I will now run the required import check and `ArtifactResult` build once more as the final independent verification, with no further edits.
