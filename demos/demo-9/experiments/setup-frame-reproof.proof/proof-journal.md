# Proof Journal

## 2026-08-18

The initial import check accepted the unchanged candidate.  The required Lean build then failed at `Behavior.lean:24:116` with the full `TerminatesWith` goal after the starter had decomposed `RuntimeReady` into the represented input, allocator globals, memory bounds, and page bound.  This establishes the intended proof boundary: execution of function 0 from the represented input store to a represented `FormalSpec.expected input` result.

I began retrieval with `KNOWLEDGE/forest.json`.  It selects the `leanexe-core` and `proposal-4f56fd45fe24-d4f7d73b3648` packages.  Before constructing an instruction-level proof, I will inspect the exact recipes for `function-0.length-dispatch-0` and `function-0.array-fold-0`, then search the selected package indexes for the dispatch, fold, singleton-result, and empty-result declarations named by those recipes.  Progress means that the next Lean check replaces the whole-function goal with branch-specific semantic premises or closes it through a complete checked composition.

I searched every selected package category index with `rg` for `leanexe.array.length-dispatch.v1|FixedArrayLengthDispatch|wp_fixed_array_length_le_dispatch_from|leProgram_spec`, then for `leanexe.array.fold.v1|FixedArrayFold|foldPrefix|guardedBackEdgeProgram_spec|continuingGuardedProgram_spec`, and then for `singletonResultProgram_spec|singleton result|empty result|fixed-array-singleton-result`.  The first query selected `leanexe-core/fixed-array-length-dispatch`.  The fold query selected `leanexe-core/fixed-array-fold-structure` and related fold entries.  The singleton query also found `proposal-4f56fd45fe24-d4f7d73b3648/fixed-array-singleton-result-from-frame`, which I have not inspected yet because the first residual goal is the public-entry dispatch.

I inspected the `entry.json` and `README.md` files for `leanexe-core/fixed-array-length-dispatch` and `leanexe-core/fixed-array-fold-structure`.  The dispatch entry matches the bounded length annotation and requires conversion of `TerminatesWith` with `Wasm.TerminatesWith.of_wp_entry_for`, followed by the structured command `wp_fixed_array_length_le_dispatch_from hArray at 7, 8`.  The fold-structure entry matches the valid branch's checked fold region and identifies the setup, block-loop rule, result placement, and singleton suffix boundaries.  I also checked the mirrored `FixedArrayLengthDispatch.leProgram_spec` and tactic definition.  The command's goal shape, represented-input premise, and annotation kind match the expected body-level residual goal.

The next edit will perform only the direct dispatch recipe: convert function 0 entry execution to a fixed-store weakest-precondition goal, rewrite `func0` with `function_0_length_dispatch_0_function_eq`, and invoke `wp_fixed_array_length_le_dispatch_from hArray at 7, 8`.  A successful reduction will expose separate `input.size ≤ 8` and `¬ input.size ≤ 8` branch obligations while keeping each generated branch program folded.

The import check passed after that edit.  The Lean build failed at the explicit rewrite because the entry theorem left the program syntactically as `func0Def.body`; Lean did not find `func0` for `function_0_length_dispatch_0_function_eq`.  The fixed-store conversion itself succeeded, so the selected entry applies.  I will remove the premature rewrite and attempt the structured command directly.  Its `change` step can unfold `func0Def.body` to the exact `leProgram 7 8 _ _ ++ _` shape, which is the command path specified by the recipe.

The second import check passed.  The direct command applied `FixedArrayLengthDispatch.leProgram_spec` and exposed the expected valid and invalid branch goals, together with frame parameters, empty values, local bounds, and `8 < UInt64.size`.  This confirms `leanexe-core/fixed-array-length-dispatch` as used.  The direct recipe improved the proof boundary from the whole function to two folded branch programs.

Before reducing the entry-frame parameters or local length, I searched `PROOF_RECIPES.json` for `toLocals|frame_params|locals_length|hParams|hValues|func0Def` and the knowledge indexes for `toLocals|frame params|locals length|local-frame-projection|Function.numParams|Function.toLocals`.  The recipes contain result-frame accessors for the later fold but no accessor for the initial `func0Def.toLocals` frame.  The `local-frame-projection` index entry concerns equality, operand-stack replacement, and internal getters rather than construction of the entry frame.  I rejected it for this residual goal without opening its catalog files.  The dispatch README directs reduction with `func0Def`, `Function.toLocals`, `Function.numParams`, and `ValueType.zero`, so I will use those checked definitions for the five elementary entry facts.

I also searched the indexes for `constantCapacityRegion_spec_withTail|fixed-array-allocation|bumpFacts|allocFrame_get_root|emptyStore_at|finishProgram_spec`.  I inspected `leanexe-core/fixed-array-capacity` and `leanexe-core/fixed-array-allocation`, including both `entry.json` and `README.md` files and the mirrored declarations they name.  Both branch prefixes have the required form: constant result length one or zero at combined local 11, followed by the shifted allocator at offset 2 and stride one.  With twenty internal locals, `constantCapacityRegion_spec_withTail` uses `tail = 4`.  I will apply that complete composition before proving either result suffix.  Progress means that each branch reaches its post-allocation store and `allocFrame`, with only the length store, fold or empty result construction, final root transfer, and public suffix remaining.

The result-length residual caused another retrieval checkpoint.  I searched the indexes for `fixed-array-singleton-wrapper|lengthStore_spec|headerProgram|singleton wrapper|result length` and inspected `leanexe-core/fixed-array-result`.  Its `lengthStore_spec`, `emptyStore_at`, singleton suffix guidance, and final root-transfer theorem match the two branches.  The singleton-wrapper entry has direct-call annotation requirements absent from this artifact, so I rejected it without opening its catalog files.

I searched for `writeLength|frameBefore|input.*preserv|array-memory-framing|length store` and inspected `leanexe-core/array-memory-framing`.  The selected composition is exact: `input_preserved_by_alloc` preserves the input through allocator header writes, and `UInt64Array.At.write64After` preserves it through the result-length store above `heapTop`.  The indexed `word_reads` command does not match the current represented-array goal, so I will use the named preservation declarations instead of its fallback.

For the loop residual, I inspected `leanexe-core/array-fold-prefix`, `fixed-array-fold-body`, `fixed-array-traversal-input`, and `annotated-fold-frame-accessors`.  Their annotation kind is `leanexe.array.fold.v1`, and their premises match the recipe: the input remains represented, the traversal index is bounded, the generated frame records the index and accumulator, and the compiler supplies the scalar body, condition, and continuing evaluations.  I will use `ArrayFold.foldPrefix` in the invariant, the generated frame getters for local projections, `continuingProgram_exit_spec` for the completed guard, and `FixedArrayFoldBody.continuingGuardedProgram_spec` for an active iteration.

I also inspected `proposal-4f56fd45fe24-d4f7d73b3648/fixed-array-singleton-result-from-frame`.  Its theorem derives the post-result-placement accumulator and root getters from the source frame and matches the exact generated singleton suffix.  I added its checked module import and plan to use `singletonResultProgram_spec_to_fromFrame` after loop completion.  This avoids reconstructing the result-frame getters already proved by the proposal.

The current edit discharges the five dispatch-frame goals and applies `constantCapacityRegion_spec_withTail` to both branches.  I left an empty local proof only at each post-allocation continuation so the next diagnostic will show the exact folded residual state.  Acceptance still requires replacing both empty proofs with checked terms; they introduce no declaration or axiom.

The import check passed.  The Lean build accepted every dispatch, frame, capacity, allocator-global, page, and allocator-fit premise.  Its only diagnostics were the two deliberate post-allocation continuations.  The valid branch now begins with the result-length store followed by the checked fold region, singleton suffix, and public local-get suffix.  The invalid branch is the eight-instruction length-store and root-transfer program.

I will close the smaller invalid branch first with `FixedArrayResult.lengthStore_spec`, `emptyStore_at`, and `finishProgram_spec`.  `Allocation.bumpFacts.wordAddress_toNat` supplies the root-store bound, and `finishFrame_return_get` supplies the exact return-local projection before the public suffix.  Progress means the next build reports only the valid fold branch.

The import check passed.  The invalid-branch construction reached only local frame-shape mismatches: I had tried to simplify named `allocFrame` projections through a local abbreviation, but the simplifier retained the allocation frame.  The destination and return validity goals likewise required rewriting `allocFrame.params.length` and `allocFrame.locals.length` with the named facts.  I replaced those reductions with `allocFrame_params`, `allocFrame_locals_length`, and explicit `validIndex` inequalities.  The first `wp_run` had already used `finishFrame_return_get`, so I removed a redundant simplification and second run.

Two further checks passed the import gate.  The first showed that local `let` names cannot serve as rewrite declarations and that the final public postcondition still used the frozen `FormalSpec.UInt64ArrayAt` name.  I changed the projection proofs to expose `FixedArrayAllocatorWindow.allocFrame` before applying its named equalities, and I changed the representation goal to `UInt64Array.At`.  The next build accepted the complete invalid branch.  Only the valid post-allocation continuation remains.

Before the extended fold construction, I added `sumFrame`, `sumInv`, and `sumMeasure`.  `sumFrame` uses the compiler-generated continuing frame and equates local 1 to `ArrayFold.foldPrefix input (fun sum element => sum + element) 0 index`; `sumInv` permits the item, scratch, done, staged, and release locals to vary; and `sumMeasure` reads the generated index at local 13.  The next edit preserves the input through allocation and the length store, rewrites the exact allocation frame to the generated scalar frame, applies `forwardSetupProgram_spec`, and stops at the post-setup boundary.  Progress means the diagnostic starts at the block-wrapped fold loop with the initialized `sumFrame`.

The import check passed.  Lean accepted input preservation, root arithmetic, the exact allocation-frame equality, the length-store theorem, and all setup premises except the writable-local length proof.  That proof had used arithmetic before exposing the generated frame's local count.  At this frame-projection checkpoint I used the already inspected recipe declaration `function_0_array_fold_0_continuing_frame_locals_length`, then applied arithmetic to the finite `setupLocals` membership.

The post-setup diagnostic has the exact block-wrapped loop followed by `function_0_array_fold_0_singleton_result_program`.  I will apply `wp_loop_cons` with `sumInv` and `sumMeasure`.  The completed-index case uses `continuingProgram_exit_spec`; the active case remains folded for `continuingGuardedProgram_spec`.  I left local empty proofs at those two callbacks so the next check reveals the loop rule's exact exit assertion and active-step assertion without expanding the instruction regions.

The import check passed.  Lean accepted the setup writable-local proof after applying the generated local-count declaration.  The attempted outer `change` failed because the complete post-setup list was not definitionally equal to a list rebuilt from the separately named loop intervals and singleton interval, even though each interval has its own checked region equality.  `wp_block_cons` and `wp_loop_cons` accept the raw frozen program, so I removed that unnecessary complete-list reconstruction.  I will refold only the loop body at the active and exit theorem boundaries.

The next import check passed, and the loop rule accepted `sumInv`, `sumMeasure`, and its initialization.  The diagnostic exposed the completed `Break 1` callback and the active iteration.  I normalized the completed callback with the generated empty-value-stack theorem, refolded only the singleton suffix, and applied the proposal's `singletonResultProgram_spec_to_fromFrame`.  The payload bound comes from `BumpFacts.wordAddress_toNat 1`; `singletonStore_at` supplies the represented output; and `foldPrefix_size` identifies its value with the formal `Array.foldl`.  The active iteration remains as the only deliberate empty proof if these suffix premises check.

The import check passed.  The completed branch reached five arithmetic premises whose only missing reduction was the length of the generated one-element parameter list.  I changed those lower-bound proofs from `omega` to `norm_num`; all exact getters, validity premises, payload memory bounds, output representation, and final public continuation were otherwise accepted.

I replaced the active-iteration placeholder with `FixedArrayFoldBody.continuingGuardedProgram_spec`.  Its structural premises use generated getters for locals 11, 13, and 15 and `function_0_array_fold_0_continuing_item_valid`.  Its semantic premises use the compiler-generated loaded-frame, body-evaluation, condition-evaluation, and continuing-evaluation theorems.  The false condition makes the exit callback contradictory; the repeat callback uses `foldPrefix_succ`, reconstructs `sumInv` at `index + 1`, and proves that `sumMeasure` decreases.  A successful check leaves no loop-body instruction proof or semantic residual.

The import check passed, but Lean inferred several dependent premises during the fold-body application and shifted the positional bullets.  The first bullet intended for item validity received the represented-input goal, and the later bullets received the step-program, body, and callback goals.  The residual included `hItem` and `hIndex`, confirming the `fixed-array-fold-body` entry's warning that dependent premises require explicit names.  I will name every structural and semantic premise in the theorem application and leave only its exit and continue callbacks as positional goals.

The import check passed after naming those premises.  The build then reached four normalization boundaries: the loaded-frame theorem ended in the generated continuing frame while the premise displayed `State.toLocals`; the body evaluator retained `U64Op.apply .add`; the invariant kept `sumFrame` folded; and the continuing state used the emitted `UInt64.ofNat index + 1` rather than the semantic successor.  I searched the recipes and selected-package indexes with `rg` for `toState.*toLocals|toLocals.*toState|toLocals_nil|loaded_frame|dynamicResultFrame|U64Op.add|ofNat_one|ofNat_add`.  The recipes name the already used generated loaded-frame equality but no additional state-to-frame accessor, and the indexes returned no new entry.  The inspected `leanexe-core/fixed-array-fold-body` README prescribes `UInt64.ofNat (index + 1)` in the semantic continuing frame and restricts `UInt64.ofNat_add` to the evaluator equation.  I will unfold the generated continuing-frame definition at the first boundary, reduce `U64Op.apply` in the body equation, expose `sumFrame` before `foldPrefix_succ`, and normalize the generated continuing evaluator to the semantic successor.  Progress means that the next diagnostic either closes the active iteration or reports only its reconstructed invariant and measure.

The import check passed, and the Lean build discharged every semantic, instruction, invariant, and measure obligation.  Its sole error was `No goals to be solved` at the `simp [acc, value]` following `foldPrefix_succ`; the preceding rewrite had already closed the generated-frame equality by definitional reduction.  I will delete that redundant command.  Progress means that the artifact target builds without a proof error.

After deleting the redundant command, the import check passed and `LeanExeGen.GeneratedR23fa7efc3fb0298b.ArtifactResult` built successfully.  I audited `Behavior.lean` with `rg` and found no `sorry`, `admit`, placeholder proof, `Source` import, or `Source` reference.  I recorded all eleven inspected entries in `KNOWLEDGE_USE.json`, sorted by package and entry.  The final verification will repeat the import gate and artifact build without another candidate edit.

The final import check accepted `Behavior.lean`, and the final independent build accepted `LeanExeGen.GeneratedR23fa7efc3fb0298b.ArtifactResult`.  The proof uses the shared dispatch, constant-capacity allocation, result-memory, fold-structure, fold-body, traversal-input, fold-prefix, generated annotation, and proposal singleton-result declarations recorded above.  No residual goal remains, and the checked candidate was not edited after its successful build.
