# Proof Journal

## 2026-08-10: Initial candidate check

I ran the required artifact-result build against the untouched candidate.  The build reached `Behavior.lean` and failed at line 23 with one goal: `TerminatesWith env module 0 initial [Value.i64 inputPtr]` whose postcondition requires a singleton result pointer representing `FormalSpec.expected input`.  The starter has already decomposed `RuntimeReady` into the input representation, exact globals, the empty free list, address facts, allocator bounds, heap bounds, and page bounds.  No earlier elaboration or import error remains.

The missing step is a checked execution theorem or tactic that consumes these runtime facts and proves the translated function's loop and allocation behavior.  I will begin retrieval at `LTG/categories.json`, then search only relevant category indexes for `TerminatesWith`, loop, `Array.foldl`, array representation, and allocator annotations before attempting the supplied exact recipe.

## LTG retrieval

I read `LTG/categories.json` first.  Its relevant categories are `proof-construction`, `arrays`, `loops`, `compiler-motifs`, and `allocation`.  My first index query was `rg -n -i 'TerminatesWith|RuntimeReady|UInt64ArrayAt|Array\.foldl|loop-carried|foldl|loop|alloc'` over those five `tools.jsonl` files.  The strongest matches are `fixed-array-length-dispatch`, `fixed-array-fold-structure`, `fixed-array-fold-body`, `array-fold-prefix`, and `fixed-array-allocation`; they collectively cover entry conversion, the compiler-matched fold, its invariant, and result allocation.  I have not inspected any entry yet because the task-specific recipe and annotations can identify which of these broad matches applies to the exact artifact.

`PROGRAM_ANNOTATIONS.json` identifies an exact `leanexe.array.length-dispatch.v1` region for the unsigned `input.size ≤ 8` test and an exact `leanexe.array.fold.v1` region.  The fold metadata specifies one accumulator at local 1, initial value zero, forward traversal, an indexed item at local 2, wrapping addition, and result placement from the loop-carried accumulator.  `PROOF_RECIPES.json` contains no complete composition.  Its required direct order is `FixedArrayLengthDispatch.leProgram_spec` (or the generated tactic invocation `wp_fixed_array_length_le_dispatch_from hArray at 7, 8`), followed by `Wasm.wp_loop_cons` for the fold region and the named fold support theorems.  I will inspect the five canonical LTG entries selected above, plus the compact loop-suffix entry because the recipe names `singletonResultProgram_spec` as the intended small continuation boundary.

I inspected `fixed-array-length-dispatch`, `fixed-array-fold-structure`, `fixed-array-fold-body`, `array-fold-prefix`, `fixed-array-allocation`, and `compact-loop-suffix-boundary`.  I will use the dispatch entry for public-entry conversion and branch separation, the fold-structure entry for setup and the complete singleton suffix, the fold-body entry for the continuing transition, and the prefix entry for the invariant.  The compact-suffix entry will be used only if the direct loop proof retains too large a continuation.  I rejected `fixed-array-allocation` for the first attempt because neither exact annotation recipe names an allocator-region equality, while the direct-recipe rule requires attempting the named regions before reducing internal allocator instructions.

My second index query was `rg -n 'leanexe\.array\.fold\.v1|singletonResultProgram_spec|continuingGuardedProgram_spec|leProgram_spec'` over `arrays`, `loops`, `compiler-motifs`, and `proof-construction`.  It confirmed the selected entries and revealed `annotated-fold-frame-accessors`, `fixed-array-result`, `fixed-array-traversal-input`, and `guarded-back-edge`.  I inspected those four entries.  I will use the generated frame accessors and `FixedArrayFoldBody.continuingGuardedProgram_spec`, which already composes the traversal-input and guarded-back-edge entries.  I rejected separate use of `fixed-array-result`, `fixed-array-traversal-input`, and `guarded-back-edge` for the initial proof because the recipe supplies stronger complete theorems at each boundary.

I inspected the exact declarations in the mirrored `Project.ProofKit` sources for `ArrayFold`, `Control`, `FixedArrayCapacity`, `FixedArrayLengthDispatch`, `FixedArrayFold`, and `FixedArrayFoldBody`.  I also read function 0 and the generated annotation declarations.  Function 0 has a shared length-dispatch suffix, a valid branch that allocates a singleton and then traverses the input with one accumulator, and an invalid branch that allocates an empty result.  For the first edit I will attempt the direct dispatch recipe exactly: convert the fixed public store with `Wasm.TerminatesWith.of_wp_entry_for`, rewrite `func0` with `function_0_length_dispatch_0_function_eq`, and invoke `wp_fixed_array_length_le_dispatch_from hArray at 7, 8`.  Progress means that Lean exposes the valid and invalid branch weakest-precondition goals without expanding the dispatch instructions.

The import check succeeded.  The Lean build rejected the generated function equality because the target still displayed `func0Def.body`; entry conversion itself succeeded and produced the fixed-store `wp` goal described by the LTG entry.  The missing adapter is a definitional `change` from `func0Def.body` to `func0`, not a semantic lemma.  I will add `change wp _ func0 _ _ _ _` before the generated equality and rerun the exact dispatch recipe.  The expected progress boundary remains the two branch obligations.

The second import check succeeded.  The build accepted the entry conversion, function equality, and exact `leProgram_spec` recipe.  It left five structural premises (`params`, empty operand stack, local 7 validity, and `8 < UInt64.size`) plus the valid and invalid branch `wp` goals.  This is the expected recipe postcondition, so the direct dispatch attempt succeeded.  I will discharge the five closed premises by reducing `func0Def.toLocals`, then apply each generated constant-capacity program with `FixedArrayCapacity.constantProgram_spec`.  Progress means each branch reaches the allocator region from a named `capacityFrame`, without reducing the capacity arithmetic or dispatch.

I inspected `FixedArrayAllocatorWindow.region_spec_withTail`, `FixedArrayAllocator`, `Allocation.bumpFacts`, the input-preservation theorem, and the result-store declarations in the mirrored sources.  Function 0's allocator has offset 2 and four unused trailing locals: capacity local 11 is internal index `2 + 8`, root local 7 is `2 + 5`, and the frame has `2 + 14 + 4 = 20` locals.  I will defer applying the allocator theorem until the capacity checks confirm this exact decomposition.

The third import check succeeded.  Both generated capacity prefixes matched `constantProgram_spec`.  I supplied only two of its three frame premises, so Lean left `validIndex 11` in both branches and then displayed each exact post-capacity program.  The valid program begins with the offset-2 allocator and normalized capacity 16; the invalid program begins with the same allocator and normalized capacity 8.  I will add the omitted validity proofs and apply `FixedArrayAllocatorWindow.region_spec_withTail` with offset 2 and tail 4.  The valid `hHeapFitMemory` reduces to `heapTop + 48 + 16` under `hSize`, while the invalid premise reduces to `heapTop + 48 + 8` under `¬ hSize`.  Progress means each branch reaches the named `allocStore` and `allocFrame` endpoint.

The fourth import check succeeded.  Both allocator region applications matched, and Lean reached their `allocStore` and `allocFrame` endpoints.  Two closed arithmetic premises remain in each application because `norm_num` did not unfold `FixedArrayCapacity.normalizedCapacity` or numeral `UInt64.toNat`; the former needs its two named definitions, and the latter is decidable by reduction.  The invalid residual program is exactly `FixedArrayResult.lengthStoreProgram 7 0 ++ FixedArrayResult.finishProgram 7 5 6`.  I will close this branch with `lengthStore_spec`, `emptyStore_at`, and `finishProgram_spec`, using `Allocation.bumpFacts` for the root address and bounds.  The progress boundary is one remaining valid-branch goal beginning with its length store and fold.

The fifth import check succeeded.  The invalid result-store and finish theorems matched.  Three presentation gaps remain: capacity normalization reduces to closed `UInt64` division propositions that need `native_decide`; the root store bound needs `BumpFacts.wordAddress_toNat 0`, rather than `rootToNat`, because the generated store address passes through `toUInt32`; and clearing `frame.values` in `branchPost` hides the finish-frame getter syntactically even though `Locals.get` ignores that field.  I will add the generated word-address fact and transport `finishFrame_return_get` across the values-only update with `simp only [Wasm.Locals.get]`.  Success leaves only the valid branch.

The sixth import check succeeded.  Native reduction closed all capacity premises, and the values-only getter transport closed the final return.  The invalid branch now has one residual presentation mismatch: `BumpFacts.wordAddress_toNat 0` retains the syntactic zero offset, while the target has already reduced it.  I will state a simplified corollary with the exact target address.  No semantic invalid-branch obligation remains.

The seventh import check succeeded.  The invalid branch is complete, and Lean reports only the valid branch after allocation.  I will define a fold invariant using `ArrayFold.foldPrefix input (fun sum element => sum + element) 0 index`.  It fixes the generated frame's parameter, loop-carried accumulator, result root, input pointer, encoded size, traversal index, stop, and effective stop while existentially retaining locals irrelevant to the mathematical relation.  The measure reads generated local 13 and subtracts its `UInt64.toNat` value from `input.size`.  Before opening the loop, I will use `lengthStore_spec`, preserve the input representation across allocation and the output-length write, and apply `forwardSetupProgram_spec`.  Progress means the exact block and loop rules accept the invariant and expose initialization and one-step obligations.

The eighth import check succeeded.  `forwardSetupProgram_spec`, `wp_block_cons`, and `wp_loop_cons` accepted the exact named program and exposed only invariant initialization and the loop step.  The generic helper currently knows the frame's parameter, length, empty operand stack, and root getter, but that does not prove that untouched locals contain `i64` values.  The exact caller frame does have the compiler-generated scalar shape.  I will add that shape as a helper premise, retaining the root at generated local 7, and use it to initialize the invariant at index zero.  The remaining goal after this change should be the single loop-step theorem.

The ninth import check succeeded.  The generated scalar-frame premise closed initialization by reduction, leaving the single loop-step goal.  I will split its invariant index into `index = input.size` and `index < input.size`.  The completed case will use `continuingProgram_exit_spec`, rewrite `foldPrefix_size`, and apply the complete `singletonResultProgram_spec`.  This directly tests the recipe's compact suffix composition before constructing the continuing callback.

The tenth import check succeeded far enough to match `continuingProgram_exit_spec` and `singletonResultProgram_spec`.  Its remaining completed-case goals all contain the same values-only wrapper produced by the zero-arity block rule: `take 0 [] ++ drop 0 forwardSetupFrame.values`.  `forwardSetupFrame_values` reduces that wrapper to the generated current frame, after which the generated getter declarations apply.  The continuing-index branch is otherwise untouched.  I will normalize this block frame once before the singleton theorem instead of weakening its premises or unfolding local storage.

The eleventh import check succeeded and reduced the exit wrapper's values.  Two getter premises retain a record with the generated frame's `params` and `locals`, so the exact accessor theorem does not rewrite syntactically.  As with the invalid return frame, `Locals.get` ignores `values`; transporting the generated accumulator and root getters with `simp only [Wasm.Locals.get]` closes both.  All other singleton-suffix premises passed, confirming the complete suffix recipe.  The continuing branch remains the sole loop-step case afterward.

The twelfth import check confirmed the root transport and every other exit premise.  The accumulator transport needs one directed `foldPrefix_size` rewrite on the target frame before the accessor with the completed `Array.foldl` value applies.  I will make that rewrite, then apply `FixedArrayFoldBody.continuingGuardedProgram_spec` to the active edge.  The generated loaded-frame equality and generated body and condition evaluator equations will define compact `ScalarTransition.State` values.  Progress means the shared theorem leaves only its false-condition continuation callback, which contains the prefix-successor invariant and measure decrease.

The thirteenth import check succeeded.  The Lean build accepted the completed-case `foldPrefix_size` rewrite and matched `FixedArrayFoldBody.continuingGuardedProgram_spec`.  Its `apply` invocation discharged the generated item-local validity premise while constructing the dependent loaded-frame premise, so the explicit item-validity bullet shifted every later argument and produced presentation type errors.  The separate next-index bound also defeated `omega` after it unfolded the large `UInt64.size` constant.  I will remove the redundant bullet and prove `index + 1 < UInt64.size` by composing `Nat.succ_le_of_lt hIndex` with `hInput.size_lt`.  Progress means the generated loaded-frame, body, condition, and continuing evaluator equations all check, leaving either the invariant callback or the public valid-branch connection.

The fourteenth import check succeeded.  The explicit successor bound checked.  Lean's `apply` tactic closed the available `hIndex` premise from the local hypothesis and postponed the dependent item-validity goal until after the continuation callback; the prior bullet order therefore remained offset.  The diagnostic also shows that the false-result exit premise closes automatically and that the continuation premise has already reduced from `false = false → P` to `P`.  I will remove the redundant index and exit bullets, retain the evaluator proofs in declaration order, and discharge the postponed generated item-validity fact last.  Progress means Lean checks the prefix-successor invariant and measure decrease rather than reporting composition-argument mismatches.

The fifteenth import check succeeded.  The body and condition evaluator equations now occupy their intended premises.  The loaded-frame transport retains the opaque local abbreviation `acc` on only one side and prints `State.toLocals []` as a record, so it needs normalization by `acc`, `loadedState`, and the generated continuing-frame definition.  The exit premise was not discharged by `apply`; it remains the explicit contradiction `false = true → Q`, followed by the false continuation premise.  I will add that contradiction proof and restore the continuation's equality introduction.  The remaining diagnostic should then come from the invariant callback or disappear from the helper theorem.

The sixteenth import check succeeded.  `foldProgram_spec` now checks without a residual goal: Lean accepted the generated loaded-frame equality, wrapping-add body evaluator, false condition evaluator, index-increment evaluator, `foldPrefix_succ` invariant, and strict measure decrease.  The only reported goal is the public valid branch at the allocator endpoint.  I will preserve the represented input across the allocation store and output-length write, derive the singleton payload bounds and representation from the runtime memory facts, and apply the checked fold helper to the exact remaining annotated program.  Progress means the artifact theorem reaches the shared dispatch suffix with the singleton representation.

The seventeenth import check succeeded, and the required artifact-result build completed.  The valid branch now uses `FixedArrayPairResult.input_preserved_by_alloc`, `UInt64Array.At.write64After`, `FixedArrayResult.singletonStore_at`, and `foldProgram_spec`; a `Wasm.wp.conseq` step transports the helper's singleton postcondition through the generated dispatch suffix.  Lean reports no residual goal in `artifact_behavior`.  I will leave `Behavior.lean` unchanged and run the required commands once more for final acceptance.

## Final acceptance

The final unchanged import check succeeded, and `LeanExeGen.GeneratedR23fa7efc3fb0298b.ArtifactResult` rebuilt successfully.  The proof uses the shared declarations `FixedArrayLengthDispatch.leProgram_spec` through its generated tactic, `FixedArrayCapacity.constantProgram_spec`, `FixedArrayAllocatorWindow.region_spec_withTail`, `FixedArrayFold.forwardSetupProgram_spec`, `Wasm.wp_loop_cons`, `FixedArrayFoldBody.continuingGuardedProgram_spec`, `ArrayFold.foldPrefix_succ`, `FixedArrayFold.singletonResultProgram_spec`, `FixedArrayPairResult.input_preserved_by_alloc`, and the `FixedArrayResult` length, singleton, payload, and finish results.  `artifact_behavior` has no residual goal.
