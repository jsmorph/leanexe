# Proof Journal

## 2026-08-11: Initial candidate check

I ran `node PROOF_IMPORT_CHECK.js LeanExeGen/GeneratedR23fa7efc3fb0298b/Behavior.lean` on the untouched candidate, and it succeeded without output.  I then ran `/home/somebody/src/leanexe/tools/leanrun --timeout 60m lake -d . --no-ansi build LeanExeGen.GeneratedR23fa7efc3fb0298b.ArtifactResult`, which failed while building `Behavior.lean`.  The exact starter residual is `TerminatesWith env module 0 initial [Value.i64 inputPtr]` with the expected existential output-pointer postcondition, under the decomposed array, globals, allocator, memory-bound, and page-bound hypotheses.

The starter has already derived `hLengthRead`, `hLengthBound`, `hInputAddress`, `hHeapTop`, `hFreeList`, and `hAllocs`.  No proof construction has begun, and no candidate source changed before this diagnostic.  The next step is the required LTG retrieval, beginning at `LTG/categories.json`, followed by inspection of task-specific recipes and annotations.

## 2026-08-11: Task metadata and initial retrieval

I opened `LTG/categories.json` first and selected the arrays, loops, compiler-motifs, allocation, memory, and proof-construction indexes from the starter goal and the frozen task features.  `PROOF_TASK_FEATURES.json` reports one 489-instruction exported function with twenty locals, three loops, memory, arithmetic, and allocation.  `PROGRAM_ANNOTATIONS.json` narrows the proof to an unsigned length dispatch at local 7 with limit 8 and a forward one-word array fold whose sum accumulator is local 1, item is local 2, index is local 13, effective stop is local 15, and result is local 10.

`PROOF_RECIPES.json` has no complete composition.  Its first direct recipe is `wp_fixed_array_length_le_dispatch_from hArray at 7, 8`, which should convert the entry and expose valid and invalid branch obligations.  Its fold recipe supplies exact setup, continuing-load, scalar-transition, result-placement, and singleton-result declarations, but it requires a caller-defined invariant and measure rather than a one-line complete theorem.

I searched the arrays, proof-construction, and compiler-motifs indexes for `leanexe.array.length-dispatch.v1|FixedArrayLengthDispatch|TerminatesWith.of_wp_entry_for`.  I searched the arrays, loops, and compiler-motifs indexes for `leanexe.array.fold.v1|forwardSetupProgram_spec|continuingGuardedProgram_spec|foldPrefix|guardedBackEdgeProgram_spec`.  I also searched the allocation, arrays, memory, and proof-construction indexes for `singletonResultProgram_spec|constantProgram_spec|RuntimeReady|heapReserveBytes`.

The searches selected `fixed-array-length-dispatch`, `fixed-array-capacity`, `fixed-array-fold-structure`, `array-fold-prefix`, `fixed-array-fold-body`, `fixed-array-traversal-input`, `guarded-back-edge`, `annotated-fold-frame-accessors`, and `compact-loop-suffix-boundary` for inspection.  The `fixed-array-result` entry is redundant because the annotation-specific singleton suffix theorem already combines result placement, payload storage, and root transfer.  The `fixed-array-filter-lt` matches only the generic heap-reserve search term and has the wrong annotation kind, so I rejected it without opening its entry.

I inspected the `entry.json` and `README.md` files for all nine selected entries.  I will use `fixed-array-length-dispatch` and `fixed-array-capacity` at the two wrapper branch boundaries, then use `fixed-array-fold-structure`, `array-fold-prefix`, and `fixed-array-fold-body` for the valid branch.  `fixed-array-traversal-input`, `guarded-back-edge`, and `annotated-fold-frame-accessors` support the fold-body composition, while `compact-loop-suffix-boundary` selects the generated `function_0_array_fold_0_singleton_result_spec` adapter for the completed loop.

I inspected `FormalSpec.lean`, the exported `func0` and `func0Def` definitions in `Program.lean`, the exact annotation equalities and adapters in `AnnotationMatches.lean`, and the named task-local ProofKit declarations.  The expected value is definitionally the wrapping `Array.foldl` sum in the valid branch and the empty array in the invalid branch.  The annotation equality decomposes `func0` into `function_0_length_dispatch_0_dispatch_program ++ function_0_length_dispatch_0_suffix_program`, so the first recipe can apply without reducing the decoded function body.

## 2026-08-11: Direct dispatch recipe attempt

I will convert the public execution with `Wasm.TerminatesWith.of_wp_entry_for (f := func0Def) rfl`, rewrite the checked whole-function decomposition, and invoke `wp_fixed_array_length_le_dispatch_from hArray at 7, 8`.  Progress means that the single `TerminatesWith` residual becomes the structural frame premises followed by separate `input.size ≤ 8` and `¬ input.size ≤ 8` branch weakest-precondition goals.  This attempt follows the direct recipe before opening its semantic instructions or reducing either branch.

The import check succeeded, and the Lean build failed at the decomposition rewrite.  `of_wp_entry_for` produced `wp module func0Def.body ... initial (func0Def.toLocals ...) env`, so Lean had no syntactic `func0` occurrence for `function_0_length_dispatch_0_function_eq`.  The residual is smaller than the starter but needs the exact refolding step described in the inspected `fixed-array-length-dispatch` README.

I treated the `func0Def.body` residual as a new retrieval checkpoint and searched the already selected recipe and entry guidance before unfolding the function.  The entry gives the exact remedy: change the goal to `Wasm.wp _ func0 _ _ _ _`, leaving `func0Def` reducible for entry-frame projections.  I will add that `change` before the checked function equality, and progress remains the separate valid and invalid branch obligations.

The checkpoint query was `entry-conversion|opaque-decoded-body|of_wp_entry_for|func0Def.body` over the proof-construction, compiler-motifs, and arrays indexes.  It returned only the already inspected `fixed-array-length-dispatch` entry, which I will use for the refolding step.  No new entry required inspection, and no returned entry was rejected.

The import check succeeded after the refolding edit, and the Lean build reached the direct recipe's expected boundary.  The remaining goals are five entry-frame facts followed by the valid branch under `input.size ≤ 8` and the invalid branch under its negation.  The function-body equality and dispatch theorem now apply exactly, so I will retain this proof boundary.

Before reducing the entry frame, I searched the proof-construction, compiler-motifs, loops, and arrays indexes for `toLocals|branchFrame|local.*length|parameter.*length|frame.*projection|exact-local-get`.  The results were the already inspected fold-frame accessor and capacity entries, plus `local-frame-projection`, whose declarations concern loop-frame equality, value-stack preservation, and internal getters rather than `Function.toLocals`.  I rejected `local-frame-projection` for these five entry facts without opening it, because the `fixed-array-length-dispatch` README explicitly identifies definitional normalization with `func0Def`, `Function.toLocals`, `Function.numParams`, and `ValueType.zero` when no generated accessor exists.

I will discharge only the five structural dispatch premises by that reduction and introduce each branch-size hypothesis.  Progress means that the next build reports the exact first instruction-region goal inside each branch, with the length dispatch kept folded.  No capacity, allocator, or fold instruction will be reduced in this attempt.

The import check succeeded, and the build discharged all five structural dispatch premises.  It now reports only the valid branch under `hSize : input.size ≤ 8` and the invalid branch under `hSize : ¬ input.size ≤ 8`, with both branch programs still named by `AnnotationMatches`.  This establishes the exact dispatch recipe and leaves no residual entry-frame obligation.

For the new branch boundary, I searched the allocation, arrays, memory, proof-construction, and compiler-motifs indexes for `fixed-array-allocation|region_result_spec|allocator|FreshFixedArrayAt|empty free-list|bump region`.  I inspected the returned `fixed-array-allocation` and `array-memory-framing` entries, and I will use the shifted allocator theorem `FixedArrayAllocatorWindow.region_spec_withTail` at offset 2 with four trailing locals and `FixedArrayPairResult.input_preserved_by_alloc` in the valid branch.  The search also returned operation-specific map, filter, and singleton-wrapper entries with incompatible annotation kinds, so I rejected them without inspection.

The invalid branch requires a represented empty result after its zero-length store, so I revisited the earlier result-store decision.  I inspected `fixed-array-result` and will use `FixedArrayResult.emptyStore_at` there, while retaining the generated singleton suffix adapter for the valid fold.  I also inspected the exact task-local allocator, allocation-arithmetic, input-preservation, and result-store declarations named by those entries.

I will now refold each branch to its checked constant-capacity program followed by `FixedArrayAllocatorWindow.region 2 1`, then apply `FixedArrayCapacity.constantProgram_spec` without reducing the instructions.  Progress means that the next diagnostic exposes only capacity-frame structural premises and the allocator continuation for each branch.  This is the direct capacity recipe supplied for both dispatch edges.

The import check succeeded, and both branch programs refolded to the capacity and shifted allocator regions.  Each capacity theorem now leaves its empty-value-stack, lower-bound, valid-local, and continuation premises.  The valid normalized capacity is sixteen bytes, while the invalid normalized capacity is eight bytes.

Before proving the capacity-local projections required by the allocator theorem, I searched the allocation, arrays, compiler-motifs, and proof-construction indexes for `capacityFrame.*get|capacity-local|capacity.*accessor|normalizedCapacity`.  The query returned only the inspected `fixed-array-capacity` entry, whose declarations include the frame constructor, program theorem, and normalized-capacity definition but no getter theorem.  I will therefore reduce the capacity frame for this projection, as well as the fixed entry-frame length facts, and will record the generated fold accessors later when that distinct frame boundary appears.

I will apply `FixedArrayAllocatorWindow.region_spec_withTail` with offset 2, tail 4, stride 1, and branch capacity 16 or 8.  The memory-fit premise follows from `hOutputFitMemory` after reducing `FormalSpec.expected` with the branch-size hypothesis, while the page and allocator-global premises are already named.  Progress means that each branch reaches the post-allocation store and frame without any allocator-instruction residual.

The import check succeeded, and the shifted allocator theorem matched both exact regions.  All allocator premises except two closed UInt64 capacity computations succeeded, and each branch now has its post-allocation continuation over `FixedArrayAllocator.allocStore` and `FixedArrayAllocatorWindow.allocFrame`.  The closed capacity equalities and `UInt64.toNat` inequalities require kernel-evaluated decision procedures rather than the preceding `norm_num` calls.

Before reducing the returned-root getter from `allocFrame`, I searched the allocation, arrays, memory, and proof-construction indexes for `allocFrame.*get|returned-root|root local|root-local|allocFrame`.  The query returned no entries, and `PROOF_RECIPES.json` names no allocator-frame accessor.  I will derive the getter by reduction, while using `Allocation.bumpFacts.wordAddress_toNat` for the root address instead of rebuilding its arithmetic.

I will close the four capacity computations with `native_decide` and complete the invalid branch through `FixedArrayResult.lengthStore_spec`, `emptyStore_at`, and `finishProgram_spec`.  The branch suffix should then return the allocated root with the represented empty array, using `FormalSpec.expected` reduced by the invalid-size hypothesis.  Progress means that the invalid branch disappears and the valid branch remains at its post-allocation continuation.

The import check succeeded, and the build reduced the proof to the valid post-allocation continuation plus two small invalid-branch residuals.  The invalid address-capacity premise still used `norm_num` on `UInt64.toNat 8`, and the final representation needed both identical array predicates unfolded before Lean would use `hEmpty`.  The allocator composition, result-length store, final root transfer, and public return all applied as intended.

Before reducing any setup-frame facts, I searched the arrays, loops, compiler-motifs, and proof-construction indexes for `forwardSetupFrame.*get|setup frame|continuing_frame_params|setupLocals|generated fold-frame accessors`.  The query returned only the inspected `fixed-array-fold-structure` entry, which supplies setup-frame preservation theorems but no setup-local getter family.  `PROOF_RECIPES.json` instead supplies exact accessors for the later generated continuing frame, so I will reduce only the post-allocation and setup structural premises and reserve those accessors for the invariant and loop-body boundaries.

I will repair the two invalid residuals, preserve the input with `FixedArrayPairResult.input_preserved_by_alloc` and `UInt64Array.At.write64After`, execute the singleton length store, and apply `FixedArrayFold.forwardSetupProgram_spec`.  The setup uses the exact annotation-selected locals and retains the remaining loop and singleton suffix.  Progress means that the invalid branch closes and the only residual begins at the initialized block-wrapped fold loop.

The import check succeeded, and the invalid branch now closes with no residual.  The valid branch preserved the input through allocation and the length store, and the exact fold setup theorem reached the block-wrapped loop.  Two premises remain before that boundary: the eight-byte length-store bound follows from the sixteen-byte allocation fit by arithmetic, and `forwardSetupProgram_spec` needs explicit `inputPtr` and `input` arguments to avoid a metavariable in its parameter-frame premise.

I will replace the exact use of the stronger allocation bound with `omega` and fix the setup theorem's two dependent arguments.  The next diagnostic should contain only the initialized loop and singleton suffix, which will establish the intended compact proof boundary.  No new catalog entry applies to either residual because both belong to the already selected allocation arithmetic and fold-setup declarations.

The import check succeeded, and the build now has one residual: the initialized block-wrapped fold loop followed by the exact singleton result suffix.  Every dispatch, capacity, allocator, input-preservation, result-length-store, and invalid-branch obligation has closed.  The current frame is `FixedArrayFold.forwardSetupFrame`, the current store contains the allocated singleton length, and `hInputLength` represents the unchanged input there.

The loop checkpoint reuses the prior queries for `leanexe.array.fold.v1`, `foldPrefix`, `continuingGuardedProgram_spec`, and `guardedBackEdgeProgram_spec`, along with the inspected `array-fold-prefix`, `fixed-array-fold-body`, `fixed-array-traversal-input`, `guarded-back-edge`, `annotated-fold-frame-accessors`, and `compact-loop-suffix-boundary` entries.  I will use all six entries: `ArrayFold.foldPrefix` defines the accumulator coordinate, the generated continuing-frame accessors supply loop getters, `continuingGuardedProgram_spec` composes each active step, and the generated singleton adapter handles completion.  No new entry was returned or rejected at this checkpoint.

I will add a compact semantic frame, invariant, and measure.  The invariant fixes the post-length store, carries an index bounded by `input.size`, equates accumulator local 1 with the fold over the consumed prefix, and permits the compiler's item and staging locals to vary.  The measure subtracts the encoded index at local 13 from `input.size`, so the generated increment gives a strict decrease on every back edge.

The first loop attempt will refold the remaining program to the checked continuing and step regions inside one block and loop, followed by `function_0_array_fold_0_singleton_result_program`, then apply `Wasm.wp_block_cons` and `Wasm.wp_loop_cons`.  Progress means that Lean accepts this exact decomposition and exposes separate invariant initialization, body preservation, measure decrease, and completion goals.  I will not reduce the loop instructions in this attempt.

The import check succeeded, and Lean accepted the exact loop and suffix decomposition.  `wp_loop_cons` exposes two obligations: initialize `foldInv` from `forwardSetupFrame`, and prove the annotated continuing-plus-step program against the loop continuation.  The latter continuation already distinguishes `Break 0` as invariant preservation with strict measure decrease and `Break 1` as entry into the singleton suffix.

I will initialize the invariant at index zero and split the step by whether its bounded index equals `input.size`.  The completed case will use `FixedArrayTraversalInput.continuingProgram_exit_spec`, while the strict case will use `FixedArrayFoldBody.continuingGuardedProgram_spec`.  This attempt should expose the exact completion callback and active-step dependent premises without reducing the continuing loader or scalar back edge.

The import check succeeded, and the loop split reached both intended semantic theorems.  Initialization exposed one incorrect invariant coordinate: setup local 16 still contains the input pointer because that slot served as the stop scratch before the scalar body later writes the done value.  I will therefore carry local 16 as another unconstrained invariant value, initialized to `inputPtr` and set to zero after an active step.

The completed guard theorem also left its encoded index argument ambiguous even though both generated getter facts had the same value.  I will pass `UInt64.ofNat input.size` explicitly and continue to use the generated getter theorems for locals 13 and 15.  Its callback is exactly the checked singleton suffix boundary selected earlier, so I will apply `function_0_array_fold_0_singleton_result_spec`, prove its payload address with `hFacts.wordAddress_toNat`, and finish the public singleton result.

The import check succeeded.  Carrying local 16 repaired invariant initialization, and the completed branch now passes through the generated singleton adapter, reconstructs the represented `Array.foldl` result, executes the public suffix, and closes.  The only residual is the strict-index branch of the annotated loop body.

I will instantiate `FixedArrayFoldBody.continuingGuardedProgram_spec` with the generated loaded, post-body, post-condition, and incremented scalar states.  Its premises will use the generated frame accessors for locals 11, 13, and 15, the generated item-validity and loaded-frame equality, and the compiler-derived body, condition, and continuing evaluator theorems.  The false condition callback will rebuild `foldInv` at `index + 1` using `ArrayFold.foldPrefix_succ` and prove the measure decrease from the generated index getter.

The import check succeeded, and the build confirmed that the generated accessor, loaded-frame, and evaluator terms have the required types.  Positional bullets shifted because Lean inferred the dependent `hItem` and `hIndex` premises from later terms, causing the item-validity proof to target `hInput` and each later proof to target the next premise.  This is the exact failure mode described in the inspected `fixed-array-fold-body` and `fixed-array-traversal-input` entries.

I will name every structural and dependent premise before applying `continuingGuardedProgram_spec`, then pass `hValues`, the three getters, `hIndexValue`, `hContinueGuard`, `hItem`, `hInput`, `hIndex`, `hLoaded`, `hStepProgram`, `hBody`, and `hCondition` by name.  Only the exit and continuing callbacks should remain as tactic bullets.  This change preserves the theorem boundary and avoids changing the invariant or reducing any instruction region.

The import check succeeded after the named-premise edit, and the Lean build reduced the active step to three residuals.  The body and increment evaluators expose `ScalarTransition.U64Op.add.apply` where the semantic states use `UInt64.add`, while the reconstructed next frame is contradictory because generated local 18 changes from zero to one.  The named theorem composition therefore works, and the remaining failure lies in exact scalar reduction and one omitted loop-carried compiler local.

I searched the loops, compiler-motifs, proof-construction, and arrays indexes for `U64Op.apply|releaseReady|staged|scalar-state|fold-frame`.  The query returned the already inspected `annotated-fold-frame-accessors`, `fixed-array-fold-body`, and `guarded-back-edge` entries, plus the generic `scalar-post-test-loop` entry.  I rejected `scalar-post-test-loop` without opening it because this proof already uses the artifact-specific fold body and back-edge theorems, and the generic entry supplies no generated local-18 accessor or stronger evaluator.

I searched `PROOF_RECIPES.json` and `PROGRAM_ANNOTATIONS.json` for `U64Op.apply|releaseReadyLocal|stagedValueStart|function_0_array_fold_0_body_eval`.  The recipe names the body evaluator already in use, and the annotation identifies local 18 as `releaseReadyLocal`, but neither file supplies another accessor or invariant theorem.  The exact checked ProofKit source defines `ScalarTransition.U64Op.apply` by cases with `.add` reducing to `left + right`, so I will use that declaration in both evaluator simplifications.

I will extend `foldFrame` and `foldInv` with the release-ready value, initialize it to zero, retain it in the loaded state, and set it to one in the next invariant frame.  The completed branch may carry either value because its suffix depends on the accumulator and result root rather than local 18.  Progress means that `hBody`, the continuing evaluator, and `hNextFrame` close, leaving either no goal or a strictly later invariant or measure residual.

The import check succeeded, and the Lean build completed `ArtifactResult` with no proof residual.  Unfolding `ScalarTransition.U64Op.apply` closed both scalar-addition equations, while carrying `releaseReady` made the generated next frame exact and allowed the fold-prefix invariant and decreasing measure to close.  The remaining messages are linter warnings, including one unused `Wasm.wp_nil` simplifier, and do not affect theorem acceptance.

I will leave the successful candidate unchanged and run the required import check and bounded build once more.  This final repetition tests the exact file that the orchestrator will read, with no intervening proof edit.  A second successful `ArtifactResult` build will constitute final acceptance.

## 2026-08-11: Final acceptance

The final import check and repeated bounded build both succeeded, and `LeanExeGen.GeneratedR23fa7efc3fb0298b.ArtifactResult` completed with 3,082 jobs.  The proof uses the shared length-dispatch, capacity, allocator-window, result-store, fold-setup, traversal-input, guarded fold-body, scalar-transition, fold-prefix, and singleton-result declarations recorded above.  `artifact_behavior` has no residual goal, and the successful candidate remained unchanged after its first accepted build.
