# Proof Journal

## Initial Lean check

I ran the prescribed `ArtifactResult` build before editing the candidate.  The build reached `Behavior.lean` and failed with one goal: `TerminatesWith env module 0 initial [Value.i64 inputPtr]` with a postcondition that returns one pointer representing `FormalSpec.expected input`.  The starter has already decomposed `RuntimeReady` into the array representation, allocator globals, input and output bounds, heap-reserve bounds, and the WebAssembly page bound.

The diagnostic confirms that the proof must interpret function 0 from the supplied initial store.  No precondition or representation subgoal remains outside the interpreter goal.  I will inspect the frozen recipe and annotations before changing the theorem, then search the task-specific LTG indexes for declarations matching `TerminatesWith`, fixed-array length dispatch, traversal input, and a loop-carried fold accumulator.

## Frozen guidance and LTG retrieval

I started LTG retrieval by reading `LTG/categories.json`, which identifies arrays, compiler motifs, loops, memory, allocation, and proof construction as the relevant indexes.  `PROOF_RECIPES.json` supplies a direct length-dispatch invocation, `wp_fixed_array_length_le_dispatch_from hArray at 7, 8`, and a fold composition based on the checked annotation equalities, `FixedArrayFold.forwardSetupProgram_spec`, `Wasm.wp_loop_cons`, traversal-input execution, fold-prefix lemmas, and result placement.  `PROGRAM_ANNOTATIONS.json` fixes the dispatch at instructions 0 through 8 and the fold inside the valid branch at instructions 39 through 65, with accumulator local 1, item local 2, result local 10, input local 11, length local 12, index local 13, stop local 14, effective-stop local 15, done local 16, staged-value local 17, and release-ready local 18.

The first LTG queries will be `leanexe.array.length-dispatch.v1|FixedArrayLengthDispatch|wp_fixed_array_length_le_dispatch_from` in the arrays and compiler-motifs indexes, `leanexe.array.fold.v1|FixedArrayFold|foldPrefix|continuingProgram_spec|wp_block_loop` in the arrays, loops, and compiler-motifs indexes, and `TerminatesWith|RuntimeReady|artifact_behavior|FixedArraySingleton|region_result_spec` in the proof-construction, arrays, and allocation indexes.  These terms come from the current goal, checked recipes, and compiler annotations.  An entry will be inspected only if its indexed declarations or structural tags match one of those proof boundaries.

The first query selected `fixed-array-length-dispatch`, and the second selected `array-fold-prefix`, `fixed-array-fold-structure`, `fixed-array-traversal-input`, and `fixed-array-result`.  I inspected each selected entry's `entry.json` and `README.md`; they match this artifact's bounded length check, forward single-accumulator fold, indexed input load, and singleton or empty result stores.  The third query also returned singleton-wrapper and residual-singleton entries, which I rejected because their annotation kind requires a singleton-input direct-call wrapper, whereas this artifact has an embedded variable-length fold and no calls.

I also searched `fixed-array-allocation|FixedArrayAllocator|bumpFacts|allocator-window|region_spec` in the allocation, arrays, and compiler-motifs indexes because both branches construct result arrays after the dispatch.  The query selected `fixed-array-allocation` and `array-memory-framing`, whose declarations address the bump allocator and preservation of the represented input across writes above `heapTop`.  I will inspect those two entries before deciding whether the exact generated suffix matches a checked allocator region.

I inspected `fixed-array-allocation` and `array-memory-framing`.  Both remain relevant: the program contains the standard empty-free-list bump allocator in each branch, and the valid branch traverses the input after allocation, so it must preserve `hArray` across allocator header and result-length writes.  I also searched `demo-9|demo9|array-fold|fold|wrapping.*sum|sum` in the worked-examples and proof-construction indexes; no Demo 9 entry exists in the task-specific view, while the results repeat only the generic fold entries already selected, so I rejected the unrelated map, filter, counter, GCD, and singleton-wrapper entries.

## Direct recipe attempt

I will now apply `Wasm.TerminatesWith.of_wp_entry_for (f := func0Def) rfl`, followed exactly by `wp_fixed_array_length_le_dispatch_from hArray at 7, 8`.  This is the direct recipe named by `PROOF_RECIPES.json` and the LTG dispatch entry.  Progress will consist of two branch-level `wp` goals whose hypotheses distinguish `input.size ≤ 8` from `¬ input.size ≤ 8`; an elaboration failure or a larger instruction-level goal would reject the direct shape.

The import check succeeded, and the Lean build accepted both the entry conversion and the direct dispatch theorem.  It left five routine frame and numeric premises plus the intended valid and invalid branch `wp` goals, with the exact branch programs and a common final `[localGet 6]` continuation.  The direct recipe therefore improved the proof boundary and will remain; the next edit will normalize only its routine premises, then compose a checked allocator theorem at the start of each branch.

I searched `capacityPrefix_spec|capacityPrefix|capacity.*prefix|allocation preparation|prepare` in the allocation, arrays, and proof-construction indexes and in the permitted proof-kit mirror.  The LTG indexes supplied no separate generic entry, while `FixedArrayPairResult.capacityPrefix_spec` handles a fixed two-element, twenty-four-local layout and therefore does not match this function's zero-or-one-element, twenty-local layout.  I also searched `FixedArraySingleton|singleton.*result|singletonStore|emptyStore|constResult`; `fixed-array-result` remains applicable to the result stores, while singleton-wrapper entries remain rejected because the artifact has no direct scalar call.

The next construction will add a small local capacity-prefix theorem for the exact emitted zero-or-one result-length calculation, then keep the allocator interval folded and apply `FixedArrayAllocatorWindow.region_spec_withTail` with offset 2, tail 4, and stride 1.  The valid branch should reach the named allocation store with capacity 16, and the invalid branch should reach it with capacity 8.  A successful check should replace each allocator's search, bump, header writes, and global updates with a post-allocation continuation goal; failure to match the exact region will trigger another LTG search rather than instruction-level allocator reduction.

The import check succeeded.  The Lean build accepted the local zero-capacity prefix, matched `FixedArrayAllocatorWindow.region_spec_withTail 2 4` exactly, executed the empty-result length store, and constructed `UInt64Array.At ... #[]` with `FixedArrayResult.emptyStore_at`.  The only invalid-branch residual is normalization of four root-transfer locals and `FixedArrayEqNode.branchPost`; I will replace the broad `wp_run` result with focused simplification over the named frames and final postcondition.

The next import check succeeded, and the Lean build closed the invalid branch after focused normalization.  The remaining goal is the valid branch alone.  I will reuse the exact capacity and allocator composition with capacity 16, preserve `hArray` through allocator and length writes, apply the checked fold-setup theorem, and stop at the loop-rule obligations.

The loop invariant will keep the store fixed and quantify a logical index bounded by `input.size`.  It will record the generated frame's parameter and local lengths, empty operand stack, root pointer, input pointer, encoded index, encoded stop, and accumulator equal to `ArrayFold.foldPrefix input (fun sum element => sum + element) 0 index`; the measure will read local 13 and subtract its natural value from `input.size`.  Reaching an initialization obligation and a body obligation under this compact invariant will count as progress, while expansion of the annotated setup or allocator would indicate a failed proof boundary.

The import check succeeded.  The build matched the capacity-16 allocator, preserved the input through allocation and the length store, accepted `FixedArrayFold.forwardSetupProgram_spec`, and reached exactly the invariant initialization and loop-step obligations from `wp_block_loop`.  One separate equation remains in expected-value normalization because Lean has not unfolded the local `sumStep` abbreviation under `Array.foldl`; I will replace that simplification with a directed reduction.

The next edit will prove the invariant at index zero and decompose the loop step at its logical index.  In the continuing case, `FixedArrayTraversalInput.continuingProgram_spec` should consume the annotated guard and dynamic input load, leaving only the arithmetic, local updates, back edge, invariant preservation, and strict measure decrease.  In the exit case, reducing the true guard should expose the checked accumulator-result placement and output payload store continuation.

The import check succeeded, and the build proved invariant initialization and applied `FixedArrayTraversalInput.continuingProgram_spec` in the continuing branch.  Its residual shows the intended next frame and measure, but generated arithmetic reads concrete `s.locals` entries while the invariant stores `Locals.get` equations; the traversal entry's README predicted this representation boundary.  The exit residual has the same cause at the index and stop getters, so I will derive exact `List.getElem?` and `getElem` facts once from the invariant before either branch.

The next import check succeeded.  The exact local facts now reduce the continuing branch through the entire back edge, leaving one arithmetic or frame proposition that the trailing `omega` call could not prove; I will expose that proposition before selecting another lemma.  The exit simplifier did not unfold `Locals.get`, so none of the exact local facts applied there; I will normalize only that getter layer before processing the true guard.

The exposed continuing residual consists of the expected witness `index + 1`, the `foldPrefix_succ` equation, and a natural-number subtraction decrease after two `UInt64` moduli.  These obligations need directed fold and encoding equalities before `omega`; no additional semantic lemma is missing.  The exit branch now reaches the payload-store bounds directly, confirming that the broad simplifier crossed the result-placement and payload-store boundaries, so I will instead stop after the four-instruction guard and apply `FixedArrayFold.resultProgram_spec` and `FixedArrayResult.payloadStore_spec`.

The next check closed the continuing branch with the `index + 1` witness, `foldPrefix_succ`, and the encoded measure decrease.  In the exit branch, the comparison rewrites to the true case, and the only diagnostic is a `simp only` call that makes no progress immediately after `if_pos`.  I will remove that no-op and continue reducing `br_if`; progress will expose the annotated result program followed by the payload store.

The import check succeeded after removing the no-op.  The build reduced `br_if` to a match on `Value.i32 1 :: s.values`; the invariant still supplies `hValues : s.values = []`, but Lean has not used that equation while selecting the nonzero branch.  I will rewrite `hValues` at this boundary and use definitional simplification to expose the result suffix.

The import check succeeded, and rewriting `hValues` changed the scrutinee to the concrete list `[Value.i32 1]`.  `simp only` still leaves the overlapping zero and nonzero `br_if` patterns because branch selection requires proving that the `UInt32` literal one differs from zero.  I will use the ordinary simplifier at this single match boundary; the desired residual remains the uninterpreted result-placement suffix.

The import check succeeded, but the ordinary simplifier selected the branch and then interpreted the entire result suffix, leaving raw local-access, store-bound, and final-frame matches.  That residual worsens the proof boundary and rejects this simplifier shape.  I will replace it with the explicit fact `(1 : UInt32) ≠ 0` under `simp only`, which should reduce the discriminator without invoking the global `wp` simplification rules.

The focused check succeeded at the intended boundary: the remaining program contains the two-instruction accumulator placement, the standard singleton payload store, and four final local transfers.  I will apply `FixedArrayFold.resultProgram_spec` and `FixedArrayResult.payloadStore_spec` as named by the fold recipe and inspected LTG entries.  The payload bound will use `BumpFacts.wordAddress_toNat` at word one, and `FixedArrayResult.singletonStore_at` will establish the final represented array after `foldPrefix_size` identifies the complete fold.

The import check succeeded, and Lean accepted `resultProgram_spec`, the result-local getter, and the shape of `payloadStore_spec`.  Its remaining root getter simplifies exactly to the previously derived `hRootGet`.  The three arithmetic diagnostics retain `(16 : UInt64).toNat` as an opaque term for `omega`, so I will reuse `hFitMemory` and `hFacts.fit32` with simplification after the checked address and page rewrites.

The import check and `ArtifactResult` build both succeeded after the bound normalization.  The completed proof uses the exact dispatch recipe, a loop invariant carrying `ArrayFold.foldPrefix`, the checked traversal and result-placement theorems, and the checked singleton payload store.  No additional LTG entry, annotation, lemma, tactic, or catalog metadata was needed.
