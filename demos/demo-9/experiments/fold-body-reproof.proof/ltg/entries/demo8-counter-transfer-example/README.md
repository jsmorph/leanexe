# Demo 8 independent counter-transfer layout

This example tests the Demo 7 composition against a scalar helper with three accumulators and different generated local coordinates.  The recognizer identifies a unique remaining counter and returned accumulator while leaving the independent audit accumulator outside the identity argument.  The same wrapper composition and residual normalization produce a 67-line proof.

The first composition run preserved proof structure but took longer than the initial artifact proof, so its package remains a timing-negative example.  A later direct-acceptance run took 212.727 seconds, compared with 313.253 seconds for the initial proof generation.  The exact artifact and independent package verification remained fixed throughout the comparison.

The task catalog excludes this entry from a measured proof of the same artifact digest.  Its distinct local layout supplies independent evidence for the counter-transfer recognizer without broadening the semantic theorem to arbitrary loops.  Consult the canonical counter and singleton-wrapper entries before this example when an exact annotation already selects them.
