# Proof Journal

## 2026-08-10: Initial candidate check

The unchanged candidate failed after the deterministic `RuntimeReady` decomposition.  The residual goal is `TerminatesWith env module 0 initial [Value.i64 inputPtr]` with one returned `i64` pointer whose final memory represents `FormalSpec.expected input`.  The available hypotheses include `UInt64Array.At`, its length read and generated address bound, normalized input address, allocator globals, the empty free-list global, output bounds, heap-reserve bounds, and the page bound.

The next step will inspect the frozen specification, Program locals, annotations, selected strategies, and exact recipe.  Progress requires a checked transition theorem that consumes the existing runtime facts and establishes the final singleton-or-empty array representation.  I will attempt a named direct recipe or complete composition before expanding its internal proof steps.

## LTG retrieval

I opened `LTG/categories.json` first.  The relevant indexes are `arrays`, `loops`, `compiler-motifs`, `allocation`, and `proof-construction`, based on the `leanexe.array.length-dispatch.v1` prefix, the `leanexe.array.fold.v1` region, the singleton result allocation, and the public `TerminatesWith` goal.

The first catalog query is `rg -n 'leanexe\.array\.(fold|length-dispatch)\.v1|FixedArrayFold|singletonResultProgram_spec|TerminatesWith\.of_wp_entry_for'` over those five `tools.jsonl` files.  This query targets the exact annotation kinds and recipe declarations, and I will inspect only canonical entries returned as plausible matches.

The query returned the promoted `fixed-array-length-dispatch` entry and the fold-specific `array-fold-prefix`, `fixed-array-fold-structure`, `fixed-array-fold-body`, `fixed-array-traversal-input`, `guarded-back-edge`, and `compact-loop-suffix-boundary` entries.  I will inspect those canonical entries because together they cover entry conversion, initialization, the accumulator invariant, the indexed load, the scalar back edge, loop exit, and the compact singleton suffix.  I reject `fixed-array-result` at this stage because the recipe names the stronger `FixedArrayFold.singletonResultProgram_spec`, which already composes result placement and representation, and I defer `local-frame-projection` unless a generated internal-local getter remains.

I inspected `entry.json` and `README.md` for all seven selected entries.  The direct entry composition starts with `Wasm.TerminatesWith.of_wp_entry_for (f := func0Def) rfl`, rewrites the checked function decomposition, and invokes `wp_fixed_array_length_le_dispatch_from hArray at 7, 8`.  The valid branch then uses the exact fold setup, `ArrayFold.foldPrefix` invariant, combined continuing-body theorem, exit theorem, and singleton-result theorem.  The invalid branch still requires capacity, allocation, and empty-result construction, so I will search for those exact structural features after the direct dispatch attempt reveals its branch frame.

Before the first edit to `Behavior.lean`, I will inspect only the relevant declarations in `Program`, `AnnotationMatches`, and the mirrored proof-kit modules.  The intended edit applies the exact direct length-dispatch recipe without opening its instructions.  Separate valid and invalid branch goals at the named capacity prefixes will count as progress.

## Direct recipe attempt

The import check succeeded.  The Lean build failed before dispatch because `of_wp_entry_for` left the body as `func0Def.body`, so rewriting `function_0_length_dispatch_0_function_eq` found no occurrence of `func0`.  The recipe prose names the required body-level conversion: an exact `change Wasm.wp _ func0 _ _ _ _` before rewriting the function equality.

The next edit adds only that exact `change`; it does not unfold `func0`.  A successful invocation will leave the generated frame premises followed by the valid and invalid branch weakest-precondition goals.  This residual boundary is smaller than the public `TerminatesWith` goal and will determine which allocator composition the artifact needs.

The import check succeeded again.  The direct recipe now applies and leaves five routine frame/constant premises plus two semantic branch goals.  The valid branch begins at `function_0_length_dispatch_0_valid_branch_program` under `input.size ≤ 8`; the invalid branch begins at the corresponding invalid program under `¬ input.size ≤ 8`.  Both use `FixedArrayLengthDispatch.branchFrame 7` and the one-instruction suffix `.localGet 6` under `FixedArrayEqNode.branchPost`.

The second catalog query is `rg -n 'empty free.list|empty-free-list|FixedArrayAllocator|FreshFixedArrayAt|singleton|empty result|result construction'` over `allocation`, `arrays`, `compiler-motifs`, and `proof-construction`.  It targets the exact allocator state (`global 1 = 0`), fresh fixed-array representation, and branch-specific singleton or empty result required by the two residual goals.

The query returned `fixed-array-allocation`, `fixed-array-result`, and `fixed-array-singleton-wrapper`, as well as unrelated worked-example and residual-normalization entries.  I will inspect the canonical allocation and result entries.  I reject `fixed-array-singleton-wrapper` because its required annotation combines a direct scalar call with a singleton-only wrapper, while this artifact has no calls and has separate bounded and invalid branches; I also reject the worked examples because the task forbids artifact-proof searches and their annotation shapes differ.

I inspected `fixed-array-allocation` and `fixed-array-result`.  The artifact allocator matches `FixedArrayAllocatorWindow.region 2 1`: every canonical allocator local is shifted by two, and the twenty internal locals decompose as `2 + 14 + 4`, so `region_spec_withTail 2 4` applies.  I will use `FixedArrayPairResult.input_preserved_by_alloc`, `FixedArrayResult.lengthStore_spec`, `emptyStore_at`, and the fold-specific singleton theorem.  The generic result entry is used for the invalid branch and the length store; its separate singleton sequence is rejected for the valid suffix because `FixedArrayFold.singletonResultProgram_spec` gives the stronger checked composition named by the recipe.

The next edit discharges the five frame and constant premises by reduction and introduces the two size assumptions.  This isolates the valid and invalid branch programs without changing their instruction representation.  The two branch weakest-precondition goals alone will count as progress before adding the allocator and loop compositions.

The import check succeeded, and the build now reports only the two branch goals.  I will prove the invalid branch first because it has no traversal loop: constant capacity `8`, shifted allocator `region 2 1`, zero-length store, root transfer through locals `7`, `5`, and `6`, then the dispatch suffix.  Progress means reaching a final representation or a small address/frame premise after applying `emptyStore_at`.

The import check succeeded.  Lean accepted the invalid-branch decomposition and every allocator premise except closed capacity reductions and root inference; the memory-fit derivation also retained `UInt64.toNat 8`, which prevented `omega` from using the frozen bound.  The remaining diagnostics are closed `normalizedCapacity 0 1 = 8` arithmetic, `8 ≤ UInt64.toNat 8`, and metavariables for the result root.  I will state the closed capacity equality once, use a natural-number fit bound, pass `root := heapTop + 48` explicitly to the store theorems, and use definitional proofs for the constant `toNat` facts.

The import check succeeded, and Lean now closes the invalid branch completely.  The only residual goal is the valid branch.  The accepted branch proof uses `FixedArrayCapacity.constantProgram_spec`, `FixedArrayAllocatorWindow.region_spec_withTail`, `Allocation.bumpFacts`, `FixedArrayResult.lengthStore_spec`, `finishProgram_spec`, and `emptyStore_at`; the result root is `heapTop + 48`, and the final expected array reduces with the invalid-size assumption.

The valid branch will reuse the same allocator composition with capacity `16`.  After writing length one, it will preserve the input representation across allocation and the disjoint length store, apply `FixedArrayFold.forwardSetupProgram_spec`, and enter the checked block-wrapped loop with invariant `accumulator = ArrayFold.foldPrefix input (· + ·) 0 index`.  A successful next boundary is the loop initialization plus explicit continuing and exit goals, with the singleton result suffix still folded.

The import check succeeded.  The allocator decomposition matches, but two closed `UInt64.toNat 16` obligations need decision rather than definitional reduction.  The forward-setup subgoals remained unsolved because their bullets were aligned with the enclosing length-store application; Lean then reported the following bullet as having no goal.  I will nest those bullets under `forwardSetupProgram_spec`, use `native_decide` for the two closed capacity propositions, and rerun without changing the invariant.

The import check succeeded.  Lean now accepts the complete valid prefix, input preservation, exact fold setup, loop-program decomposition, and invariant initialization.  The sole residual goal is the loop step under `foldInv`; the target keeps the annotated continuing and guarded-back-edge programs folded.  I will split on `index = input.size`, apply `continuingProgram_exit_spec` on completion, and inspect the resulting singleton-suffix goal before composing it with `singletonResultProgram_spec`.

The import check succeeded.  The exit theorem reduces the completed branch to `wp` over the exact annotated singleton-result program, with the completed `foldFrame` and unchanged fold store.  The continuing branch remains the original annotated body goal with `index < input.size`.  I will close the exit through `FixedArrayFold.singletonResultProgram_spec`, prove its compact postcondition implies the dispatch suffix and public postcondition, and rewrite `sumPrefix input input.size` with `foldPrefix_size`.

The import check succeeded.  The compact-postcondition implication reached the public existential, but an extra `simp only [hReturn]` ran after `wp_run` had already used the return-local fact and therefore failed for making no progress.  The singleton theorem applied and exposed only structural facts about the folded generated frame; the existing simplifier lists omitted `ScalarTransition.State.toLocals` and `ScalarTransition.U64State.toState`, so none reduced.  I will add small checked lemmas for `foldFrame` values, parameter shape, local length, and required getters, then use the shared `resultFrame` length-preservation facts instead of repeatedly expanding the generated frame.

The import check succeeded, and Lean accepted the new `foldFrame` lemmas.  The singleton premises remained visible because their bullets were aligned with the enclosing `Wasm.wp.conseq` application rather than nested under `singletonResultProgram_spec`.  The compact implication also exposes the return-local load as a match over `resultFrame.get 6`; I will refold that getter with `change`, rewrite it by the compact postcondition, and nest all singleton premises under the theorem application.

The import check succeeded.  The nested singleton composition now closes every premise except the accumulator getter and the root getter after result placement; elaboration unfolded those getters before the simp lemmas could match.  The public target also uses the generated `FormalSpec.UInt64ArrayAt` definition, while the checked suffix returns the definitionally equivalent proof-kit predicate.  I will pass the folded getter facts as named theorem arguments, add two checked result-frame getter lemmas, and use `change UInt64Array.At` at the final representation boundary.

The import check succeeded, and the completed-loop branch now proves the singleton representation and the public postcondition.  The only residual goal is the continuing loop branch under `index < input.size`; the invalid-input branch also remains closed.  I will apply `FixedArrayFoldBody.continuingGuardedProgram_spec` to the exact generated load, body, condition, and back-edge transitions.  Progress requires a successor invariant whose accumulator is `sumPrefix input (index + 1)` and whose measure is `input.size - (index + 1)`.

The import check succeeded.  The combined fold-body theorem accepted the exact annotation programs, local roles, input representation, generated scalar states, and loop guard.  Three preliminary facts remain: `omega` needs a closed `8 < UInt64.size` fact for the index bounds, and the generated loaded-frame equality needs one explicit unfolding step to identify `U64State.toState.toLocals []` with the annotated continuing frame.  I will add the closed bound, unfold only `foldFrame` and `loadedState` at the load boundary, and retain the generated equality as the proof of the memory-load transition.

The import check succeeded.  The closed bound and loaded-frame equality now check.  One side condition from `Nat.mod_eq_of_lt` remains even though it is exactly `hIndexSucc64`; I will supply that hypothesis directly.  The combined theorem also generalized its module and environment because its postcondition was inferred through callbacks, so I will fix `module_`, `env`, `st`, and `frame` to the current weakest-precondition goal and let Lean infer only `Q`.

The import check succeeded, and fixing the theorem parameters aligned the combined theorem with the loop assertion.  The remaining goals are internal equalities in the back-edge callback: normalize `UInt64.toNat 1` before using the no-wrap bound, use the generated continuation evaluation without broad simplification, and reduce the measure through the checked `foldFrame_get_index` projections.  The successor invariant itself checks, including `sumPrefix input (index + 1)` and the unchanged store.

The import check succeeded.  The generated body, false condition, and index-increment evaluation now close, and Lean accepts the successor `foldInv`.  The last two goals are closed reductions: `UInt64.toNat 1 = 1` in the no-wrap addition proof, and the loop measure after `wp_loop_cons` restores the prior empty value stack onto the successor locals.  I will discharge the constant by reduction and change the measure goal directly to the two natural-number subtractions, since `Locals.get 13` reads only the successor parameters and locals.

## Acceptance

The import check and `LeanExeGen.GeneratedR23fa7efc3fb0298b.ArtifactResult` build succeeded.  The proof uses the shared length-dispatch, capacity, allocator-window, fixed-array result, traversal-input, fold-body, fold-prefix, loop, and scalar-transition declarations, together with the checked generated annotation equalities.  The loop invariant carries `ArrayFold.foldPrefix` as the wrapping `UInt64` accumulator, the back edge advances the input index once, and the natural-number measure decreases.  No residual goal remains.
