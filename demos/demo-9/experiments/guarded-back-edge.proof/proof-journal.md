# Proof Journal

## Initial check

I ran the prescribed artifact-result build against the unchanged candidate.  The build reached `Behavior.lean` and left one goal after the starter's `RuntimeReady` decomposition: prove `TerminatesWith env module 0 initial [Value.i64 inputPtr]` with a singleton result pointer whose memory represents `FormalSpec.expected input`.  The available hypotheses include the represented input array, its readable length and address bounds, allocator globals, output and heap bounds, and the page bound; no earlier starter step failed.

## LTG retrieval

I began retrieval with `LTG/categories.json`, which identifies arrays, compiler motifs, loops, allocation, memory, and proof construction as the relevant indexes.  My first index query was `rg -n 'leanexe\.array\.(fold|length-dispatch)\.v1|forwardSetupProgram_spec|continuingProgram_(spec|exit_spec)|wp_fixed_array_length_le_dispatch_from|foldPrefix' LTG/categories/{arrays,compiler-motifs,loops,proof-construction}/tools.jsonl`.  It selected the canonical entries `fixed-array-length-dispatch`, `fixed-array-capacity`, `fixed-array-fold-structure`, `array-fold-prefix`, `fixed-array-traversal-input`, `guarded-back-edge`, `local-frame-projection`, and `fixed-array-result` for inspection because their annotation kinds or declarations match the checked recipes and the current termination goal.

I inspected the `entry.json` and `README.md` files for all eight selected entries.  I retained `fixed-array-length-dispatch` for the direct entry tactic, `fixed-array-capacity` for each branch prefix, `fixed-array-fold-structure` for fold setup and result placement, `array-fold-prefix` for the accumulator invariant, `fixed-array-traversal-input` for the loop guard and load, `guarded-back-edge` for the annotated step, `local-frame-projection` for generated local getters, and `fixed-array-result` for output stores and ABI reconstruction.  None of these entries has been rejected because each supplies a declaration named by `PROOF_RECIPES.json` and each matches one checked region or a residual frame obligation.

My second index query was `rg -n 'FixedArrayAllocator|Allocation\.bumpFacts|empty free|singleton wrapper|region_result_spec|FreshFixedArrayAt' LTG/categories/{allocation,arrays,memory,proof-construction}/tools.jsonl`; it selected `fixed-array-allocation`.  My third query was `rg -n 'FixedArraySingletonWrapper|singleton.*wrapper|wrapperProgram_spec|whole-function|complete.*composition' LTG/categories/{allocation,arrays,compiler-motifs,proof-construction}/tools.jsonl`; it selected `fixed-array-singleton-wrapper` alongside map, filter, and worked-example entries whose annotation kinds do not match this artifact.  I will inspect the allocation entry and the singleton-wrapper entry, while rejecting the map, filter, and worked-example entries without opening them because this artifact has an inline array fold, no scalar callee, and no matching whole-function annotation.

I inspected `fixed-array-allocation` and retained its shifted-window allocator theorem and `Allocation.bumpFacts` for the post-dispatch branch proofs.  I inspected and rejected `fixed-array-singleton-wrapper` because its premises require a singleton-input dispatch, direct scalar call, store-preserving callee theorem, and whole-wrapper equality, while the frozen artifact accepts lengths through eight and performs an inline fold.  The absence of a complete composition in `PROOF_RECIPES.json` agrees with that structural rejection, so the proof must compose the checked length, fold, allocator, and result regions.

## Direct length-dispatch recipe

I will now attempt the recipe's exact direct entry sequence: apply `Wasm.TerminatesWith.of_wp_entry_for (f := func0Def) rfl`, then invoke `wp_fixed_array_length_le_dispatch_from hArray at 7, 8`.  The intended progress is a conversion from the public termination goal to two weakest-precondition branch goals under `input.size ≤ 8` and its negation.  A failure before those two semantic branch boundaries would identify a recipe-shape or initial-frame mismatch and would require another LTG search before instruction reduction.

The import check succeeded, and the Lean build confirmed that the exact direct recipe matches the decoded function entry.  It produced five reducible premises for the entry frame, local index, and maximum size, followed by the intended `hValid` and `hInvalid` branch goals.  This improves the proof boundary, so I will retain the recipe and discharge the five closed premises by reduction before composing either semantic branch.

The next import check succeeded, while the build rejected `decide` for the local-index bound because its expected proposition still contained the free variable `inputPtr`.  The other four closed premises discharged, and Lean again left only the valid and invalid branch goals after that diagnostic.  I will replace that `decide` with simplification through the concrete generated `func0Def`, which should reduce only the initial local-list length while retaining the semantic branch boundary.

The import check succeeded again, but `[func0Def]` alone unfolded only the generated function record and left `Function.toLocals` opaque.  The diagnostic shows the remaining closed inequality as `7 < 1 + (...toLocals ...).locals.length`; both semantic branches remain unchanged.  I will add the exact local-frame definitions named by the LTG guidance, namely `Function.toLocals`, `Function.numParams`, and `ValueType.zero`, to this focused simplification.

The focused frame simplification succeeded, as did the import check.  Lean now reports only the two branch obligations produced by `leProgram_spec`, so the direct recipe has reached its advertised postcondition without reducing either branch.  I will inspect the exact proof-kit theorem signatures for capacity, shifted-window allocation, fold setup, traversal, guarded back edge, result stores, and final return before constructing a branch composition.

## Invalid branch composition

I will compose the invalid branch first because it isolates capacity, allocation, one length store, and return without the application fold.  The intended first construction applies `FixedArrayCapacity.constantProgram_spec` at capacity local 11 and `FixedArrayAllocatorWindow.region_spec_withTail` with offset 2, tail 4, capacity 8, and stride 1.  Progress will consist of a single post-allocation goal over `FixedArrayAllocator.allocStore`; any earlier failure will identify an exact frame, program-decomposition, or heap-bound mismatch.

The import check accepted the three LTG-selected proof-kit imports, and every capacity and shifted-window allocator premise succeeded.  Lean left the intended compact post-allocation goal containing eight instructions: store the zero length, copy the result root through locals 5 and 6, and enter the outer return continuation.  I will apply `FixedArrayResult.lengthStore_spec`, construct the empty-array representation with `emptyStore_at`, apply `finishProgram_spec`, and reduce the one-instruction outer return.

The import check succeeded, and the result composition reached its final representation obligation.  Three focused issues remain: the length-store bound needs the `wordAddress_toNat` equality rewritten before using the memory fit; `decide` cannot close two lower-index facts that contain `inputPtr`; and the final simplifier has already chosen the existential output pointer before the explicit `refine`.  I will preserve the root-address equality with a typed `convert`, simplify the two frame-length facts through the generated frame definitions, and supply `hOutput` directly after changing the final expected-array goal.

The import check succeeded, and Lean now reports only `hValid`; the invalid branch composition is complete.  The root-address rewrite, generated-frame simplifications, empty-array reconstruction, and final return all checked.  I will next prove input preservation under the allocator's six header writes, compose the valid capacity-16 allocation and singleton-length store, and apply the exact annotated forward-fold setup so that the residual goal begins at the block-wrapped traversal loop.

The import check succeeded, and the allocator framing lemma, capacity-16 branch, allocation, length store, preserved input representation, and fold-setup theorem all checked.  One minor length-store premise needs `omega` because the available 16-byte fit is stronger than its 8-byte requirement.  The substantive residual goal now starts at the annotated block-wrapped loop with the exact `forwardSetupFrame`, so I will add an invariant carrying the generated scalar state, prefix sum, index, input pointer, and effective stop, together with the remaining-input measure.

The import check succeeded, and the build accepted the stronger memory-bound proof, the invariant definition, its exact generated-state initialization, and the loop rule.  The sole residual is the loop step under `FoldInv`; the generated continuation confirms that `Break 0` requires invariant preservation and strict decrease, while `Break 1` reaches the accumulator-result suffix.  I will destruct the scalar-state witness, split on `index = input.size`, and apply `continuingProgram_exit_spec` on the completed edge before handling the loaded continuing edge.

The import check succeeded, and both exit-guard getter premises discharged from the generated scalar state.  The broad simplifier then entered the result stores and exposed their low-level memory semantics, while the unfinished `index ≠ input.size` edge remained at the checked traversal boundary.  I will keep the exit suffix folded, add the allocator root as an invariant coordinate, and compose `resultProgram_spec`, `payloadStore_spec`, singleton reconstruction, and `finishProgram_spec` on that edge.

The import check succeeded, and the completed-edge composition now reaches the final public existential.  Its remaining arithmetic equality is a reducible constant-address identity, and the final return needs an explicit output-pointer witness because the focused simplifier retained the existential.  I will close those two presentation gaps, then apply `continuingProgram_spec` to the nonterminal edge and refold its loaded frame as the generated scalar state with `v2 = input[index]`.

The prescribed build process ended before its output could be recovered after context compaction, so I repeated that build without changing the candidate.  The diagnostic shows that the completed edge reaches the right singleton contents but retains `FixedArrayResult.singletonStore` on the supporting theorem while the goal spells out `writePayload (writeLength ...)`; the missing reduction is the named singleton-store definition.  On the nonterminal edge, an unclosed `simp` premise shifted the subsequent traversal arguments: `hArrayLength` was offered where Lean expected the step program, `hIndexLt` was offered where Lean expected item-local validity, and the final `change` therefore acquired an extra `[]` argument.  I will inspect the exact traversal and guarded-transition signatures, unfold `singletonStore` only at the representation conversion, and realign every traversal premise before constructing the back edge.

The import check accepted the LTG-selected `GuardedBackEdge` module, and the singleton-store conversion now succeeds.  The build still rejects the item-local premise because simplification does not unfold `Locals.validIndex` through the generated state, so the remaining traversal premises continue to shift; it also reports one unmatched parenthesis in my explicit `toLocals []` term, which causes the invalid branch to detach syntactically.  The item-local fact has the closed coordinate bound `2 < 21`, so I will state that bound with `change` and `decide`, add the missing opening parenthesis around the explicit frame, and require the next diagnostic to reach `guardedBackEdgeProgram_spec` with both allocation branches intact.

The import check succeeded, and the build confirms that the explicit frame now parses while both allocation branches remain attached.  Lean inferred the traversal theorem's dependent `hItem` argument as a postponed metavariable instead of presenting it as the next tactic goal, so my new positional bullet was applied to `hInput`; every later premise shifted by one position.  I will pass `hItem` as a named argument in the theorem application, remove the extra positional bullet, and preserve the exact loaded-state `change`; reaching the annotated scalar program will establish the intended traversal boundary.

The import check succeeded, but naming only `hItem` changed elaboration of the theorem application: Lean inferred several getter premises from the target, then assigned the remaining positional bullets to incompatible premises.  The diagnostic still identifies the intended loaded item and store, but tactic-goal ordering has become an unreliable interface for this dependent theorem.  I will construct the encoded comparison separately and supply every traversal premise by name, including the generated local getters, represented input, strict index bound, and item-local bound; the theorem should then leave only its `hNext` weakest-precondition goal.

The import check and every named traversal premise succeeded.  Lean now leaves one goal whose program is exactly `ScalarTransition.guardedBackEdgeProgram 11` applied to the generated body, condition, and continuing statement, with the loaded element in local 2.  I will apply the retained `guardedBackEdgeProgram_spec` composition using the generated body and condition evaluation theorems, evaluate the one-assignment continuing statement, and prove the loop postcondition at `index + 1` from `ArrayFold.foldPrefix_succ` and the remaining-input measure.  Progress requires the postcondition to reduce to `FoldInv` for the successor state and a strict natural-number subtraction inequality; a worse residual would prompt an LTG search for that exact remaining goal.

The import check succeeded, and the exact guarded-back-edge theorem accepted the generated body evaluation, condition evaluation, false exit, successor prefix invariant, and strict measure decrease.  The only failure is the broad simplifier used for the continuing assignment: unfolding generic `State.get` and `State.set?` over value lists exceeded the simplifier step limit.  The checked proof-kit theorem `Stmt.eval_toState` reduces this obligation to the corresponding `U64State` evaluator, matching the generated evaluation method; I will use that theorem and a finite `simp only` set for local 13's increment instead of increasing global limits or reducing WebAssembly instructions.

The import check and artifact-result build both succeeded after replacing the generic-state evaluation with `Stmt.eval_toState` and the finite `U64State` reduction.  The final theorem now covers the invalid empty-result branch, the valid singleton allocation and representation, the annotated input-traversal loop, the loop-carried wrapping `UInt64` accumulator, and the public return postcondition.  The remaining diagnostics are linter warnings about unused simplifier arguments and generated annotation variables; they leave no goals and reveal no missing lemma, tactic, annotation, recipe, or LTG metadata.  I will now run the prescribed import check and artifact-result build once more as the final independent validation, without editing the candidate afterward.
