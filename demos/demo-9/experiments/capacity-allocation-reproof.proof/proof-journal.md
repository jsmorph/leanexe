# Proof Journal

## 2026-08-11: starter diagnostic

I ran the untouched candidate with `/home/somebody/src/leanexe/tools/leanrun --timeout 60m lake -d . --no-ansi build LeanExeGen.GeneratedR23fa7efc3fb0298b.ArtifactResult`.  The build reached `Behavior.lean` and failed with one unsolved goal after the supplied `RuntimeReady` decomposition.  The context contains `hArray`, the exact input-length read and address facts, allocator globals, input/output bounds, heap-reserve bounds, and the page bound.  The residual goal is `TerminatesWith env module 0 initial [Value.i64 inputPtr]` with an output pointer representing `FormalSpec.expected input`.

I will first try the exact length-dispatch recipe from `PROOF_RECIPES.json`, whose prescribed command is `wp_fixed_array_length_le_dispatch_from hArray at 7, 8`.  Before changing `Behavior.lean`, I will inspect the recipe composition around that command and search the relevant LTG indexes for the `leanexe.array.length-dispatch.v1` annotation, the tactic name, the fold region, and the singleton result suffix.  Progress means splitting the public execution into the valid `input.size ≤ 8` fold-and-singleton branch and the invalid empty-array branch while preserving their generated continuations.

## 2026-08-11: initial LTG retrieval

I opened `LTG/categories.json`, then searched `LTG/categories/arrays/tools.jsonl`, `compiler-motifs/tools.jsonl`, `loops/tools.jsonl`, and `proof-construction/tools.jsonl` with the query `length-dispatch|wp_fixed_array_length_le_dispatch_from|array-fold|foldPrefix|singletonResult|RuntimeReady|TerminatesWith`.  The search returned the canonical entries `fixed-array-length-dispatch`, `fixed-array-fold-structure`, `array-fold-prefix`, `fixed-array-fold-body`, `compact-loop-suffix-boundary`, and `fixed-array-capacity`.  I inspected only each entry's `entry.json` and `README.md`.

I selected `fixed-array-length-dispatch` for the public-entry conversion and exact bounded dispatch, `fixed-array-fold-structure` for fold setup and singleton result placement, `array-fold-prefix` for the wrapping-addition invariant, `fixed-array-fold-body` for the checked load and scalar back edge, and `fixed-array-capacity` for the two constant-capacity allocator prefixes.  I retained `compact-loop-suffix-boundary` as guidance because the recipe supplies `function_0_array_fold_0_singleton_result_spec`, but I will use it only if the loop continuation becomes expensive.  None of these entries was rejected: each matches an annotation or residual structure named by `PROOF_RECIPES.json`; duplicate category-index hits refer to the same canonical entries and were not separately inspected.

`PROOF_RECIPES.json` contains no complete composition.  Its direct length-dispatch tactic matches the anticipated body-level `Wasm.wp` goal and requires the represented input plus locals 7 and 8.  Its fold recipe names exact setup, continuing, scalar-step, result, and singleton-suffix declarations, so I will inspect those generated declarations and the mirrored ProofKit signatures before the first candidate edit.

## 2026-08-11: direct dispatch result

I added the fixed-store entry conversion `Wasm.TerminatesWith.of_wp_entry_for (f := func0Def) rfl` and invoked the exact recipe command `wp_fixed_array_length_le_dispatch_from hArray at 7, 8`.  The import check succeeded.  The Lean build failed in 7.6 seconds with the dispatch theorem's five structural premises and two semantic branches: the valid branch assumes `input.size ≤ 8`, and the invalid branch assumes its negation.

The direct recipe improved the boundary from a 489-instruction public execution goal to separate branch programs.  The frame-shape and numeric premises are reducible from `func0Def`; I will discharge those with `simp [func0Def, Wasm.Function.toLocals]` or `norm_num` after confirming the exact reducer names.  The branch programs begin with the constant-capacity motifs named in the recipe and then the allocator, so the next retrieval checkpoint concerns composed capacity-allocation theorems and represented singleton or empty results.

## 2026-08-11: branch and frame retrieval

I searched the allocation, arrays, memory, compiler-motifs, and proof-construction indexes with `constantCapacityRegion_spec_withTail|region_spec_withTail|allocFrame_get_root|empty array|singleton|writeLength|uint64_array|FixedArrayAllocator|FixedArrayResult|finishFrame|payloadBound|zero`.  I inspected `fixed-array-allocation` and `fixed-array-result`.  The allocation entry applies with offset 2, tail 4, stride 1, and logical output lengths one and zero; its composed theorem covers each branch's capacity prefix and allocator.  The result entry applies to the valid length-one store and payload suffix and to the invalid zero-length store and final root transfer.

The same query returned `fixed-array-singleton-wrapper`, but I rejected it from the index metadata because its program requires a scalar direct call and a `leanexe.array.singleton-wrapper.v1` annotation, while this artifact has an inline `leanexe.array.fold.v1` loop.  It also returned map, filter, and counter worked examples whose annotations and operations differ; I did not inspect those entries or any artifact proof.  `fixed-array-allocation` and `fixed-array-result` remain selected.

I then searched the arrays, memory, and proof-construction indexes with `frameBefore|writeLength|input preservation|array-memory-framing|frame.*array|write64After`.  I inspected and selected `array-memory-framing`, `annotated-fold-frame-accessors`, `fixed-array-traversal-input`, and `local-frame-projection`.  The allocator preservation theorem transports `hArray` through the six header stores, `UInt64Array.At.write64After` transports it through the result-length store, the generated fold accessors cover every scalar-frame getter, and the generated traversal adapter covers the indexed load.  The `word_reads` tactic does not yet match any residual read equation, so I rejected that tactic command for the current boundary and retained its fallback only if a nested read goal appears.

The next candidate edit will discharge the dispatch structure and apply `FixedArrayAllocatorWindow.constantCapacityRegion_spec_withTail` to both semantic branches.  I will stop at the post-allocation continuations if the theorem applies, because those residuals will reveal the exact frame and remaining-program presentation needed for the length-store and fold theorems.

## 2026-08-11: capacity and allocation composition

I discharged the dispatch premises and changed each branch to `FixedArrayCapacity.constantProgram ++ FixedArrayAllocatorWindow.region ++ rest`.  The import check succeeded.  The Lean build failed after ten seconds with both composed capacity-allocation applications accepted.  The valid continuation starts at the length-one store followed by the annotated fold setup, loop, singleton suffix, and return suffix; the invalid continuation contains only the zero-length store and two-local root transfer.

Three structural goals remained because `norm_num [func0Def, Wasm.Function.toLocals]` did not unfold `Wasm.Function.numParams`.  I will add that exact reducer.  For each post-allocation continuation, I will use `allocFrame_get_root` rather than unfold the allocation frame, derive address bounds from one `Allocation.bumpFacts`, and apply `FixedArrayResult.lengthStore_spec`.  The invalid branch should then close through `finishProgram_spec` and `emptyStore_at`; progress on the valid branch means reaching `FixedArrayFold.forwardSetupProgram_spec` with the represented input preserved through allocation and the output-length write.

## 2026-08-11: result-store boundaries

The import check succeeded after the next edit.  The twenty-second build failed with focused diagnostics: both `lengthStore_spec` applications and the invalid `finishProgram_spec` application matched, all generated frame getters and frame-shape premises succeeded, and the valid residual now starts at the fold setup.  The two length-store bounds failed only because `rw` could not match `heapTop + 48 + UInt64.ofNat 0` against its simplified `heapTop + 48` form; the word-capacity side goals also remained because `norm_num` did not reduce the `UInt64` conditional.

I will name the accessor result with `have hRootAddress := hFacts.wordAddress_toNat 0 (FixedArrayCapacity.normalizedCapacity_toNat_ge_eight _ _)` and use `simpa` before rewriting.  This uses the exact capacity theorem named by the recipe and avoids reducing the `UInt64` expression.  The invalid residual is now a `wp []` goal at `branchPost`, so I will construct `FixedArrayResult.emptyStore_at`, reduce the empty continuation, execute the one-instruction suffix, and normalize `FormalSpec.expected` with the invalid-size hypothesis.

## 2026-08-11: invalid branch and fold plan

The import check succeeded.  The next build accepted both named root-address facts, both length-store bounds, and the invalid result transfer.  Its invalid diagnostics show two small endpoint errors: `emptyStore_at` had already simplified away `writeLength_pages`, and the suffix execution needed one additional `Wasm.wp_nil` reduction after `wp_localGet_cons`.  I will remove the absent rewrite and add that reduction.

The valid residual now begins exactly at `FixedArrayFold.forwardSetupProgram`.  Before constructing the loop, I searched the recipe for `_continuing_frame_` accessors and retained all generated getters, parameter length, local length, value stack, loaded-frame equality, and evaluator declarations.  I will define a semantic `sumFrame` as the generated continuing frame with local 1 equal to `ArrayFold.foldPrefix input (· + ·) 0 index`, local 7 equal to the result root, locals 11 through 15 equal to the input pointer, input size, index, and stop values, and the remaining mutable scalar slots existentially quantified.  The invariant fixes the post-length-store memory and bounds the index by `input.size`; the measure reads generated local 13 and subtracts its natural value from `input.size`.

After `forwardSetupProgram_spec`, the residual program begins with the annotated block-wrapped loop.  The `fixed-array-fold-structure` tactic record matches that goal, its annotation kind is exact, and the invariant and measure provide its two premises.  I will therefore attempt `wp_block_loop invariant ... decreasing ...` before using the fallback `Wasm.wp_loop_cons`.  Progress means that the command accepts the loop shape and exposes separate initial, exit, and repeat obligations without unfolding the loop body.

## 2026-08-11: loop command result

The import check succeeded, and the build completed its diagnostic in 7.5 seconds.  The `wp_block_loop` command matched the annotated block and loop, producing the expected initialization and step obligations; I used the structured tactic command rather than its fallback declaration.  The initial-frame simplifier reduced the full allocation and setup equality to `ValueType.i64.zero = Value.i64 0`, so the missing reducer is the named `Wasm.ValueType.zero` declaration.

The exit edge accepted `FixedArrayTraversalInput.continuingProgram_exit_spec` with the generated value-stack, index, and stop getters.  Its sole residual is the singleton-result continuation after `Break 1`, which I will discharge with the recipe's generated `function_0_array_fold_0_singleton_result_spec` after rewriting the completed accumulator by `ArrayFold.foldPrefix_size`.  The continuing edge accepted `FixedArrayFoldBody.continuingGuardedProgram_spec`; because I left its scalar states unspecified, Lean exposed metavariable goals for `initial`, `afterBody`, `afterCondition`, and `result` before the structural premises.  I will provide the exact compiler-generated states explicitly, set the result to `false`, and use the generated loaded-frame, body-evaluation, condition-evaluation, and continuing-evaluation declarations.

The invalid branch closed after a focused `change` from `FormalSpec.UInt64ArrayAt` to `UInt64Array.At` followed by expected-value normalization.  No invalid-branch execution goal remains.  All residual goals belong to the valid fold initialization, exit, or continuing edge.

## 2026-08-11: named fold-body premises

The next import check succeeded, and the build diagnosed the continuing theorem's dependent-premise ordering.  As the LTG entry warned, Lean inferred `hItem` through the later loaded-frame premise and shifted positional bullets: the item-valid proof was presented against `hInput`, and every later bullet moved one premise.  I replaced those bullets with named facts and passed `hValues`, all getters, the continue guard, `hItem`, `hInput`, `hIndex`, `hLoaded`, `hStepProgram`, `hBody`, and `hCondition` by name.

The following build finished in 5.8 seconds.  Every structural fact and the condition evaluation now succeeds.  Three scalar equalities need focused normalization: the loaded-frame proof must unfold the local `accumulator` abbreviation, and the body and back-edge evaluator facts must unfold `ScalarTransition.U64Op.apply` so compiler-described `.add` agrees with ordinary `UInt64` addition.  The only other residuals are the expected exit suffix and the continuing callback's invariant-plus-measure pair.

## 2026-08-11: continuing-edge reconstruction plan

I inspected `FixedArrayFoldBody.continuingGuardedProgram_spec`, the generated scalar evaluator equations, and the exact singleton-result adapter named by the recipe.  The continuing callback receives `continued.toState.toLocals []` under `Break 0`; the loop rule then requires `sumInv` for index `index + 1` and strict decrease of `sumMeasure`.  I will normalize the generated additions with `ScalarTransition.U64Op.apply`, use `ArrayFold.foldPrefix_succ` to identify `nextAccumulator`, reconstruct the generated continuing frame, and prove the measure from the generated local-13 accessor and bounded `UInt64.ofNat` conversions.  Progress means that the continuing branch closes and only the recipe-supplied singleton suffix remains.

The import check succeeded.  The 6.3-second build accepted both scalar evaluator equalities after unfolding `ScalarTransition.U64Op.apply`; the generated body addition and index increment now agree with their semantic `UInt64` expressions.  The loaded-frame equality still needs the definition `function_0_array_fold_0_continuing_frame`, because one side remained a generated-frame name while `loaded.toState.toLocals []` unfolded to its scalar state.  The callback diagnostic confirms that `Break 0` reduces to the invariant and strict-measure pair once the predecessor's generated empty value stack is normalized.  No new LTG retrieval is needed because this is the exact residual shape described by the selected `fixed-array-fold-body` entry.

The next import check succeeded, and the 8.1-second build accepted the loaded-frame equation, `foldPrefix_succ`, the successor-frame equality, and the reconstructed invariant.  Only the measure proof remained on the continuing edge.  The generated local-13 accessor reduced the predecessor, but the loop rule represents the successor as a record that projects the successor frame's parameters and locals while resetting its value stack; this blocked syntactic matching of the accessor.  Since `sumMeasure` depends only on `Locals.get`, I will change the residual to `input.size - (index + 1) < input.size - index`, using the already established `UInt64` bounds.  This discharges the projection without introducing a new local fact or bypassing the selected accessor: the accessor has already normalized the generated frame, while definitional reduction removes the value-only record wrapper.

The import check succeeded, and the 5.3-second build accepted that definitional measure boundary.  The continuing branch now has no residual goal: its proof uses the shared `ArrayFold.foldPrefix_succ` theorem, the generated continuing-frame and scalar evaluator declarations, and `FixedArrayFoldBody.continuingGuardedProgram_spec`.  The sole remaining goal is the completed-loop suffix beginning with the exact 19-instruction singleton-result program.  I will now attempt the direct generated adapter `function_0_array_fold_0_singleton_result_spec`, as prescribed by `PROOF_RECIPES.json`, before reducing any payload or final-return instructions.  Progress means that the adapter recognizes the projected completion frame and leaves only its named payload-bound and public-postcondition premises.

The import check succeeded.  The 5.9-second build shows that the direct adapter matched the projected completion frame and complete suffix without reduction.  It left exactly `hPayloadBound` for word one of the allocated result and `hNext` for the `branchPost` continuation, so the recipe's shape improved the proof boundary as expected.  For the payload premise I will use `Allocation.BumpFacts.wordAddress_toNat` at word one, then preserve pages through `writeLength` and `allocStore`.  For `hNext` I will use `FixedArrayResult.singletonStore_at` on the same allocation, `FixedArrayResult.finishFrame_return_get` for generated return local 6, and `ArrayFold.foldPrefix_size` to identify the completed accumulator with the specification's `Array.foldl`.  These accessors were named by the selected `fixed-array-result`, `fixed-array-fold-structure`, and `array-fold-prefix` entries; no residual goal calls for another LTG search.

The next import check succeeded.  The 5.6-second build accepted the singleton-store theorem but exposed four normalization failures.  The existing `hCapacity` proves equality at type `UInt64`, while the memory inequalities contain `(normalizedCapacity 1 1).toNat`; rewriting produced `UInt64.toNat 16`, which `norm_num` left opaque.  I will add the exact checked fact `(normalizedCapacity 1 1).toNat = 16` and use it directly in the allocator bounds.  The two `resultFrame` shape facts also need the explicit shared `resultFrame_params` and `resultFrame_locals_length` theorems followed by the generated completion-frame declarations, rather than an unrestricted `simp`.  Finally, I will unfold the two local frame aliases in `hReturn` so that `wp_localGet_cons` can match the getter exactly.  The intended semantic endpoint remains unchanged.

## 2026-08-11: acceptance

The import check succeeded, and the artifact target built in 7.9 seconds with no residual goal.  The proof uses the shared length-dispatch tactic, `FixedArrayAllocatorWindow.constantCapacityRegion_spec_withTail`, `FixedArrayResult.lengthStore_spec`, `FixedArrayFold.forwardSetupProgram_spec`, the structured `wp_block_loop` tactic, `FixedArrayTraversalInput.continuingProgram_exit_spec`, `FixedArrayFoldBody.continuingGuardedProgram_spec`, `ArrayFold.foldPrefix_succ`, `ArrayFold.foldPrefix_size`, the generated scalar evaluator and frame-accessor declarations, `function_0_array_fold_0_singleton_result_spec`, `FixedArrayResult.singletonStore_at`, and `FixedArrayResult.finishFrame_return_get`.  The accepted theorem covers both length branches, preserves the represented input across allocation and result writes, proves strict decrease on every loop back edge, and identifies the completed wrapping accumulator with the specified left fold.  No proof obligation remains.
