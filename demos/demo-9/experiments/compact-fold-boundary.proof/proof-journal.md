# Proof Journal

## 2026-08-10: starter check and initial retrieval

I ran `node PROOF_IMPORT_CHECK.js LeanExeGen/GeneratedR23fa7efc3fb0298b/Behavior.lean` on the untouched candidate, and it exited successfully.  I then ran `/home/somebody/src/leanexe/tools/leanrun --timeout 60m lake -d . --no-ansi build LeanExeGen.GeneratedR23fa7efc3fb0298b.ArtifactResult`, which failed only at `Behavior.lean`.  The starter decomposed `RuntimeReady` as intended, leaving `TerminatesWith env module 0 initial [Value.i64 inputPtr]` with the required represented `FormalSpec.expected input` result.

The diagnostic supplies the input representation, its length read and address facts, allocator globals, result and heap bounds, and the page bound.  No application, representation, or arithmetic premise is missing at the public theorem boundary.  The remaining work is semantic composition of the checked length dispatch, valid-branch fold and allocation, and invalid-branch empty-result construction.

I started LTG retrieval at `LTG/categories.json`, as required.  The relevant indexes are `compiler-motifs`, `arrays`, `loops`, `allocation`, `memory`, and `proof-construction`, based on the two checked annotations and the residual `TerminatesWith` goal.  I will search those indexes for `leanexe.array.length-dispatch.v1`, `leanexe.array.fold.v1`, `leProgram_spec`, `forwardSetupProgram_spec`, `foldPrefix`, `TerminatesWith`, and exact declarations named by `PROOF_RECIPES.json` before opening any entry.

`PROOF_RECIPES.json` contains two direct recipes and no complete composition.  The first prescribes `wp_fixed_array_length_le_dispatch_from hArray at 7, 8`; the second identifies the fold setup, traversal, invariant, result-placement declarations, and `Wasm.wp_loop_cons`, but does not give a complete invocation.  I will attempt the length-dispatch tactic unchanged before reducing its instructions, because it is the only exact direct invocation at the public proof boundary.

I ran the recorded LTG queries against only the six relevant `tools.jsonl` indexes.  The exact queries were `leanexe\.array\.length-dispatch\.v1`, `leanexe\.array\.fold\.v1`, `leProgram_spec|wp_fixed_array_length_le_dispatch_from`, `forwardSetupProgram_spec|resultProgram_spec`, `foldPrefix|continuingProgram_spec`, `TerminatesWith|wp_entry_to_loop|wp_loop_cons`, `constantProgram_spec|region_result_spec`, `FixedArrayAllocator|region_spec|bumpFacts`, `FixedArrayResult|singletonStore_at|emptyStore_at`, `wp_block_loop|wp_entry_to_loop|wp_loop_cons`, `loop-carried|decreasing measure|loop-body`, and `RuntimeReady|heapReserveBytes|result-region`.  The searches found the canonical entries `fixed-array-length-dispatch`, `fixed-array-fold-structure`, `array-fold-prefix`, `fixed-array-traversal-input`, `fixed-array-capacity`, `fixed-array-allocation`, `fixed-array-result`, and `local-frame-projection`.

I inspected only `entry.json` and `README.md` for those eight entries.  I will use `fixed-array-length-dispatch` for public-entry conversion and the branch split, and I expect to use the fold-structure, prefix, traversal, capacity, allocation, result, and local-frame entries inside the two branches.  The `RuntimeReady|heapReserveBytes|result-region` query also returned `fixed-array-filter-lt`, which I rejected without opening because its `leanexe.array.filter-lt.v1` annotation and stable-filter program do not match either checked annotation or the wrapping-sum specification.

The first proof edit will apply `wp_entry func0Def as frame`, followed immediately by the exact direct recipe `wp_fixed_array_length_le_dispatch_from hArray at 7, 8`.  Success means replacing the public `TerminatesWith` residual with valid and invalid branch weakest-precondition goals and their generated frame premises.  If the program cannot refold to the recipe shape, the diagnostic should identify whether entry conversion or length-dispatch matching is the missing boundary.

The import check passed after the first proof edit, while the Lean build failed inside the direct recipe.  Its `change` succeeded and displayed the exact `leProgram 7 8 validBranch invalidBranch ++ [localGet 6]` decomposition, but `leProgram_spec` could not use `hArray`.  The cause is the selected entry's explicit distinction between `of_wp_entry` and `of_wp_entry_for`: `wp_entry` uses the former and introduces a fresh arbitrary store, while every `RuntimeReady` premise describes the fixed `initial` store.

I searched the same relevant indexes for `residual-specification-normalization|residual.*goal`, `continuation|branchPost|conseq`, `of_wp_entry_for|entry-conversion`, and `localGet 6|return continuation|result continuation`.  The only applicable result remains `fixed-array-length-dispatch`, whose README names `Wasm.TerminatesWith.of_wp_entry_for`; I used that declaration after reading its exact definition in the named `CodeLib.Entry` source file.  I rejected `residual-specification-normalization` without opening it because its singleton-wrapper annotation does not address the failed store quantification or either annotation in this artifact.

The next edit replaces `wp_entry func0Def as frame` with `apply Wasm.TerminatesWith.of_wp_entry_for (f := func0Def) rfl` and retains the exact recipe invocation.  Progress is the recipe's valid and invalid branch goals over `initial`, with `hArray` accepted.  This change follows the promoted LTG entry's store-specific public-entry instruction rather than reducing the dispatch theorem.

The import check passed, and the build confirmed the corrected direct composition.  The recipe now leaves five closed frame-shape facts plus the valid and invalid branch weakest-precondition obligations over `initial`; no dispatch instructions remain.  The frame facts are definitional consequences of `func0Def`, `Function.toLocals`, `Function.numParams`, and `ValueType.zero`, so I will discharge them with focused `simp` calls and introduce the branch hypotheses.

I inspected the exact signatures in the permitted proof-kit mirror for `FixedArrayCapacity.constantProgram_spec`, `FixedArrayAllocatorWindow.region_spec_withTail`, `FixedArrayResult` store and finish theorems, `FixedArrayFold` setup and result theorems, `FixedArrayTraversalInput.continuingProgram_spec`, `ArrayFold.foldPrefix_succ`, `ArrayFold.foldPrefix_size`, the loop rules, and frame projection.  The program matches allocator-window offset `2`, tail `4`, stride `1`, with combined result root local `7`; the valid branch allocates capacity `16`, and the invalid branch allocates capacity `8`.  The next diagnostic should begin after branch introduction, retaining the exact capacity prefix at the head of each branch.

The import check passed, and the build closed all five dispatch frame facts.  The two remaining goals start at the exact valid and invalid branch programs under `FixedArrayEqNode.branchPost`, with hypotheses `input.size ≤ 8` and `¬ input.size ≤ 8`, respectively.  This confirms that the direct recipe reached its advertised postcondition and that the next exact boundaries are the two annotation-matched constant-capacity programs.

I will refold the branch heads to `FixedArrayCapacity.constantProgram 1 1 11` and `constantProgram 0 1 11`, then apply `constantProgram_spec` without reducing its instructions.  The branch frame has one parameter, twenty internal locals, an empty value stack, and writable combined local `11`, so its three structural premises should close by reduction.  Progress is a pair of residual goals beginning with allocator window `region 2 1`, one from capacity `16` and one from capacity `8`.

The import check passed, and both capacity applications succeeded.  Lean reduced the normalized capacities only in the remaining frames, so the two residual programs now begin at the allocator initialization for combined local `16`.  Their instruction sequences match `FixedArrayAllocatorWindow.region 2 1`, including the four unused trailing locals.

The next edit applies `region_spec_withTail` with offset `2`, tail `4`, heap top `heapTop`, and allocation counter `allocs` in both branches.  Frame, capacity, page, memory-mode, and allocator-global premises follow from the named frames and `RuntimeReady`; branch-specific memory fit follows from `hOutputFitMemory` after reducing `FormalSpec.expected` under `hSize`.  Progress is a valid residual starting with the length-one result store and an invalid residual starting with the length-zero result store, both over `FixedArrayAllocator.allocStore initial heapTop capacity 1 allocs`.

The import check passed, and Lean accepted the allocator program decomposition and every premise unrelated to capacity inference.  `apply` left the theorem's `capacity` argument as a metavariable, so the capacity-local equality, lower bound, memory fit, and continuation could not share the computed branch value.  The diagnostic displays the intended residual programs after allocation, confirming that the only failure is the missing explicit instantiation.

I will set `(capacity := 16)` in the valid branch and `(capacity := 8)` in the invalid branch.  These are the reductions of `normalizedCapacity 1 1` and `normalizedCapacity 0 1`, already visible in the capacity-local goals.  The existing focused reductions should then establish all allocator premises and leave only the two post-allocation result programs.

The import check passed, and the explicit capacity arguments resolved the shared metavariables.  Lean now leaves only concrete decidable UInt64 normalization goals for the capacity-local equality and `8 ≤ capacity.toNat`; the memory-fit and allocator-global premises close.  The post-allocation residuals have the expected length-one and length-zero store programs, so the allocator theorem matches the artifact exactly.

The next edit adds `native_decide` after the focused structural normalization of those four concrete capacity premises.  This does not reduce artifact instructions; it evaluates closed UInt64 expressions such as `normalizedCapacity 1 1 = 16`.  Progress is elimination of every allocator premise, leaving the result stores as the only branch goals.

The import check passed, and `native_decide` closed all concrete capacity premises.  Both allocator theorems now succeed, leaving only the valid post-allocation program beginning with a length-one store and the invalid program consisting of a length-zero store plus root transfer.  The allocator store and frame carry capacities `16` and `8` exactly, with result root `heapTop + 48` in combined local `7`.

Before constructing the fold proof, I searched the relevant array, memory, allocation, and proof-construction indexes for `array-memory-framing|frameBefore|write64After`, `allocStore.*At|input.*preserv`, and `headerMem|writeLength.*At`.  I inspected `LTG/entries/array-memory-framing/entry.json` and `README.md`; I will use its `FixedArrayPairResult.input_preserved_by_alloc` theorem and `UInt64Array.At.write64After` lemma in the valid branch.  The singleton-wrapper search result was rejected without opening because its direct-call annotation and scalar-callee program do not match this fold artifact.

The next extended edit first applies `FixedArrayResult.lengthStore_spec` in each branch, using one shared `Allocation.bumpFacts` value per branch for the root address bound.  It then completes the invalid branch with `emptyStore_at` and `finishProgram_spec`; in the valid branch it preserves `hArray` through the allocator and length store, leaving the exact fold setup program.  Progress is a closed invalid branch and a sole valid residual at `FixedArrayFold.forwardSetupProgram` with a represented input in the current store.

The import check passed.  The valid branch reached the exact fold setup with `hInputCurrent` established in the post-length-store memory.  The invalid length store and `emptyStore_at` proof succeeded, but the finish theorem's premise bullets were aligned as siblings of the outer length-store premise, leaving all finish-frame facts open and producing a later `No goals to be solved` error.

I will correct the invalid finish proof's bullet nesting.  For the valid branch I will define an invariant that records the unchanged current store, a logical index bounded by `input.size`, the root and array-pointer locals, the encoded index and stop locals, and the accumulator as `ArrayFold.foldPrefix`; the measure reads local `13` and subtracts its `toNat` value from `input.size`.  I will apply `forwardSetupProgram_spec`, prove invariant initialization, and enter `Wasm.wp_loop_cons`; the residual transition goal will count as progress and will determine the precise continuation shape required for the exit and repeat edges.

The import check passed, the setup theorem matched, and invariant initialization succeeded.  The loop body diagnostic shows the precise rule: a `Break 0` back edge must establish the invariant and a strict decrease, while a `Break 1` exits through the enclosing block to the result-placement suffix.  The only non-loop issue is the final invalid representation conversion from `Project.ProofKit.UInt64Array.At` to the definitionally parallel `FormalSpec.UInt64ArrayAt`, which needs both definitions named in the final simplification.

The next edit splits the invariant index into `index < input.size` and `index = input.size`.  It applies `FixedArrayTraversalInput.continuingProgram_spec` to the first edge and `continuingProgram_exit_spec` to the second, supplying the invariant's array, index, and stop getters and the represented current input.  I will initially use `wp_run` only after the continuing theorem; the residual goal will show which updated-local equalities or fold-prefix rewrite remain, while the exit theorem will expose the post-loop suffix without expanding the indexed load.

The import check passed.  `continuingProgram_spec` accepted the exact program, getters, comparison, and item-local fact; `apply` discharged its dependent item-validity and natural index-bound premises from the local hypotheses, so my later bullets shifted and attempted to use `hItem` as the represented-input premise.  The exit theorem also matched and reduced the loop exit to the expected `Break 1` continuation.

I will correct the continuing theorem's remaining premise order to supply only `hInputCurrent` and the tail proof after the comparison.  On the exit edge I will use an exact `change` to the checked `resultProgram 1 10`, rewrite `foldPrefix_size`, apply `payloadStoreProgram 7 10 0`, construct the singleton representation, and apply `finishProgram 7 4 6`.  Progress is a fully closed exit branch and a continuing-edge residual confined to straight-line accumulator and index updates.

The import check passed, and the continuing theorem now leaves only its straight-line tail.  `wp_run` expanded that tail but could not normalize local getters through `dynamicResultFrame`; the residual shows the expected generated additions, staging locals, zero done flag, incremented index, invariant, and strict-measure goals.  The exit composition elaborated through the selected result, payload, representation, and finish theorems, so I will retain its semantic boundaries while focusing the next check on the continuing transition.

I will pass `(hItem := hItem)` explicitly so the dependent result frame uses the named proof, then replace broad execution with the proof-kit's focused local-update simplifier vocabulary.  The simplifier will use the invariant getter equations, `ArrayFold.foldPrefix_succ`, the encoded successor equality `UInt64.ofNat index + 1 = UInt64.ofNat (index + 1)`, and `toNat` bounds for the measure.  Success closes the `Break 0` edge as an updated `foldInvariant` with measure `input.size - (index + 1)`.

The import check passed, but the continuing transition did not close.  Two preliminary facts need stronger statements: the successor equality must expose the literal one as `UInt64.ofNat 1`, and the successor bound should combine `index + 1 ≤ input.size` with `hInputCurrent.size_lt` instead of asking `omega` to unfold `UInt64.size`.  More importantly, the focused simplifier reached the correct nested local-update term but exhausted the theorem's one-million-heartbeat limit because the large outer loop continuation remained inside each weakest-precondition reduction.

I searched the loop, proof-construction, and compiler-motif indexes for `postcondition-generic|loop-body|wp_loop_body_intro`, `local-update|local-set|transition summary`, and `counter-transition|accumulator.*transition`.  No entry covers this fold's straight-line staged update; `counter-transition` concerns a different `leanexe.loop.scalar-post-test.v1` annotation and was rejected without opening.  Following the selected elaboration guidance, I will introduce a continuation-generic theorem for the exact post-load instruction tail, with a compact `foldUpdateFrame` postcondition, then prove the fold invariant and measure from that frame.

The import check passed after adding the generic update theorem, but the build exposed three precise composition errors.  The theorem's simplifier reached raw `frame.locals` elements, while its hypotheses remained stated through `frame.get`; the continuing traversal theorem inferred its natural index premise from `hContinue`, so the explicitly supplied copy shifted the update proof into an already solved premise; and the exit continuation remained as a reducible nested match on `Break 1` before the result-program boundary.  The repeated diagnostic check was necessary because the preceding build output was lost during session compaction; it made no candidate change.

I will use `Frame.internal_getElem_of_get`, from the inspected `local-frame-projection` entry, to project locals `0`, `1`, and `12` before simplifying the update program.  I will remove the redundant traversal premise and normalize only the concrete `Break 1` match and zero-width stack restoration before applying `FixedArrayFold.resultProgram_spec`.  Progress means that the update helper proves its compact postcondition and both loop edges reach their intended semantic continuations.

The import check passed, and the projected local facts made `foldUpdateProgram_spec` succeed.  The next failures are narrower: the first two dynamic-result frame-shape premises need `Wasm.Locals.set` reduced under the known singleton parameter list; after the helper returns `Break 0`, the loop rule's continuation match still needs concrete reduction; and `continuingProgram_spec` leaves its dependent natural-index proof after the continuation premise rather than before it.  My attempted exit normalization sat inside the proof of `hAccumulatorFinal`, where the goal contained none of the continuation expressions and `simp` correctly reported no progress.

I will reduce `Wasm.Locals.set` for the dynamic frame length and values, normalize the outer `Break 0` goal before stating the invariant conjunction, and discharge the delayed natural-index goal after the update continuation.  On the exit edge I will move the same narrow simplification to the outer `Break 1` goal before constructing `hAccumulatorFinal`.  Progress means both continuation `change` commands become definitional and the build reaches only invariant field equalities or post-loop result facts.

The import check passed.  Both concrete control-flow matches now reduce, and the continuing edge reaches the invariant conjunction.  The seven invariant field goals and measure goal still used one broad `all_goals simp`, which exceeded the configured simplifier step limit because it repeatedly normalized the nested generated frames.  The exit edge now reaches the exact post-loop instruction list; its restored value stack is the `forwardSetupFrame` value field, whose named theorem states that it is empty.

I will prove each invariant field separately, using existing getters only for unchanged locals and direct reduction for the updated index and accumulator.  A named updated-index equality will reduce `foldMeasure` without simplifying the complete frame twice.  I will also rewrite the setup frame's value field with `forwardSetupFrame_values`, identify the resulting empty-stack frame with `loopFrame`, and then apply the result-program theorem.

The import check passed.  The separate invariant fields and measure proof all close except the updated-index fact, whose proof still expands `dynamicResultFrame` before observing that `foldUpdateFrame` overwrites local `13`; that unnecessary expansion alone exceeds the simplifier step limit.  Both branches now reach the final public postcondition.  Their residual goals are matches on local `6` of `FixedArrayResult.finishFrame`, so constructing the existential before proving that getter has the wrong expected type.

I will add two artifact-local structural lemmas.  The first proves that `foldUpdateFrame` sets combined local `13` from only the parameter and local lengths, without inspecting its input frame.  The second proves that `finishFrame frame destination 6 root` returns `root` from local `6` under the same frame lengths.  These lemmas isolate list-update normalization from the large weakest-precondition goals; each branch can then reduce its final local-get instruction with the getter equality before introducing the output pointer.

The import check passed, and both structural lemmas compile.  The updated-index application now leaves only equality under `some (.i64 ·)`; simplifying that equality with `hIndexSucc` repeats too much context, so a direct congruence is sufficient.  The final local-get operates on `{finishFrame with values := []}` because `branchPost` clears the stack, which explains why the getter lemma did not rewrite.  In the invalid branch, naming the frame also means its root simplification must unfold `invalidFrame`, while its lower and validity premises follow directly from `hInvalidParams` and `hInvalidLocals`.

I will add the stack-reset form of the finish getter as a corollary using `Locals.get`'s independence from `values`, replace the successor simplification with `congrArg`, and use the named invalid-frame facts for all finish premises.  Progress means both final matches reduce to existential postconditions and no loop proof goals remain.

The import check passed.  The stack-reset getter closes both final local-get matches, and the invalid branch now completes.  The only remaining diagnostics are elaboration of the unqualified `.i64` constructor inside `congrArg` and a closed `1 ≤ func0Def.numParams` side condition in the valid branch's result-stack equality.  I will qualify the constructor as `Value.i64` and include `Function.numParams` in that final structural simplification; no semantic goal remains.

The import check passed after those two closed normalization edits, and `/home/somebody/src/leanexe/tools/leanrun --timeout 60m lake -d . --no-ansi build LeanExeGen.GeneratedR23fa7efc3fb0298b.ArtifactResult` completed all 3,078 targets successfully.  The proof establishes the annotation-matched length dispatch, a genuine input-traversal loop with the fold-prefix accumulator and decreasing measure, the wrapping `UInt64` addition transition, and the singleton or empty represented output.  The remaining Lean messages are linter warnings about redundant simplifier arguments and already-closed tactic alternatives; they do not affect elaboration or the artifact theorem.  I will now run the required final import check and independent rebuild without changing the candidate.
