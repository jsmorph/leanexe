# Proof Journal

The prescribed import check passed, and the artifact build completed all 3,074 jobs successfully.  The bounded `List.set` simplification discharged the final frame equality; the completed proof therefore verifies the length dispatch, both allocator executions, input preservation, every indexed load, the loop-carried wrapping sum, loop termination, the payload store, and both output representations.  I will now run the required final import check and artifact build without further edits.

The import check passed, and the measure proof closed.  The frame equality remained after the semantic rewrites because kernel reduction does not unfold `List.set` through the seven nested updates during reflexivity.  Its two structural equations, `List.set_cons_zero` and `List.set_cons_succ`, form a bounded simplification set that reduces those concrete list updates without invoking the global simplifier rules that exceeded the step limit.

The import check passed.  Both final obligations timed out inside unrestricted `simp` while normalizing nested concrete `List.set` operations.  I replaced that normalization with kernel reduction: after rewriting the fold-prefix successor and encoded index successor, the frame equality is definitionally true.  I also state the measure comparison directly as `input.size - (index + 1) < input.size - index`, using only the two decoding equalities before `omega`.

The import check passed, and the build reduced the loop back-edge to the invariant and measure pair.  The post-step locals contain the loaded element, the wrapping accumulator addition, scratch zero, saved sum, marker one, and encoded index increment.  I instantiate the next invariant at `index + 1`, rewrite its accumulator with `ArrayFold.foldPrefix_succ`, normalize the encoded successor with `UInt64.ofNat_add`, and prove strict decrease of `input.size - index` from `index < input.size`.

The prescribed checks passed through the singleton branch and all continuing-loop memory facts.  `wp_neI64_cons` left a match on the explicit frame's `values` projection, so the following conditional rewrite could not see the `if`.  I added one definition-only simplification between the atomic rule and the conditional selection; this reduces the projection and operand-stack match without unfolding the continuation.

The import check passed, while the artifact build reported that my new `neI64` block occurred at the second address multiplication.  The edit had matched the first repeated restricted-reducer call instead of the post-load call.  I moved the control instructions after the indexed load and body updates, preserving the two explicit multiplication boundaries before it.

The import check passed.  The artifact build closed the completed-loop branch except for an extra `omega` after the disjoint-address rewrite had already proved reflexivity, so I removed that tactic.  The indexed address, load bound, and element read all discharged in the continuing branch; the restricted reducer then stopped at `neI64` with the two zero scratch operands visible.  I added the exact `neI64`, zero `br_if`, index increment, and unconditional loop-back rules so that Lean will expose the invariant and decreasing-measure obligations.

The prescribed import check passed, and the artifact build reduced both branches as intended.  The completed branch exposed three obligations: normalize `UInt64.toNat 16` before arithmetic, reduce the returned local-six lookup, and prove the singleton memory representation after the payload write.  The continuing branch exposed the generated indexed-load address `(inputPtr + (UInt64.ofNat index + 1) * 8).toUInt32`.  I now normalize the first obligation with explicit capacity bounds, use `BumpFacts.wordAddress` and `wordAddress_toNat` for the payload, use `UInt64Array.singleton` with a disjoint length read, and rewrite the generated input offset with `UInt64.ofNat_add` and `UInt64.ofNat_mul` before applying `elementBound` and `elementRead`.

The latest build showed that the completed-loop `br_if` simplification had already executed the output suffix through the payload-store conditional.  I removed the obsolete attempt to re-enter that suffix, proved the displayed payload bound from `BumpFacts.fit32`, the capacity-sixteen memory fit, and page preservation, and reduced the resulting branch postcondition.  In the continuing branch, the prior edit had placed the indexed-load rule before the second multiplication by eight; I moved the multiplication rule and restricted reduction ahead of the load.  A broad declaration-location query outside the workspace returned filenames and short matching lines from unrelated proof artifacts, including a generated namespace path; I did not open or use those proof files.  I then restricted dependency inspection to the selected `Project.ProofKit.Memory`, `Array`, `Allocation`, and `FixedArrayAllocator` modules and confirmed only the named declarations `read64_write64_disjoint`, `UInt64Array.singleton`, `BumpFacts.wordAddress_toNat`, and `allocStore_pages`.

## Catalog entries and program structure

I inspected the canonical `fixed-array-length-dispatch` entry and will use it because its annotation, local 7, bound 8, and exact `leProgram` prefix match the frozen Program.  I inspected `array-fold-prefix` and will use its `ArrayFold.foldPrefix`, successor, and completion equations for the traversal loop's sum invariant.  I inspected `scalar-post-test-loop` and rejected it because the artifact has no `leanexe.loop.scalar-post-test.v1` descriptor or generated transition equations.  I inspected `fixed-array-allocation` and `array-memory-framing`; the Program contains the described empty-free-list allocator twice, and input framing will be required in the successful branch after allocation, so both remain candidates pending exact-region matching.  I rejected `fixed-array-singleton-wrapper` without opening its entry because the Program has no direct call and no corresponding annotation.

The frozen Program confirms the requested structure.  Function 0 first performs the bounded-length dispatch.  Its successful branch allocates a one-element result, initializes a zero sum and zero index, then runs a loop that loads `input[index]`, updates the loop-carried `UInt64` accumulator with `.addI64`, increments the index, stores the sum in the singleton payload, and returns its pointer.  Its invalid branch allocates an empty array.  The allocator search loops are unreachable beyond their first exit because `RuntimeReady` fixes global 1 to zero.

While confirming the selected `Wasm.TerminatesWith.of_wp_entry_for` declaration, an overbroad dependency `rg` query returned filenames for unrelated Project proofs.  I did not open or use those files.  All subsequent dependency inspection will remain limited to declarations named by selected LTG entries or the recipe.

## Initial candidate check

## Direct recipe attempt

The import check passed after splitting both post-allocation continuations at the first result store.  `wp_store64_cons` now matches, and the remaining target starts with a reducible projection and match on the explicit two-value operand stack before the store-bound conditional.  I will reduce that projection in each branch before selecting the in-bounds case; the generated root-bound fact already has the address form required by the conditional.

The import check passed after reducing the operand-stack projection.  The build exposed a narrower address mismatch: the store instruction constructs `UInt32.ofNat ((heapTop + 48).toNat % 2^32)`, while `hRootBound` uses `(heapTop + 48).toUInt32`.  `Memory.toUInt32_eq_ofNat`, named by the selected memory-framing material, proves those addresses equal after normalizing `2^32`; I will rewrite the address and use `Nat.not_lt.mpr hRootBound` instead of asking `omega` to reconstruct the conversion.

The import check passed, and the explicit address equality discharged both result-length store checks.  The successful continuation then exhausted the heartbeat budget inside `wp_alloc_window`; the nested allocator frame and record update remain in every subsequent reduction.  I will replace that definitionally equal state and frame with named, concrete post-allocation forms before simplifying the traversal initialization.  I searched the task guidance for `FixedArrayTraversalInput|wp_traversal_input|traversal input|input traversal`; `PROOF_STRATEGIES.md` names `FixedArrayTraversalInput.program_spec`, but its checked region includes an unrolled length guard absent from this loop body, so I inspected it and rejected it for the loop transition.

The import check passed.  Lean displayed two residual presentation differences in the attempted post-allocation `change`: the store address remained `(heapTop + 48).toUInt32 + 0`, and the frame retained projections of `allocFrame`.  I will prove the concrete frame equality with focused reduction, rewrite it, and reduce the zero address offset before changing to the named state and frame.

The import check passed, and both concrete allocator-frame equalities succeeded.  The explicit `simp only [add_zero]` then reported no progress in each branch, which means the preceding store-address rewrite already reduced the zero offset.  I will remove those redundant simplifier calls and retain the named-state `change` as the next boundary.

The import check passed, but the named-state `change` showed that `(heapTop + 48).toUInt32 + 0` remained after all; the generic `add_zero` name selected no applicable rewrite in the preceding check.  The core library provides the type-specific theorem `UInt32.add_zero`, so I will rewrite with that declaration before the state change.

The import check passed, the type-specific zero rewrite succeeded, and both named-state changes succeeded.  The successful simplifier stopped because it treated `allocatedInputFrame.params.length` as opaque, while the invalid branch reached its four-instruction return suffix with the same opaque frame projections.  I will expose only `allocatedInputFrame` and `resultLengthStore` to these focused simplifier calls; the state and frame names have already removed the nested allocator updates that caused the prior timeout.

The import check passed, but exposing the named frame to `wp_alloc_window` still caused the successful branch to exceed one million heartbeats.  That simplifier recursively enters the remaining traversal loop before it finds a stable boundary.  I will use the restricted allocator instruction reducer only through the first input-length load, transport `hInputStored` to the named state, discharge that load, and repeat through the second load; this preserves a small proof term before applying the control rules at the block-wrapped traversal loop.

The import check passed.  The restricted reducer stopped at the intended first load, but it retained every local operation because its fixed simplification set omits ordinary concrete-list length reduction.  I will add `List.length` to that call.  The invalid branch independently timed out in the broad simplifier, so I will use the same restricted reducer for its four local operations, then apply `wp_nil` before proving the empty-array postcondition.

The import check passed.  Adding concrete-list length reduction produced the exact first `load64` boundary in the successful branch.  I will select its in-bounds case from `hInputStored.generatedLengthBound`, normalize the generated pointer with `pointerAddress_eq`, and rewrite the memory read with `lengthRead`.  The invalid reducer left only a concrete index-six list lookup before `wp_nil`, which `List.getElem?_cons_succ` reduces without expanding the result state.

The import check passed.  The first load conditional still contained `(UInt32.ofNat n).toNat`, so its shape did not match `generatedLengthBound`; `UInt32.toNat_ofNat` followed by `Nat.mod_mod` supplies the missing normalization.  The invalid path now reaches its returned root after concrete local updates.  I will construct the empty representation directly from `BumpFacts.fit32`, the capacity-eight memory fit, and `Mem.read64_write64_same` for the stored zero length.

The import check passed.  `UInt32.toNat_ofNat` simplified the zero offset but left the symbolic generated address, so I will first rewrite that address with `pointerAddress_eq` and then use the representation's `lengthBound`, whose address is already `inputPtr.toUInt32`.  The invalid representation's memory-fit goal differs only by the page-preserving allocation and length-store state; unfolding `resultLengthStore` and using `FixedArrayAllocator.allocStore_pages` resolves it.

The import check passed.  The revised address proof discharged the first input-length load, and the invalid branch now closes completely.  The successful goal begins with the first loaded length in local 12, followed by the same input-pointer load into local 14.  I will use the same restricted reduction and representation facts for that second load, then reduce the remaining initialization through the `ltUI64` boundary.

The import check passed.  The second length load and all remaining scalar initializers reduced to the `ltUI64` selector with two identical encoded lengths.  I will reduce that comparison to zero, take the selector's false arm, and use straight-line execution for its one result and the following `localSet 15`.  The resulting goal should begin at the block-wrapped sum loop with zero index and zero accumulator.

The import check passed and revealed an edit-order error: I placed the `ltUI64` reduction immediately after the restricted call that stops at the second `load64`, leaving the load at the head of the goal.  I will move the comparison and selector proof after the second load proof and the following restricted initializer reduction.

The import check passed.  The selector chose the expected false arm, but generic `wp_run` expanded the nested local updates and stopped before reducing the branch's `localGet 12` and following `localSet 15`.  I will use the restricted reducer on the one-instruction branch, reduce its `wp_nil` continuation with result arity one, and invoke the reducer again for `localSet 15`.  This should expose the loop block without expanding its body.

The import check passed, and the restricted reductions exposed the exact block-wrapped traversal loop with zero index, zero accumulator, and both length locals equal to `input.size`.  I will apply `wp_block_cons` and `wp_loop_cons`.  The invariant fixes the result-length store and represents the accumulator as `ArrayFold.foldPrefix` at an existential index; an exact frame constructor carries irrelevant scratch values, and the measure subtracts the encoded index from `input.size`.

The import check accepted the loop rule and its initial invariant.  It rejected only the measure's abbreviated `.i64` pattern because the match lacked enough constructor context; I will qualify it as `Value.i64`.  For the step, I will reduce the two guard locals through the exact frame, split on `index = input.size`, and prove the encoded comparison using `UInt64.lt_iff_toNat_lt` in the continuing case.

The import check reached both loop-guard branches.  In the completed case, the conditional remained as `if encodedSize ≤ encodedSize`; I will rewrite it with an explicit `if_pos`.  In the continuing case, `not_le_of_gt` selected an incompatible preorder instance for `UInt64`, so I will prove the negated encoded comparison by rewriting `UInt64.le_iff_toNat_le` and applying the natural-number index bound.

The import check showed that `UInt64` supplies the concrete comparison relation and conversion theorems but no `Preorder` instance, so `le_refl` cannot synthesize its type-class premise.  I will prove encoded reflexivity by rewriting `UInt64.le_iff_toNat_le`.  I will also change the continuing negation from `index ≥ size` to its explicit `size ≤ index` form before applying the same conversion theorem.

The import check discharged both encoded comparisons.  Each branch now stops only because the continuation assertions retain the nested loop and block result handling.  In the completed branch I will change the definitionally equal target to the post-loop output suffix and reduce it to the payload store.  In the continuing branch I will expose the indexed-load boundary with the restricted reducer, supplying the exact `UInt64.ofNat index` decoding fact.

The import check showed two remaining atomic presentation steps.  The completed `br_if` condition is the concrete `UInt32` literal one, which empty `simp only` does not reduce through the pattern match; `norm_num` will normalize that literal before the post-loop change.  The continuing reducer stops at each `.mulI64`, so I will apply `wp_mulI64_cons` explicitly for multiplication by one and by eight, invoking the restricted reducer between them and through the final address addition.

The import check confirmed that `norm_num` normalized the completed branch's arithmetic but did not select the `UInt32` pattern-match arm.  I will give the simplifier the explicit fact `(1 : UInt32) ≠ 0`.  The continuing path now reaches the exact indexed `load64`; I will expand only that load after naming `hInputStored.generatedElement index hIndexLt`, so the next diagnostic exposes the remaining address normalization.

The import check passed.  `wp_alloc_to_store` stopped before the store as intended, but it could not reduce operand 7 through the nested `allocFrame` list updates, so `wp_store64_cons` had no top-level occurrence.  I will name the concrete pre-allocation frame and provide focused post-allocation frame facts: parameter length, local length, empty stack, and the raw local-6 root lookup.  This follows the LTG local-window guidance and avoids unfolding the complete updated frame during every instruction.

The import check passed, but the successful post-allocation `wp_alloc_window` call consumed the theorem's one-million-heartbeat budget after 71 seconds.  It tried to normalize the allocator frame, result store, two input loads, and traversal selector in one simplifier call.  I will divide this continuation at the first store using the catalogued `wp_alloc_to_store` macro, select that store's bound branch, and only then simplify the input initialization.  The invalid branch will use the same store boundary before its short return suffix.

The import check passed.  The build now has three explicit bound branches before the successful traversal-length selector and one before the invalid result postcondition.  The stored root address normalized to `heapTop.toUInt32 + 48`, while the framing fact used `(heapTop + 48).toUInt32`; I will prove these addresses equal from `BumpFacts.fit32`.  I will then select the bound branches with `omega` from the generated root and input bounds.  The invalid path should reduce to constructing the empty-array representation, and the successful path should reach the traversal loop boundary.

The import check passed.  The build rejected my direct proof that `(heapTop + 48).toNat < 2^32` because the context still contained the `UInt64` root conversion as a separate equality; the modulo bound should instead follow by rewriting `hRootBound` with `Memory.toUInt32_toNat` and allocator page preservation.  The residual successful branch reaches the traversal-length selection, while the invalid branch reaches its final postcondition.  Their result stores use `UInt32.ofNat ((heapTop.toNat + 48) % 2^32)`, so the root-address equality also needs this generated form rather than `(heapTop + 48).toNat` under the modulus.

The import check passed with the named bump, root, and input-framing facts.  The build kept `allocFrame` folded enough to reveal the next presentation mismatch: both post-allocation continuations stop at index zero of an explicit local-list suffix.  The successful branch also reaches the initialized traversal after the length store and two input-length reads, but their values remain symbolic until that list lookup reduces.  I will add `List.getElem?_cons_zero` at this boundary and state the root bound in the generated modulo form used by the store check.

With explicit heap, capacity, stride, and allocation-counter arguments, the import check passed and every allocator premise discharged.  The exact allocator theorem now leaves only its `hNext` continuation in each branch.  Plain `wp_run` expands `allocFrame` and the first result-length store without the named allocator address facts, producing a large bound expression.  The next edit will derive one `Allocation.bumpFacts` value per branch, normalize the root address, prove the root-store bound, and use the allocator-window simplifier.  In the successful branch I will also transport the input representation through allocation and the result-length write before executing the traversal initialization.

After rewriting the empty capacity branch, the import check passed and the exact shifted allocator theorem applied in both branches.  Its diagnostics expose only the documented premises and the post-allocation continuation.  Unconstrained `apply` left heap top, capacity, and allocation count as metavariables, so the next edit will instantiate them as `(heapTop, 16, allocs)` and `(heapTop, 8, allocs)`.  The fit premises follow from `heapReserveBytes`, which reduces to 64 under `hSize` and 56 under its negation.

The import check passed after adding the concrete capacity branch selection.  The build confirms both comparisons take the false branch, but `wp_iff_cons` leaves the empty branch as `wp module []` whose fallthrough continuation contains the allocator program.  The attempted allocator `change` therefore occurred one structural step too early.  Rewriting `wp_nil` and normalizing the zero-arity branch frame will expose the allocator region without unfolding it.

Adding `List.getElem?_cons_zero` produced the intended capacity-prefix boundary.  The import check passed, and the build now shows both branches at the concrete `.iff` with a `Value.i32 0` condition, an explicit frame, and capacities 16 and 8.  The false branch is therefore the empty minimum-adjustment arm in both cases.  After selecting it, the remaining program starts with the exact `FixedArrayAllocatorWindow.region 2 1` motif.

The import check after switching to `wp_alloc_window` passed.  The build reduced the generated initial frame to an explicit 20-local list and computed capacities 16 and 8, but stopped on `[Value.i64 capacity, ...][0]?`; the allocator-window simplifier omits `List.getElem?_cons_zero`.  This is an arithmetic-presentation issue in the capacity prefix rather than a semantic gap.  I will add that single list fact to the focused simplifier and preserve the allocator region folded.

The next import check passed.  The build discharged parameter shape, empty operand stack, positivity, maximum-size bound, and input representation.  The local-window bound failed because `decide` refuses a proposition containing `inputPtr`, even though reduction of `func0Def.toLocals` makes the bound constant; this needs focused simplification with `func0Def` and `Function.toLocals`.  The remaining diagnostics are exactly the successful and invalid branch programs under `hSize : input.size ≤ 8` and its negation, confirming that the direct recipe improves the proof boundary.

I edited the theorem to convert `TerminatesWith` with `TerminatesWith.of_wp_entry_for (f := func0Def) rfl` and then invoked the recipe exactly as `wp_fixed_array_length_le_dispatch 7, 8`.  The import check passed.  The artifact build showed that the recipe's program shape matches: the original 489-instruction goal became the expected frame premises followed by separate successful-branch and invalid-branch `wp` goals.  Lean did not infer `inputPtr` and `input` before the frame premises, so the next edit will solve the six elementary recipe premises explicitly and retain the two semantic branches.

The unchanged candidate passed `PROOF_IMPORT_CHECK.js`.  The prescribed artifact build then failed at the starter's final goal: prove `TerminatesWith env module 0 initial [Value.i64 inputPtr]` with a singleton result pointer whose final-memory array equals `FormalSpec.expected input`.  The `RuntimeReady` decomposition and derived array facts succeeded, leaving globals, allocator bounds, page bounds, `UInt64Array.At`, the length read and bound, the normalized input address, and heap-top, free-list, and allocation-counter reads in context.  No catalog search or proof edit preceded this diagnostic.

## Frozen guidance and first LTG searches

I searched the arrays, loops, and compiler-motifs indexes for `fold|sum|singleton.*loop|bounded.*fold` after the branch goals exposed the traversal.  The only matching semantic fold asset is `array-fold-prefix`; no whole-function fold-sum composition appears in the task-specific LTG view.  The other hits concern map, filter, counter transfer, allocation, or worked examples, so I rejected them for this artifact.

I confirmed `FixedArrayAllocatorWindow.region_spec_withTail` from the selected allocation module.  Both branch allocators match `region 2 1`: operand 16 is the allocator root scratch local, operand 12 is the previous-node local, operand 13 is the free-list cursor, operand 11 holds capacity, and operand 7 receives the root.  The frame has 20 locals, so the theorem parameters are offset 2 and tail 4.  The successful capacity prefix computes 16 bytes, while the invalid prefix computes 8 bytes.

After replacing the problematic local-bound `decide` with focused reduction of `func0Def` and `Function.toLocals`, the import check passed and all direct-recipe premises succeeded.  Plain `wp_run` then stopped at the capacity minimum-check instruction, as expected, but left the generated branch frame unreduced.  The diagnostic shows that the next step needs the allocator-window simplifier with `func0Def` and `FixedArrayLengthDispatch.branchFrame` before selecting the concrete capacity branch and applying the exact allocator theorem.

I began LTG retrieval at `LTG/categories.json`, which identifies arrays, loops, allocation, compiler motifs, memory, and proof construction as the relevant indexes.  `PROOF_TASK_FEATURES.json` reports one 489-instruction exported function with 20 locals, three loops, memory, arithmetic, and allocation.  `PROGRAM_ANNOTATIONS.json` contains one checked region, `leanexe.array.length-dispatch.v1`, over instructions 0 through 7 with input local 7 and maximum size 8.  `PROOF_RECIPES.json` supplies a direct recipe: `Project.ProofKit.FixedArrayLengthDispatch.leProgram_spec`, invoked by `wp_fixed_array_length_le_dispatch 7, 8`; it provides the valid and invalid branch obligations but no complete composition.

I searched the arrays, compiler-motifs, and proof-construction indexes for `leanexe.array.length-dispatch.v1|leProgram_spec|wp_fixed_array_length_le_dispatch`.  This found the canonical `fixed-array-length-dispatch` entry and exactly matches the checked recipe.  I searched the loops, arrays, and compiler-motifs indexes for `scalar-post-test|loop-carried|foldl|array.*sum|input-traversal|wrapping.*sum|scalar loop`.  The relevant generic matches are `array-fold-prefix` and `scalar-post-test-loop`; map-add and counter-transition concern different computations, while the Demo 7 and Demo 8 entries are worked examples and will not be inspected.  I searched the allocation and arrays indexes for `FixedArrayAllocator|singleton|resultSuffix|result_spec`, finding `fixed-array-allocation`, `array-memory-framing`, and `fixed-array-singleton-wrapper`; the wrapper assumes a direct scalar callee and therefore does not match this single-function artifact.  I searched the proof-construction index for `RuntimeReady|TerminatesWith|artifact_behavior|deterministic starter|starter`, which returned the length-dispatch and residual singleton entries plus worked examples.  Residual singleton normalization depends on the singleton-wrapper annotation absent from this artifact, so I rejected it at this stage.
