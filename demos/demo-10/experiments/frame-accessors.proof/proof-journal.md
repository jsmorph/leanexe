# Proof Journal

## Initial check

I ran the untouched candidate with the required resource-limited `ArtifactResult` build.  The build failed in `Behavior.lean` at line 23 with one goal after the supplied `RuntimeReady` decomposition and array facts.  The goal is `TerminatesWith env module 0 initial [Value.i64 inputPtr]` with a postcondition requiring one output pointer whose final-memory representation equals `FormalSpec.expected input`; there are no preceding residual goals.

## LTG retrieval

I began retrieval at `LTG/categories.json`, whose relevant categories are arrays, compiler motifs, loops, allocation, memory, and proof construction.  My first query was `leanexe\.array\.length-dispatch\.v1|leProgram_spec|wp_fixed_array_length_le_dispatch_from` over the arrays, compiler-motifs, and proof-construction indexes; it identified `fixed-array-length-dispatch` and `fixed-array-capacity`.  My second query was `leanexe\.array\.fold\.v1|forwardSetupProgram_spec|singletonResultProgram_spec|foldPrefix` over the arrays, loops, and compiler-motifs indexes; it identified `array-fold-prefix`, `fixed-array-fold-body`, `fixed-array-fold-structure`, `fixed-array-result`, `fixed-array-traversal-input`, and `compact-loop-suffix-boundary`.

My third query was `RuntimeReady|TerminatesWith|FixedArrayAllocator|singleton` over the allocation, arrays, and proof-construction indexes.  It added `fixed-array-allocation` and returned singleton-wrapper entries whose annotations require a direct scalar call and a singleton-sized input, unlike this artifact's bounded variable-length fold.  I rejected `fixed-array-singleton-wrapper`, `residual-specification-normalization`, and the Demo 7 and Demo 8 worked-example entries from inspection because their structural features do not match the current program or annotation kinds.

I inspected the canonical metadata and README files for `fixed-array-length-dispatch`, `fixed-array-capacity`, `fixed-array-fold-structure`, `fixed-array-fold-body`, `array-fold-prefix`, `fixed-array-traversal-input`, `fixed-array-result`, `fixed-array-allocation`, and `compact-loop-suffix-boundary`.  The first eight provide declarations named by the two checked recipes or the allocator and result continuations those recipes expose; I will use them for entry dispatch, branch capacity, allocation, fold setup, the loop step, and represented results.  I retain `compact-loop-suffix-boundary` as conditional guidance because the annotation supplies `singletonResultProgram_spec`, but I will reject that extra declaration split unless a build reaches the documented elaboration limit.

The checked recipe file has no whole-function composition.  Its exact direct entry recipe is `wp_fixed_array_length_le_dispatch_from hArray at 7, 8`, after `Wasm.TerminatesWith.of_wp_entry_for`, and its fold recipe names exact setup, continuing, step, and singleton-result regions rather than a complete tactic invocation.  I will attempt the direct dispatch recipe first and count progress only if it exposes the two bounded-length branches without expanding the 489-instruction body.

## Direct recipe attempt

I will convert the public execution goal with `Wasm.TerminatesWith.of_wp_entry_for (f := func0Def) rfl`, introduce the generated initial frame, rewrite `func0` with `function_0_length_dispatch_0_function_eq`, and invoke the exact bounded-length tactic.  This change uses the recipe as a complete entry step before I reduce any supporting theorem internally.  The desired residual boundary is one valid-branch goal under `input.size ≤ 8` and one invalid-branch goal under `¬ input.size ≤ 8`, each retaining the named suffix continuation.

The import check accepted the direct-recipe edit.  The Lean build failed at `intro frame` because `of_wp_entry_for` already instantiated the local frame as `func0Def.toLocals (List.take func0Def.numParams [Value.i64 inputPtr]).reverse`; its resulting goal is the desired body-level `wp`.  I will remove the redundant introduction, keep the generated frame implicit in `change`, and retry the exact function equality and dispatch tactic.

The second import check passed, and the Lean build confirmed that the exact dispatch recipe matches.  It leaves five structural premises for the generated frame and maximum value, followed by the expected valid and invalid branch goals under `input.size ≤ 8` and `¬ input.size ≤ 8`; the named one-instruction suffix remains folded in `FixedArrayEqNode.branchPost`.  The recipe improved the proof boundary, so I will use it and discharge its structural premises by reducing `func0Def`, `Function.toLocals`, `Function.numParams`, and `ValueType.zero` before composing each branch.

I will next apply the checked constant-capacity theorem at both branch heads: length one and capacity sixteen in the valid branch, and length zero and capacity eight in the invalid branch.  The artifact definitions make each branch prefix definitionally equal to `FixedArrayCapacity.constantProgram`, while `constantProgram_spec` should leave the exact allocator suffix and its named capacity frame.  Progress means that both branches reach their allocator regions with concrete capacity-local facts and no remaining dispatch or capacity instructions.

The import check passed.  The Lean build accepted both constant-capacity program matches and discharged each capacity destination's validity, leaving only the lower-bound fact for combined local 11 and the allocator suffix in each branch.  The lower-bound diagnostic still contains the folded branch frame, so I will prove it with the same focused generated-frame reduction used at dispatch; the `hNext` programs now begin with `FixedArrayAllocatorWindow.region 2 1` definitionally.

I now use the inspected `fixed-array-allocation` entry.  I will instantiate `region_spec_withTail` with offset 2, tail 4, stride one, and capacities sixteen or eight, deriving the memory premise from `heapReserveBytes` under the branch hypothesis.  The intended residual goals begin after `FixedArrayAllocator.allocStore`, with root `heapTop + 48` in combined local 7, so the allocation search, bump arithmetic, header stores, and allocation counter update will no longer appear in the artifact proof.

The import check passed, and the Lean build accepted both exact allocator-region applications after closed `UInt64` capacity calculations were discharged with `native_decide`.  In the valid branch it also accepted input preservation across the allocator, the singleton length store, input preservation across that store, and `forwardSetupProgram_spec`; the only valid residual goal starts at the block-wrapped annotated fold and its singleton-result suffix.  The invalid branch now starts at its four-instruction zero-length store followed by the four-instruction root transfer, so no allocation instructions remain in either branch.

I will use `array-fold-prefix`, `fixed-array-fold-body`, `fixed-array-traversal-input`, and `fixed-array-fold-structure` for the remaining valid goal.  I will add small task-local definitions for the generated loop frame, an invariant carrying the consumed index and `ArrayFold.foldPrefix`, and a measure read from combined local 13; the loop store remains fixed until the result suffix.  Progress means `wp_block_loop` leaves one equal-index exit handled by `continuingProgram_exit_spec` and one strict-index step handled by `continuingGuardedProgram_spec`, with the latter rebuilding the invariant at `index + 1` and proving a smaller measure.

The import check passed, and the Lean build accepted the invariant and measure types at `Wasm.wp_loop_cons`.  Its two residual premises are the expected initialization fact for `forwardSetupFrame` and a body theorem from any state satisfying `productInv`; the loop rule introduces no additional obligations.  I will prove initialization by expanding the generated frame once, then split the step witness on `index = input.size`: the equal case exits through `Break 1`, while the strict case runs the checked continuing fold body.

The next import check passed, and the build proved invariant initialization and all three local-frame premises for the equal-index traversal exit.  It left the loop rule's structured `Break 1` continuation, which definitionally selects the post-loop singleton suffix, and the strict-index body goal at the exact concatenation of the annotated continuing and scalar-step programs.  I will normalize only the zero-arity block and loop value restoration at the exit, refold the residual program as `function_0_array_fold_0_singleton_result_program`, and apply the complete singleton-result theorem before addressing the back edge.

The invalid branch composition also passed its import check and Lean elaboration.  It now uses `emptyStore_at`, `lengthStore_spec`, and `finishProgram_spec`, then executes the named one-instruction function suffix and reduces `FormalSpec.expected` under `¬ input.size ≤ 8`.  No invalid-branch goal remains.

The exit-normalization edit passed the import check and refolded the residual goal exactly as `function_0_array_fold_0_singleton_result_program`.  The next build accepted the output bounds, singleton representation, result-local getter facts, payload store, and result-frame validity premises supplied to `singletonResultProgram_spec`.  Three lower-bound premises still need the generated frame's parameter length reduced, and the consequence proof must rewrite the return-local getter after replacing the result frame's value stack with `[]`; these are local frame-normalization obligations rather than suffix semantics.

The next import check passed.  The build accepted the three corrected parameter lower bounds and every semantic premise of `singletonResultProgram_spec`; its consequence also reached the public result after executing local 6, and the remaining `simp` command was redundant because `wp_run` had already used the preserved getter.  I will remove that command, then instantiate `continuingGuardedProgram_spec` with the compiler-generated loaded state, multiplied accumulator state, false done condition, and incremented-index state.

The import check passed.  The Lean build exposed two precise adjustments: `continuingGuardedProgram_spec` consumes `ScalarTransition.State`, so the generated `U64State` values need `.toState`, and the generated `continuing_item_valid` theorem should supply the item-local premise directly.  The suffix consequence still contains an unfolded getter match because `wp_run` expanded `Locals.get`; I will replace that broad step with `wp_localGet_cons` and rewrite with the preserved getter before expansion.

The next import check passed, and the build accepted the converted scalar-state arguments.  `apply` postponed the dependent item-validity metavariable until after the callbacks, shifting the positional bullets, so I will pass `hItem` explicitly and restore the documented premise order.  The return-local match also reduces `Locals.get` before rewriting; unfolding the same definition in `hReturn` will provide an equality for that exact discriminant.

The import check passed after the explicit item-validity edit.  The build now leaves the return-local discriminant because only the hypothesis has unfolded `Locals.get`, and it shows that `apply` also postponed `hIndex`, which shifts the loaded-frame, step-program, body, and condition proofs by one premise.  I will unfold `Locals.get` in the suffix goal, pass `hInput` and `hIndex` explicitly, and construct the false-condition continuation from the generated increment transition; progress means the loop invariant reappears at `index + 1` with a smaller `input.size - index` measure.

The import check passed, and the build accepted the suffix getter, explicit represented input and index premises, false-condition callback shape, reconstructed invariant, and measure reduction after frame rewriting.  Three focused failures remain: the loaded-frame proof must unfold the generated continuing-frame abbreviation on its right side, the broad simplification of the generated increment evaluator exceeds the simplifier step limit, and `omega` does not reduce the opaque `UInt64.size` constant while proving the successor bound.  I will use definitional unfolding for the loaded frame, rewrite directly with the generated increment evaluator, and derive `index + 1 < UInt64.size` by transitivity through `input.size`.

The final import check accepted every import, and the required `ArtifactResult` build completed successfully with no residual goal.  The accepted proof uses the shared length-dispatch, capacity, allocator-window, fold-setup, traversal-input, fold-body, scalar-transition, singleton-result, array-prefix, and represented-array declarations recorded above.  The loop invariant carries `ArrayFold.foldPrefix` in generated local 1, while the generated continuing transition increments local 13 and proves the measure decreases.
