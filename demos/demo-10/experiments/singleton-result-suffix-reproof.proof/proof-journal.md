# Proof Journal

## 2026-08-10: Initial Lean check

The required unchanged build failed in `Behavior.lean` at line 22 after the deterministic `RuntimeReady` decomposition.  The context contains the globals, input representation, length read, address bounds, heap bounds, and page bound, while the sole residual goal is `TerminatesWith env module 0 initial [Value.i64 inputPtr]` with an existential output pointer and `FormalSpec.expected input` represented in final memory.  The starter therefore exposes the intended execution proof boundary; it lacks a theorem or tactic that composes the annotated length dispatch, traversal loop, and result allocation into this postcondition.

## 2026-08-10: LTG retrieval and recipe selection

I began at `LTG/categories.json`, then searched the arrays, loops, compiler-motifs, proof-construction, and allocation indexes for `leanexe.array.fold.v1`, `foldPrefix`, `singletonResultProgram_spec`, `forwardSetupProgram_spec`, `leanexe.array.length-dispatch.v1`, `leProgram_spec`, `wp_fixed_array_length_le_dispatch`, `RuntimeReady`, `TerminatesWith`, result allocation, singleton results, complete compositions, wrappers, full-array traversal, and allocator support.  The searches selected `fixed-array-length-dispatch`, `fixed-array-fold-structure`, `array-fold-prefix`, `fixed-array-traversal-input`, `guarded-back-edge`, `fixed-array-capacity`, `fixed-array-allocation`, and `compact-loop-suffix-boundary` for inspection.  I rejected the map, filter, search, and singleton-callee wrapper entries from their index metadata because their annotation kinds and program structures do not match this in-function array fold, and I rejected the demo entries because they concern different compiler annotations and artifacts.

`PROOF_RECIPES.json` contains no complete composition.  Its first exact direct recipe is the bounded length-dispatch tactic `wp_fixed_array_length_le_dispatch_from hArray at 7, 8`, and its second direct recipe identifies the checked array-fold interval and `Wasm.wp_loop_cons`, with setup, traversal, guarded-back-edge, prefix-algebra, and singleton-result declarations supporting the loop proof.  I will inspect only the canonical `entry.json` and `README.md` files for the selected LTG entries, then attempt the length-dispatch recipe at the whole-function boundary before expanding either branch.

The inspected entries confirm that the dispatch recipe must follow `Wasm.TerminatesWith.of_wp_entry_for (f := func0Def) rfl`, while the generated function equality keeps the decoded body folded as the named dispatch plus its one-instruction suffix.  I will add that entry conversion, rewrite by `function_0_length_dispatch_0_function_eq`, and invoke the prescribed `_from` tactic without reducing either branch.  Progress means that Lean replaces the public termination goal with separate valid and invalid branch `wp` obligations; a failure before those obligations would identify an entry-frame or exact-program mismatch in the recipe metadata.

The import check succeeded, and the first recipe attempt reached the store-specific `wp` goal.  Rewriting by `function_0_length_dispatch_0_function_eq` then failed because the target retained the projection `func0Def.body` rather than the definitionally equal name `func0`; no branch or instruction obligation had yet been generated.  The selected LTG entry explains this possible folded-body boundary but does not give the exact `change` statement, so I will add `change Wasm.wp _ func0 _ _ _ _` before the checked function rewrite and retain the direct recipe unchanged.

The second import check succeeded, and the direct length-dispatch recipe then produced the expected branch boundary.  Five residual premises describe the initial generated frame (`params`, empty values, positive and valid local 7, and maximum 8 below `UInt64.size`), followed by a valid-branch `wp` under `input.size ≤ 8` and an invalid-branch `wp` under its negation.  This confirms use of the `fixed-array-length-dispatch` entry; the remaining missing pieces are exact capacity, allocation, valid fold-loop, invalid empty-result, and final-suffix compositions.

I will next normalize only the five structural premises from `func0Def`, then use `function_0_length_dispatch_0_valid_capacity_eq` and `function_0_length_dispatch_0_invalid_capacity_eq` with `FixedArrayCapacity.constantProgram_spec`.  The residual goal that counts as progress is a pair of branch goals starting at named post-capacity frames, before their allocator regions.  I will not reduce the allocator or fold while testing these exact capacity boundaries.

The import check succeeded, all five dispatch-frame premises closed, and both `FixedArrayCapacity.constantProgram_spec` applications matched their exact branch prefixes.  Each branch now has three reducible capacity-frame premises and one continuation beginning at the allocator initialization, with normalized capacities `normalizedCapacity 1 1` and `normalizedCapacity 0 1`.  The direct capacity recipes therefore succeeded; they leave the expected allocation boundary rather than worsening the proof state.

The allocator instruction operands match `FixedArrayAllocatorWindow.region 2 1`: capacity operand 11, allocator result operand 7, scratch operands 12 through 16, twenty internal locals, and four unused trailing locals.  I will import the LTG-selected window module, close the capacity structural premises, refold each continuation as `region 2 1 ++ rest`, and apply `region_spec_withTail` with offset 2 and tail 4.  Progress means that the valid continuation begins after allocation at the length store and fold setup, while the invalid continuation begins at the empty-array length store; allocator mismatch or heap-fit arithmetic would remain as explicit premises.

The import check succeeded, and both shifted-window allocator theorems matched the decoded branches.  All allocator premises closed except two concrete `UInt64` capacity normalizations per branch: `normalizedCapacity 1 1 = 16` or `normalizedCapacity 0 1 = 8`, and `8 ≤ capacity.toNat`; `norm_num` reduced these to decidable closed expressions but did not finish the machine division.  The remaining semantic continuations now begin after allocation exactly as expected, so I will close the concrete expressions with `decide` and proceed from the established allocation stores and frames.

The next import check succeeded, and `decide` closed all four machine-capacity facts.  Only two goals remain: the valid post-allocation continuation beginning with a length-one store followed by the annotated fold, and the invalid post-allocation continuation consisting of a length-zero store, root transfer, and the common return suffix.  No allocator, capacity, entry, or dispatch premise remains.

I will close the invalid continuation first with `FixedArrayResult.lengthStore_spec`, `finishProgram_spec`, and `emptyStore_at`, using `Allocation.bumpFacts` for the output-root address and the branch-specific output bounds from `RuntimeReady`.  The residual goal that counts as progress is the valid length store and fold alone.  If the common suffix does not reduce from `FixedArrayEqNode.branchPost`, I will search LTG for that residual continuation shape before expanding it manually.

The import check succeeded, and the invalid branch refolded as the exact length-store and finish programs.  `lengthStore_spec` closed its store bound and reached `finishProgram_spec`; Lean now asks only for the allocated-frame root and index facts plus the post-finish common suffix.  The allocation frame has one parameter, twenty locals, an empty value stack, and root `heapTop + 48` at operand 7, so I will name those facts once, use `emptyStore_at` for the written result, and reduce the one-instruction suffix with the established return-local getter.

The import check succeeded, all invalid-branch frame and suffix obligations closed, and the only invalid-branch diagnostic is a representation-name mismatch.  The checked result theorem produces `Project.ProofKit.UInt64Array.At`, while the public postcondition retains the definitionally equal generated name `FormalSpec.UInt64ArrayAt`; the payload facts and expected empty array already match.  I will change the local result hypothesis to the generated predicate before the final `simpa`, which should leave the valid fold as the sole residual goal.

The import check succeeded after the predicate conversion, and the invalid branch now has no residual goal.  The sole goal is the valid continuation after allocation: length-one store, exact forward fold setup, block-wrapped traversal loop, singleton payload store, root transfer, and common suffix.  The `fixed-array-allocation`, `fixed-array-capacity`, and `fixed-array-length-dispatch` entries are now fully used, while `fixed-array-singleton-wrapper` remains rejected because this artifact performs its scalar fold inside the wrapper and has no direct call.

Before applying the loop rule, I will add a named public assertion and a checked suffix theorem that composes `FixedArrayFold.singletonResultProgram_spec` with the common one-instruction return suffix.  This follows the inspected `compact-loop-suffix-boundary` entry and prevents the loop theorem from retaining allocator and public-postcondition reduction inside every step.  Progress means that the valid branch reaches a loop goal whose exit needs only the compact suffix theorem and whose repeat edge needs only the fold invariant and decreasing measure.

The import check succeeded, and the suffix theorem reduced to two presentation issues before reaching any singleton-result premise.  `branchPost` clears the operand stack before the common suffix, so the return-local getter needs an explicit proof that `Locals.get` ignores the `values` field; the combined refolding of `func0Def.body` and the named public assertion also failed as one `change`.  I will derive the getter across the record update and split the main refolding into separate function-body and postcondition changes, preserving the compact theorem boundary.

The next import check succeeded, and `productSuffix_spec` now checks with no residual premise.  The function-body refolding also succeeds, but Lean does not treat the anonymous entry assertion and `publicPost` as definitionally equal, despite their constructor branches having the same propositions.  I will add a small extensional equality proved by cases on `Continuation` and rewrite the entry assertion with it; this is a representation boundary, not a semantic or artifact mismatch.

The extensional equality checks, but the uninstantiated rewrite did not find the anonymous assertion under `wp`.  Lean's diagnostic prints the theorem's left side and the target assertion with the same branch propositions, leaving only the theorem parameters as metavariables.  I will instantiate `inputPtr` and `FormalSpec.expected input` explicitly in the rewrite before changing the assertion design.

Explicit theorem arguments did not change the rewrite failure, so rewriting under `wp` is the wrong adapter for this assertion presentation.  The suffix theorem remains checked, and `Wasm.wp.conseq` provides the intended pointwise assertion boundary without requiring syntactic equality.  I will replace the unused extensional equality with a consequence proof by cases on the continuation, then continue the exact dispatch composition under `publicPost`.

The import check succeeded, the pointwise consequence adapter closed, and the entire proof again has one valid post-allocation goal under the named public assertion.  I will now define the fold invariant over the compiler-generated scalar frame: store equality, an index bounded by `input.size`, accumulator equality with `ArrayFold.foldPrefix`, fixed input, length, stop, root locals, and existential scratch locals.  The measure reads operand 13 from the frame and subtracts its natural value from `input.size`.

The next construction will apply `lengthStore_spec`, preserve the input across allocation and the disjoint length store, apply the exact `forwardSetupProgram_spec`, and invoke `wp_block_cons` with `wp_loop_cons`.  Initialization should establish index zero and accumulator one; the intended residual is one loop-step goal split between `index = input.size` for `productSuffix_spec` and `index < input.size` for the generated traversal and guarded-back-edge theorems.  A failure before that split would identify a setup-frame or invariant-presentation mismatch.

The import check succeeded and reached the exact `wp_loop_cons` step goal, so the length store, input preservation, setup theorem, block rule, and loop rule all match.  One earlier store-bound premise used the sixteen-byte allocation fit where the length store asks for eight bytes and needs an `omega` weakening.  Invariant initialization and the large loop-step assertion each exhausted the one-million-heartbeat limit during weak-head reduction, with the step diagnostic showing the expected Break 0 invariant-and-measure obligation and Break 1 singleton suffix.

The timeout boundary requires a smaller declaration before another check.  I will add a checked exact equality from the post-allocation setup frame to the generated scalar frame, then use that equality in invariant initialization instead of reducing the nested frame under `wp_loop_cons`; I will also weaken the length-store bound explicitly.  After that check, I will move the traversal and guarded-back-edge proof into a continuation-generic loop-body theorem if the step remains the only expensive boundary; LTG describes the needed declarations but lacks a complete fold-body composition theorem.

The import check succeeded, `productFoldFrame_eq` checked, the store bound closed, and the previous heartbeat failures disappeared.  Initialization now differs only by the unreduced identities `foldPrefix input step 1 0 = 1` and `UInt64.ofNat 0 = 0`, while the loop step remains the sole substantive goal.  I searched the loop, proof-construction, and compiler-motif indexes for `guardedBackEdgeProgram_spec`, `continuingProgram_exit_spec`, Break 0, decreasing measure, loop body, and arbitrary postcondition; the results confirm the already inspected `fixed-array-traversal-input` and `guarded-back-edge` entries, and no stronger complete composition appears.

I will close initialization with the prefix definition and extract a continuation-generic `productLoopBody_spec`.  That theorem will destruct `productInv`, use `continuingProgram_exit_spec` when the index equals the input size, use the generated `function_0_array_fold_0_continuing_spec` otherwise, and apply `guardedBackEdgeProgram_spec` with the compiler-derived body, condition, and index-update equations.  Its callbacks will expose only Break 1 completion and Break 0 invariant-and-measure facts to the enclosing loop rule.

The import check succeeded after adding `productLoadedFrame_eq`, `productMeasure_continuing_frame`, and the continuation-generic loop-body construction.  The build reached the new helper without any diagnostic against the generated traversal or scalar-transition applications, but destructing the store equality with an `rfl` pattern removed the name `foldSt` at four later uses.  The public theorem still reports the two known unfinished obligations: the prefix identities at initialization and the loop step, because I have not yet invoked the helper there.

I will preserve `foldSt` by naming the store and frame equalities and substituting `st` and `frame` explicitly.  I will close initialization by simplifying `foldPrefix` at zero, then invoke `productLoopBody_spec` from the loop step.  Its exit callback will identify the invariant index with `input.size`, rewrite `foldPrefix_size`, construct the singleton result representation from the allocator bounds, and call the checked suffix theorem; its repeat callback should reduce to the invariant-and-measure pair required by `wp_loop_cons`.

The import check succeeded, and the build now reports two presentation residuals.  The loop helper reaches `guardedBackEdgeProgram_spec`, but its conclusion spells the named guarded program with `++ []`, while the goal retains the generated `function_0_array_fold_0_step_program`; the generated program definition must be rewritten before applying the theorem.  The public exit callback, singleton representation, suffix theorem, initialization, generated transition equations, and measure proof produced no diagnostic, while the repeat callback needs an explicit equality between `nextFrame` and the loop rule's reconstruction of its parameter and local fields with an empty value stack.

I will rewrite the named step program to the guarded-back-edge program with an empty remainder, then apply the exact scalar theorem.  For the repeat callback, `productInv_values` proves that `nextFrame.values = []`, so cases on the frame establish the required reconstruction equality.  These changes leave the semantic invariant and accumulator equations unchanged.

The import check succeeded, and rewriting the step program allowed `guardedBackEdgeProgram_spec` to discharge its body, condition, and index-update equations.  The two remaining loop-helper diagnostics concern the presentation of `afterContinue.toState.toLocals []`: the invariant proof needs one structural equality that first converts `UInt64.ofNat index + 1` to `UInt64.ofNat (index + 1)`, and the measure rewrite needs the same equality before `productMeasure_continuing_frame` can match.  A malformed fully specified record literal in the public repeat callback also prevented Lean from parsing that local equality and disrupted the following invalid-branch bullet.

I will name the exact equality from `afterContinue.toState.toLocals []` to the generated continuing frame at `index + 1`, use it before both callback obligations, and replace the malformed record literal with the established record-update form `{ nextFrame with values := [] }`.  The prefix-successor equation then proves the new invariant, and the two checked measure lemmas reduce the decrease to natural subtraction.  No exit-path or artifact-postcondition diagnostic remains.

The import check succeeded, and the build now has one residual equality: `UInt64.ofNat index + 1 = UInt64.ofNat (index + 1)`.  Every declaration after that point elaborated, including the loop invariant, decrease, repeat reconstruction, valid completion suffix, and invalid branch.  The rewrite failed because the machine literal `1` was not syntactically `UInt64.ofNat 1`, so I will change the literal to that form and apply the symmetry of `UInt64.ofNat_add` directly.

The final import check succeeded, and the required `ArtifactResult` build completed all 3,081 jobs.  The accepted proof uses the shared declarations `Wasm.TerminatesWith.of_wp_entry_for`, `FixedArrayLengthDispatch`, `FixedArrayCapacity.constantProgram_spec`, `FixedArrayAllocatorWindow.region_spec_withTail`, `FixedArrayFold.forwardSetupProgram_spec`, `FixedArrayTraversalInput.continuingProgram_exit_spec`, the generated traversal and scalar evaluation theorems, `ScalarTransition.guardedBackEdgeProgram_spec`, `ArrayFold.foldPrefix_succ`, `ArrayFold.foldPrefix_size`, `FixedArrayResult.singletonStore_at`, and `FixedArrayFold.singletonResultProgram_spec`.  The theorem `LeanExeGen.GeneratedRa8e90ffc5781d113.Behavior.artifact_behavior` has no residual goal, and I made no change to `Behavior.lean` after acceptance.
