# Proof Journal

## 2026-08-18

The required initial Lean check failed in `Behavior.lean` after 27.9 seconds.  The deterministic starter decomposed `RuntimeReady` and established the input length read, input address facts, allocator globals, and all supplied bounds.  The remaining goal is `TerminatesWith env module 0 initial [Value.i64 inputPtr]` with the represented expected array as its postcondition.  No proof code had been added before this check.

I started retrieval from `KNOWLEDGE/forest.json`.  It selects the `leanexe-core` and `proposal-aa263bbfa89c-6b4877ec130b` packages.  I searched their `arrays`, `compiler-motifs`, and `proof-construction` indexes with `rg -n 'length-dispatch|leProgram_spec|wp_fixed_array_length_le_dispatch_from|TerminatesWith'`.  I inspected `leanexe-core/fixed-array-length-dispatch` because its annotation kind and tactic match the current entry and dispatch boundary.  I also inspected `proposal-aa263bbfa89c-6b4877ec130b/bounded-uint64-sum-fold-composition` because it has the same dispatch, allocation, fold, singleton-result, and empty-result structure.  The latter is operation-specific only at the scalar fold step; its branch composition remains relevant.

`PROOF_RECIPES.json` contains no complete composition.  Its first direct recipe converts entry execution with `Wasm.TerminatesWith.of_wp_entry_for`, rewrites the checked whole-function equality, and invokes `wp_fixed_array_length_le_dispatch_from hArray at 7, 8`.  I will attempt that exact tactic because its body-level goal shape, represented-array premise, and `leanexe.array.length-dispatch.v1` annotation all match.  Progress means replacing the entry goal with valid and invalid branch `wp` obligations while preserving the named suffix.

The import check accepted the first edit.  The Lean check failed after 15.9 seconds at the whole-function `rw`: entry conversion left the body as the projection `func0Def.body`, so Lean found no occurrence of `func0`.  The direct recipe therefore needs the explicit body-level `change` described by `leanexe-core/fixed-array-length-dispatch`; the tactic itself has not run.  I will normalize only this projection to `func0`, then retry the checked equality and the exact tactic.  The residual class is an entry-projection mismatch, and the selected entry supplies the exact normalization.

After the explicit `change`, the import check passed and the Lean check reached both branch obligations in 7.3 seconds.  The structured `wp_fixed_array_length_le_dispatch_from` command matched and removed the checked length-load and comparison prefix.  It also left five frame and numeric premises; these reduce from `func0Def`.  The valid branch assumes `input.size ≤ 8`, and the invalid branch assumes its negation.

For these new branch goals, I searched the selected packages' `allocation`, `arrays`, `memory`, and `proof-construction` indexes with `rg -n 'constantCapacityRegion_spec_withTail|fixed-array-capacity|allocStore|singletonResult|empty-result|result-construction'`.  I inspected `leanexe-core/fixed-array-capacity`, `leanexe-core/fixed-array-allocation`, and `leanexe-core/fixed-array-result`.  The first two match each branch's checked constant-capacity prefix and shifted allocator at offset 2 with six trailing locals.  The result entry matches the valid singleton and invalid empty-array stores.  I will discharge the five direct-recipe premises, introduce the branch assumptions, and preserve each branch program for the composed capacity-and-allocation theorem.

The import check passed.  The Lean check confirmed exact prefix matches for both branches and discharged the valid fit premise.  The valid residual now starts with a length-one result store, then the full annotated fold and singleton suffix.  The invalid residual is the standard zero-length store and root transfer.  The invalid capacity fit proof failed because `norm_num` normalized the branch assumption to `8 < input.size`; I will derive `¬ input.size ≤ 8` with `omega` before simplifying `heapReserveBytes`.

The valid residual creates a fold checkpoint.  I searched the `arrays`, `loops`, `compiler-motifs`, and `proof-construction` indexes with `rg -n 'leanexe.array.fold.v1|foldPrefix|continuingGuardedProgram_spec|forwardSetupProgram_spec|wp_block_loop|annotated-fold-frame-accessors|traversal-input'`.  I inspected `leanexe-core/annotated-fold-frame-accessors`, `leanexe-core/array-fold-prefix`, `leanexe-core/fixed-array-fold-body`, `leanexe-core/fixed-array-fold-structure`, and `leanexe-core/fixed-array-traversal-input`.  These entries match the generated continuing frame, prefix-product invariant, checked setup, block-wrapped loop, multiplication body, indexed input load, result placement, and singleton suffix.  I will use the generated accessors and `continuingGuardedProgram_spec` instead of reducing the 21-slot scalar frame.

The next import check passed.  The Lean check accepted the invalid branch's allocation-frame facts, root accessor, store bounds, zero-length store theorem, and finish-program premises.  Its final continuation failed because I tried to simplify `branchPost` while the goal still had an empty-program `wp` wrapper.  I will apply `Wasm.wp_nil` first, then reduce `branchPost` and the named one-instruction suffix.  The valid fold residual remains unchanged.

The import check passed, and the next Lean check reached the fold loop step in 30 seconds.  The exact result-length store, generated setup theorem, allocation-frame equality, input preservation, and `wp_block_loop` command all matched.  The structured tactic's goal shape, explicit invariant and measure premises, and fold annotation kind matched the residual; it replaced the block and loop with initialization and one-step obligations.  Three local corrections remain before the loop step: use a reducible proof for `8 ≤ (16 : UInt64).toNat`, supply the omitted twelfth noise-local witness in the initial invariant, and unfold both array predicates at the invalid branch's final representation conversion.

The loop-step residual has the generated continuing frame under the product-prefix invariant.  The selected `fixed-array-fold-body` entry applies: its `continuingGuardedProgram_spec` premises match the generated continuing and step programs, input representation, exact frame accessors, loaded-frame equality, and compiler evaluator equations.  On the continuing edge, `ArrayFold.foldPrefix_succ` should reconstruct the invariant and the generated index accessor should prove measure decrease.  On the exit edge, `FixedArrayTraversalInput.continuingProgram_exit_spec` should pass control to the generated singleton suffix.

The import check passed.  The Lean check rejected the first fold-body application because dependent premises shifted: Lean inferred the item-validity proof from the loaded-frame premise, so positional bullets began targeting later premises.  This is the exact failure mode described by `leanexe-core/fixed-array-fold-body`.  I will name `hValues`, all three getter premises, `hItem`, `hInput`, `hIndex`, `hLoaded`, `hStepProgram`, `hBody`, and `hCondition` in the application.  The generated frame accessors for slots 11, 13, and 15 all matched before the shift.  I will also derive the encoded-index range from `hInputFold.size_lt` explicitly, since `omega` did not use that theorem through a projection.

The next import check passed.  Naming every dependent fold-body premise fixed the application, and Lean accepted the compiler-derived body and condition evaluators.  The continuing callback reached only the semantic frame equality; its two unmatched terms are definitionally equal after unfolding the local abbreviation `value := input[index]`.  The exit callback reached the expected nested `Break 1` control postcondition.  I will normalize the empty operand stacks with the generated frame-values accessor and `Wasm.wp_nil`, which should expose the named singleton-result program without expanding it.

The import check passed.  The next Lean check accepted the complete continuing callback, including `ArrayFold.foldPrefix_succ`, the semantic next frame, generated slot-13 accessors, and the decreasing measure.  Exit normalization exposed exactly `function_0_array_fold_0_singleton_result_program` at the completed generated frame.  No loop residual remains.  I will apply the generated singleton-result adapter, derive its payload bound from `Allocation.BumpFacts.wordAddress_toNat`, and close the public continuation with `FixedArrayResult.singletonStore_at` and `ArrayFold.foldPrefix_size`.

The final import check passed, and the Lean build accepted `LeanExeGen.GeneratedRa8e90ffc5781d113.ArtifactResult` after 37 seconds for `Behavior` and 6.4 seconds for the result target.  The proof uses the checked length-dispatch tactic, constant-capacity allocator composition, allocation input framing, generated fold setup and frame accessors, `wp_block_loop`, `FixedArrayFoldBody.continuingGuardedProgram_spec`, `ArrayFold.foldPrefix_succ`, `ArrayFold.foldPrefix_size`, the traversal exit theorem, and the generated singleton-result adapter; the invalid branch uses the shared length-store, empty-store, and finish-program theorems.  Both branches establish the frozen `FormalSpec.expected` representation, and no residual goal remains.
