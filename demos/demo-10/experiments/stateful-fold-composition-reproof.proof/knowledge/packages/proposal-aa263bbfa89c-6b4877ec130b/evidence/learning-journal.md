# Learning Journal

## 2026-08-18

I read the accepted run metadata, proof journal, telemetry, task features, and knowledge evaluation.  The accepted proof has 650 lines and took 1,638,250 milliseconds from the start of stage 5 to acceptance.  It used eleven `leanexe-core` entries and rejected the archived singleton-result adapter because the compiler-generated singleton suffix theorem matched the artifact's frame and instruction interval more closely.

The proof journal identifies two boundaries that required local declarations.  The first is the equality between `FixedArrayFold.forwardSetupFrame` and the compiler-generated continuing frame; isolating this equality prevented `wp_block_loop` from exhausting one million heartbeats during invariant initialization.  The second is one continuing fold iteration, where `FixedArrayFoldBody.continuingGuardedProgram_spec` composes the generated body, condition, and back edge but leaves artifact-specific frame getters and scalar evaluators to the caller.

The task-feature extractor reports one 489-instruction function, twenty locals, three loops, memory, arithmetic, and allocation.  Its `fixedArrayLengthDispatches` list is empty even though the annotations and accepted proof contain a checked length-dispatch region, so the task features alone do not describe the useful compiler structure.  I will use the annotations and recipes to compare the exact compositions before treating either local declaration as a general candidate.

I searched the array, loop, compiler-motif, proof-construction, and worked-example indexes.  The closest checked theorem to `sumFrame_step` is `Project.ProofKit.FixedArrayFoldBody.continuingGuardedProgram_spec`, and the accepted proof applies that theorem to the exact recipe composition `continuing_program ++ step_program`.  The generated loaded-frame equality and the generated body, condition, and continuing evaluators supply the compiler semantics; `ArrayFold.foldPrefix_succ` and the caller's `UInt64` sum invariant supply application semantics.

I reject a new checked one-step theorem because the existing fold-body entry covers the repeated boundary.  The remaining work in `sumFrame_step` converts the natural index to `UInt64`, instantiates the artifact-generated scalar state, applies the sum-specific prefix equation, and reconstructs this artifact's generated frame.  A shared theorem would either duplicate `continuingGuardedProgram_spec` or encode the sum operation and the generated twenty-one-slot frame.

The closest support for `sumFrame_initial` is `FixedArrayFold.forwardSetupProgram_spec`, `FixedArrayFold.forwardSetupFrame`, `Frame.ext`, and the `wp_block_loop` tactic.  The compiler recipe supplies an exact setup-program equality and continuing-frame accessors, but it supplies no equality between `forwardSetupFrame` and the initial generated continuing frame.  The accepted proof therefore isolates that equality as an eight-million-heartbeat local theorem before invoking `wp_block_loop`.

I reject a shared setup-frame lemma because the missing equality depends on the generated frame constructor, the enclosing dispatch and allocation frames, and the artifact's local layout.  A theorem that accepts this equality as a premise would restate a rewrite around `forwardSetupProgram_spec`; it would leave the costly equality unchanged.  The useful general change belongs in compiler-generated annotation evidence, while `fixed-array-fold-structure`, `local-frame-projection`, and `array-fold-prefix` already advise named setup frames, focused equality proofs, and separate elaboration boundaries.

I also compared the archived `fixed-array-singleton-result-from-frame` theorem with the recipe's generated singleton adapter.  Both compose `FixedArrayFold.singletonResultProgram_spec_to`, but the generated theorem discharges the exact result and root frame premises for this region.  The archived theorem adds no help to this accepted proof, matching the rejection recorded in `knowledge-evaluation.json`.

I compared a benchmark-local worked example with the four archived worked examples.  The archived map and filter examples apply whole-function theorems, while the counter-transfer examples compose a scalar summary with a singleton wrapper; none records a manual `leanexe.array.fold.v1` proof with length-dispatch branches, allocation, an input-fold invariant, and generated scalar evaluators.  The accepted proof therefore supplies a distinct example even though its individual semantic boundaries already have canonical entries.

I select a worked example for the bounded `UInt64` sum fold.  Its reusable content is the composition order: split at the checked length dispatch, allocate empty or singleton output by branch, preserve the input across result writes, isolate the setup-frame equality before `wp_block_loop`, use `continuingGuardedProgram_spec` with generated evaluator equations on the continuing edge, and use the generated singleton adapter on exit.  The local numbers, addition state, heap arithmetic, and exact frame equality remain artifact-specific, so the entry has benchmark-local scope and exposes no new declaration or tactic.
