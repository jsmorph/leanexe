# Proof Journal

## 2026-08-10: Initial candidate check

I ran `/home/somebody/src/leanexe/tools/leanrun --timeout 60m lake -d . --no-ansi build LeanExeGen.GeneratedRa8e90ffc5781d113.ArtifactResult` before editing `Behavior.lean`.  The build reached `artifact_behavior` and failed at the deterministic starter's final goal: `TerminatesWith env module 0 initial [Value.i64 inputPtr]` with a postcondition requiring one `Value.i64 outputPtr` and `FormalSpec.UInt64ArrayAt final outputPtr (FormalSpec.expected input)`.  The `RuntimeReady` decomposition and common array facts elaborated, so the missing proof is the compiled function's execution from the supplied globals, memory bounds, and input representation.

The diagnostic left no earlier elaboration or import failure.  I will inspect the exact frozen definitions and task metadata, then try the named direct recipe or complete composition before breaking it into internal steps.  Progress means replacing the final `TerminatesWith` goal with either no goals or a narrower arithmetic, frame, or representation obligation.

## Frozen artifact and recipe inspection

`FormalSpec.expected` branches on `input.size ≤ 8`; the valid result is `#[input.foldl (fun product element => product * element) 1]`, and the invalid result is empty.  `Program.func0` begins with the annotated unsigned length dispatch and its valid branch contains the annotated forward fold loop with accumulator local 1, item local 2, index local 13, effective-stop local 15, and result local 10.  `PROGRAM_ANNOTATIONS.json` confirms the exact `leanexe.array.length-dispatch.v1` prefix and `leanexe.array.fold.v1` region, including the loop-carried multiplication transition.

`PROOF_RECIPES.json` contains no complete composition.  Its first direct recipe is `wp_fixed_array_length_le_dispatch_from hArray at 7, 8`, using `function_0_length_dispatch_0_function_eq`; its second recipe exposes the fold setup, continuing load, guarded back edge, exit, and singleton-result declarations but requires a caller-supplied invariant and measure.  I will attempt the exact length-dispatch tactic first, as required by the recipe order, and use the fold recipe only in the valid branch left by that direct tactic.

I read `LTG/categories.json` first.  The relevant catalog categories are `compiler-motifs`, `arrays`, `loops`, `allocation`, and `proof-construction`, selected by the two annotation kinds and the current `TerminatesWith` goal.  I will search only their `tools.jsonl` indexes for `length-dispatch`, `array.fold`, `forwardSetupProgram_spec`, `singletonResultProgram_spec`, and `TerminatesWith` before opening any entry metadata.

## LTG retrieval and first direct composition

I ran `rg -n 'length-dispatch|array[.-]fold|forwardSetupProgram_spec|singletonResultProgram_spec|TerminatesWith|wp_fixed_array_length_le_dispatch_from'` over the `compiler-motifs`, `arrays`, `loops`, `allocation`, and `proof-construction` indexes.  The search selected the canonical entries `fixed-array-length-dispatch`, `fixed-array-capacity`, `array-fold-prefix`, `fixed-array-fold-structure`, `fixed-array-fold-body`, `compact-loop-suffix-boundary`, `fixed-array-traversal-input`, and `guarded-back-edge`; I inspected only each selected entry's `entry.json` and `README.md`.  Repeated index records for the same canonical entries were duplicates and required no further inspection.

I will use `fixed-array-length-dispatch` immediately because it names the exact direct tactic and store-specific entry conversion.  I will use `fixed-array-capacity` in both branch prefixes, and I retain the prefix, structure, body, traversal, and guarded-back-edge entries for the valid fold branch.  I reject `compact-loop-suffix-boundary` at this stage because no timeout or memory failure has occurred; its guidance applies only if the public continuation makes the loop boundary too expensive.

The next edit applies `Wasm.TerminatesWith.of_wp_entry_for (f := func0Def) rfl`, rewrites `func0` with `function_0_length_dispatch_0_function_eq`, and invokes `wp_fixed_array_length_le_dispatch_from hArray at 7, 8`.  A successful direct recipe attempt should leave the dispatch theorem's structural premises and two branch obligations, while eliminating the top-level `TerminatesWith` goal.  If the exact shape fails, the next diagnostic will identify whether the mismatch lies at entry conversion, function rewriting, or tactic inference.

The import check accepted the first edit.  The Lean build eliminated `TerminatesWith`, then the generated equality rewrite failed because the target still displayed `func0Def.body` instead of `func0`; all entry and postcondition specialization had succeeded.  The direct recipe therefore needs the body boundary exposed by a `change Wasm.wp module func0 _ initial _ env` before the generated whole-function equality can match, with no change to the recipe's internal instructions.

After adding that boundary, the import check passed and the Lean build accepted the exact direct dispatch recipe.  The residual goals are five structural facts about the initial frame and constants, followed by the intended valid and invalid branch weakest preconditions.  This uses and accepts the `fixed-array-length-dispatch` entry; the direct recipe improved the proof boundary from a whole-function `TerminatesWith` goal to two exact branch programs.

The structural facts reduce from `func0Def` and contain no semantic obligation.  Each branch begins with the checked `FixedArrayCapacity.constantProgram`, then allocator code and either fold or empty-result construction, so I will search the relevant LTG indexes for `FixedArrayAllocator`, `region_spec_withTail`, `empty free list`, and `capacityFrame`.  A useful entry must cover the exact allocator window after the capacity theorem while preserving the represented input and public result continuation.

That allocation search selected the canonical promoted `fixed-array-allocation` entry, which I inspected and will use through `FixedArrayAllocatorWindow.region_spec_withTail` at offset 10 with four trailing locals.  A second query for `emptyStore_at`, `empty result`, and `FixedArraySingletonWrapper` selected `fixed-array-result`; I inspected it because its generic length store, empty representation, payload store, and final transfer match the two branch suffixes.  I rejected `fixed-array-singleton-wrapper` from its index metadata because this artifact has no singleton-wrapper or direct-call annotation and its program shape includes a scalar callee.

The next check will reduce only the five dispatch structural facts and introduce `hSize` in both branches.  Its residual branch frames will expose the exact definitional program boundary needed for `constantProgram_spec` and the shifted allocator theorem.  Progress means no remaining dispatch-structure goal and branch goals whose hypotheses include the selected size inequality.

The import check passed, and the build closed all five structural goals.  The only residual goals are the valid branch under `hSize : input.size ≤ 8` and the invalid branch under its negation, exactly as the direct recipe promised.  No dispatch premise or entry-conversion obligation remains.

Inspection of the exact allocator operands corrects my preliminary offset estimate from the LTG screening note.  `FixedArrayAllocatorWindow.region 2 1` maps capacity local 11, scratch locals 12 through 16, and root local 7; the twenty-local function requires `tail = 4`.  I will apply capacity 16 for the valid singleton and capacity 8 for the invalid empty result, deriving both memory-fit premises by reducing `FormalSpec.expected` under the branch hypothesis.

This edit composes `FixedArrayCapacity.constantProgram_spec` with `FixedArrayAllocatorWindow.region_spec_withTail` in both exact branch programs.  The residual goals that count as progress begin after allocation at the valid length store and fold, or at the invalid zero-length store and final root transfer.  Any earlier failure will identify a program-decomposition or frame-normalization mismatch.

The import check passed, and both exact capacity-plus-allocator program decompositions matched.  The allocator theorem discharged its structural, memory, page, module, and global premises; only constant normalization facts and the two post-allocation suffixes remain.  This accepts and uses the `fixed-array-allocation` entry with offset 2 and tail 4.

The constant residuals require unfolding `FixedArrayCapacity.normalizedCapacity` and `unnormalizedCapacity`, while the literal `UInt64.toNat` inequalities close by decision.  I will then complete the invalid branch through `FixedArrayResult.lengthStore_spec`, `emptyStore_at`, and `finishProgram_spec`, followed by the generated one-instruction function suffix.  The intended residual after this edit is only the valid fold branch.

The import check passed.  The build retained the two closed capacity equalities because `norm_num` stopped at `UInt64` division, so I will discharge them by computation with `decide`.  The invalid result program matched both selected store theorems, but one address rewrite needed a named simplified zero-word equality, and `finishProgram_spec` generalized its frame because I had left `st` and `frame` implicit.

I will name the allocator frame's parameter length, local length, destination and return validity, zero-word root address, and final return getter.  These facts fix every dependent theorem argument and prevent the final suffix simplifier from creating metavariable validity goals.  Progress remains closure of the invalid branch without changing its semantic composition.

The import check passed, all capacity computations closed, and the invalid branch reached its final public result equality.  The empty-array representation is complete; the sole invalid residual is reduction of `func0Def.numParams` inside `List.drop`.  Adding `Wasm.Function.numParams` to that final normalization should close the invalid branch, leaving only the valid fold suffix.

That normalization closed the invalid branch.  The build now reports one goal: the valid post-allocation program consisting of the singleton length store, annotated fold setup and loop, singleton payload store, root transfer, and function suffix.  The `fixed-array-result` entry is used and accepted for the invalid branch.

I will define a compact fold frame through the generated `function_0_array_fold_0_continuing_frame`.  Its accumulator is `ArrayFold.foldPrefix input (fun product element => product * element) 1 index`; it fixes stable locals and tracks the mutable item, temporary product, done value, staged product, and release-ready value.  The invariant equates the current store with the post-length-store state and bounds the logical index, while the measure reads local 13 and computes `input.size - index.toNat`.

The next extended edit applies the valid length-store theorem, preserves the input representation across allocation and that store, applies `forwardSetupProgram_spec`, and enters `Wasm.wp_loop_cons` with the compact invariant.  Progress means that Lean accepts the exact decomposition and invariant initialization, leaving only the loop body or its completed suffix.  A decomposition failure would identify missing catalog metadata for the full fold interval around its block.

The import check passed.  Lean accepted the exact valid decomposition, input preservation across allocation and the length store, the generic singleton-output representation, `forwardSetupProgram_spec`, and `Wasm.wp_loop_cons`.  Invariant initialization reduced to the definitional equality `heapTop + 48 = root`, and the remaining semantic goal is one execution of the exact generated continuing program followed by the guarded scalar step.

I will unfold the proof-local `root` abbreviation in initialization.  In the loop body I will split on `index = input.size`, apply `continuingProgram_exit_spec` to the completed edge, and retain the continuing edge for `continuingGuardedProgram_spec`.  Progress means separate completed-suffix and strict-index guarded-step goals, with no raw load or scalar instruction sequence exposed.

The import check passed, and invariant initialization now closes.  The loop split produced the intended completed and strict-index goals; `continuingProgram_exit_spec` matched the completed program, leaving two internal-list getter facts and its nested block/loop continuation.  The getter facts are concrete positions 12 and 14 of the generated frame and should close by reduction, after which restricted simplification should expose the named singleton-result program.

The import check passed, both completed-edge getters closed by reduction, and restricted simplification exposed `function_0_array_fold_0_singleton_result_program` with the completed invariant frame.  I will apply `singletonResultProgram_spec` under `Wasm.wp.conseq`, use `ArrayFold.foldPrefix_size` only when comparing its represented singleton with `FormalSpec.expected`, and execute the one-instruction function suffix in the consequence.  This uses the complete result composition named by the fold recipe and avoids reopening its payload and root-transfer instructions.

The import check passed, and the complete singleton-result theorem matched.  Its residuals are frame projections: root local 7 must be preserved when result local 10 is written, and destination and return validity refer to the resulting frame rather than the pre-result frame.  The public suffix also resets the operand stack before `localGet 6`, so its return getter needs transport through a record update that changes only `values`.

I will name the completed frame's parameter length, local length, accumulator, root, and valid local facts.  `resultFrame_get_of_ne`, `resultFrame_get_result`, `resultFrame_params`, and `resultFrame_locals_length` will then discharge the result-frame premises without unfolding local arrays.  The consequence will derive a stack-reset return getter with `Wasm.Locals.get` and use the named `foldPrefix_size` equality at the final representation equation.

The import check passed after that edit.  The Lean build accepted every named frame fact, the complete singleton-result theorem, and the public return suffix.  Two obligations remain.  The completed path has `UInt64Array.At final root #[input.foldl ...]` where the public postcondition requires `FormalSpec.UInt64ArrayAt final root #[input.foldl ...]`; these definitions use the same memory layout but require an allowed conversion declaration or a direct proof.  The strict-index branch still begins at the exact continuing-plus-guarded-step program, as intended.

I will search the task-specific array and proof-construction indexes for `UInt64ArrayAt`, `UInt64Array.At`, and representation conversion before constructing the strict fold step.  I will then use `FixedArrayFoldBody.continuingGuardedProgram_spec`, named by the inspected `fixed-array-fold-body` and `guarded-back-edge` entries, with the generated scalar transition evaluators.  Progress means that one step loads `input[index]`, updates the prefix product, increments local 13, re-establishes `productInv`, and proves a strict decrease of `input.size - index`.

The query `UInt64ArrayAt|UInt64Array\.At|representation.*convert|convert.*representation` found the canonical `array-memory-framing` entry and no representation-conversion entry.  I inspected its `entry.json` and `README.md`.  I use its `UInt64Array.At.write64After` and `input_preserved_by_alloc` declarations earlier in the proof, but reject it for the completed-path mismatch because both array predicates have definitionally identical fields and no framing operation is needed.  A directed `change UInt64Array.At ...` at the public postcondition preserves the proof boundary and avoids duplicating the four representation fields.

For the strict step I will instantiate `continuingGuardedProgram_spec` with the generated loaded state, body-result state, zero-condition state, and incremented state.  The generated `continuing_loaded_frame_eq`, `body_eval`, `condition_eval`, and `step_continuing_eval` theorems prove the operational transitions.  `ArrayFold.foldPrefix_succ` identifies the updated accumulator, while `UInt64.lt_iff_toNat_lt` and the represented input's `size_lt` fact prove the encoded continuing guard and the measure decrease.

The import check passed.  The build confirms that the directed representation change closes the completed singleton path.  The strict-step theorem also matches the exact continuing and guarded-step programs, but `apply` postponed its proof-dependent `hItem` and `hIndex` premises until after the transition callbacks.  My bullet order therefore supplied the item-valid fact where Lean expected `hInput`, shifting the remaining arguments; the final diagnostic displayed the postponed item-valid and logical-index goals explicitly.

The only arithmetic failure preceded the theorem application: `omega` did not use the opaque numerical value of `UInt64.size` to infer `index + 1 < UInt64.size`.  I will derive that fact by transitivity from `index + 1 ≤ input.size` and `hInputLoop.size_lt`.  I will reorder the theorem premises according to the diagnostic, leaving the generated item-valid proof and `hIndexLt` as the two deferred final goals.

The import check passed.  The build accepted the generated load, multiplication body, false guarded condition, index increment, prefix-product update, restored invariant, and strict measure decrease.  It reports only two `No goals to be solved` errors: rewriting the encoded successor already closes `hContinuedFrame`, and the theorem application finds `hIndexLt` from the context after the final item-valid premise.  I will remove those two redundant tactics; no semantic or structural goal remains in the diagnostic.

## Acceptance

After the final candidate edit, `PROOF_IMPORT_CHECK.js` passed and the required `leanrun` build completed `LeanExeGen.GeneratedRa8e90ffc5781d113.ArtifactResult` successfully.  The proof uses the shared length-dispatch, capacity, allocator-window, fixed-array result, fold setup, traversal, guarded fold-body, scalar-transition, array-prefix, and loop-control declarations recorded above.  `artifact_behavior` has no residual goal, and the checked candidate contains no `sorry`, added axiom, `Source` import, or external artifact proof.
