# Proof Journal

## 2026-08-10

I ran the unchanged candidate with the required `leanrun` build command before inspecting or editing the proof.  The build reached `Behavior.lean` and failed at line 22 with the deterministic starter goal.  After the `RuntimeReady` decomposition and the derived array facts, the sole goal is `TerminatesWith env module 0 initial [Value.i64 inputPtr]` whose postcondition requires one returned pointer representing `FormalSpec.expected input`.

The context contains `hLengthRead`, `hLengthBound`, `hInputAddress`, `hHeapTop`, `hFreeList`, and `hAllocs`, together with the input representation and all output, heap, memory, and page bounds.  I will inspect the task-selected recipes and strategies, then search the LTG indexes by the remaining `TerminatesWith` goal, the fixed-capacity length dispatch, and the fold loop.  A complete recipe that closes this postcondition without expanding the product into element cases will count as progress.

`PROOF_RECIPES.json` contains two direct recipes and no complete composition.  The first applies `wp_fixed_array_length_le_dispatch_from hArray at 7, 8` to expose the valid and invalid branches.  The second identifies the checked fold setup, traversal, scalar back edge, fold-prefix invariant, and accumulator result placement, but it leaves the invariant and surrounding allocation/result composition to the artifact proof.

I searched `LTG/categories/*/tools.jsonl` with four queries: `leProgram_spec|wp_fixed_array_length_le_dispatch_from|leanexe\.array\.length-dispatch`, `FixedArrayFold|foldPrefix|guardedBackEdgeProgram_spec|leanexe\.array\.fold`, `TerminatesWith\.of_wp_entry_for|wp_entry_to_loop|wp_block_loop`, and `FixedArrayPairResult|singleton|empty array|resultSuffix|UInt64Array`.  The relevant results are `fixed-array-length-dispatch`, `fixed-array-capacity`, `array-fold-prefix`, `fixed-array-fold-structure`, `fixed-array-traversal-input`, `fixed-array-result`, `fixed-array-allocation`, and the control/scalar-transition entries returned by the loop query.  The singleton-wrapper and its worked examples target different compiler annotations and a scalar callee, so I reject them for this inline fold artifact.  I will inspect only the canonical `entry.json` and `README.md` files for the relevant entries.

I inspected the canonical metadata and README for `fixed-array-length-dispatch`, `fixed-array-capacity`, `array-fold-prefix`, `fixed-array-fold-structure`, `fixed-array-traversal-input`, `guarded-back-edge`, `fixed-array-result`, and `fixed-array-allocation`.  All eight apply: the artifact has an exact bounded-length dispatch, constant capacities one and zero, an inline `UInt64` fold, the standard traversal guard and load, the descriptor-guided multiplication and index back edge, standard singleton or empty result stores, and the fixed-array allocator.  I will use these entries as separate checked boundaries.  I rejected the older constant-index loader and fixed equality-node paths mentioned in the traversal entry because this artifact uses the annotated dynamic traversal.

The first elaboration attempt will add only the prescribed entry conversion and exact length-dispatch tactic after the starter facts.  Success means replacing the public `TerminatesWith` goal with two branch-level `wp` goals under `input.size ≤ 8` and its negation.  I will not expand either branch until this direct recipe has been tested against the frozen function.

The import check succeeded.  The required Lean build failed with the five expected mechanical premises (`hParams`, `hValues`, the positive and bounded input-local facts, and `8 < UInt64.size`) plus `hValid` and `hInvalid`.  This confirms that the exact direct length-dispatch recipe matches the decoded function and improves the proof boundary from the public `TerminatesWith` goal to branch programs.  I will discharge the five frame and numeric premises by reduction, introduce the branch size hypotheses, and then apply the checked constant-capacity theorem at the start of each branch.

The second import check succeeded.  The build shows that `hParams`, `hValues`, `hInputLocalPositive`, and `hMaximumSize` close, while `hInputLocal` needs explicit unfolding of `Function.toLocals`.  Both constant-capacity applications match.  Each leaves only three mechanical frame premises and its continuation, with normalized capacities sixteen for the singleton branch and eight for the empty branch.  I will add the exact `toLocals`, `numParams`, `numLocals`, `List.take`, `List.replicate`, and `ValueType.zero` reductions named by the proof kit, then continue into the offset-two allocator window with four trailing locals.

The third import check succeeded.  All dispatch and capacity premises now close, leaving one continuation in each branch.  The exact allocator layout is `FixedArrayAllocatorWindow.region 2 1`: offset two maps the capacity operand to combined local 11 and the root to local 7, while `tail = 4` accounts for twenty internal locals.  I will first complete the invalid branch through this allocator boundary.  Progress means reaching the standard zero-length store with an `allocStore` and `allocFrame`, without opening allocator instructions.

The fourth import check succeeded.  The allocator theorem matches the invalid branch and reaches `FixedArrayAllocator.allocStore` plus `FixedArrayAllocatorWindow.allocFrame`; its semantic and frame premises close except for two closed `UInt64` arithmetic normalizations.  The remaining result-store proof also matches `lengthStoreProgram 7 0` and `finishProgram 7 5 6`.  Its bound goal retains `allocStore.mem.pages`, so I will rewrite with `FixedArrayAllocator.allocStore_pages`, use decision reduction for the closed capacity arithmetic, and retain `emptyStore_at` as the representation boundary.

The fifth import check succeeded.  The invalid branch now closes completely through the checked capacity, allocator, zero-length store, `emptyStore_at`, final root transfer, and the public result continuation.  Only the valid branch continuation remains.  I will apply the same allocator theorem with capacity sixteen, preserve the represented input with `input_preserved_by_alloc` and `UInt64Array.At.write64After`, execute the singleton length store, and apply `forwardSetupProgram_spec`.  Reaching the block-wrapped annotated loop with the initialized accumulator will count as progress.

The sixth import check succeeded.  The valid allocator, input preservation, length store, and fold setup all match.  `forwardSetupProgram_spec` leaves the desired block-wrapped loop, but its parameter-frame premise retains an unconstrained inferred `inputPtr`; the call needs the explicit argument `(inputPtr := inputPtr)`.  After that correction, I expect only the fold loop and its result suffix to remain.

The seventh import check succeeded and leaves only the annotated loop plus result suffix.  I will state a compact invariant using `function_0_array_fold_0_state`: it fixes the input pointer, allocated root, encoded size, traversal index, and fold-prefix accumulator while existentially retaining the item and scalar scratch locals.  The store remains the allocated store after its length word has been written.  The measure is `input.size` minus the natural value of local 13.  I will first apply `wp_block_cons` and `wp_loop_cons`; their exact residual goals will determine the initialization, repeat, and exit proof order.

The eighth import check succeeded.  The loop-rule application produces only `hInit` and `hStep`, and the explicit generated-state invariant closes `hInit` by reduction.  For `hStep`, I will split on `index < input.size`.  The continuing branch will apply `continuingProgram_spec` and the exact generated `guardedBackEdgeProgram_spec`, use `foldPrefix_succ`, and prove the local-13 measure decreases.  The equal-index branch will apply `continuingProgram_exit_spec`, rewrite with `foldPrefix_size`, and continue through the checked result placement and singleton store.

The ninth import check succeeded.  The continuing traversal theorem matches the loop prefix, but `native_decide` cannot close its valid-local premise because the generated frame contains free scalar values.  That failure shifts the following diagnostics to `hInput` and `hNext`; it does not indicate a mismatch in the traversal or guarded-back-edge regions.  I will prove the valid-local premise by reducing `Locals.validIndex`, `foldLoopState`, the generated state constructor, and `U64State.toState`.

The tenth import check succeeded and clarifies that `apply continuingProgram_spec` infers both `hItem` and `hIndex` from the dependent continuation.  The first residual after `hContinue` is `hInput`, so the attempted valid-index reduction was applied to the wrong premise and the later bullets shifted.  I will remove the two redundant bullets, supply `hInputFold`, and let the dependent continuation retain the inferred local-validity and index facts.

The eleventh import check succeeded and confirms that the checked guarded-back-edge theorem accepts all three generated evaluator equalities.  The continuing branch now has three residual issues: `hItem` remained as a side goal, rewriting `UInt64.ofNat index + 1` with `ofNat_add` did not match the literal one, and broad simplification exceeded its step limit on the invariant and measure.  I will pass `hItem` and `hIndex` by name, prove the encoded successor through `UInt64.toNat`, and replace the broad simplifications with explicit `rw`, `dsimp`, and `change` steps.

The twelfth import check succeeded.  The successor equality, fold-prefix update, and measure decrease now close.  Supplying only `hItem` and `hIndex` by name changed which remaining premises `apply` inferred, so the positional bullets again shifted.  I will eliminate this ambiguity by supplying every semantic premise of `continuingProgram_spec` by name and leave only its dependent `hNext` continuation as a tactic goal.

The thirteenth import check succeeded.  The complete continuing loop branch now closes through the generated traversal and guarded-back-edge theorems; `norm_num` also closes the encoded-successor equality before the following rewrite, so that rewrite must be removed.  The only semantic goal is the equal-index exit continuation.  I will compose `resultProgram_spec`, `payloadStore_spec`, `singletonStore_at`, and `finishProgram_spec` from that `Break 1` path.

The fourteenth import check succeeded.  The build closes the loop exit, accumulator transfer, payload store, memory bounds, final root transfer, and all public-return framing.  The last goal compares `singletonStore` in `hOutput` with its definitionally equal `writePayload (writeLength ...)` expression in the execution state.  I will unfold `FixedArrayResult.singletonStore` in the final representation conversion; no semantic premise remains.
