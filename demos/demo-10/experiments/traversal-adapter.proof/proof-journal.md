# Proof Journal

## Initial candidate check

I ran the prescribed `ArtifactResult` build before editing the candidate.  The build failed at `Behavior.lean:19:116`, after the deterministic `RuntimeReady` decomposition and common array facts had elaborated.  The residual goal is `TerminatesWith env module 0 initial [Value.i64 inputPtr]` with a postcondition requiring one output pointer and `FormalSpec.UInt64ArrayAt final outputPtr (FormalSpec.expected input)`.

The context contains the input array representation, its readable length and address bounds, the heap-top, free-list, and allocation-counter globals, the output and heap-reserve memory bounds, and the WebAssembly page bound.  No preliminary proof tactic failed before this goal.  The next step is to inspect `PROOF_RECIPES.json` and attempt its exact direct recipe or complete composition before decomposing any instruction sequence.

## Recipe and LTG retrieval

I started LTG retrieval by reading `LTG/categories.json`.  I then searched `LTG/categories/{compiler-motifs,loops,arrays,allocation,proof-construction}/tools.jsonl` with the query `leanexe\.array\.fold\.v1|FixedArrayFold|foldPrefix|wp_loop_cons|TerminatesWith|singleton|empty array|array result|allocator`.  The query identified the canonical entries `fixed-array-fold-structure`, `array-fold-prefix`, `fixed-array-traversal-input`, `guarded-back-edge`, `fixed-array-length-dispatch`, `fixed-array-allocation`, and `fixed-array-result` as relevant to the current function boundary and annotated fold.

`PROOF_RECIPES.json` contains one direct regional recipe and no complete composition.  Its direct theorem is `Wasm.wp_loop_cons`, justified by `AnnotationMatches.function_0_array_fold_0_eq`, and its support list divides the annotated interval into checked setup, traversal, scalar back-edge, and result-placement declarations.  Since the current goal is `TerminatesWith` at function entry rather than a loop weakest-precondition goal, the recipe can apply only after entry conversion, length dispatch, and successful-branch allocation reach the annotated region.

I inspected the `entry.json` and `README.md` files for `fixed-array-fold-structure`, `array-fold-prefix`, `fixed-array-traversal-input`, `guarded-back-edge`, `fixed-array-length-dispatch`, `fixed-array-allocation`, and `fixed-array-result`.  All seven entries apply: together they provide the entry conversion, bounded-length split, capacity and allocator boundary, fold setup, prefix invariant, exact traversal and scalar step, output stores, and return transfer.  The search also found `fixed-array-singleton-wrapper`, `fixed-array-map-add`, `demo7-counter-transfer-example`, and `demo8-counter-transfer-example`, which I rejected without opening their entries because their indexed annotation kinds require a direct scalar call, a map operation, or a scalar post-test loop absent from this artifact.

I inspected the recipe-named declarations in the local `PROOF_KIT_SOURCE` mirror and the generated declarations in `AnnotationMatches.lean`.  The successful branch uses `FixedArrayCapacity.constantProgram 1 1 11`, `FixedArrayAllocatorWindow.region 2 1`, and the exact annotated fold subregions; the oversized branch uses the same allocator window after `constantProgram 0 1 11`.  The next edit will convert the public goal with `wp_entry`, expose `FixedArrayLengthDispatch.leProgram 7 8`, and apply `leProgram_spec`; separate valid and invalid branch goals would constitute progress.

The import check succeeded after the first candidate edit, but the Lean build rejected the entry conversion.  `Project.ProofKit.wp_entry` uses the store-parametric `TerminatesWith.of_wp_entry`, so its introduced `entryFrame` had type `Store Unit`, while the subsequent `wp` target expected `Locals`.  The fixed initial-store hypotheses require the LTG-named `Wasm.TerminatesWith.of_wp_entry_for`; the next edit will use that theorem directly and leave its generated `Function.toLocals` frame in the `wp` goal.

The second import check succeeded.  The build accepted `of_wp_entry_for`, the exact `leProgram 7 8` decomposition, and `leProgram_spec`, then produced the valid and invalid branch obligations.  The trailing `all_goals simp` reduced capacity instructions inside the invalid branch and generated a large raw `wp` expression, so it worsened the proof boundary; I will replace it with explicit proofs of the five structural premises and keep each branch folded.

The oversized branch has the clean checked composition `constantProgram 0 1 11`, `FixedArrayAllocatorWindow.region 2 1` with four trailing locals, `lengthStoreProgram 7 0`, and `finishProgram 7 5 6`.  Its memory premise reduces to `heapTop.toNat + 56 ≤ initial.mem.pages * 65536`, which follows from `hOutputFitMemory` after rewriting `FormalSpec.expected input` to `#[]`.  The next residual goal that would count as progress is the post-allocation empty-array representation or the final outer `.localGet 6` continuation.

The third import check succeeded.  The build accepted the oversized branch’s region decompositions, allocator memory argument, length-store bound, empty-array representation, and finish-program shape; the remaining oversized-branch goals are closed numeral reductions for capacity and `func0Def.numParams`.  The failed `norm_num` calls did not reduce `UInt64.toNat 8`, so I will use decision reduction for those closed propositions and supply the one-parameter fact alongside the output representation at the public continuation.

The fourth import check succeeded, and the build left only the successful branch, so the oversized branch is complete.  I then introduced named public-postcondition, fold-frame, invariant, and measure definitions; the next check rejected an ambiguous dotted constructor in the measure and showed that the shortened public postcondition is propositionally, rather than definitionally, equal to the entry theorem’s postcondition.  I will use `Value.i64` in the match and define the named postcondition with the exact generated result-length and argument-drop expressions.

The fifth import check succeeded.  The exact generated expressions still did not make the named postcondition definitionally equal at `change`, although the two definitions have the same branches and formulas.  I will cross this elaboration boundary once with `Wasm.wp.conseq`, prove the postcondition implication by simplification, and retain the named postcondition through the branch and result proofs.

The sixth import check succeeded.  Splitting the continuation before simplifying proved the `wp.conseq` implication, and the build again left only the successful branch.  I also inspected `FixedArrayMapAdd.wrapperProgram_spec` in the permitted `PROOF_KIT_SOURCE` mirror for its allocator-to-loop framing pattern; its whole-wrapper theorem was rejected because it implements a map and does not match this fold, but its use of an allocation-preserved input and an index-derived measure confirms the selected LTG structure.

The next construction will reach the annotated fold without reducing its instructions.  It will compose `constantProgram 1 1 11`, `region 2 1`, `lengthStoreProgram 7 1`, and the generated `forwardSetupProgram`, preserve the input through the allocator and length store, then apply the recipe’s exact `wp_loop_cons`.  A residual loop-step goal over `foldFrame` would show that every pre-loop checked region and the invariant initialization match the artifact.

The next two import checks succeeded.  The first build exposed two presentation errors before the fold, which were an invalid `rw` of a local store definition and one extra list bracket around the loop instruction; after correcting them, the build accepted every successful-branch prefix theorem, the exact recipe’s `wp_loop_cons`, and invariant initialization.  Its residual goal was precisely the loop step over `foldFrame`.

The following import checks also succeeded.  I split the step on `index = input.size`; the completed branch now uses `continuingProgram_exit_spec`, `resultProgram_spec`, `payloadStore_spec`, `singletonStore_at`, and `finishProgram_spec`, and the build accepted that complete path.  The remaining continuing edge will apply the generated `function_0_array_fold_0_continuing_spec`, then `guardedBackEdgeProgram_spec` with the generated multiplication body, false done-condition, index increment, `foldPrefix_succ`, and a strict decrease of `input.size - index`.

The next import check succeeded.  A parser cascade came from naming a local value `prefix`, which Lean reserves for syntax declarations; renaming it to `accumulated` exposed the intended proof.  The subsequent build accepted the generated traversal theorem, loaded-frame equality, multiplication-body evaluator, false condition evaluator, continuing evaluator, and index arithmetic, leaving only two simplifier step-limit failures while refolding the successor invariant and evaluating its measure.

Replacing the broad simplification with `foldPrefix_succ` and an exact measure `change` removed both step-limit failures.  The import check succeeded, and the build left one equality between `function_0_array_fold_0_state.toState.toLocals` and `function_0_array_fold_0_continuing_frame` after the fold-prefix rewrite.  The generated continuing-frame definition reduces this equality by reflexivity, so the next edit will add that reduction at the successor invariant boundary.

The import check succeeded after adding the definitional reduction.  The `ArtifactResult` build then proved `artifact_behavior`, including the bounded-length dispatch, allocation branches, annotated input-traversal loop, loop-carried wrapping product, strict loop measure, and represented result array.  The remaining output consists only of linter warnings, so the proof has no residual goals and requires no further candidate edit.
