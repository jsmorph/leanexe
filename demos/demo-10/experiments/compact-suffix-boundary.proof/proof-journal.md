# Proof Journal

The untouched candidate passed `PROOF_IMPORT_CHECK.js`.  I then ran the prescribed `leanrun` build against `LeanExeGen.GeneratedRa8e90ffc5781d113.ArtifactResult`.  The command stopped after building `ArtifactRawCache`, printed no success line for `Behavior` or `ArtifactResult`, and produced neither corresponding `.olean`, so the starter has not passed and still requires a proof.

LTG retrieval began with `LTG/categories.json`, which identifies arrays, compiler motifs, loops, allocation, memory, and proof construction as the relevant indexes.  I searched those category `tools.jsonl` files with `rg -n 'RuntimeReady|fold|Fold|singleton|length.dispatch|LengthDispatch|traversal|loop.carried|loop'`.  The relevant entries are `fixed-array-length-dispatch`, `fixed-array-fold-structure`, `array-fold-prefix`, `fixed-array-traversal-input`, `guarded-back-edge`, `fixed-array-capacity`, `fixed-array-result`, `fixed-array-allocation`, `fixed-array-eq-node`, `array-memory-framing`, `local-frame-projection`, and `residual-specification-normalization`; singleton-wrapper and scalar-post-test entries describe different annotation kinds and are rejected for this artifact.

`PROOF_RECIPES.json` supplies no complete composition.  Its strongest first step is the exact length-dispatch invocation `wp_fixed_array_length_le_dispatch_from hArray at 7, 8`, followed by checked capacity programs in each branch.  The fold recipe identifies the exact generated region, setup theorem, prefix invariant lemmas, traversal theorems, guarded back-edge transition, and result-placement theorem, so I will attempt the dispatch tactic before decomposing any recipe internals.

I inspected the canonical metadata and README files for `fixed-array-length-dispatch`, `fixed-array-capacity`, `fixed-array-fold-structure`, `array-fold-prefix`, `fixed-array-traversal-input`, `guarded-back-edge`, `local-frame-projection`, `fixed-array-allocation`, `fixed-array-result`, and `array-memory-framing`.  The first nine entries apply directly to checked regions or residual frame goals, while array-memory framing will apply only if the allocator theorem does not already preserve the represented input through the valid branch.  A search for `FixedArrayEqNode|branchPost` found no canonical LTG entry, but `PROOF_RECIPES.json` names `FixedArrayEqNode.branchPost`, and the imported task-local module defines the continuation used by the dispatch theorem.

The next candidate change applies `Wasm.TerminatesWith.of_wp_entry_for (f := func0Def) rfl` and then the recipe's exact `wp_fixed_array_length_le_dispatch_from hArray at 7, 8`.  Success means that Lean accepts the public-entry and dispatch decomposition and leaves only the generated local-frame facts plus the two branch programs.  I am keeping both branch bodies folded so the next change can apply their checked capacity and allocation theorems.

The import check passed after the direct-recipe edit.  The Lean build failed at the intended boundary: `of_wp_entry_for` and `wp_fixed_array_length_le_dispatch_from` both applied, leaving five decidable entry-frame premises and the valid and invalid branch weakest-precondition goals.  The exact direct recipe therefore improved the proof boundary and will remain in the candidate.

The next edit closes the entry-frame premises by reducing `func0Def`, then refolds the valid branch's first eighteen instructions as `FixedArrayCapacity.constantProgram 1 1 11` and applies `constantProgram_spec`.  Progress means that the valid branch begins at `capacityFrame` with capacity sixteen and the remaining allocator, fold, and result instructions still folded.  I will leave the invalid branch unchanged during this check so its diagnostic remains separate from the valid-branch continuation.

The import check passed, and the Lean build accepted all five entry-frame premises plus the exact valid capacity refolding.  Its residual valid goal now starts at `capacityFrame`, and the normalized capacity is still displayed symbolically as expected from the theorem boundary.  The capacity theorem left only its three frame premises and continuation, while the invalid branch remains at its original branch program.

The next edit proves the capacity-frame premises by reduction, normalizes the capacity to sixteen, refolds the following checked instruction interval as `FixedArrayAllocatorWindow.region 2 1`, and applies `region_spec_withTail` with four trailing locals.  The residual goal that counts as progress begins after allocation in `FixedArrayAllocator.allocStore` and `FixedArrayAllocatorWindow.allocFrame`.  The allocator premises should reduce to the RuntimeReady global facts, page bound, and valid-branch reserve bound.

The import check passed, and Lean accepted the exact allocator refolding and theorem application.  The remaining allocator premises show the intended 20-local offset-two layout, capacity sixteen, the RuntimeReady memory and global hypotheses, and a continuation beginning with the singleton length store.  This confirms that `fixed-array-allocation` applies, while manual execution of the allocator instructions is unnecessary.

The next proof construction derives one `Allocation.bumpFacts` value from the valid-branch reserve bound, proves the allocator frame premises, and applies `FixedArrayResult.lengthStore_spec`.  It preserves `hArray` first through `input_preserved_by_alloc` and then through the result-length write with `UInt64Array.At.write64After`; this retained representation becomes the input premise of `FixedArrayFold.forwardSetupProgram_spec`.  Progress means that Lean reaches the block-wrapped product loop in the generated `forwardSetupFrame`, with setup instructions and memory loads fully discharged.

The import check passed.  Lean reached the fold loop, but reported six local normalization defects before that boundary: `wordAddress_toNat` needed a separately simplified zero-word equality, concrete `UInt64` capacity propositions needed decision reduction, `lengthStore_spec` inferred its root through a zero-word expression, the setup membership split retained an impossible `slot ∈ []` case, and the preserved input store differed by the same unsimplified zero-word expression.  These are residual proof-shape issues rather than failed checked-region applications.

I searched the allocation, memory, and proof-construction indexes with `rg -n 'normalizedCapacity|wordAddress_toNat|UInt64\.toNat|capacity'`.  The search returned only the already-used `fixed-array-capacity` entry; it offers no additional concrete-`UInt64` normalization declaration, so no new LTG entry was inspected or adopted.  The next edit names the simplified root-address equality, fixes the result root explicitly, uses decision reduction for the closed capacity facts, and removes the empty-list membership case before returning to the loop goal.

The import check passed, and Lean accepted every capacity, allocation, preservation, result-length-store, and fold-setup premise.  The valid branch now has one residual goal beginning exactly at the block-wrapped loop, while the invalid branch remains untouched.  No pre-loop arithmetic or frame goal remains.

I searched the loop and proof-construction indexes with `rg -n 'wp_block_loop|wp_loop_cons|foldPrefix|decreasing|invariant'`.  The search returned the already-inspected `array-fold-prefix` and `local-frame-projection` entries, which remain applicable; `counter-transition` and `euclidean-gcd-loop` concern `leanexe.loop.scalar-post-test.v1` and are rejected for this array-fold region.  The next edit adds a compact generated-frame invariant whose accumulator is `ArrayFold.foldPrefix`, whose index is bounded by the input size, and whose measure is the remaining element count.

The import check passed after the invariant edit.  The Lean build ended without a theorem diagnostic and produced no `Behavior.olean` or `ArtifactResult.olean`, so it did not establish success; no Lean process remained afterward.  I will repeat the prescribed checks once to recover the missing elaborator diagnostic before deciding whether the invariant initialization requires a smaller declaration boundary.

The repeated import check passed, but the repeated Lean build again ended without a diagnostic or target artifact.  The runner slice reported a memory peak at the six-gibibyte child limit, while its parent recorded no aggregate OOM event; the large open loop-body goal is therefore exhausting the diagnostic boundary before Lean can report it.  I inspected the task-local `fixed-array-map-add` LTG entry and proof-kit source because its README permits use as a proof pattern for another operation: its whole-function theorem is rejected for this `leanexe.array.fold.v1` artifact, but its exact-frame invariant and remaining-count measure confirm the selected loop organization.

The next diagnostic edit limits pretty-print depth without changing the theorem or proof state.  A successful diagnostic should expose the loop-rule continuation shape without expanding the complete public postcondition, decoded suffix, and generated frame.  This option can remain local to the proof file because Lean still checks the complete term.

The import check passed, but the depth-limited build reached the same six-gibibyte child-memory peak and ended without a diagnostic or target artifact.  Pretty printing is therefore not the root cause; elaborating `wp_loop_cons` under the full result suffix and public continuation creates the oversized boundary.  The next change inserts `Wasm.wp.conseq` after `wp_block_cons`, proves the completed-fold suffix once, and gives the loop rule a compact completion assertion.

The import check passed after adding the compact loop postcondition, but the build again ended at the child-memory limit without a diagnostic.  The nested proof of the completed-fold suffix still elaborates inside the public theorem and retains the same large continuation context.  I will move result placement, payload storage, singleton reconstruction, and final local transfer into a separate helper theorem whose type contains only the compact generated frame and output assertion.

The import check passed, and the separate suffix theorem restored a normal Lean diagnostic under the resource limit.  Lean exposed local issues: closed frame-validity goals used `decide` while their types still contained variables, the temporary printer option does not exist in this Lean version, the formal fold lambda needed an explicit definitional rewrite to `productStep`, the payload bound needed `rootToNat`, the final `localGet 6` needed its getter hypothesis, and the multiline loop-rule application failed to assign `Inv` and `μ`.  The compact theorem boundary is retained because it removed the memory failure.

I searched the proof-construction, array, and loop indexes with `rg -n 'wp\.conseq|wp_localGet_cons|result-transfer|finishProgram_spec'`.  Only the already-used `fixed-array-result` entry matched, so no new entry was inspected; its result-transfer guidance directly addresses the remaining suffix goals.  The next edit replaces variable-dependent decisions with focused frame reductions, normalizes the root relation in the payload bound, applies the final getter explicitly, and places both loop-rule parameters in one tactic expression.

The import check passed after those reductions.  The Lean build accepted the separate suffix theorem and all valid-branch preparation, then reported two local failures: the public continuation reads local six after clearing the value stack, and the initialization and step bullets sat outside `wp_loop_cons`, leaving both rule premises unsolved.  The initialization frame also needs focused normalization rather than `rfl`; no suffix, allocation, or memory goal remains.

I searched the loop, proof-construction, and array indexes with `rg -n 'continuingProgram_spec|guardedBackEdgeProgram_spec|internal_getElem\?|Locals\.get|wp_loop_cons'`.  The results are the previously inspected `fixed-array-traversal-input`, `guarded-back-edge`, and `local-frame-projection` entries, all of which match `leanexe.array.fold.v1`; no new entry was inspected or rejected.  The next edit proves the getter after resetting `values`, nests the loop premises under the rule application, and reduces the exact index-zero setup frame.  Progress means that Lean exposes only the checked traversal and scalar-transition step obligation.

The import check passed, and the Lean build now reports the compact loop step as intended.  Initialization reduced to `0 = UInt64.ofNat 0 ∧ inputPtr = 0`, revealing that the invariant incorrectly fixed local sixteen to zero even though fold setup stores `inputPtr` there; the body later clears and reuses that local as the scalar condition scratch.  The final continuation had already reduced past its local-get expression, so the added `simp` made no progress, while the invalid branch remains the expected untouched obligation.

The next edit adds local sixteen as an existential frame value in the invariant and completed-loop postcondition, passes it through the suffix helper, and initializes it with `inputPtr`.  The back edge may then set it to zero without changing the invariant's semantic accumulator.  Success means that initialization closes and the only valid-branch goal is the traversal-plus-scalar step.

The import check passed.  The Lean build reduced initialization to the reflexive encoded-zero equality, confirming that the generalized condition scratch fixed the substantive frame mismatch.  It also showed that the public continuation still contains one `localGet 6`; I will execute that instruction with `wp_localGet_cons` before rewriting its getter, rather than asking `simp` to unfold the instruction and the assertion together.

The next extended construction refolds the loop body as the generated continuing and step programs.  It applies `continuingProgram_exit_spec` when the natural index equals the input size and the generated `function_0_array_fold_0_continuing_spec` otherwise.  Progress means that the exit edge closes with the compact loop postcondition and the continuing edge stops at the generated scalar transition with the current input element loaded into local two.

The import check passed.  The build accepted the program refolding, both guard getter facts, the natural-to-`UInt64` comparison, and the generated indexed-load theorem.  Its valid continuing goal now begins exactly at `function_0_array_fold_0_step_program` over the dependent `dynamicResultFrame`; the exit goal reduced its unchanged store equality to `True`, and the public continuation requires one explicit `branchPost` unfolding before the local getter can execute.

The next edit applies `guardedBackEdgeProgram_spec` to three named generated states: the loaded state, the state after multiplication and condition evaluation, and the state after incrementing the index.  The compiler-generated evaluator theorems prove the three transitions.  The back-edge postcondition will use `foldPrefix_succ` for the accumulator and the remaining-input measure for strict decrease.

The import check passed, and the build accepted all three generated scalar evaluators, the guarded back edge, the fold-prefix successor argument, and the strict-measure setup.  Two rewrite-order issues remain in the valid branch: the formal expected-array rewrite removes the prefix expression from the target before `foldPrefix_size` can act, and the advanced-frame proof must unfold `productLoopFrame` before rewriting the encoded successor.  These are the only valid-branch diagnostics; the invalid branch remains open.

The next edit rewrites `foldPrefix_size` in the stored-array hypothesis and unfolds the frame constructor before applying the encoded-index equality.  Success means that the complete valid branch closes, including the zero-length exit and every loop back edge.  I will then construct the empty-array invalid branch from the same capacity, allocator, and result-placement assets without a fold.

The import check passed, and the Lean build reports only the invalid length-dispatch branch.  The valid branch now proves the zero-iteration case, each input traversal and multiplication step, strict loop termination, singleton construction, and the public return value.  No valid-branch residual goal remains.

I searched the generated annotations, proof recipes, result declarations, and relevant LTG indexes with `rg -n 'invalid_branch|invalid branch|length_dispatch_0_.*branch|emptyStore|empty.*_at|lengthStoreProgram|finishProgram'`.  The generated invalid-capacity program is exactly `FixedArrayCapacity.constantProgram 0 1 11`; the already-inspected `fixed-array-result` entry names `emptyStore_at`, `lengthStore_spec`, and `finishProgram_spec`, so it is used again, and no new LTG entry was inspected or rejected.  The next construction follows the checked capacity and allocator regions, writes a zero length at the allocated root, transfers that root through local five to local six, and proves the empty-array representation.

The import check passed.  The Lean build accepted the entire invalid branch through `finishProgram_spec`, including capacity eight, the allocator premises, the zero-length store, and both destination-local bounds.  Its sole residual goal is the final local-six getter after `branchPost` clears the value stack; the getter already proved for `finishFrame` has identical parameters and locals.

The next edit transports that getter across the value-stack reset using `Wasm.Locals.get`, then applies `emptyStore_at` with the RuntimeReady result bounds.  A successful build after this edit would leave no theorem goal.  I will still run the mandated final import and build pair once more after the first successful pair, without editing the candidate between the two successful builds.

The import check passed after the final candidate edit, and the Lean build completed both `Behavior` and `ArtifactResult` successfully.  Acceptance uses the shared declarations `FixedArrayLengthDispatch.leProgram`, `FixedArrayCapacity.constantProgram_spec`, `FixedArrayAllocatorWindow.region_spec_withTail`, `FixedArrayFold.forwardSetupProgram_spec`, `FixedArrayTraversalInput.continuingProgram_exit_spec`, the generated continuing and scalar evaluator theorems, `ScalarTransition.guardedBackEdgeProgram_spec`, `ArrayFold.foldPrefix_succ`, `ArrayFold.foldPrefix_size`, and the `FixedArrayResult` store and transfer theorems.  The valid branch carries the prefix product and remaining-length measure through the compiled loop, the invalid branch constructs the empty array, and no residual goal remains.  I will now run the required final import check and build pair without editing `Behavior.lean`.
