# Proof Journal

The first required build ran before any edit and failed at the deterministic starter's final `TerminatesWith` goal.  The starter has already decomposed `RuntimeReady` into allocator globals, input representation, input-length read and bound facts, the result-region memory bound, and the WebAssembly page limit.  The proof must now execute exported function 0 and establish the expected output array.

Inspection of the frozen `Program` shows one exported function with sixteen locals and no calls.  The input-length guard selects a successful branch that allocates `8 * (input.size + 1)` bytes, writes the result length, and loops over the input with a wrapped `UInt64` increment, or an invalid branch that allocates eight bytes and writes length zero.  The successful allocator uses the cataloged shifted allocator window at offset 2, while the empty-result allocator uses offset 0 with two trailing locals.  I will apply `FixedArrayAllocatorWindow` to both allocator regions and reserve the explicit loop invariant for the map loop.

The second build exposed a misuse of the store-parametric `wp_entry` helper: it introduced a fresh store, so none of the concrete `RuntimeReady` facts applied to the function body.  I replaced it with `TerminatesWith.of_wp_entry_for`, stated the generated sixteen-local entry frame explicitly, and kept the concrete initial store.  The input representation proves the encoded size comparison needed to select the successful branch.

The third build reported that the `simp` following `wp_run` made no progress.  `wp_run` had already used the length read and comparison facts while reducing the entry instructions.  I removed the redundant simplification so the next diagnostic will show the first remaining control boundary.

The fourth build showed that `wp_run` retained arithmetic in the local-frame lookup because the entry frame used `List.replicate`.  The tactic's restricted simplification expanded the list but did not normalize the resulting length expressions.  I replaced the replicated frame with the exact literal sixteen-local frame used by the generated function.

The fifth build retained the same entry lookup after literalizing the frame.  The restricted `wp_run` simplifier leaves `Nat` expressions such as `0 + 1` in this Lean version, preventing the surrounding matches from reducing.  I will follow `wp_run` with a focused ordinary simplification using the checked input address, length bound, length read, and encoded comparison facts.

The sixth build reached the expected outer `iff` with condition one and the successful branch intact.  The ordinary simplification normalized the local frame and discharged the checked length load.  The next proof step applies the `iff` rule, selects the condition-one arm, and reduces the successful branch's capacity calculation up to its minimum-capacity guard.

The seventh build reached the minimum-capacity `iff`.  Its local frame contains the emitted rounded capacity `(8 + UInt64.ofNat input.size * 8 + 7) / 8 * 8` in combined local 11, and the following instructions begin the offset-two allocator region.  I will prove that this capacity has natural value `8 * (input.size + 1)`, select the empty minimum-capacity adjustment branch, and match the exact remaining allocator prefix to `FixedArrayAllocatorWindow.region 2 1`.

The eighth build identified three local proof issues before allocator application.  Case enumeration did not unfold the local capacity definition inside its dependent expression, `Nat.le` lacks the projected `not_lt` name I used, and the empty arm of `wp_iff_cons` still required reduction through `wp []` before the allocator suffix became the current program.  I replaced case enumeration with the standard `UInt64.toNat` arithmetic lemmas, derive the negated comparison with `omega`, and reduce the empty arm before changing to the named allocator decomposition.

The ninth build showed that the capacity arithmetic still contained unreduced `UInt64.toNat` constants, so `omega` could not bound either modulo operation.  It also showed that simplification expresses the minimum-capacity guard as an unsigned `8 ≤ capacity` fact, and that broad `wp_run; simp` began executing the allocator prefix through `globalGet 1`.  I will give the constant conversions explicitly, state the unsigned capacity bound, and use only `wp_nil` plus stack normalization to expose the untouched allocator region.

The tenth build accepted the capacity arithmetic and exact allocator decomposition.  The remaining allocator premises are local: the minimum-capacity guard needs the emitted expression rather than its local abbreviation, and `decide` cannot close frame-length propositions containing arbitrary local values.  Replacing those with `simpa [capacity] using hCapacityU` and `rfl` leaves the allocator theorem's `hNext` continuation at `mapSuffix` over `allocStore` and `allocFrame`.

The eleventh build discharged the complete allocator theorem and symbolically executed the result-length store.  The current obligation is its generated address bound paired with the block-wrapped map loop over the allocated store.  I will prove the bound from the allocator bump facts, then use an invariant carrying the exact loop frame, the preserved input representation, the fixed result-length word, and the already-written output prefix.

The twelfth build accepted the loop theorem application and exposed its initialization and step obligations.  The invariant definition first needs unchecked array notation because the logical implication supplies the index proof after elaboration.  For initialization, the bump facts identify the generated length-store address with `(heapTop + 48).toUInt32`; the allocator frame is then exact, the allocator and length stores preserve the input below `heapTop`, and the length word reads back by the standard same-address theorem.

The thirteenth build proved invariant initialization and reduced one loop iteration to the generated size comparison.  In the exit case, the invariant's complete prefix directly reconstructs `UInt64Array.At` for `input.map (· + 1)`.  In the repeat case, `generatedElement` supplies the checked input load, `BumpFacts.wordAddress` identifies the output store address, and read-over-write disjointness preserves the input, result length, and earlier output words while the measure decreases from `input.size - index` to `input.size - (index + 1)`.

The fourteenth build reduced the repeat case through the invariant reconstruction and found three presentation mismatches.  The exit comparison occurs inside the scrutinee of a `match`, so `simp only [if_pos]` must reduce it instead of `rw`; `UInt32.ofNat(...).toNat` had already reduced before an explicit `simp only`; and the result-length frame proof needs the separate root-address `toNat` equality because the zero-word expression simplifies to the bare root.  These changes preserve the semantic argument.

The fifteenth build showed that substituting `index = input.size` already reduces the reflexive loop guard, making the explicit simplification redundant.  The only repeat-case failure is the output-address proof: applying `UInt32.toNat.inj` reintroduced an unhelpful `UInt32.ofNat` modulo term.  The bump fact's checked `wordAddress` equality matches the generated address directly after removing the known non-wrapping root modulo and normalizing multiplication order.

The sixteenth build accepted the full repeat case, including address equality, framing, prefix extension, and measure decrease.  The exit goal still displays the reflexive comparison inside a `Value.i32` list scrutinee; neither direct `rw if_pos` nor a standalone `simp only` traversed that presentation.  I will first prove an equality for the complete `UInt32` conditional and rewrite that subterm, which reduces the match to the public result obligation.

The seventeenth build accepted the loop exit and left one successful-branch arithmetic rewrite: the final array's encoded byte size must be rewritten from `8 * (input.size + 1)` back to `capacity.toNat` before applying `BumpFacts.fit32`.  I will now prove the oversized-input branch with the offset-zero allocator window and an eight-byte capacity, followed by its zero length store and empty-array representation.  The offset-zero layout uses combined locals 5 and 9 through 14, leaving the final two locals untouched.

The eighteenth build completed the successful branch and applied the offset-zero allocator theorem in the oversized-input branch.  One constant conversion is missing from the proof that the encoded input size exceeds eight.  The allocator continuation is now the eight-instruction `emptySuffix`; its length-store bound and final empty-array representation follow from a second `bumpFacts` value specialized to capacity eight.

The nineteenth build succeeded for `LeanExeGen.GeneratedRc77513211b55010d.ArtifactResult`.  The proof establishes both size branches, uses the decoded map loop with a decreasing index measure for every successful element, applies the checked allocator-window theorem to both allocations, and reconstructs the exact `Array UInt64` representation.  I will remove two unused simplifier arguments reported by the linter and perform the required final rebuild without further candidate edits.
