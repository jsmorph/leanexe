# Proof Journal

The initial prescribed build ran against the unchanged deterministic starter and failed in `Behavior.lean` at the expected `TerminatesWith` goal for function 0.  The starter has already decomposed `RuntimeReady` into the input `UInt64Array.At` fact, input-address facts, allocator globals, result-region fit bounds, and the page limit.  The remaining proof must execute a 456-instruction, 16-local function with three loops; no direct recipe or checked composition appears in `PROOF_RECIPES.json`.

Inspection of `Program.lean` shows that the valid branch computes capacity `8 * (input.size + 1)`, allocates through the exact `FixedArrayAllocatorWindow.region 2 1` sequence, stores the result length, and maps the input with a block-wrapped indexed loop.  The invalid branch allocates capacity eight through the offset-zero allocator instruction sequence but keeps a 16-local frame, while the catalog theorem requires exactly 14 locals.  I will use the checked offset-two allocator theorem for the valid branch, a prefix-read invariant for the mapping loop, and direct symbolic execution of the empty-free-list bump path in the invalid branch.

The first edited check found two definition errors before reaching the intended branch: the prefix predicate indexed the input without passing its bound proof, and the loop measure used an ambiguous dotted constructor in a dependent pattern.  It also showed that `wp_run` does not reduce the entry frame while the local list remains expressed as `List.replicate`.  I will give the prefix index its derived proof, qualify `Value.i64`, and expose the literal 16-word local list used by `func0Def.toLocals`.

The second edited check confirmed that a proof embedded inside a conjunction does not enter the elaboration context of a later conjunct, so the dependent array index still lacked its bound.  The entry frame now appears literally, but the generic `wp_run` simplifier still stops before reducing the first local operations.  I will state the prefix over an explicit input-bound proof and use the same focused local-frame simplification set as the checked allocator and loop proofs.

The third edited check accepted the invariant and measure definitions and reduced the entry instructions to concrete list lookups plus the length load.  Its remaining term identifies two missing simplifier inputs: `List.getElem?_cons_zero` and the three named input-length facts.  I will parameterize the focused execution tactic, add the list lemma, and supply the checked address, bound, and read equalities at the load.

The fourth edited check reaches the outer `iff` with the exact compiler condition `UInt64.ofNat input.size ≤ 8`, the input pointer saved in combined local 5, and an empty stack beneath the Boolean.  This confirms that the focused tactic has discharged the entry load without expanding either branch.  I will split on `input.size ≤ 8`, prove the encoded comparison from `UInt64Array.At.size_lt`, and execute each selected branch only to its capacity-normalization `iff`.

The fifth edited check selects both outer branches and stops at their capacity-normalization `iff` instructions.  In the valid branch, the computed value appears as `((8 + UInt64.ofNat input.size * 8 + 7) / 8) * 8`; in the invalid branch it has already reduced to eight.  I will prove the valid capacity has natural value `8 * (input.size + 1)`, select both no-adjustment arms, and apply `FixedArrayAllocatorWindow.region_spec` at offset two to the exact valid suffix.

The sixth edited check found that the initial arithmetic proof left constant `UInt64.toNat` terms and modular products opaque to `omega`.  It also showed that selecting an empty `iff` branch leaves a `wp []` boundary, so the allocator suffix is not yet definitionally visible.  I will normalize the capacity with a targeted `simp` using the `UInt64` arithmetic equations, state the constant comparison goals with `change`, and reduce `wp_nil` before matching either allocator region.

The seventh edited check proves the capacity arithmetic and confirms the valid allocator match: the theorem continuation starts at the emitted length store with `FixedArrayAllocator.allocStore` and `FixedArrayAllocatorWindow.allocFrame 2`.  Two numeric premises failed only because `decide` cannot close propositions containing free values, and `rfl` suffices for both literal-list lengths.  I will name the allocator result state, derive one `BumpFacts`, preserve the input through header and length writes, and initialize the block-loop invariant after the checked length-store theorem.

The eighth edited check accepted every allocator premise and exposed a structural mismatch at the length store: this artifact loads the dynamic length from combined local 6, while `FixedArrayResult.lengthStoreProgram` embeds a constant.  The emitted store still has the same memory effect, and `BumpFacts` supplies its checked address and bound.  I will execute that four-instruction store and the loop-counter initialization directly, rewriting the generated address to the named `writeLength` state before applying the loop rule.

The ninth edited check stopped inside the first local lookup because the focused simplifier kept the locally named `allocFrame` folded.  The diagnostic confirms that no semantic premise is missing: it is waiting to reduce combined locals 7, 6, and 8 in that exact frame.  I will expose only `allocFrame` and its checked constructor during this short execution region, while retaining the named allocator store and loop invariant.

The tenth edited check reduces all three locals and stops at the store bound, presented with `heapTop.toNat + 48` rather than `(heapTop + 48).toNat`.  The existing facts use the latter form because they follow the WebAssembly pointer, while `BumpFacts.rootToNat` proves their equality.  I will derive generated-form address and bound facts once from `rootToNat` and pass those exact forms to the execution tactic.

The eleventh edited check reaches `wp_loop_cons` and attempts the invariant initialization.  The interpreter now presents the stored address as `heapTop.toUInt32 + 48`, whereas `writeLength` uses `(heapTop + 48).toUInt32`; the bump fit bound makes these equal without either 32-bit or 64-bit wrapping.  I will prove that address equality, transport the preserved-input fact to the exact generated store, and close the page and length-read fields without broad simplification.

The twelfth edited check reports one parser error in the explicit record update and one unfinished constant reduction in the address proof.  The address goal has reduced to `heapTop.toNat + UInt32.toNat 48 = heapTop.toNat + 48`, so a named `rfl` fact for the constant closes it after the two no-wrap modulo rewrites.  I will parenthesize the record-update value and use explicit heap-top and root 32-bit bounds to control those rewrites.

The thirteenth edited check closes the generated length-store address and reveals the complete loop-step goal.  The only initialization failure is an expensive `simpa` between the generated record update and `writeLength`; a directed rewrite and `change` avoid that search.  I will add one checked small-index equality between the compiler's address expression and the ABI element address, then handle loop exit by constructing `UInt64Array.At` from the invariant and handle the active case from `generatedElement`.

The fourteenth edited check reaches the active loop back edge after proving the input load and executing the emitted payload store.  Its remaining conjunction is exactly the generated output bound, the invariant at `index + 1`, and the decreasing measure.  I will derive the write address from the single `BumpFacts` value, preserve the input and length through the disjoint write, extend the output-prefix read predicate, and normalize the counter addition without unrolling the loop.

The fifteenth edited check leaves two local issues in the valid branch: the exit branch has reduced to a concrete nonzero-`br_if` match whose second arm is the public result, and another record-update value needs parentheses.  The active-case argument is otherwise elaborating through its generated bound and invariant construction.  I will select the concrete exit arm with `simp only`, supply the already proved output representation, and correct the record syntax.

The sixteenth edited check confirms the active branch's memory proof and isolates two expensive frame reductions.  The next frame differs from the expected `mapFrame` only by `UInt64.ofNat index + 1 = UInt64.ofNat (index + 1)`, and the measure compares the two encoded counters at local-list index seven.  I will replace both broad simplifications with directed rewrites and the two `List.getElem?` constructor equations, and let `refine` reduce the concrete exit match directly.

The seventeenth edited check proves the complete valid branch, including loop initialization, both loop paths, the decreasing measure, the wrapping payload write, and the public result.  The remaining goal starts at the invalid branch's allocator and has the expected 16-local frame with capacity eight in combined local nine.  I will execute the empty-free-list search and existing-memory bump path directly, using one `Allocation.bumpFacts heapTop 8` value for every allocator arithmetic obligation.

The eighteenth edited check proves the invalid allocator's initialization and empty-free-list search loop.  The nonzero `br_if 1` exits both loop wrappers and leaves the bump branch with the original store and the expected local values.  I will select the no-overflow and no-grow paths from `hBump`, then establish the six header-store bounds from its root and header-offset equations.

The nineteenth edited check selects the allocator's bump arm and proves that the computed top does not wrap below the heap base.  As with the capacity-normalization branches, the interpreter leaves the selected empty `iff` arm as an explicit `wp []` boundary before the page-count computation.  I will reduce that boundary, execute through `memorySize`, and discharge the page comparison with `hBump.noGrow`.

The twentieth edited check reaches `memorySize` after computing the required page count in combined local thirteen.  Reduction stops because the interpreter retains the module's `memIs64` field in `sizeValue`, even though the frozen module defines 32-bit memory.  I will rewrite that field to `false`, select the no-grow arm with `hBump.noGrow`, and continue to the header writes.

The twenty-first edited check shows that rewriting `memIs64` before symbolic execution is premature because the term does not occur until the `memorySize` rule expands.  The frozen equality belongs in the focused simplifier's lemma set, allowing it to rewrite the field as soon as that rule introduces it.  I will pass a named `hMemory32` fact to the execution step and retain the existing no-grow proof.

The twenty-second edited check reaches the page-growth `iff`, but the focused simplifier normalizes the extended `UInt32` page count to `UInt64.ofNat pages % 2^32`.  `BumpFacts.noGrow` states the equivalent comparison with `(UInt32.ofNat pages).toUInt64`.  I will prove those page words equal by `UInt64.toNat` injectivity and the checked page limit, then rewrite the comparison before applying `noGrow`.

The twenty-third edited check found two precise issues in that conversion.  Substitution in the loop step renamed the original store to `st`, and the `iff` condition remains the compiler's normalized Boolean `(if comparison then 1 else 0) ≠ 0`.  I will state the page-word equality over `st.mem.pages` and simplify the normalized Boolean with both that equality and `hBump.noGrow`.

The twenty-fourth edited check accepts the normalized Boolean argument and leaves only the proof of the page-word equality.  Its final natural-number goal contains the unreduced constant `(4294967296 : UInt64).toNat`, so `omega` cannot infer that reduction.  I will name the constant reduction, rewrite it, and apply `Nat.mod_eq_of_lt` using the page limit.

The twenty-fifth edited check proves the page-word equality, selects the no-grow arm, and reaches the six header stores.  The local frame now contains the checked top and required-page values, while the store remains unchanged.  I will derive the base and five shifted header bounds from `hBump.rootToNat` and its five header-offset projections, then use those facts to execute the stores and allocator finish.

The twenty-sixth edited check executes all six header stores, both allocator-global updates, the returned-root assignment, the empty-array length store, and the public local assignments.  The residual goal contains the five normalized shifted-header bounds, the result-length bound, and `UInt64Array.At` for the empty result in the exact final memory.  I will transport the stored header bounds through the `BumpFacts` offset equations and construct the empty-array representation from the final root store.

The twenty-seventh edited check accepts every allocator bound and the complete final-state array proof.  Its sole diagnostic is an unknown lemma name, `UInt64.ofNat_zero`, in a `simp only` list; the zero conversion is definitional and needs no named theorem.  I will state the length-read goal with an explicit `UInt64` zero, remove the unused-bound warning in the address helper, and run the prescribed final build.
