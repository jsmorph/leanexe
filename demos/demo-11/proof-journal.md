# Proof Journal

## 2026-08-10: Initial check

I ran the unchanged candidate with the prescribed 60-minute Lean build command.  The build reached `Behavior.lean` and failed with one unsolved `TerminatesWith` goal for function 0 after the starter decomposed `RuntimeReady`.  The context contains the represented input, the six allocator globals, the input and output memory bounds, the page bound, and the derived length, address, heap-top, free-list, and allocation-counter facts.

I began LTG retrieval by reading `LTG/categories.json`.  The current goal points to the arrays, loops, compiler-motifs, allocation, memory, and proof-construction indexes.  I will inspect `PROOF_RECIPES.json` before reducing any internal instruction because the request requires an exact direct recipe or complete composition first.

## 2026-08-10: Initial retrieval

`PROOF_RECIPES.json` contains no complete composition.  Its direct length-dispatch recipe is `wp_fixed_array_length_le_dispatch_from hArray at 7, 8`, which should expose the valid and invalid branches, while its direct fold recipe names `Wasm.wp_loop_cons` and the checked `function_0_array_fold_0_eq` interval.  The fold recipe also names checked setup, continuing-load, scalar-transition, singleton-result, frame-accessor, and fold-prefix declarations that can close the loop without instruction-level execution.

I searched the arrays, compiler-motifs, and proof-construction indexes for `length-dispatch|leProgram_spec|wp_fixed_array_length_le_dispatch_from`.  I searched the arrays, loops, compiler-motifs, and proof-construction indexes for `array-fold|foldPrefix|singletonResultProgram_spec|continuingGuardedProgram_spec|guardedBackEdgeProgram_spec`.  I also searched the allocation, arrays, and memory indexes for `constantProgram_spec|capacityFrame|bumpFacts|singleton` because both length branches allocate a result array.

The searches selected `fixed-array-length-dispatch`, `fixed-array-capacity`, `array-fold-prefix`, `fixed-array-fold-structure`, `fixed-array-fold-body`, `guarded-back-edge`, and `compact-loop-suffix-boundary` for inspection.  Each selected entry matches a declaration or exact annotation kind named by the recipe.  I rejected the singleton-wrapper and counter-transfer entries because this artifact contains no direct call or scalar post-test annotation, and I deferred the general result-store and allocation entries until a residual goal shows that the fold singleton-result theorem does not cover their regions.

I inspected both metadata files for all seven selected entries.  The length-dispatch entry confirms that `Wasm.TerminatesWith.of_wp_entry_for` must convert the public goal before the generated dispatch equality and tactic apply, and the capacity entry identifies the exact branch prefixes at lengths one and zero.  The fold-prefix, fold-structure, fold-body, and guarded-back-edge entries provide the complete semantic path from setup through a loop-carried accumulator to the checked singleton suffix, while the compact-loop-suffix entry supplies a smaller declaration boundary if the public proof exhausts resources.

I inspected the recipe-named declarations in `PROOF_KIT_SOURCE` and the generated equalities in `AnnotationMatches.lean`.  The artifact fold uses accumulator local 1, item local 2, index local 13, effective-stop local 15, root local 7, result local 10, destination local 4, and return local 6, and its scalar descriptor applies `UInt64.xor` before incrementing the traversal index.  The first proof edit will apply `of_wp_entry_for`, rewrite `function_0_length_dispatch_0_function_eq`, and invoke the exact dispatch tactic, with separate bounded and oversized branch goals constituting progress.

## 2026-08-10: Direct dispatch attempt

The import check succeeded after the first proof edit.  The Lean build rejected the generated function equality because the target still displayed `func0Def.body`, so it contained no syntactic occurrence of `func0`.  The residual goal remains a body-level `wp` goal, and no semantic obligation has worsened.

The inspected `fixed-array-length-dispatch` entry gives the exact remedy for this presentation boundary: change the body-level target to `Wasm.wp _ func0 _ _ _ _` before rewriting the generated function equality.  This uses the recipe's named body rather than reducing the decoded instruction list.  The next edit adds that change, and progress remains the two branch obligations produced by the dispatch theorem.

The import check succeeded after adding the body-level `change`, and the build reached the exact valid and invalid branch obligations.  Five preceding goals ask for the entry frame's parameter list, empty value stack, writable local bound, and maximum-size bound.  This confirms that the direct dispatch recipe has the intended shape and improves the proof boundary.

Before reducing those frame facts, I searched the proof-construction, compiler-motifs, arrays, and loops indexes for `toLocals|Function.toLocals|hParams|local-frame|frame.*params|locals.*length|func0Def`.  The search found only `local-frame-projection`, whose declarations convert an established combined getter into an internal-list projection and do not state entry-frame parameters or length, so I rejected it for these goals.  The dispatch entry instead directs normalization with the generated function definition, `Function.toLocals`, `Function.numParams`, and `ValueType.zero`; the next edit uses those named definitions and introduces the two size hypotheses without opening either branch program.

The import check and build confirmed that the five structural dispatch premises close by the selected normalization.  The only residual goals now begin at the exact generated valid and invalid branch programs under hypotheses `input.size ≤ 8` and `¬ input.size ≤ 8`, respectively.  No frame-structure premise remains at the dispatch boundary.

The recipe gives `FixedArrayCapacity.constantProgram_spec` as the next direct theorem for both branches, together with the exact generated capacity equalities for lengths one and zero.  I will refold each branch prefix to `constantProgram 1 1 11` or `constantProgram 0 1 11` and apply that theorem before addressing its premises.  Reaching the post-capacity frame in each branch will establish progress and reveal the next exact region boundary.

The import check succeeded, and both applications of `constantProgram_spec` matched.  Each branch now has three structural premises for the dispatch frame and one continuation from `capacityFrame`; the valid branch normalizes to capacity 16, and the invalid branch normalizes to capacity 8.  The continuation begins at the shifted fixed-array allocator with capacity local 11, root local 7, scratch window ending at local 16, and four unused trailing locals.

I searched the allocation, arrays, memory, compiler-motifs, and proof-construction indexes for `region_spec_withTail|region_spec|bumpFacts|empty-free-list|lengthStore_spec|emptyStore_at|singletonStore_at|payloadStore_spec|input_preserved_by_alloc`.  I inspected `fixed-array-allocation`, `array-memory-framing`, and `fixed-array-result`, which match this residual allocator, the need to preserve the input before the fold, and the length and payload stores.  All three entries will be used: `FixedArrayAllocatorWindow.region_spec_withTail` for offset 2 and tail 4, `FixedArrayPairResult.input_preserved_by_alloc` for the valid branch, and the `FixedArrayResult` representation and store theorems for branch completion.

Before proving the allocator's capacity getter, I checked `PROOF_RECIPES.json` again and searched the allocation, arrays, compiler-motifs, and proof-construction indexes for `capacityFrame.*get|hCapacityLocal|capacity-local|capacity local|allocFrame.*get|root.*getter`.  The recipe names `capacityFrame` but no capacity-frame getter theorem, and the LTG search returned no exact accessor.  I will therefore reduce this concrete projection with the generated entry frame and the named `branchFrame` and `capacityFrame` definitions; reaching the `allocFrame` continuation in both branches will count as progress.

The imports passed their check, and `region_spec_withTail 2 4` matched both residual allocator programs exactly.  Lean left the semantic allocator parameters metavariable-valued because the theorem's conclusion does not determine the heap top, capacity, or allocation counter from the instruction sequence.  The resulting goals confirm the expected `capacityFrame` structure and end at the valid singleton-length store and invalid empty-length store.

The next edit fixes the allocator parameters to `(heapTop, 16, 1, allocs)` in the valid branch and `(heapTop, 8, 1, allocs)` in the invalid branch.  I will use the named parameter, local-length, and value facts for `branchFrame` and `capacityFrame`, reduce only the missing concrete capacity projection, and discharge memory fit from `hOutputFitMemory` after simplifying `FormalSpec.expected` with the branch hypothesis.  The residual length-store goals in the two named allocation stores will constitute progress.

The import check succeeded, and fixing the semantic parameters closed the frame shape, memory fit, page, memory-mode, and global premises.  `norm_num` left four closed `UInt64` computations: the two normalized-capacity projections and the two capacity lower bounds.  The only semantic residuals are the expected valid length-one store followed by the fold and the invalid length-zero store followed by final root transfer.

The closed machine-word goals contain no variables and require no new accessor or semantic theorem; kernel decision closes them without changing the proof boundary.  I will then complete the invalid branch with `lengthStore_spec`, `emptyStore_at`, and `finishProgram_spec`, using one `Allocation.bumpFacts` value for the returned-root address and bounds.  A residual public postcondition after the final suffix, or complete closure of the invalid branch, will count as progress.

The import check succeeded, kernel decision closed all four machine-word goals, and the invalid branch now closes completely.  `emptyStore_at` proves the result representation after the zero-length store, `finishProgram_spec` transfers the root through locals 5 and 6, and the one-instruction dispatch suffix returns that root to the public postcondition.  The build's only unsolved goal is the valid branch continuation after allocation.

I will now name the XOR fold's exact generated loop frame, invariant, and measure.  The valid branch will apply `lengthStore_spec`, preserve the input with `input_preserved_by_alloc` and `At.write64After`, apply `forwardSetupProgram_spec`, and enter the block-wrapped loop with `Wasm.wp_loop_cons`.  The initial loop-state equality and the iteration goal produced by the loop theorem will constitute the next diagnostic boundary; generated frame accessors from the fold recipe will handle later parameters, local lengths, operand values, and getters.

The import check succeeded, and the valid branch reached `Wasm.wp_loop_cons` with the named XOR invariant and index-based measure.  The initial frame reduced to the two definitional equalities `heapTop + 48 = root` and `0 = UInt64.ofNat 0`, while the iteration goal retained the complete public singleton-result continuation.  The exact loop program decomposition therefore matches the artifact, but the displayed iteration boundary is too large for focused work.

I searched the loops, compiler-motifs, proof-construction, and arrays indexes for `singletonResultPost|Wasm.wp.conseq|large postcondition|postcondition|suffix theorem|compact-elaboration-boundary`.  The search selects the already inspected `compact-loop-suffix-boundary` entry and confirms its direct recommendation: prove the singleton suffix against `FixedArrayFold.singletonResultPost`, then place the consequence to the branch and public postcondition in a separate declaration.  I will name the public entry assertion and prove that compact consequence before constructing the loop iteration with `continuingGuardedProgram_spec`.

The new compact consequence theorem elaborated, but the public proof's combined `change` failed because it asked Lean to reduce `func0Def.body` and fold the new `publicPost` definition in one conversion.  The diagnostic occurs before the loop, and no semantic goal changed.  The next edit restores the previously accepted body-only `change` and then folds `publicPost` as a separate presentation step.

The body-only change succeeded, but this Lean environment has no `fold` tactic.  The remaining goal shows `func0` and the exact assertion lambda, confirming that only the assertion presentation remains.  I will use a second `change` for `publicPost` now that the function body has already normalized.

The assertion-only `change` also failed, so the entry theorem's anonymous postcondition is propositionally equivalent to `publicPost` but not definitionally identical.  I will use `Wasm.wp.conseq` at the body boundary and prove the small continuation implication by cases, which preserves the named compact assertion for the rest of the proof.  This addresses the presentation mismatch without unfolding any artifact instruction.

The entry-level consequence succeeded, the loop initialization now closes, and the only residual goal is the loop step under the named `publicPost`.  The postcondition display is smaller, but the exit still needs the generated singleton suffix and its frame projections.  I will add a separate `xorSingleton_spec` theorem that applies `singletonResultProgram_spec` to an exit `xorFrame`, using the recipe-named generated parameter, local-length, value-stack, and getter accessors plus `resultFrame_get_result` and `resultFrame_get_of_ne`.

`xorSingleton_spec` passed the import check and Lean check without a residual goal.  It uses the exact generated singleton-result program, the generated frame accessors, and the shared result-frame getters, so the enclosing loop no longer needs to reduce the result-local update or root preservation.  The remaining goal is still the single loop-step function.

I will split that step on `index = input.size`.  The exit uses `continuingProgram_exit_spec`, `xorSingleton_spec`, and the separate `singletonPost_conseq`; the continuing case uses `FixedArrayFoldBody.continuingGuardedProgram_spec` with the generated XOR body, false condition, index increment, and loaded-frame equality.  The continuing callback will rebuild `xorInvariant` at `index + 1` with `ArrayFold.foldPrefix_succ` and prove strict decrease of `xorMeasure`.

The exit theorem and compact singleton composition both apply.  The remaining exit mismatch is a frame normalization inserted by the enclosing zero-result block: Lean displays explicit parameters and locals with an empty value stack, while `hSuffix` uses the propositionally equal generated frame.  I will close that equality with the recipe-supplied `function_0_array_fold_0_continuing_frame_params` and `function_0_array_fold_0_continuing_frame_values` accessors, preserving the local list definitionally.

The first normalization edit contained an incomplete `Locals` record literal, so Lean reported a parser error before it could check the equality and consequently lost the later invalid-branch proof structure.  The intended record has explicit `params`, `locals`, and `values` fields.  I will add the empty `values` field and rerun the same semantic boundary.

Adding `values` did not repair the parser error, which shows that this inherited frame structure does not accept the proposed fresh-record syntax at this point.  I will remove the temporary literal and use `convert hSuffix using 1`, then prove the resulting frame equality by extensionality with the generated parameter and value-stack accessors.  Success means that the exit branch closes without unfolding the frame's local list.

The import check succeeded, and `convert` reduced the exit mismatch to the intended `Locals` equality.  Lean reports that `Locals` has no registered extensionality theorem, so the generated parameter and value-stack facts cannot be applied through the generic `ext` tactic.  The only other residual remains the unimplemented continuing branch, which confirms that the exit composition itself now has the right shape.

Before proving the frame equality another way, I will search `PROOF_RECIPES.json`, the proof-construction and compiler-motifs LTG indexes, and `PROOF_KIT_SOURCE` for a named `Locals` equality, eta, or field-congruence theorem.  An exact theorem will be preferred; otherwise I will use structure cases plus the two generated projection facts.  Closing this equality and exposing only the continuing branch will count as progress.

I searched those sources for `Locals.(ext|mk|eta)`, `Locals` equality and congruence terms, frame equality and eta terms, and combinations of `params`, `locals`, and `values`.  The LTG indexes and proof kit provide no `Locals` equality theorem; `Frame.lean` contains getter-to-local projection lemmas, which do not apply, while `ScalarTransition.State.toLocals` confirms the exact three-field construction.  Because the generated continuing frame is definitionally `toState.toLocals []`, I will try reflexivity at the equality left by `convert`, avoiding a full expansion of the twenty locals.

The import check succeeded, and reflexivity closed the exit-frame equality and the complete exit branch.  The build now reports only the continuing case `index ≠ input.size`, with the exact concatenation of the annotated traversal prefix and guarded XOR back edge.  This is the residual goal expected after the loop split.

For this checkpoint I revisited the exact fold composition in `PROOF_RECIPES.json`, the previously inspected `fixed-array-fold-body` entry, and `FixedArrayFoldBody.continuingGuardedProgram_spec`.  I also searched the local proof kit and relevant LTG indexes for `ofNat` addition, successor, and order lemmas, `bitXor`, and `foldPrefix_succ`; the useful declarations are `UInt64.ofNat_add`, `UInt64.lt_iff_toNat_lt`, `UInt64Array.At.size_lt`, `ScalarTransition.U64Op.apply`, and the recipe-named `ArrayFold.foldPrefix_succ`.  I will instantiate the combined theorem with the generated loaded, body, condition, and continuing states; reaching only its false-result callback will confirm the complete composition before I prove the invariant and measure.

The import check succeeded, and the combined theorem matched the concatenated artifact programs.  The first application left dependent hypotheses such as item validity and the indexed bound later in Lean's goal order, so positional bullets shifted and produced type mismatches even though the generated loaded, body, and condition terms have the requested types.  I will name every premise through `hCondition` and pass it by argument name, using the recipe-named generated item-validity and loaded-frame declarations; this should leave only the impossible true-result callback and the continuing invariant callback.

The named-premise version passed through the combined theorem and exposed its single false-result callback.  One preceding local `hLoaded` still needs the definitional expansion of the `accumulator` abbreviation and the generated `continuing_frame` constructor; the diagnostic shows no semantic discrepancy.  The callback contains exactly the scalar index-increment evaluation, reconstructed fold invariant, and strict suffix-length measure required by the recipe.

I searched the loops, arrays, and proof-construction indexes for `measure`, `decrease`, `back edge`, `foldPrefix_succ`, index-successor terms, and `loop-carried`.  The results confirm the already inspected `array-fold-prefix`, `fixed-array-fold-body`, and `guarded-back-edge` entries; all three apply here, while `counter-transition` concerns a decrementing UInt64 post-test loop and `euclidean-gcd-loop` concerns a remainder measure, so I rejected both.  Before reducing the measure getter I also checked the recipe's exact accessor list and selected `function_0_array_fold_0_continuing_frame_get_13`; the next edit will use that accessor, `foldPrefix_succ`, and the generated step evaluator to prove the back edge.

## 2026-08-10: Acceptance

The final import check succeeded, and the prescribed artifact build completed all 3,082 jobs, including `Behavior` and `ArtifactResult`.  The proof composes the shared length-dispatch, capacity, allocator-window, result-store, fold-setup, loop, traversal-input, scalar-transition, singleton-result, array-representation, and allocation declarations, together with the generated annotation equalities, frame accessors, and transition evaluators.  `ArrayFold.foldPrefix_succ` establishes the loop-carried XOR update, `ArrayFold.foldPrefix_size` identifies the completed fold, and no residual goal remains.
