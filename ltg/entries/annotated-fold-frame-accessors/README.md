# Generated fold-frame accessors

A matched `leanexe.array.fold.v1` region emits `<region>_continuing_frame_params`, `<region>_continuing_frame_locals_length`, `<region>_continuing_frame_values`, and one `<region>_continuing_frame_get_<index>` theorem for every combined parameter-and-local index.  Each theorem states an exact property of the generated scalar frame and checks by reduction in the artifact annotation module.  The fold recipe lists these artifact-specific declarations, so search the recipe before unfolding `ScalarTransition.U64State`, `Wasm.Locals.get`, or the internal local list.

When an application defines a semantic abbreviation around the generated continuing frame, use `simp only [<semantic-frame>, <generated-accessor>]`.  The restricted simplifier unfolds the semantic abbreviation and applies the exact accessor without reducing `Wasm.Locals.get` first.  An unrestricted `simp` may expand that getter into parameter and internal-list branches before the generated theorem can match.

Use `FixedArrayFold.resultFrame_get_result` after accumulator placement to read the written result local.  Use `FixedArrayFold.resultFrame_get_of_ne` to preserve a distinct nonparameter local, including a result root needed by the singleton suffix.  Discharge their frame-length premises with the generated parameter and local-length theorems instead of unfolding the complete generated state.

Treat the first residual frame-projection goal as a new retrieval checkpoint, even when an earlier query selected the surrounding fold or result theorem.  Search the recipe for `_continuing_frame_` declarations and the category indexes for `exact-local-get` or `frame` before introducing a local fact by reduction.  Record whether an exact accessor matched so later analysis can distinguish retrieval failure from an absent theorem.

The Demo 9 manual substitution removed ten proof-local frame-projection theorems and passed independent verification over the unchanged artifact.  Its proof decreased from 588 to 555 lines and from 2,484 to 2,084 whitespace-delimited words, while the longer generated declaration names limited the byte reduction to 682 bytes.  Demo 10 independently checks the same generated accessor family.

A fresh Demo 9 proving agent selected this entry and used the generated shape and getter declarations with both shared result-frame theorems.  Its proof declared no local frame-projection theorem, but Stage 5 took 2,230.869 seconds and the source contained 616 lines, compared with 2,074.169 seconds and 588 lines for the preceding fold-body reproof.  This result establishes retrieval and use while supplying negative proof-time and source-size evidence; the remaining revisions concerned dispatch, allocation, loop initialization, result construction, and dependent composition.

A fresh Demo 10 agent used the generated value-stack and index getters and both shared result-frame getter theorems without retrieving this entry by name.  It instead discharged eleven frame facts by reduction despite receiving the exact accessor names and selected strategy guidance.  Its 2,099.237-second, 600-line proof was slower than the preceding fold-body reproof but fifty lines shorter, supplying cross-operation use evidence and the retrieval-checkpoint rule above.

Held-out Demo 11 applied the retrieval-checkpoint rule to a bitwise-XOR fold.  Its journal searched the recipe and catalog before reducing a capacity projection, recorded that no capacity accessor existed, and later used the full generated fold-frame family with both result-frame theorems.  Independent verification accepted the 676-line proof in 2,452.384 seconds, providing a third accumulator operation and negative proof-time and proof-size evidence.

The entry is promoted because independently verified proofs use the generated interface across addition, multiplication, and bitwise XOR, including complete use in two fresh sessions.  Promotion records recurring applicability and stable checked declarations.  The measurements support no proof-time improvement, and later work should address compact public-continuation and completed-frame equality boundaries before another timing screen.
