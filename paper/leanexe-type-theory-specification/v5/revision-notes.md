# Editorial revision

This replacement addresses all four remarks on accepted submission `d75fc8acf1bc`, marXiv `2609.00005`.  The accepted source and PDF remain in v4.

The structure-recognition paragraph identifies the `className` predicate and lists every declaration in `nonRuntimeEvidenceTypeNames`.  The primitive table names `validateModuleReleases` and links its explanation.  The recursion inventory identifies the first retained function parameter as the Nat fuel argument.  The ownership-mask paragraph begins with the missing 64-slot bound.  These changes clarify the accepted report's definitions and evidence.  They add no implementation change, Lean run, runtime result, or proof claim.
