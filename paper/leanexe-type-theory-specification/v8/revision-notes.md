# Fifth archive revision

Archive version 4 was accepted with two remarks.  This revision identifies the diagnostic report command's `Classification` record and complete extraction's `Except String IR.Module` result.  It also states allocation containment and lifetime as separate requirements: the allocation contains the visible byte range and remains live while that range is used.  The source checkpoint, technical claims, title, authors, and abstract remain the same.
