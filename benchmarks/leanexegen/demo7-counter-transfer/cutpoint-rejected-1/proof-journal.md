# Proof Journal

The untouched candidate passed `PROOF_IMPORT_CHECK.js`.  The first resource-limited Lake build failed at the three deterministic starter cut points.  The goals are the scalar helper's `TerminatesWith` property, `FormalSpec.expected input = input` under `input.size ≠ 1`, and the singleton equation under `input.size = 1`.  I will retain the starter's complete fixed-array singleton-wrapper theorem and use the scalar-loop recipe before proving the two formal equations directly.

`PROOF_RECIPES.json` selects `AnnotationMatches.function_0_scalar_post_test_loop_0_terminates_with_of_loop` followed by `ScalarTransition.postTestProgram_spec`.  The checked compact transition shows that a zero remaining counter sets the exit flag, while a nonzero counter becomes `remaining - 1` and the result becomes `result + 1`.  I will use the invariant `remaining + result = value`, the measure `remaining.toNat`, `CounterTransition.decrement_add_increment`, and `CounterTransition.decrement_toNat_lt`.  The complete singleton-wrapper composition already proves the array load, scalar call boundary, allocator, result stores, and public return, so no lower-level array or allocator recipe is needed.

The first edited import check passed.  The Lake build stopped at a parser error because Lean accepts `=>`, rather than `⇒`, after `fun` in the two local definitions.  Parsing stopped before elaborating the loop or either array equation, so this diagnostic does not change the proof approach.

After correcting the lambdas, the import check passed and the build reached the zero-counter exit case.  Lean could not infer the existential `afterCondition` state from an underscore placed before the condition-evaluation proof.  The generated condition transition preserves its input state, so I will state that compact state explicitly in both the exit and repeat branches.  No transition equation or invariant obligation failed in this run.

With the explicit states, the import check passed and elaboration advanced through both generated transition equations and the exit continuation.  The only failure was `simp only [if_false]` making no progress because the false branch had already reduced to the invariant-and-measure conjunction.  I will remove that redundant simplification and let `constructor` address the conjunction directly.

The next import check passed.  The build reduced the measure goal to growth of the result counter, which exposed a coordinate error: `State.localU64ToNat` indexes the `locals` list, while the annotation names combined locals.  The remaining counter is combined local 3 and therefore `State.locals[2]`, since the function has one parameter.  I will change the measure index from 3 to 2; the generated transition and sum invariant remain valid.

After correcting the measure index, the import check passed and the complete `ArtifactResult` target built successfully.  `function_0_scalar_post_test_loop_0_terminates_with_of_loop` and `ScalarTransition.postTestProgram_spec` now prove the scalar callee, using the generated `body_eval` and `condition_eval` equations for both branches.  The non-singleton formal equation reduces by reflexivity, while the singleton equation follows from array extensionality and the size-one hypothesis.  I will leave `Behavior.lean` unchanged and run the prescribed final command pair.
