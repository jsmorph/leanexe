# Proof Journal

The initial prescribed artifact build failed at `Behavior.lean:14:116`, as expected for the deterministic starter.  The RuntimeReady decomposition produced the input representation, allocator globals, memory bounds, and page bound.  After the starter rewrote with `AnnotationMatches.function_1_singleton_wrapper_0_eq`, Lean left one `wp` goal for the complete singleton-wrapper program under `FixedArrayPairResult.publicPost (FormalSpec.expected input)`.

`PROOF_RECIPES.json` selects the version-one fixed-array singleton-wrapper composition and `FixedArraySingletonWrapper.wrapperProgram_spec`.  The checked region equality identifies all of `func1` with `wrapperProgram 0`, while the wrapper theorem reduces the public proof to the scalar callee and two semantic array equations.  I applied that theorem with `id` as the scalar transform, preserving the complete wrapper region rather than reducing its dispatch, load, allocator, or result stores.

The import check succeeded, and the following artifact build reduced the proof to the three documented premises of `wrapperProgram_spec`.  The invalid-input equation is definitional because `FormalSpec.expected` is the identity, and the valid-input equation requires the standard size-one array extensionality argument.  The substantive premise is a store-preserving identity theorem for function zero, which the scalar post-test recipe reduces to an invariant over the remaining and result counters.

After adding the counter invariant, both prescribed checks reached the scalar recipe application, and the import check again succeeded.  Lean reported an instruction-presentation mismatch between the named annotated program and `ScalarTransition.postTestProgram`; the two are definitionally equal, but the theorem application did not unfold the named descriptor.  I unfolded only `function_0_scalar_post_test_loop_0_program` at that boundary so that `postTestProgram_spec` sees its exact expected instruction list.

The next import check succeeded, while the build showed that the instruction lists now agree exactly.  Lean left the postcondition implicit in `postTestProgram_spec`, preventing elaboration from unifying the theorem result with the match-shaped postcondition introduced by `terminates_with_of_loop`.  I supplied that same fallthrough-and-return postcondition explicitly, including the one-result `take`, without changing the semantic boundary.

The subsequent import check succeeded, and Lean printed the instantiated recipe conclusion and target in the same normalized form.  The remaining failure arose while `refine` elaborated both proof arguments as metavariables at once, before turning them into focused goals.  I changed this theorem use to `apply`, which lets the tactic unify the conclusion first and then creates the invariant initialization and transition goals.

The import check remained successful, but `apply` exposed the same elaboration failure even though Lean printed identical conclusions.  I separated construction of the complete `postTestProgram_spec` term from its use: the invariant proofs now fill the theorem's named `hInit` and `hStep` arguments, after which a focused simplification normalizes the initial `State.toLocals` presentation.  This keeps the checked scalar transition theorem and avoids reducing the loop instructions.

The latest import check succeeded, and the build accepted the post-test theorem application and both generated transition equations.  Two representation goals remained: the zero-counter exit suffix still contained unreduced accesses through the generated compact-state constructor, and the final scalar theorem boundary retained the same constructor on both sides.  I added the generated state definition and `U64State.toState` to those two focused simplifications so that Lean compares explicit local frames.

The next import check succeeded, and the build discharged the zero-counter suffix after reducing the generated compact state.  Only the final use of the completed post-test theorem remained; simplification printed its type and the goal identically, indicating that recursive definitional unification inside `wp` was the obstacle rather than a semantic discrepancy.  I replaced that final simplification with `convert hSpec using 1`, which compares the outer theorem arguments separately and leaves any real mismatch as an explicit equality.

The import check succeeded again, and `convert` isolated one equality between the two textually identical match-shaped postconditions.  This confirms that the scalar semantics, program, store, and initial frame all agree.  The residual function equality closes by reflexivity, so I added that focused proof without changing either postcondition.

The following import check succeeded, but direct reflexivity did not unfold the continuation match under the two function binders.  The diagnostic still printed identical branches for fallthrough, return, and the impossible continuations.  I changed the equality proof to function extensionality followed by cases on the continuation, which reduces each match branch before reflexivity.

The next import check and artifact build both succeeded.  `postTestProgram_spec`, the generated body and condition evaluations, `CounterTransition.decrement_add_increment`, and `CounterTransition.decrement_toNat_lt` prove that function zero preserves the store and returns its input.  `FixedArraySingletonWrapper.wrapperProgram_spec` then proves the exact public wrapper, with direct identity equations for `FormalSpec.expected`; the required final check pair follows this journal entry.
