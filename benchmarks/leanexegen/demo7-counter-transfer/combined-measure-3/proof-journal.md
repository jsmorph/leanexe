# Proof Journal

The first unchanged build failed at the deterministic starter's final `wp` goal.  The starter had already decomposed `RuntimeReady`, changed the generated array representation to `UInt64Array.At`, entered function 1, and rewritten it with `AnnotationMatches.function_1_singleton_wrapper_0_eq`.  The residual goal was the complete `FixedArraySingletonWrapper` program with `FixedArrayPairResult.publicPost (FormalSpec.expected input)`.

`PROOF_RECIPES.json` selects the `fixed-array-singleton-wrapper-v1` composition and `Project.ProofKit.FixedArraySingletonWrapper.wrapperProgram_spec`.  Its scalar dependency is function 0's checked `leanexe.loop.scalar-post-test.v1` region.  I proved that callee through the generated `function_0_scalar_post_test_loop_0_terminates_with_of_loop` theorem and supplied it to the wrapper theorem.

The first edited build succeeded.  The scalar proof applies `Project.ProofKit.ScalarTransition.postTestProgram_spec` with an invariant that fixes the original argument and states `remaining + result = value`; its measure reads the remaining counter from combined local 3.  The generated `body_eval` and `condition_eval` theorems expose the two transitions, while `CounterTransition.decrement_add_increment` preserves the invariant and `CounterTransition.decrement_toNat_lt` proves strict decrease.  The zero branch executes the checked post-loop suffix and returns the result counter, and the wrapper theorem then proves both public length branches, the scalar call, and singleton result allocation.  `FormalSpec.expected` closes the invalid branch by reflexivity, and `Array.ext` closes the singleton equation from `input.size = 1`.
