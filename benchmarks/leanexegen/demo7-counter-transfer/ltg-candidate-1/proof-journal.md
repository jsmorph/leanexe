# Proof Journal

The initial import check succeeded.  The prescribed artifact build then failed at the deterministic starter's final goal: `wp module AnnotationMatches.function_1_singleton_wrapper_0 (FixedArrayPairResult.publicPost (FormalSpec.expected input)) initial (FixedArraySingletonWrapper.entryFrame inputPtr) env`.  The RuntimeReady decomposition, public-postcondition conversion, and checked complete-wrapper equality all succeeded.  I will prove function 0 at its generated scalar post-test-loop boundary, then supply that callee theorem to `Project.ProofKit.FixedArraySingletonWrapper.wrapperProgram_spec`.

The checked `leanexe.loop.scalar-post-test.v1` recipe supplies `function_0_scalar_post_test_loop_0_terminates_with_of_loop`, the exact post-test program, and generated `body_eval` and `condition_eval` equations.  The loop executes one administrative exit iteration when the remaining counter is zero.  On every repeat edge, the remaining counter is nonzero before the body and decreases by one, so `State.localU64ToNat` at `State.locals` index 2, which corresponds to combined local 3, is a valid measure.  The invariant will retain an exact generated U64 state and the wrapping equation `remaining + result = input`; `CounterTransition.decrement_add_increment` preserves that equation, and `CounterTransition.decrement_toNat_lt` proves the decrease.

The first edited build reduced the proof to two local issues in the repeat branch.  The generated `body_eval` equation states subtraction and addition through `ScalarTransition.U64Op.apply`, while the invariant uses the ordinary `UInt64` operations; adding `U64Op.apply` to that focused simplification closes the representation difference.  The `if false` selected by the generated condition had already reduced before the next tactic, so the explicit `simp only [if_false]` attempted to simplify an existing conjunction and should be removed.  No wrapper or public-specification goal failed.

The next import check and artifact build succeeded.  `Project.ProofKit.FixedArraySingletonWrapper.wrapperProgram_spec` consumed the store-preserving scalar theorem and the existing RuntimeReady array, allocator-global, memory-fit, and page facts.  The remaining formal equations reduce to identity for a nonsingleton input and array extensionality for a singleton input, completing the exact public behavior without expanding the wrapper's loader, allocator, or result stores.
