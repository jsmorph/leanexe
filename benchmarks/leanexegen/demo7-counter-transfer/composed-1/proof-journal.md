# Proof Journal

The initial prescribed build failed in `Behavior.lean` at the final wrapper goal.  The deterministic starter has already reduced `ArtifactSpec`, decomposed `RuntimeReady`, converted the generated array predicate to `Project.ProofKit.UInt64Array.At`, and rewritten function 1 with `AnnotationMatches.function_1_singleton_wrapper_0_eq`.  The remaining goal is `wp module AnnotationMatches.function_1_singleton_wrapper_0 (FixedArrayPairResult.publicPost (FormalSpec.expected input)) initial (FixedArraySingletonWrapper.entryFrame inputPtr) env`, so the complete `FixedArraySingletonWrapper.wrapperProgram_spec` composition is the first applicable recipe.

The wrapper theorem requires a store-preserving semantic theorem for scalar function 0.  I used `AnnotationMatches.function_0_scalar_post_test_loop_0_terminates_with_of_post_test`, with an invariant that represents the complete generated `U64State` and states that the wrapping sum of local 3's remaining counter and local 4's result counter equals the input.  The measure is `ScalarTransition.State.localU64ToNat state 2`, and the nonzero transition uses `CounterTransition.decrement_add_increment` and `CounterTransition.decrement_toNat_lt` after rewriting with the generated `body_eval` and `condition_eval` equations.

The first edited check passed `PROOF_IMPORT_CHECK.js` and failed in the nonzero scalar transition.  Rewriting `body_eval` left only equalities between `ScalarTransition.U64Op.apply .sub` or `.add` and the corresponding `UInt64` operations, so the focused fix unfolds `ScalarTransition.U64Op.apply` as the recipe directs.  The Boolean `false` supplied to the post-test premise had already reduced its conditional, which made a following `simp only [if_false]` redundant, and I removed that command.

The next import check and artifact build both succeeded.  `scalar_terminates` now proves that function 0 returns its input and preserves the store, while `FixedArraySingletonWrapper.wrapperProgram_spec` proves the complete function 1 behavior from that callee theorem.  The remaining wrapper obligations reduce to the formal identity definition and the elementary fact that an array of size one equals the singleton containing its zero-indexed element.
