# Proof Journal

The initial prescribed build checked the untouched deterministic starter and failed in `Behavior.lean` after the `RuntimeReady` decomposition.  The sole residual goal is `wp module AnnotationMatches.function_1_singleton_wrapper_0 (FixedArrayPairResult.publicPost (FormalSpec.expected input)) initial (FixedArraySingletonWrapper.entryFrame inputPtr) env`, with the array representation, allocator globals, memory bounds, heap-reserve bounds, and page bound available.  This confirms that the starter has already applied the checked `function_1_singleton_wrapper_0_eq` composition boundary.

The selected complete composition is `Project.ProofKit.FixedArraySingletonWrapper.wrapperProgram_spec`.  I will attempt it before the lower-level length-dispatch, direct-call, and scalar-loop recipes, preserving `function_1_singleton_wrapper_0` as the current folded program.

The import-policy check accepted the first candidate edit.  The subsequent build showed that `FixedArraySingletonWrapper.wrapperProgram_spec` matches the folded wrapper exactly.  It reduces the public proof to the existing array, memory, page, module-width, and allocator facts; a store-preserving scalar-callee theorem returning its input; and the two direct equations for `FormalSpec.expected`.  The complete composition therefore supplies the correct proof boundary, so I will retain it and prove the scalar helper separately with the checked `leanexe.loop.scalar-post-test.v1` recipe.

For the scalar recipe, `function_0_scalar_post_test_loop_0_terminates_with_of_loop` reaches the exact loop boundary, and `ScalarTransition.postTestProgram_spec` accepts the checked body and condition descriptors.  The planned invariant represents the generated 16-word scalar state existentially, fixes operand 3 as the remaining counter and operand 4 as the result counter, and asserts `remaining + result = value` in `UInt64`.  The measure is the natural value of local slot 2, which corresponds to WebAssembly operand 3 after the single parameter.  The generated `body_eval` and `condition_eval` equations should split the zero exit from the decrement/increment repeat, while `CounterTransition.decrement_add_increment` and `decrement_toNat_lt` discharge preservation and decrease.

The import check accepted the invariant and scalar theorem scaffold.  The build confirms that `function_0_scalar_post_test_loop_0_terminates_with_of_loop` followed by `ScalarTransition.postTestProgram_spec` matches without any program restructuring.  Its only scalar goals are invariant initialization and the expected step: evaluate the checked body, evaluate the checked condition, prove the exact three-instruction return suffix at the zero exit, or re-establish the invariant with a smaller local-slot-2 measure after a nonzero decrement.  No fallback proof-kit module is needed.

The next import check succeeded.  The scalar proof then discharged both generated transition equations, the zero-case return suffix, invariant preservation through `CounterTransition.decrement_add_increment`, and strict decrease through `CounterTransition.decrement_toNat_lt`.  Lean reported only that `simp only [if_false]` made no progress after the explicit `false` witness had already reduced the conditional.  I will remove that redundant tactic and then connect the proved scalar theorem and the direct formal-specification facts to the wrapper composition.

After removing the redundant simplification, the import-policy check succeeded and the prescribed artifact build completed all 3,078 jobs.  `scalar_identity` proves function 0 store-preserving and identity-valued for every `UInt64`; `wrapperProgram_spec` then proves function 1, using the existing output-memory bound and allocator facts.  `FormalSpec.expected` supplies the invalid-length equation by reflexivity, while structural analysis of a size-one array supplies the singleton equation.  The selected modules and checked annotations were sufficient, and no lower-level wrapper recipe or fallback module was needed.
