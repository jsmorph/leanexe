# Proof Journal

The initial prescribed `ArtifactResult` build failed at the deterministic starter's final goal.  After the starter decomposed `RuntimeReady`, converted the generated array predicate to `Project.ProofKit.UInt64Array.At`, and rewrote function 1 with `AnnotationMatches.function_1_singleton_wrapper_0_eq`, Lean required `wp module AnnotationMatches.function_1_singleton_wrapper_0 (FixedArrayPairResult.publicPost (FormalSpec.expected input)) initial (FixedArraySingletonWrapper.entryFrame inputPtr) env`.  This confirms that the complete `fixed-array-singleton-wrapper-v1` composition is the intended public-function boundary.

`PROOF_RECIPES.json` selects `Project.ProofKit.FixedArraySingletonWrapper.wrapperProgram_spec` for that complete wrapper.  Its scalar dependency is function 0's checked `leanexe.loop.scalar-post-test.v1` region; the generated `function_0_scalar_post_test_loop_0_terminates_with_of_post_test` theorem should reduce the callee proof to an application invariant, measure, and one checked transition.  I inspected those generated declarations before editing `Behavior.lean`.  I then added a private store-preserving proof for scalar function 0.  Its invariant represents the exact generated scalar state and asserts `remaining + result = value`; `State.localU64ToNat` at combined local 3 supplies the measure.  The zero case executes the generated no-op body and the three-instruction return suffix, while the nonzero case uses `CounterTransition.decrement_add_increment` and `CounterTransition.decrement_toNat_lt`.  The public theorem applies `FixedArraySingletonWrapper.wrapperProgram_spec` with the identity transform and proves its singleton equation through `Array.size_eq_one_iff`.

The prescribed import check succeeded after this edit.  The candidate imports only the frozen generated specification, Program and annotation support, plus `Project.ProofKit.FixedArraySingletonWrapper`; the annotation support provides the selected scalar-transition declarations.  The following `ArtifactResult` build reached line 75 and failed because `simp only [if_false]` made no progress in the nonzero transition branch.  My first reading incorrectly associated line 75 with the earlier zero-case suffix; the numbered source shows that supplying `false` as the existential condition result had already reduced the conditional definitionally.  I removed that redundant simplification and retained the following conjunction proof.

The import check succeeded after removing `simp only [if_false]`.  No import or declaration restriction changed.  The next build reduced the measure goal to `nextResult.toNat < result.toNat`, revealing an indexing error in my measure.  `State.localU64ToNat` indexes `State.locals`, while the generated annotation numbers combined parameters and locals; generated combined local 3 is `State.locals[2]`.  I changed the measure index from 3 to 2 so that it follows the remaining counter.

The import check succeeded with the corrected local-list index.  The edit changes only the loop measure.  The subsequent `ArtifactResult` build succeeded for all 3,078 jobs.  `Behavior.lean` proves the scalar call with `function_0_scalar_post_test_loop_0_terminates_with_of_post_test` and proves the public array function with `FixedArraySingletonWrapper.wrapperProgram_spec`.  The only diagnostics were unused-variable warnings in the frozen generated `AnnotationMatches.lean`; the candidate produced no warning or error.

The final source review found no `sorry`, axiom, admission, or `Source` import.  Because the proof refers directly to `UInt64Array.At`, I added the requested direct `Project.ProofKit.Array` import instead of relying on the wrapper module's transitive imports.  This dependency edit does not change the proof terms or theorem boundaries; the prescribed final import check and full artifact rebuild remain.
