# Proof Journal

The initial prescribed Lean build failed at the deterministic starter's final goal.  The starter had already reduced `artifact_behavior` to `wp module AnnotationMatches.function_1_singleton_wrapper_0 (FixedArrayPairResult.publicPost (FormalSpec.expected input)) initial (FixedArraySingletonWrapper.entryFrame inputPtr) env`.  This confirms that the checked `function_1_singleton_wrapper_0_eq` composition applies to the complete exported wrapper and that no instruction-level wrapper proof is needed.

`PROOF_RECIPES.json` selects `FixedArraySingletonWrapper.wrapperProgram_spec` for the complete public function and `AnnotationMatches.function_0_scalar_post_test_loop_0_terminates_with_of_post_test` for the scalar helper.  The wrapper theorem requires a store-preserving proof that function 0 returns a chosen scalar transform, plus equations for the valid and invalid array branches.  I will use the identity transform, prove the scalar helper through the generated post-test transition theorem, and discharge the wrapper equations from `FormalSpec.expected`.

The first proof edit passed `PROOF_IMPORT_CHECK.js`, so its imports remain within the permitted set.  The Lean build stopped while elaborating `scalarInv` because the pipeline notation around the generated compact state's `toState` conversion captured the following conjunction.  I replaced that notation with a parenthesized method call; the later invariant and wrapper obligations have not yet been checked because elaboration stopped at this definition.

After the syntax correction, Lean reached the scalar transition proof and showed that broad simplification had preserved the existential successor-state choices instead of choosing them.  The zero case's diagnostic displayed the exact generated state with exit flag one, while the nonzero diagnostic displayed the mapped compact state before its witness had been supplied.  I now name each successor state explicitly, rewrite `body_eval` and `condition_eval` at those states, use `decrement_add_increment` for invariant preservation, and use `decrement_toNat_lt` for the nonzero back edge.

The explicit successor witnesses discharged both generated evaluator equations.  Two administrative goals remained: the zero-case continuation still contained the local `after` abbreviation when `wp_run` needed a concrete frame, and the nonzero branch's `if false` had already reduced before the explicit simplification command.  I unfold the generated compact frame before executing the three-instruction scalar suffix and remove the redundant simplification from the back-edge branch.

The scalar proof now closes the exit continuation, the successor invariant, and both evaluator equations.  Its remaining measure goal exposed an indexing distinction in `ScalarTransition.State.localU64ToNat`: the function indexes the local list rather than combined parameter-and-local positions.  Generated combined local 3, which stores `remaining`, is local-list index 2, so I corrected the measure from 3 to 2.

With the corrected measure, the complete artifact target built successfully.  `FixedArraySingletonWrapper.wrapperProgram_spec` accepted the store-preserving scalar theorem, the runtime memory and allocator facts, and the direct identity equations for both array-length branches.  I removed one unused scalar-operation simplifier argument and will now run the prescribed import check followed by the full artifact build as the final verification.
