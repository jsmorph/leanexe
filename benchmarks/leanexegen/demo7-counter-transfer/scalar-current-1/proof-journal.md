# Proof Journal

The required initial build failed at the deterministic starter's final goal.  All `RuntimeReady` facts had already been decomposed, and the remaining goal was `wp module AnnotationMatches.function_1_singleton_wrapper_0 (FixedArrayPairResult.publicPost (FormalSpec.expected input)) initial (FixedArraySingletonWrapper.entryFrame inputPtr) env`.  This confirms that the complete `fixed-array-singleton-wrapper-v1` composition is the intended next proof boundary.

`PROOF_RECIPES.json` selects `FixedArraySingletonWrapper.wrapperProgram_spec` for the complete public function and `ScalarTransition.postTestProgram_spec` for function 0.  The wrapper theorem reduces the public proof to a store-preserving scalar theorem, the invalid-size identity equation, and the singleton equation.  I will prove the scalar theorem through the generated `function_0_scalar_post_test_loop_0_terminates_with_of_loop`, using an invariant that records the remaining and result counters and the equation `remaining.toNat + result.toNat = argument.toNat`.

The first edited candidate passed `PROOF_IMPORT_CHECK.js`.  The Lean build rejected the loop measure's `.i64` pattern because the expected value type was not inferred there, and two later `omega` calls consequently referred to unresolved goals rather than the intended counter arithmetic.  I will replace the pattern match with `Wasm.Value.i64 remaining`, which fixes the measure's codomain before reassessing those arithmetic diagnostics.

The explicit value constructor fixed the measure elaboration, and the import check passed again.  Lean now reaches only the invariant-preservation equation `(v3 - 1) + (v4 + 1) = value` and the strict measure decrease `(v3 - 1).toNat < v3.toNat`.  `omega` treats the modular UInt64 operations as opaque here, so the first obligation needs additive-group normalization and the second needs a UInt64 subtraction-to-Nat lemma or an equivalent checked conversion.

The modular sum now closes with `UInt64.add_comm`, `UInt64.add_assoc`, and `UInt64.sub_add_cancel`.  `UInt64.sub_lt` reduces the measure proof to `1 ≤ v3`; the sole diagnostic is that `norm_num` did not rewrite `(1 : UInt64).toNat`.  The core theorem `UInt64.toNat_one` supplies that missing normalization, while `UInt64.pos_iff_ne_zero` already converts the loop's nonzero branch into positivity.

After adding the explicit `UInt64.toNat_zero` and `UInt64.toNat_one` rewrites, `PROOF_IMPORT_CHECK.js` passed and the complete `ArtifactResult` build succeeded.  The scalar callee proof now uses `function_0_scalar_post_test_loop_0_terminates_with_of_loop` and `ScalarTransition.postTestProgram_spec`; its exit branch executes only the checked post-loop suffix, and its repeat branch proves both the modular counter invariant and strict measure decrease.  `FixedArraySingletonWrapper.wrapperProgram_spec` then proves the public singleton dispatch, checked input load, store-preserving call, allocation, singleton result, and unchanged invalid-size branch.
