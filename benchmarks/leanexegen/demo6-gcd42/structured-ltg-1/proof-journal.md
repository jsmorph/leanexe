# Proof Journal

The initial prescribed Lean build failed at the deterministic theorem starter, after the `RuntimeReady` decomposition and array-representation conversion.  The remaining goal is `wp module AnnotationMatches.function_1_singleton_wrapper_0 (FixedArrayPairResult.publicPost (FormalSpec.expected input)) initial (FixedArraySingletonWrapper.entryFrame inputPtr) env`, with the decomposed globals, bounds, and representation hypotheses in context.  The starter therefore reaches the intended artifact-specific wrapper boundary, and the proof now needs a theorem or complete composition for that wrapper.

LTG retrieval began at `LTG/categories.json`, whose arrays, compiler-motifs, allocation, and proof-construction categories match the wrapper goal.  I searched those four category indexes for `singleton_wrapper|FixedArrayPairResult|publicPost|entryFrame|function_1`.  The query returned no entry, so no LTG entry was inspected or rejected from this search; the missing catalog metadata is an index term connecting singleton-wrapper annotations or `FixedArraySingletonWrapper` declarations to their proof support.

`PROOF_RECIPES.json` supplies the complete composition `fixed-array-singleton-wrapper-v1`: rewrite with `AnnotationMatches.function_1_singleton_wrapper_0_eq`, then apply `Project.ProofKit.FixedArraySingletonWrapper.wrapperProgram_spec`.  I inspected that selected theorem in its named dependency module; it reduces the current goal to the pure callee theorem, the invalid-branch equation, and the singleton expected-value equation while using the existing array, memory, page, and allocator-global hypotheses.  The same recipe file selects `ScalarTransition.postTestProgram_spec` and the generated entry, body-evaluation, and condition-evaluation theorems for function 0, whose frozen annotation is the imperative Euclidean remainder loop.

I searched the arrays, compiler-motifs, loops, and proof-construction indexes for `wrapperProgram_spec|postTestProgram_spec|ScalarTransition|scalar-post-test|Euclid|Nat.gcd|gcd`.  The relevant canonical entry was `scalar-post-test-loop`, whose `entry.json` and `README.md` confirm the direct generic loop theorem, generated transition equations, application invariant, and decreasing measure.  I used that entry for the helper boundary; I rejected `counter-transition`, `demo7-counter-transfer-example`, and `demo8-counter-transfer-example` from their index summaries because they concern counter-transfer arithmetic rather than the Euclidean remainder transition.

Following the scalar-loop entry's related-entry metadata, I searched all category indexes for `fixed-array-singleton-wrapper|leanexe.array.singleton-wrapper.v1`.  I inspected and used the canonical `fixed-array-singleton-wrapper` and `residual-specification-normalization` entries; they confirm the whole-wrapper theorem and the two expected-result equations, including `Array.size_eq_one_iff` for the singleton branch.  The query also returned the same two counter-transfer examples, which I rejected without opening because their index metadata identifies a different scalar computation.

I searched every category index for `Euclid|euclid|gcd|remainder|remU|rem-u|Nat.gcd`.  No LTG entry covers the Euclidean invariant or the needed `Nat.gcd` remainder identity.  The catalog therefore supplies the checked scalar-loop execution theorem but lacks application-level GCD guidance, so the helper proof must state that invariant from Mathlib's named GCD theorem after confirming an appropriate declaration.

The first edit applied the exact `wrapperProgram_spec` composition with transform `fun value => UInt64.ofNat (Nat.gcd value.toNat 42)`.  The import-boundary check succeeded, and the prescribed Lean build reduced the artifact theorem to the store-preserving function-0 result plus the two expected-result equations; this is the intended smaller proof boundary.  The helper goal is `TerminatesWith env module 0 initial [.i64 value] (fun final results => final = initial ∧ results = [.i64 (UInt64.ofNat (Nat.gcd value.toNat 42))])`, so the direct wrapper recipe improved the boundary and remains in the proof.

A temporary declaration check confirmed `Nat.gcd_rec (m n) : gcd m n = gcd (n % m) m`, `UInt64.toNat_mod`, `UInt64.ofNat_toNat`, and `Array.size_eq_one_iff`; I then removed those checks.  The next edit added `gcdInvariant`, instantiated the recipe's generated termination adapter and `ScalarTransition.postTestProgram_spec`, and closed both wrapper-result equations.  The import check succeeded, and Lean left only the loop transition goal, with the invariant equation `gcd v3.toNat v4.toNat = gcd value.toNat 42` and measure `v4.toNat`; the missing proof is now exactly the zero exit or nonzero remainder step.

The complete loop proof splits on the second Euclidean accumulator.  The zero branch returns the first accumulator, while the nonzero branch uses `UInt64.toNat_mod`, `Nat.gcd_rec`, and GCD commutativity to preserve the invariant, and `Nat.mod_lt` to decrease the measure.  The import-boundary check and the prescribed artifact-result build both succeeded, and the final audit found no `sorry`, `admit`, axiom, Source reference, or temporary diagnostic declaration in `Behavior.lean`.
