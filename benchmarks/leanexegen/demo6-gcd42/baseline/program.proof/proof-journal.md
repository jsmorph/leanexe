# Proof Journal

The initial prescribed `ArtifactResult` build failed in `Behavior.lean` at the deterministic starter's final goal.  After the `RuntimeReady` decomposition, conversion to `Project.ProofKit.UInt64Array.At`, public-postcondition consequence, and `AnnotationMatches.function_1_singleton_wrapper_0_eq` rewrite, the remaining goal is `wp module AnnotationMatches.function_1_singleton_wrapper_0 (FixedArrayPairResult.publicPost (FormalSpec.expected input)) initial (FixedArraySingletonWrapper.entryFrame inputPtr) env`.  This is the complete fixed-array-singleton-wrapper boundary described by the task guidance, so I will preserve it and supply `FixedArraySingletonWrapper.wrapperProgram_spec` with a proof for the scalar Euclidean-loop callee.

`PROOF_RECIPES.json` supplies the version-one `fixed-array-singleton-wrapper-v1` composition and names `Project.ProofKit.FixedArraySingletonWrapper.wrapperProgram_spec`.  `AnnotationMatches.function_1_singleton_wrapper_0_eq` proves the whole decoded function has that form with callee index zero.  The checked wrapper theorem requires a store-preserving scalar theorem and two equations for the formal result, and it covers the length dispatch, input read, call, allocation, singleton stores, and public return.

The frozen scalar helper is function zero.  Its straight-line prefix initializes the Euclidean state to `(x, 42)`, and its block-wrapped loop exits when the second component is zero and otherwise updates the state to `(b, a % b)`.  Neither `PROGRAM_ANNOTATIONS.json` nor `PROOF_RECIPES.json` contains a scalar-loop annotation, generated `condition_eval` or `body_eval` theorem, or generated `terminates_with_of_loop` theorem for function zero.  I therefore need to instantiate `Wasm.wp_loop_cons` directly.  A generated annotation for this loop would have removed the need to recover its frame transition from the 106 decoded instructions.

The first scalar edit passed `PROOF_IMPORT_CHECK.js`; the subsequent `ArtifactResult` build failed at the new loop proof, as expected.  The `hInit` diagnostic reduced the semantic portion to the right initial Euclidean pair but retained an unconstrained entry store `initial'`, and the loop-step display showed that `wp_entry_to_loop` carried the entire unreduced prefix frame into the block continuation.  I will replace that convenience tactic with the explicit `TerminatesWith.of_wp_entry_for` entry used successfully by the public starter, discharge its store-and-argument premise, and normalize the prefix before applying the loop rule.

The second scalar edit also passed the import check.  The build now discharges loop initialization and leaves only `hStep`, with the current store, Euclidean values `a` and `b`, all scratch words, and `hGcd` exposed; the program starts at the first loop instruction over the folded `scalarFrame`.  The explicit entry theorem and frame refolding therefore fixed the entry-store and prefix-normalization problems, so the next edit will split on `b = 0` and reduce each concrete control path.

The third scalar edit passed the import check and split the loop step into zero and nonzero divisor cases.  `wp_run` did not unfold the task-local `scalarFrame`, so both diagnostics stopped inside the first `Locals.get` and `Locals.set?` reductions instead of at the first WebAssembly conditional.  I will unfold only `scalarFrame` at the loop-step boundary, then resume control-flow reduction; no semantic change is needed.

The fourth scalar edit passed the import check, and unfolding `scalarFrame` exposed concrete parameter and local lists.  The `wp_run` simplification set still retained arithmetic expressions such as `0 + 1 + 1` in local-index bounds, so it stopped before reducing the concrete list operations.  A focused ordinary simplification with `scalarFrame` and the divisor-case fact will normalize those indices and should leave the first structured `iff` as the next proof boundary.

The fifth scalar edit passed the import check and reached the first structured conditional in both divisor cases.  The zero case has an operand-stack condition of `i32 1`, while the nonzero case has `i32 0`, confirming the recovered control predicate.  I will apply `Wasm.wp_iff_cons` at that boundary and use the same case-specific simplification to reach the next conditional.

The sixth scalar edit passed the import check.  One `Wasm.wp_iff_cons` application selected the expected branch and reduced to the second Boolean-normalization conditional, with the case conditions reversed as dictated by the emitted equality and `eqz` sequence.  Three more checked conditional steps reach the application branch: the zero case copies `(a, 0)`, while the nonzero case enters the guarded remainder calculation.

The seventh scalar edit passed the import check and reached those two application-specific points.  The zero branch has copied `(a, 0)` into locals 16 and 17 and now begins the emitted done-flag normalization; the nonzero branch has saved `(a, b)` in locals 13 and 14 and now presents the guarded remainder `iff` with a false zero-divisor condition.  After selecting the remainder branch, both paths share four done-flag conditionals before exiting or repeating the loop.

The eighth scalar edit passed the import check and discharged every instruction-level loop obligation.  The exit branch now asks only for `a = UInt64.ofNat (Nat.gcd input.toNat 42)` under `hGcd : Nat.gcd a.toNat 0 = Nat.gcd input.toNat 42`, and the repeat branch asks for the invariant and measure over the exact next frame `(b, a % b)`.  These are the standard Euclidean identities: `Nat.gcd_rec` after `UInt64.toNat_mod`, and the strict modulo bound from `b ≠ 0`.

The ninth scalar edit passed the import check and the build accepted `scalar_correct` in full.  The only remaining diagnostic is the deterministic starter's complete singleton-wrapper `wp` goal.  I will now apply the named composition theorem `FixedArraySingletonWrapper.wrapperProgram_spec`, using `scalar_correct` for callee zero, `hOutputFitMemory` for its result-region bound, the existing page and allocator-global facts, and direct simplification of `FormalSpec.expected` in the valid and invalid length cases.

The wrapper-composition edit passed `PROOF_IMPORT_CHECK.js`, and the `ArtifactResult` build completed successfully.  `FixedArraySingletonWrapper.wrapperProgram_spec` accepted `scalar_correct`, the result-region memory bound, the module and allocator facts, and both `FormalSpec.expected` equations without residual goals.  The completed proof establishes function zero's store-preserving Euclidean loop and composes it with the exact checked function-one wrapper; I will now perform the required final validation pair without further candidate edits.
