# Proof Journal

The initial required build failed in `Behavior.lean` at the exact deterministic-starter goal.  The starter has already reduced `RuntimeReady`, converted the generated array predicate to `Project.ProofKit.UInt64Array.At`, entered function 1, and rewritten the complete wrapper with `AnnotationMatches.function_1_singleton_wrapper_0_eq`.  The remaining goal is `wp module AnnotationMatches.function_1_singleton_wrapper_0` with `FixedArrayPairResult.publicPost (FormalSpec.expected input)`, so the public-wrapper composition remains the right proof boundary.

`PROOF_RECIPES.json` identifies `Project.ProofKit.FixedArraySingletonWrapper.wrapperProgram_spec` as the complete function-1 composition.  It also identifies the function-0 scalar helper as a checked post-test loop and supplies `AnnotationMatches.function_0_scalar_post_test_loop_0_terminates_with_of_loop`, `condition_eval`, and `body_eval`.  I will prove the scalar callee once through those generated declarations, then supply that theorem and the two formal-result equations to `wrapperProgram_spec`.

The first edited candidate passed `PROOF_IMPORT_CHECK.js`.  Its Lean build reached only two control-reduction errors inside the scalar transition: `wp_run` could not reduce the still-folded `func0.drop 11` suffix, and `simp only [if_false]` had no work because the post-test theorem had already reduced that branch definitionally.  The generated `body_eval` and `condition_eval` equations, both Euclidean cases, the invariant, and the complete singleton-wrapper application elaborated through those points.  I will unfold `func0` at the exit suffix and remove the redundant branch simplification.

The second edited candidate again passed the import check.  Unfolding `func0` let `wp_run` execute the three-instruction exit suffix, and Lean accepted the complete nonzero-remainder invariant and decrease proof.  One goal remains after the zero-divisor exit: local gets and the local-12 set are expressed through the folded generated `U64State` constructor.  The diagnostic suggests an explicit representation normalization with `function_0_scalar_post_test_loop_0_state`, `U64State.toState`, and the scalar `State` getter and setter; no semantic lemma or recipe is missing.

The third edited candidate passed `PROOF_IMPORT_CHECK.js` and built `LeanExeGen.GeneratedR4337040846e16beb.ArtifactResult`.  Explicitly unfolding the generated scalar-state constructor and `U64State.toState` closed the exit-suffix representation goal.  The completed proof applies `ScalarTransition.postTestProgram_spec` through the generated `function_0_scalar_post_test_loop_0_terminates_with_of_loop`, proves Euclidean preservation with `Nat.gcd_rec`, and supplies the resulting store-preserving callee theorem to `FixedArraySingletonWrapper.wrapperProgram_spec`.  Lean reported only generated-annotation linter warnings and two unused simplifier arguments in the successful candidate; neither affects the theorem, and I will leave the successful candidate unchanged before the required final verification pair.
