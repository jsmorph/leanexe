# Proof Journal

The initial prescribed `ArtifactResult` build reached the deterministic starter and failed at its single remaining goal.  After decomposing `RuntimeReady`, the context contains the input-array representation, allocator globals, memory bounds, and the generated-to-ProofKit array-predicate equivalence.  The goal is `wp module AnnotationMatches.function_1_singleton_wrapper_0 (FixedArrayPairResult.publicPost (FormalSpec.expected input)) initial (FixedArraySingletonWrapper.entryFrame inputPtr) env`, which confirms that the complete `fixed-array-singleton-wrapper` recipe matches the public function.

I will keep the starter's complete-wrapper theorem application and prove the scalar callee at its call boundary.  This avoids expanding the checked length dispatch, input loader, allocator, result stores, and public return.

I inspected `FixedArraySingletonWrapper.wrapperProgram_spec` and the scalar post-test-loop recipe.  The wrapper theorem requires a store-preserving scalar `TerminatesWith` theorem, plus the two direct equations for the formal array result.  For function 0, I applied the generated `function_0_scalar_post_test_loop_0_terminates_with_of_loop` entry theorem and `ScalarTransition.postTestProgram_spec`.  The invariant states that the generated accumulator locals preserve `Nat.gcd a.toNat b.toNat = Nat.gcd input.toNat 42`, and the measure is `b.toNat`.  The nonzero transition uses `UInt64.toNat_mod`, `Nat.gcd_rec`, `Nat.gcd_comm`, and `Nat.mod_lt`; the zero transition interprets the generated suffix as the scalar return.

The import-policy check passed after the first proof edit.  The build then found an elaboration error in the measure's pattern: dotted `.i64` notation lacked a constructor type in that match position.  The reported false decrease goal followed from the malformed measure definition.  I changed the pattern to the explicit `Wasm.Value.i64` constructor before reassessing the arithmetic goal.

The second import-policy check passed, and the complete `ArtifactResult` build succeeded.  The explicit value constructor made the measure reduce to the second Euclidean accumulator, after which the existing `Nat.mod_lt` proof discharged strict decrease.  `FixedArraySingletonWrapper.wrapperProgram_spec` accepted the scalar theorem, the runtime memory and allocator facts, and both formal-result equations without exposing any wrapper instructions.  No additional proof-library abstraction was missing.
