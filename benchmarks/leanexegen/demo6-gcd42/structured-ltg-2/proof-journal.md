# Proof Journal

## 2026-08-09

The required initial build of the unedited candidate failed at the deterministic starter goal.  RuntimeReady decomposition, entry conversion, and the consequence to `FixedArrayPairResult.publicPost` all succeeded.  The remaining goal is `wp module AnnotationMatches.function_1_singleton_wrapper_0 (FixedArrayPairResult.publicPost (FormalSpec.expected input)) initial (FixedArraySingletonWrapper.entryFrame inputPtr) env` under the represented input, allocator-global, allocation-bound, memory-bound, and page-bound hypotheses shown by Lean.

`PROOF_RECIPES.json` supplies a complete `fixed-array-singleton-wrapper-v1` composition for function 1.  Its exact direct theorem is `Project.ProofKit.FixedArraySingletonWrapper.wrapperProgram_spec`, with the checked region equality already used by the starter.  I will attempt this complete composition before opening its internal scalar-loop instructions.  The current missing information is the direct theorem's premise order and the semantic connection between its scalar result and `FormalSpec.expected`.

I started LTG retrieval at `LTG/categories.json`, then searched the `arrays`, `compiler-motifs`, `loops`, and `proof-construction` indexes.  The exact queries were `fixed-array-singleton-wrapper-v1|wrapperProgram_spec|FixedArraySingletonWrapper`, `leanexe\.loop\.scalar-post-test\.v1|postTestProgram_spec|scalar-post-test`, `Nat\.gcd|gcd|Euclidean|remainder`, and `RuntimeReady|publicPost|artifact_behavior`.  The last query returned no entry.  The first three queries selected `fixed-array-singleton-wrapper`, `residual-specification-normalization`, `scalar-post-test-loop`, and `euclidean-gcd-loop`; I inspected only each selected entry's `entry.json` and `README.md`.

I will use all four inspected entries.  The wrapper entry gives the whole-function theorem and requires a store-preserving scalar theorem.  The normalization entry gives the two `FormalSpec.expected` obligations.  The scalar-loop entry gives the generic post-test composition, and the GCD entry gives the invariant `Nat.gcd a.toNat b.toNat = Nat.gcd value.toNat 42` with measure `b.toNat`, using `UInt64.toNat_mod`, `Nat.gcd_rec`, `Nat.gcd_comm`, and `Nat.mod_lt`.  I rejected the indexed counter-transition and Demo 7/Demo 8 entries without opening them because their decrement-and-increment transition does not match the generated Euclidean remainder transition.  I also rejected the map and filter entries because their whole-array annotations do not match the singleton-wrapper annotation.

The selected wrapper theorem matches the residual goal exactly after unfolding `AnnotationMatches.function_1_singleton_wrapper_0`.  Its remaining premises are the memory/page/global facts already available, a theorem that function 0 returns the scalar transform without changing the store, and the two expected-array equations.  I therefore need to prove the scalar theorem before the direct wrapper application can close.

I edited the candidate to apply the exact complete wrapper recipe, then ran the import check and build in the required order.  The import check succeeded.  The build failed with exactly three residual goals: the store-preserving function-0 theorem returning `UInt64.ofNat (Nat.gcd value.toNat 42)`, the non-singleton expected equation, and the singleton expected equation.  No wrapper-semantic, allocator, representation, or bound goals remain, so the direct recipe improves the proof boundary and will remain in use.  The two expected equations match the inspected normalization entry; the scalar goal matches the scalar-loop and Euclidean-GCD entries.

I added `gcdInv`, a relation between each checked scalar state and the requested natural-number GCD, and proved the function-0 summary through the generated `function_0_scalar_post_test_loop_0_terminates_with_of_loop` adapter and `ScalarTransition.postTestProgram_spec`.  The zero transition returns the first accumulator and uses `Nat.gcd_zero_right`.  The nonzero transition uses the generated body and condition equations, `UInt64.toNat_mod`, `Nat.gcd_rec`, and two commutations to preserve the invariant; `Nat.mod_lt` proves strict decrease of the divisor measure.  The wrapper's two remaining equations close by unfolding `FormalSpec.expected` and using `Array.size_eq_one_iff` in the singleton case.

After this edit, the required import check succeeded and the artifact-result build completed successfully.  Lean checked `Behavior` and `ArtifactResult`; the only output was the pre-existing unused-variable warnings from generated `AnnotationMatches.lean`.  No further theorem or catalog metadata was missing.  I will now run the required final import check and build pair without further edits.
