# Proof Journal

The required initial `ArtifactResult` build failed in `Behavior.lean` after the deterministic starter reduced the theorem to the complete matched wrapper.  The remaining goal is `wp module AnnotationMatches.function_1_singleton_wrapper_0 (FixedArrayPairResult.publicPost (FormalSpec.expected input)) initial (FixedArraySingletonWrapper.entryFrame inputPtr) env`, with the input representation and allocator facts in context.  The candidate therefore needs the `FixedArraySingletonWrapper.wrapperProgram_spec` composition and a proof of the scalar GCD callee.

`PROOF_RECIPES.json` identifies `function_1_singleton_wrapper_0` as the exact `fixed-array-singleton-wrapper-v1` composition.  It also identifies function 0 as a checked `leanexe.loop.scalar-post-test.v1` region and recommends `function_0_scalar_post_test_loop_0_terminates_with_of_loop` together with `ScalarTransition.postTestProgram_spec`, the generated condition and body evaluation theorems, and the generated exit theorem.  I will preserve these proof boundaries instead of symbolically executing the wrapper or generated scalar transition.

The import check succeeded after I added temporary declaration queries.  The subsequent build confirmed `Nat.gcd_rec (m n) : gcd m n = gcd (n % m) m`, `UInt64.toNat_mod`, `UInt64.ofNat_toNat`, `UInt64.toNat_inj`, and `Nat.mod_lt`.  It also showed that `UInt64.eq_of_toNat_eq` does not exist, so the proof will recover the final word by rewriting through `UInt64.ofNat_toNat` instead.  I removed all temporary queries.

I added `scalar_gcd_terminates`.  Its invariant existentially records the exact generated nineteen-word compact state and asserts that the GCD of accumulator locals 3 and 4 equals the target GCD.  Its measure reads local 4 as a natural number.  The zero-divisor branch uses the generated body and condition equations followed by the generated `exit_wp`; the nonzero branch uses `UInt64.toNat_mod`, `Nat.gcd_rec`, and `Nat.mod_lt`.  The public proof now applies `FixedArraySingletonWrapper.wrapperProgram_spec` with this callee theorem and direct equations for the invalid and singleton cases of `FormalSpec.expected`.

The import check succeeded.  The build stopped while parsing `scalarInvariant`: pipeline notation before the conjunction left the compact `U64State` unconverted and moved the GCD clause outside the existential scope.  I replaced the pipeline notation with an explicitly parenthesized `.toState` projection; no semantic proof goal had been reached.

The next import check succeeded.  The build reached the zero-accumulator exit and nonzero GCD transition.  Three presentation issues remained: the zero word's `toNat` blocked `simp only [Nat.gcd_zero_right]`; unfolding the local `after` definition exposed a record and prevented rewriting with the folded generated `exit_wp`; and `rw` cannot unfold a local `let` value such as `remainder`.  I changed the first step to ordinary `simp`, apply the forward direction of `exit_wp` with its exact state arguments, and simplify the local remainder definition.

The import check succeeded again.  The build found two omitted explicit instantiations in the zero branch: `UInt64.ofNat_toNat` needs `(x := v3)` when used backwards, and the positional scalar arguments to `exit_wp` follow its explicit `Q` argument.  I supplied the word and added `(Q := _)` so the target determines the assertion.

The next import check and `ArtifactResult` build succeeded.  `scalar_gcd_terminates` now establishes store preservation and the exact `UInt64.ofNat (Nat.gcd input.toNat 42)` result for function 0.  `FixedArraySingletonWrapper.wrapperProgram_spec` composes that theorem with the exact function-1 wrapper, its invalid-length identity branch, its singleton formal-specification equation, the checked input representation, and the allocator facts.  I will run the required final import check and artifact build without further edits.
