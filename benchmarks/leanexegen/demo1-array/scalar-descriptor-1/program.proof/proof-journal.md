# Proof Journal

The required initial build ran before any file edit and failed in `Behavior.lean` with the deterministic starter goal.  The starter has decomposed `RuntimeReady` into the input-array representation, allocator globals, input/result memory bounds, and page bound.  The remaining goal is the exact `TerminatesWith` statement for exported function 2, so the candidate requires a proof.

Inspection found three reachable functions.  Function 0 performs trial division in the scalar loop certified by `AnnotationMatches.function_0_while_loop_0_eq`, function 1 handles inputs at most one and otherwise calls function 0, and function 2 dispatches on singleton length before constructing a singleton result.  I will use `ScalarTransition.whileProgram_spec`, the exact equality length-dispatch recipe, and `FixedArraySingleton.region_result_spec`, with an invariant that equates the accumulated count plus the remaining factor count to the original factor count.

The first edited build reached three local errors in the mathematical support lemmas before returning to the unchanged public goal.  The product proof needs an explicit nonzero-product argument, rewriting the product equality must avoid rewriting the argument of `primeFactorsList`, and `Nat.le_minFac` returns a disjunction for quotient one.  I will repair those statements directly and add the active-loop lower bound that excludes quotient one.

The next build accepted all but the nonzero-product invocation.  In this Mathlib version, `List.prod_ne_zero` asks directly for the exclusion of zero from the list, rather than a two-argument pointwise premise.  The factor-primality, quotient-factor, candidate-advance, trial-bound, state, and invariant declarations now elaborate.

The mathematical support layer now checks, leaving only the public starter goal and one harmless unused-variable warning.  The invariant stores the four mutable parameters, fixes the two loop-control locals at zero on every repeat edge, permits arbitrary scratch locals, and records the factor-count equation, the candidate lower bound on `minFac`, and a fuel potential.  I will now state the function-0 result theorem and expose its exact entry frame before applying the checked scalar loop theorem.

The function-0 entry theorem produced the expected weakest-precondition goal with four parameters and sixteen initialized locals.  No unexpected caller state or result-stack condition appeared: the postcondition requires an unchanged store and one result word.  I will execute the two setup instructions to expose the annotated loop and its precise continuation.

The setup diagnostic revealed the interpreter's operand-stack ordering for multi-argument calls.  `TerminatesWith` reverses the supplied stack values when constructing parameter locals, so the helper theorem must accept `[count, divisor, current, fuel]` while the resulting parameter locals remain `[fuel, current, divisor, count]`.  I will correct the helper statement before reducing the setup instructions.

After correcting the operand order and simplifying the setup, the current frame has the intended four parameter locals and sixteen zero internal locals.  The remaining program begins with the exact annotated `ScalarTransition.whileProgram` and continues with the generated fallback-result suffix.  I will change the goal to that checked program, establish invariant initialization, and inspect only the transition obligation that remains.

The exact `change` to the annotation program succeeded, and `ScalarTransition.whileProgram_spec` accepted the invariant and measure.  Initialization has one literal-reduction issue for `UInt64.toNat 2`, while the step goal now exposes only the checked condition evaluation, body transition, invariant preservation, decrease, and fallback continuation.  I will normalize the literal with decidability and split the transition on whether fuel is zero.

The literal and invariant initialization now check.  The zero-fuel condition evaluation reduced to the fallback suffix, and the positive-fuel case reduced to the single body-transition obligation; both condition evaluations need the known scratch-tail length during simplification.  I will add small conversion lemmas for the final count word, then prove the zero-fuel suffix from the potential and minimum-factor facts.

The count conversion lemmas check, including the no-wrap proof for incrementing the count.  The zero-fuel branch now starts at the final generated comparison, while the positive-fuel branch remains the semantic body evaluation.  For zero fuel, the potential bounds the remaining value by half the candidate; values above one are therefore prime by the minimum-factor square bound, and values at most one have an empty factor list.

The fallback mathematics now checks through branch selection, but the helper used the store-parametric entry bridge.  That bridge quantifies over a fresh store, so its final state cannot equal the theorem's fixed `store`; `TerminatesWith.of_wp_entry_for` is the correct declaration for this store-preserving result.  The UInt64 comparison branch also needs an explicit machine-level proposition so `simp` can normalize the dependent decision term.

The store-specific bridge fixed the store-preservation goals, and the fallback branch is otherwise complete.  The positive-fuel body exposed a necessary invariant correction: terminal body branches set the done flag and repeat once without decrementing fuel, so the repeat invariant must include both active and completed states.  I will record result and done locals in `factorState` and use the measure `2 * fuel + activeBit`, which decreases on both ordinary steps and the administrative done transition.

The corrected invariant initializes and the active zero-fuel fallback remains proved.  The positive-fuel active state now has only the body transition, while the completed state needs a fuel split for the short-circuit condition and direct simplification of the return suffix.  I will prove the first body case, where the remaining value is at most one, using the completed-state alternative and the one-step active-bit decrease.

The at-most-one body case has the intended explicit successor state, invariant, and measure proof; only a literal normalization blocks its completion.  The completed-state condition also checks after splitting on fuel, and its continuation has reached the outer result-selection `if` with a false condition.  I will finish those two local reductions and add the second terminal body case, where the quotient is smaller than the divisor and the remaining value is prime.

The completed-state path now checks through the function result, and the at-most-one transition needs only a direct conversion from the negated UInt64 comparison to a natural-number inequality.  The quotient-terminal state and semantic proof are accepted, but its evaluator equality still contains the folded scalar `divU.apply` operation.  I will unfold the scalar operation in that focused equality before addressing the two ordinary repeat branches.

Both terminal body transitions now check, including their evaluator equalities, completed-state invariants, and active-bit measure decreases.  Function 0 has one remaining body case after the quotient test fails, which splits on whether the candidate divides the current value.  I will prove the divisible transition by transporting the factor-count equation through division and proving the fuel and potential decrease.

The divisible transition's semantic obligations now check.  Its evaluator equality differs only because the descriptor evaluates division, remainder, and division again, writing the same two scratch locals three times; I will make the explicit tail match that exact state.  The final nondivisible transition will advance the candidate, preserve the factor count, and decrease the ceiling-half potential as fuel decreases.

The divisible evaluator now matches the exact repeated scratch writes.  The nondivisible semantic state, minimum-factor bound, candidate parity, potential, and measure proofs are present; three representation details remain: convert the machine `divisor = 2` alternative to a natural equality, pass the natural candidate disjunction to the arithmetic lemma, and split the evaluator simplification on the machine equality.  These are local presentation gaps rather than semantic gaps.

The natural candidate conversions now check.  The nondivisible evaluator split rewrites the state but does not specialize the quotient and remainder hypotheses after replacing the divisor by two, so the scalar evaluator retains both large branches.  I will state branch-specialized quotient and remainder facts before simplification; the non-two branch already keeps the original facts in matching form.

The specialized facts close the final evaluator equality, and function 0 now checks completely.  Its theorem preserves the store and returns `UInt64.ofNat original.toNat.primeFactorsList.length` for every UInt64.  I will prove function 1 by resolving its zero-or-one branch and composing the checked function-0 theorem in the other branch.

Function 1 now checks completely.  Its small-input branch uses the empty factor lists of zero and one, and its other branch applies `func0_spec` through the direct-call rule.  I will enter function 2 with the store-specific bridge and apply the exact equality length-dispatch recipe before executing either branch.

The public wrapper now uses the store-specific entry bridge and changes the decoded body to the cataloged equality dispatch at combined local 5 for length one.  The next check will expose the two branch frames supplied by `FixedArrayLengthDispatch.eqProgram_spec`.  I will keep the valid branch on the singleton allocator theorem and prove the invalid branch by returning the represented input pointer.

The equality-dispatch shape matched, but the shorthand tactic could not infer its semantic `inputPtr` and `input` arguments from the generic wrapper postcondition.  Its diagnostic therefore included the dispatch frame facts plus two unresolved metavariables rather than branch-specific failures.  I will apply the same theorem after the same exact program change, naming the pointer and array explicitly.

Naming the semantic arguments removed the pointer and array metavariables.  The next unresolved obligation is the entry-frame parameter equation, followed by the theorem's literal frame, index, encoded-size, and input-representation premises.  These premises reduce from the generated function definition, after which Lean will report the invalid and valid branch goals without the expanded entry frame.

The entry parameter and value equations reduce definitionally.  The combined-local bound still contains the generated `Function.toLocals` expression, so it requires unfolding that declaration rather than a closed `decide` proof.  The invalid branch is now explicit; I added list-match lemmas identifying the formal result for both length cases and will execute its two local operations plus the shared return.

The formal singleton lemma's first attempt used `rw` beneath dependent array indexing, which Lean rejects because the index proof depends on the rewritten list.  `simp [hList]` handles proof transport.  In the invalid branch, symbolic execution already selects the returned pointer and discharges its value equation, leaving the array predicate itself; I will change that predicate to `UInt64Array.At` and reuse `hArray`, then expose the valid loader's first control boundary.

The list lemma and unchanged-input branch now check.  The valid branch stopped before executing its first instruction because `wp_run` deliberately leaves the dispatch theorem's `branchFrame` opaque.  I will unfold only that frame constructor before symbolic execution, preserving the valid program and its control boundaries.

Unfolding `branchFrame` exposed its lists but left their reverse, length, indexing, and update computations inside the weakest-precondition match.  The straight-line tactic uses a restricted simplifier and therefore retained those closed list terms.  I will run ordinary simplification after the structural execution to normalize this generated frame before addressing the memory-bound conditional.

Ordinary simplification reduced the generated frame to the exact header load.  Its out-of-bounds condition is the negation of `hLengthBound`; the safe read then normalizes through `hInputAddress`, `hLengthRead`, and the singleton-size equation.  I will select the true indexed-loader branch and execute its payload-address calculation to the second checked memory read.

The safe header read and true indexed-loader guard now check.  Executing the valid arm reached the payload load, but its frame still contains closed list indices and the generated multiplication-and-addition address expression.  A focused ordinary simplification will expose the address in the form certified by `generatedElement 0` and `firstElementRead_add`.

The payload expression now matches `(inputPtr.toNat + 8) % 2^32`.  I derive its memory bound from `generatedElement 0` and identify its address with `inputPtr.toUInt32 + 8`, allowing `firstElementRead_add` to supply the loaded word.  After that read, I will apply `func1_spec` at `input[0]` and inspect the capacity-normalization boundary immediately before the singleton allocator region.

The payload bound and read now check, and `func1_spec` returns the prime-factor count while preserving the current store.  The post-call goal consists of a closed local assignment followed by the capacity formula, whose value is 16, and its `16 < 8` normalization branch.  I will simplify these terms before matching the remaining suffix to `FixedArraySingleton.region_result_spec`.

The capacity computation now reduces to local 9 containing 16 and a false condition word.  Selecting the empty normalization branch leaves the exact one-element allocator region followed by `FixedArraySingleton.resultSuffix`.  The formal reserve bound reduces to `heapTop + 48 + 16` after rewriting the expected singleton, which supplies the allocator theorem's memory-fit premise.

The last edit matched an earlier call-continuation sequence in `func1_spec` and placed the allocator block after a completed goal.  Lean reported this immediately as “No goals to be solved,” while the public valid branch remained at the capacity boundary.  I moved the unchanged block to the uniquely identified public call to `func1_spec`.

The moved block reaches the intended point.  The false capacity branch is represented as an empty branch program whose fallthrough continuation contains the allocator suffix, so the suffix is not yet definitionally the current program.  Executing that empty branch will restore the direct `FixedArrayAllocator.region 1 ++ FixedArraySingleton.resultSuffix` goal.

The general straight-line tactic reduced the empty branch and continued through the allocator's initial local assignments and global read, stopping at its block.  That loses the complete allocator-region shape required by the cataloged theorem.  I will reduce only `wp []` with `Wasm.wp_nil`, leaving every allocator instruction intact.

The single `wp_nil` reduction preserves the exact region, and every `region_result_spec` premise now checks: parameter and local lengths, empty value stack, capacity local, scalar-result local, result-region memory fit, page limit, 32-bit memory mode, and allocator globals.  Its sole remaining continuation receives the final singleton representation.  I will reduce the theorem remainder and shared public return, then rewrite the formal expected array with the singleton semantic lemma.

The singleton continuation closes by simplifying `resultFrame`, `allocFrame`, the shared branch postcondition, and the two identical array predicates after rewriting `expected`.  The prescribed build now succeeds for both `Behavior` and `ArtifactResult`; the remaining diagnostics are linter warnings and do not affect the theorem.  I will run the prescribed build once more as the final verification action, with no subsequent candidate edit.
