# Proof Journal

The initial prescribed artifact build failed in `Behavior.lean` at the deterministic starter's final goal.  RuntimeReady had already decomposed into the input representation, allocator globals, result-region bounds, page limit, and the generated-to-ProofKit array predicate equivalence.  Rewriting with `AnnotationMatches.function_2_singleton_wrapper_0_eq` left `wp module AnnotationMatches.function_2_singleton_wrapper_0 (FixedArrayPairResult.publicPost (FormalSpec.expected input)) initial (FixedArraySingletonWrapper.entryFrame inputPtr) env`.  I will try the `fixed-array-singleton-wrapper-v1` composition from `PROOF_RECIPES.json`, which selects `FixedArraySingletonWrapper.wrapperProgram_spec`, after proving the scalar callee required by that theorem.

The first edited candidate passed `PROOF_IMPORT_CHECK.js` and reached the scalar proof in the artifact build.  `ScalarTransition.whileProgram_spec` remains the appropriate boundary, but the diagnostic exposed several local issues before elaborating the loop: Lean parsed `namespace AM := ...` as a nested namespace rather than an alias, the least-factor division lemma used the least factor's lower bound where it needed the trial factor's lower bound, and two `wp_run` calls stopped before selecting an `iff` branch.  Arithmetic diagnostics also showed that the invariant needs an explicit bound on the original factor-count length and that the increment proof must normalize the numeral two before proving no wrap.  I will retain the direct recipe and correct these local statements rather than expand the checked scalar program.

Each subsequent candidate edit passed `PROOF_IMPORT_CHECK.js`.  The artifact-build diagnostics then narrowed in four stages.  First, explicit `UInt64.toNat` normalization established the subtraction, addition, division, and remainder equations and revealed the operand-stack order for function 0 as `[count, factor, n, fuel]`.  Second, `AnnotationMatches.function_0_while_loop_0_entry_to_loop` matched after normalizing the function-entry frame, and `ScalarTransition.whileProgram_spec` reduced the proof to the invariant transitions.  Third, the generated `function_0_while_loop_0_condition_eval` and `function_0_while_loop_0_body_eval` theorems discharged the compiler scratch-local behavior once each post-transition state was supplied explicitly.  Fourth, the post-loop suffix required `Wasm.wp_iff_cons` for the generated done flag and final-prime test after simplifying the compact scalar state.

I tried `wp_entry_single_call` from the function-1 direct-call recipe.  It stopped at function 1's preceding `x ≤ 1` conditional, so its documented straight-line shape did not match the whole function.  I selected the valid branch with `Wasm.wp_iff_cons`, applied `Wasm.wp_call_tw` to `func0_correct`, and proved the zero-and-one branch directly.  This preserved the recipe's call boundary without restructuring the scalar function.

The complete `fixed-array-singleton-wrapper-v1` composition matched `AnnotationMatches.function_2_singleton_wrapper_0_eq`.  `FixedArraySingletonWrapper.wrapperProgram_spec` consumed the input representation, result-region memory bound, page limit, allocator globals, scalar callee theorem, and the valid and invalid equations for `FormalSpec.expected`.  The prescribed artifact build then succeeded, including `LeanExeGen.GeneratedRbade8cb1a4e3a423.ArtifactResult`.  The remaining diagnostics were linter-only warnings, which the final candidate edit removes before the required final import check and artifact build.
