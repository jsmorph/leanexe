# Proof Journal

The initial artifact build failed at the deterministic starter's final `wp` goal.  RuntimeReady had already supplied the array representation, allocator globals, memory bounds, and page bound, and the starter had rewritten function 2 with `function_2_singleton_wrapper_0_eq`.  The remaining goal is the complete `FixedArraySingletonWrapper` program with `FixedArrayPairResult.publicPost (FormalSpec.expected input)`.

The checked `fixed-array-singleton-wrapper-v1` composition selects `Project.ProofKit.FixedArraySingletonWrapper.wrapperProgram_spec`.  I will retain this complete-wrapper boundary and prove the scalar callee used by the wrapper, rather than expand the public length check, input load, allocation, or result stores.  The function-0 loop recipe selects `Project.ProofKit.ScalarTransition.whileProgram_spec` together with the generated condition and body transition equations.

The first edited candidate passed `PROOF_IMPORT_CHECK.js` but failed its artifact build in three preliminary arithmetic lemmas.  Rewriting `prod_primeFactorsList` backward changed the argument of `primeFactorsList`, `omega` did not normalize a square to a product, and the odd-factor case needed its existential witness before arithmetic could exclude factor two.  These diagnostics concern local normalization; the build then reached the same complete-wrapper goal because the scalar theorems have not yet been applied.

The next import check passed, and the build accepted the length bound, trial-bound primality lemma, quotient minimum-factor lemma, and next-candidate lemma.  Only the factor-list division lemma failed because rewriting `n` before applying `primeFactorsList_add_two` left syntactically unreduced copies of `n - 2 + 2`; I will derive the library equation separately and normalize it before rewriting the goal.  The `UInt64` addition equality also needs no range premise because both sides reduce to the same modulo value.

The third import check passed, and the artifact build accepted every preliminary number-theory and `UInt64` lemma.  The only remaining diagnostic is the deterministic starter's complete-wrapper goal.  I will now state the function-0 invariant over the exact generated scalar state and use a partial entry proof to expose the precise loop boundary before filling the transition cases.

The invariant and function-0 theorem statement passed the import check.  The build stopped before the loop because `wp_run` intentionally does not simplify the explicit reversed entry-argument list, leaving the initial `localSet 5` as a match expression.  I will normalize that fixed list before symbolic execution; no program or semantic mismatch appeared.

The attempted pre-normalization ran before `of_wp_entry_for` exposed the entry frame, so Lean reported every reverse-list rewrite as unused and produced the same `localSet 5` match.  The next proof will run the entry reduction first, normalize the now-visible reversed list, and resume `wp_run`.  This ordering follows the diagnostic's exact reduction boundary.

The next import check passed, but the targeted reverse-list simplifier made no progress after `wp_run`.  I will use `change` to state the exact generated scalar state and the checked annotated loop plus its post-loop suffix, avoiding dependence on normalization of `Function.toLocals`.  This also gives `whileProgram_spec` a stable invariant and measure boundary.

The explicit boundary matched the artifact, and `ScalarTransition.whileProgram_spec` reduced function 0 to its generated transition premise.  The initial invariant needs two decidable facts about `UInt64.toNat 2`, while the fuel-only measure is insufficient because terminal iterations set the done flag without decrementing fuel.  I will use twice the fuel plus a one-step allowance for an unset done flag, and I will prove the shared post-loop suffix once for both false-condition cases.

The import check passed, but the build found a malformed fallback pattern in the new two-expression measure match.  That definition error also prevented the later `change` from recognizing the boundary that matched in the preceding build.  I will correct the fallback to two patterns before evaluating any downstream diagnostic.

After correcting the measure match, the explicit boundary failed only because I had required the entry theorem's generated postcondition to be definitionally equal to the named `scalarPost`.  I will retain the previously successful boundary with an inferred postcondition, prove the invariant initialization and transition as local facts, and use `Wasm.wp.conseq` to connect `scalarPost` to the entry postcondition.  This preserves the scalar loop theorem while avoiding a brittle definitional-equality requirement.

The revised boundary and initial invariant compile.  Lean could not infer the result type of the local `hStep` lambda before `rcases` eliminated the existential invariant, because that unconstrained metavariable might have lived in `Type` rather than `Prop`.  I will give `hStep` the exact proposition from `whileProgram_spec`; the transition proof body can then eliminate the invariant normally.

The explicit transition proposition fixed invariant elimination, and the common exit proof compiled through both done-flag branches.  One equality used the implicit theorem `UInt64.ofNat_toNat` as an explicit function, which requires a named implicit argument.  The remaining transition diagnostic is the expected true-condition body goal after substituting `done = 0`; I will reduce that Boolean conditional and prove the two branches that terminate by setting the result and done locals.

Both terminal body branches now compile: `n ≤ 1` returns the accumulated count, while the trial bound proves the remaining `n` prime and returns count plus one.  The only local error is the proof that a trial factor with natural value at least two is a nonzero `UInt64`; I will prove it by contradiction and substitution.  The two remaining goals are exactly the divisible and nondivisible continuation branches selected by the generated body transition.

The divisible branch's generated body equality and invariant shape match.  Its remaining diagnostics concern a type annotation for `not_lt.mp`, the theorem name `List.length_pos_iff`, an overly strong direct nonlinear proof of the fuel bound, and simplification of the measure.  I will bound division by the trial factor using division by two, compare the resulting linear expressions, and add an exact measure formula for generated states.

The exact measure formula compiles, and the division-by-two argument removed the nonlinear bound failure.  Two attempted rewrites used a `UInt64` comparison theorem on goals already expressed in `Nat`; I will instead transform typed hypotheses and simplify their `toNat` forms.  Once the resulting positive-fuel fact is present, it also supplies the missing premise for the divisible branch's measure decrease.

The full advancement branch now elaborates against the generated transition state.  The build found the same underconstrained `lt_of_not_ge` term in both quotient conversions, and the two advancement bounds need explicit normalization of `UInt64` numerals and natural subtraction.  I will use typed order case splits and supply `fuel.toNat - 1 + 1 = fuel.toNat` to the arithmetic solvers.

The advancement bounds now compile, including the odd-factor no-wrap proof.  Lean still leaves the generic order behind `lt_of_not_ge` underconstrained despite the typed case hypothesis, so I will rewrite `hTrial` itself into its `Nat` form and use `omega`.  I will also prove the divisible branch's measure decrease from `Nat.sub_lt_of_pos_le` instead of asking `omega` to infer the subtraction fact.

Rewriting `hTrial` solved both quotient conversions, and every body branch now compiles.  The sole function-0 error is lexical scope: the natural positive-fuel fact was defined inside the bound proof but reused in the following measure proof.  I will move that fact to the shared divisible-branch scope; the redundant conditional simplifier can also be removed.

Function 0 now compiles through the complete `ScalarTransition.whileProgram_spec` application and the entry postcondition consequence.  The artifact build reports only the original complete function-2 wrapper goal.  I will prove function 1 by selecting its zero-or-one branch or applying `func0_spec` at its single direct call, then use the checked singleton-wrapper composition.

Function 1 reached its scalar conditional, but the first branch rewrite selected the inner `value ≤ 1` conditional and left the normalized `UInt32` selector unresolved.  Its entry frame also retained the reversed singleton parameter list, which stopped later straight-line reduction.  I will refold that entry frame before unfolding the body, prove the complete selector with `simp [hSmall]`, and normalize `UInt64.toNat 1` explicitly in the small-input arithmetic.

Function 1 now compiles, including both zero-and-one cases and the exact call to function 0.  The build again reports only the complete function-2 wrapper goal.  I will now apply `FixedArraySingletonWrapper.wrapperProgram_spec` with the proved function-1 theorem and discharge the invalid and singleton equations for `FormalSpec.expected`.

The next import check passed, and the complete artifact build succeeded after applying `FixedArraySingletonWrapper.wrapperProgram_spec`.  Function 0 uses the checked `ScalarTransition.whileProgram_spec`, function 1 uses the proved scalar callee at its exact call site, and function 2 uses the checked singleton-array wrapper composition.  The successful target was `LeanExeGen.GeneratedRbade8cb1a4e3a423.ArtifactResult`, and the proof contains neither axioms nor `sorry`.
