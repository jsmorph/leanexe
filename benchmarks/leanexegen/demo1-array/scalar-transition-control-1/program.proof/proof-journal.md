# Proof Journal

The required initial `ArtifactResult` build reached the deterministic starter's intended boundary and failed with one goal: `wp module AnnotationMatches.function_2_singleton_wrapper_0` from `FixedArraySingletonWrapper.entryFrame inputPtr` to `FixedArrayPairResult.publicPost (FormalSpec.expected input)`.  The starter has already decomposed `RuntimeReady`, changed the generated array predicate to `Project.ProofKit.UInt64Array.At`, and rewritten the exact function body with `function_2_singleton_wrapper_0_eq`.  The complete `fixed-array-singleton-wrapper-v1` composition therefore applies once the scalar transformation implemented by callee 1 has a store-preserving termination theorem.

The first edited build accepted the imports, definitions, loop entry, and selected `Wasm.wp_loop_cons`, leaving its step obligation.  It also exposed an argument-order error in my initial function-0 theorem: `TerminatesWith` receives the operand stack in top-first order, and entry reverses that list into parameter order.  Function 0 therefore needs arguments `[count, divisor, n, fuel]`, matching the generated call's four pushes, rather than parameter order `[fuel, n, divisor, count]`.

The next build confirmed that argument order but revealed that `wp_entry_to_loop` uses the store-parametric `TerminatesWith.of_wp_entry`.  The scalar postcondition names the caller's fixed store, so the generated arbitrary entry store cannot satisfy `final = initial`.  I will use `TerminatesWith.of_wp_entry_for` and then apply the same block and loop rules.  The build also identified three local arithmetic corrections: specify the numerator in `Nat.div_le_div_left`, normalize the `if divisor = 2` branch explicitly, and require a quotient above one before asserting a divisor lower bound for its `minFac`.

The fixed-store build accepted every arithmetic lemma and both entry obligations.  Alternating `wp_run` with simplification reduces the generated guard, but the second `wp_run` in each case reports that the remaining goal is semantic rather than an instruction weakest precondition.  I will expose those four residual goals and discharge them directly instead of continuing symbolic execution blindly.

The completed-state branches now close.  The running-state proof reaches the exact `n ≤ 1`, quotient comparison, and remainder boundaries.  The small-input branch leaves the expected invariant reconstruction and one-bit measure decrease; its only failed arithmetic step retains `(1 : UInt64).toNat`, so I will state that converted inequality with `change`.  Simplification after the quotient comparison appears to consume its `if`, so I will inspect that residual goal before adding another `wp_iff_cons`.

The next build closes the zero-fuel, small-input, and prime-terminal branches.  The quotient operation introduced its explicit nonzero-divisor premise, which is now discharged from the divisor lower bound.  The only function-0 branch left begins at the analogous nonzero-divisor `if` for remainder; after selecting it, the proof will split on whether the remainder is zero and reconstruct the two recursive states.

The import check after splitting the remainder cases succeeded.  The build shows that the three attempted Boolean selections in each remainder branch did not consume the next instruction: the residual program begins with the divisor-zero guard for unsigned division in the divisible case and the divisor-equality chain in the nondivisible case.  I will remove those premature selections and reduce each branch from the reported residual program, adding explicit selections only where `wp_run` stops.

The scalar loop now checks completely.  The divisible branch uses `primeFactorsList_eq_cons_minFac`, `divisor_eq_minFac`, and `divisor_le_minFac_div` to preserve the count and divisor bound after division, while the nondivisible branch uses `next_divisor_le_minFac` and an explicit no-wrap proof for the incremented divisor.  I have added the direct-call proof for function 1 through `Wasm.wp_call_tw` and applied the checked `FixedArraySingletonWrapper.wrapperProgram_spec` composition to the exported function.

The import check for the complete candidate succeeded, and the `ArtifactResult` build completed all 3,077 jobs.  Function 1 now proves the zero-or-one case directly and uses `Wasm.wp_call_tw` with `func0_correct` for every larger input; the wrapper theorem then discharges length dispatch, input loading, allocation, result stores, and the public return.  Earlier remainder diagnostics represented successive enclosing Boolean-normalization layers, so the final proof consumes those layers before splitting the divisor increment into its two exact instruction branches.
