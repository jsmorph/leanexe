# Proof Journal

The required initial build failed in `Behavior.lean` after the deterministic starter completed the `RuntimeReady` decomposition.  Its sole remaining goal is `wp module AnnotationMatches.function_2_singleton_wrapper_0 (FixedArrayPairResult.publicPost (FormalSpec.expected input)) initial (FixedArraySingletonWrapper.entryFrame inputPtr) env`.  This confirms that `function_2_singleton_wrapper_0_eq` matched the complete exported wrapper and that the next proof should apply `FixedArraySingletonWrapper.wrapperProgram_spec` with a store-preserving theorem for the scalar callee.

`PROOF_RECIPES.json` supplies `FixedArraySingletonWrapper.wrapperProgram_spec` for the complete exported function and the generated `function_0_while_loop_0_terminates_with_of_loop`, `condition_eval`, and `body_eval` theorems for the scalar loop.  I retained the complete wrapper boundary and added an invariant for the loop's `(fuel, remaining, divisor, count, result, done)` state.  The invariant records the exact trial candidate, a lower bound on the remaining number's least prime factor, the factor-count equation, and an exact fuel budget; the measure is twice the fuel plus one in a running state.

The arithmetic helpers establish that prime-factor-list length fits in `UInt64`, a running state has nonzero fuel, the early `remaining / divisor < divisor` branch implies primality, and advancing the trial divisor preserves its least-factor bound.  These lemmas use the authoritative `Nat.primeFactorsList`, `Nat.minFac`, and division results from Mathlib rather than unfolding prime factorization.  The next check will test these definitions before I add the scalar weakest-precondition proof.

The import check accepted the added imports.  The Lean build then found four local formulation errors: the measure needed explicit `Wasm.Value.i64` patterns, rewriting the product theorem rewrote the argument of `primeFactorsList`, the `UInt64` bound projection is `toNat_lt_size`, and the fuel lemma had to destruct `RunningFacts` before unfolding its candidate equation.  It also showed that the zero-index divisor advance should use `minFac` primality's `two_le` field directly; I revised these points without changing the invariant.

The second import check succeeded, and the build accepted every arithmetic helper.  I then applied `function_0_while_loop_0_terminates_with_of_loop` followed by `ScalarTransition.whileProgram_spec`, using the generated `condition_eval` and `body_eval` theorems at every loop transition.  The three running branches now construct annotated successor states, while the done branch executes only the short post-loop suffix.

The import check accepted the loop proof.  The first loop build diagnostic stopped at eliminating the running state's done value because the equality was a structure projection; I named that projection before substitution.  The diagnostic also exposed the exact post-loop `iff` with a false `i32` condition, so I replaced an invalid simplifier name with `wp_iff_cons`, selected its empty branch, and executed the final result load.

The next build reached all running branches.  Most failures came from asking `omega` to discover fields inside `RunningFacts`; I now destruct that record once and name its seven facts.  I also corrected uses of `UInt64.toNat_inj` to select the forward implication, kept the divisor-to-`minFac` equality local so it does not rewrite quotient types, and supplied explicit machine-order-to-natural-order conversions for positive fuel.

After those revisions, Lean accepted the semantic arguments for all three loop branches.  The remaining scalar errors were three occurrences of the unreduced natural value of the `UInt64` literal one and one redundant `omega` after `simp` had already discharged a contradiction.  I added one computed equality for that literal and reused it in the small-input and positive-fuel comparisons.

The following build compiled `func0_factor_spec` in full, leaving only the unchanged complete-wrapper goal.  I added `func1_factor_spec`: its `x ≤ 1` branch uses `primeFactorsList_eq_nil`, while its other branch applies `wp_call_tw` to the compiled function-0 theorem.  The artifact theorem now applies `FixedArraySingletonWrapper.wrapperProgram_spec` and supplies the scalar theorem plus the valid-size and invalid-size equations for `FormalSpec.expected`.

The import check succeeded, and the subsequent exact `ArtifactResult` build completed all 3,078 jobs.  `func1_factor_spec` and the complete `FixedArraySingletonWrapper.wrapperProgram_spec` application required no revisions after their first check.  The successful build emitted only three `unnecessarySeqFocus` style warnings in measure proofs; all theorem goals, including `artifact_behavior`, are closed.
