# Proof Journal

The required initial `ArtifactResult` build reached the deterministic starter's intended goal and failed there.  After the starter rewrote the decoded function with `AnnotationMatches.function_2_singleton_wrapper_0_eq`, Lean requested `wp module AnnotationMatches.function_2_singleton_wrapper_0 (FixedArrayPairResult.publicPost (FormalSpec.expected input))` from the standard singleton-wrapper entry frame.  The context already contains the input `UInt64Array.At` fact, allocator globals, result-region memory bounds, and the formal-to-ProofKit array-predicate equivalence.

`PROOF_RECIPES.json` identifies the complete `fixed-array-singleton-wrapper-v1` composition and directs the proof to `FixedArraySingletonWrapper.wrapperProgram_spec`.  I will use that theorem at the current boundary and prove its scalar-callee and semantic-equation premises.  This avoids expanding the wrapper's length dispatch, input load, allocator, and result stores.

The scalar callee reaches `AnnotationMatches.function_0_while_loop_0_program`.  I will apply `ScalarTransition.whileProgram_spec` after rewriting the entry with `function_0_while_loop_0_entry_to_loop`, and I will use the generated `condition_eval` and `body_eval` equations for every transition.  The loop invariant records the accumulated factor count, the lower bound on the current trial divisor, its parity, and the progress inequality `n < fuel + divisor`; the measure combines twice the fuel with a one-bit active-state component so that both fuel-decrementing iterations and done-setting iterations decrease.

The first edited build passed `PROOF_IMPORT_CHECK.js` and found three issues before elaborating the loop body.  `Nat.div_le_div_left` needs its numerator supplied explicitly; the constant-two branch of `next_trial_spec` needs ordinary simplification of its conditional; and the generated entry equation did not match because the `TerminatesWith` argument stack was in parameter order instead of WebAssembly stack order.  I will reverse the four scalar arguments and normalize the entry frame before applying `function_0_while_loop_0_entry_to_loop`.

The second build reached every loop branch.  The generated compact state remained folded in measure and suffix goals, so those proofs need `U64State.toState` and `State.toLocals` in their reductions.  The remaining arithmetic diagnostics identify constant `UInt64` conversions, the quotient-divisibility witness's equality orientation, and the need to convert `divisor ≠ 2` into `divisor.toNat ≠ 2` before simplifying `next_trial_spec`.

The third build reduced the state and measure as intended.  It exposed the two control-flow selections in the post-loop suffix, so I will use `wp_iff_cons` for the done test and the residual-prime test.  The quotient state needs the fact `divisor ≤ n / divisor`, supplied by the failed shortcut comparison, to eliminate the `n / divisor = 1` case from `Nat.le_minFac`; the loop measure also needs an explicit positive natural fuel fact.

The fourth build discharged the complete annotated loop except for two elaboration details.  The converted fuel hypothesis is `1 ≤ fuel.toNat`, from which `omega` proves positivity; and the implicit arguments for `Nat.le_minFac` must be supplied to the theorem before selecting the reverse implication, rather than to `Iff.mpr`.

The fifth build accepted the quotient `minFac` argument and left one fuel-positivity proof.  I will derive `fuel.toNat ≠ 0` directly from `fuel ≠ 0` through `UInt64.toNat.inj`, avoiding an arithmetic conversion in that local proof.

The sixth build compiled `func0_correct`, including the complete `ScalarTransition.whileProgram_spec` application and both generated transition equations.  I will now prove function 1's zero-and-one dispatch and its direct call to function 0, then supply that theorem to `FixedArraySingletonWrapper.wrapperProgram_spec` with the two formal array equations.

The seventh edited build passed `PROOF_IMPORT_CHECK.js` and built `LeanExeGen.GeneratedRbade8cb1a4e3a423.ArtifactResult`.  `func1_correct` preserves the store, returns zero for inputs zero and one, and applies `func0_correct` for larger inputs.  `FixedArraySingletonWrapper.wrapperProgram_spec` then proves the exact decoded function 2 after `function_2_singleton_wrapper_0_eq`; its invalid-size premise returns the input unchanged, and its singleton premise reduces `FormalSpec.expected` to the scalar factor count.
