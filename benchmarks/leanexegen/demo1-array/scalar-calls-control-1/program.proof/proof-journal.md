# Proof Journal

## 2026-08-08

The required initial build of the untouched candidate failed at the starter's final `TerminatesWith` goal.  The `RuntimeReady` decomposition and its derived array and allocator-global facts all elaborated.  The remaining proof must compose the scalar factor-count helper (`func0`), its zero-and-one wrapper (`func1`), and the singleton branch of the public array wrapper (`func2`); the other public branch returns the input pointer unchanged.

The generated annotations report only the two direct calls and provide no checked scalar-loop descriptor.  The public singleton branch contains the standard one-parameter, fourteen-local fixed-array allocator followed by the standard singleton suffix, so `Project.ProofKit.FixedArraySingleton.region_result_spec` applies after the scalar call.  The main unknown is a semantic theorem for the fuel loop in `func0` and its relationship to `Nat.primeFactorsList.length`.

The first edited build confirmed that the scalar descriptor's exact equality reduces by `rfl`, apart from a namespace-alias syntax error that prevented the descriptor declarations from elaborating.  It also revealed that `wp_entry` uses the store-polymorphic entry theorem.  These helper and public proofs depend on the supplied store, so they require `TerminatesWith.of_wp_entry_for`; the arbitrary-store goals explained why the array reads and allocator hypotheses did not rewrite in the public wrapper.

The second edited build reached the exact scalar-loop decomposition.  The remaining prefix persisted because `wp_run` does not reduce an appended instruction list until the append is normalized.  The function-call diagnostic also showed the interpreter's top-first operand list: the four values pushed as fuel, number, divisor, and accumulator appear in the callee theorem as accumulator, divisor, number, and fuel, while `Function.toLocals` reverses them into parameter order.

The third edited build elaborated the loop theorem and entered all four semantic body cases: completion at zero or one, completion past the trial bound, exact division, and advancing to the next candidate.  The terminal branches exposed the compiler's administrative back edge after setting the done flag.  The loop measure therefore uses twice the fuel plus one while running and twice the fuel after completion, exactly accounting for both a fuel-decreasing iteration and the final done-flag iteration.

The fourth edited build left no structural mismatch in the scalar descriptor.  A malformed fallback pattern in the two-discriminant measure caused the spurious terminal comparisons.  The evaluator also retains checked division's zero-divisor branch until the invariant's `2 ≤ divisor` fact is supplied as a `UInt64` nonzero fact.  The public wrapper reached the checked first-element loader with condition one.

The fifth edited build reduced the loop failures to the measure's local index, the preserved done local, and two projected arithmetic facts.  Combined local 5 is internal-local index 1, while index 0 stores the scalar result.  Substituting `done = 0` in the active branch aligns every body evaluator result with the invariant frame and leaves the terminal transition to change that local to one.

The sixth edited build confirmed the explicit two-conditional proof of the zero-fuel postlude.  Its remaining arithmetic goal retained `UInt64.toNat 0`, so the fuel bound now receives a typed normalization before `omega`.  The build also showed that splitting on whether the divisor equals two before exposing the body transition duplicated the outer active-condition goal; the split now belongs only to the evaluator equality.  The public wrapper has reduced through the scalar call and stops at the capacity-normalization conditional immediately before the checked allocator region.

The next two builds discharged both false done-word branches and matched the singleton allocator theorem at its exact first instruction.  Only the advance evaluator still exceeds the simplifier's step limit, so its divisor-two case now receives trial and remainder hypotheses already rewritten at divisor two.  The allocator theorem also revealed that the formal expected array remained hidden behind the `input.toList` match.  One explicit singleton equation now supplies both the memory-fit arithmetic and the final array representation without broad simplification.

The latest build completed every scalar loop branch, including the advance evaluator, and reached only the singleton equation for the formal result.  Rewriting `input = #[input[0]]` directly fails Lean's dependent motive check because the array access carries the original index-bound proof.  Applying `Array.toList` to that equation produces the nondependent equality consumed by `FormalSpec.expected`.

The complete artifact target now builds.  The final proof connects the exact scalar fuel loop to `Nat.primeFactorsList.length`, proves the zero-and-one wrapper, preserves nonsingleton inputs, and uses the checked singleton allocator region for the singleton result.  The remaining diagnostics are linter warnings about unused hypotheses and simplifier arguments; they do not affect the theorem or artifact result.
