# Corrected structured-tactic guidance censored run

This directory preserves a fresh Demo 9 proof task started on 11 August 2026 after the fold-body LTG guidance had incorporated the repair from the first structured-tactic screen.  The task held the formal specification, source, 1,979-byte WASM artifact, decoded program, and SHA-256 artifact digest `aa263bbfa89c333f9fab497f1a2c370f476afc3419015d17b368cb7c8a6086d5` fixed.  The owned session was interrupted after approximately forty-eight minutes because the candidate and journal had remained unchanged for about twenty-two minutes during one Lean check.

The journal records a category-index search followed by selection and successful use of `wp_fixed_array_length_le_dispatch_from`.  That command converted the public `TerminatesWith` goal into elementary entry-frame obligations and valid and invalid branch goals, so the candidate retained it instead of using `FixedArrayLengthDispatch.leProgram_spec`.  The journal also rejects `word_reads` because the residual goal was a branch-level `Wasm.wp` obligation rather than a nested memory-read equation.

The last candidate decomposes both branches and applies `FixedArrayCapacity.constantProgram_spec`.  The unchanged Lean check had entered the constant-capacity theorem before either branch reached allocation, traversal, loop induction, result construction, or the revised continuation-frame guidance.  This run therefore provides evidence that the structured dispatch record remained useful, but it provides no evidence about the corrected fold guidance, autonomous proof acceptance, or proof-generation time.

`candidate.lean` is the unfinished source at its final edit, with SHA-256 digest `e4c32106dd3436a0f2f39003b6494ae6f20df96d4b81b0ddfabc688de70cde1a`.  `proof-journal.md` has SHA-256 digest `8af15acc15310e7a1b8c6efafb83ffbde85d753b9148617f4fae0c06ff2fef9b` and records the agent's searches, selections, and next intended boundary.  The remaining files preserve the generated annotation equality, frozen LTG task digest, compiler annotations, proof recipes, selected strategies, task features, supplied proof-library summary, and original request.
