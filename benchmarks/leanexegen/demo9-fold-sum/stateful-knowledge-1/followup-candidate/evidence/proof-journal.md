# Proof Journal

## 2026-08-18

The unchanged starter failed its first Lean check at the intended theorem boundary.  `RuntimeReady` decomposed successfully, leaving `TerminatesWith env module 0 initial [Value.i64 inputPtr]` with the expected represented-array postcondition.  The diagnostic exposed the prepared input length read, address bound, allocator globals, heap bounds, and page bound; no instruction-region theorem had yet been applied.

I started retrieval from `KNOWLEDGE/forest.json`.  It selects the versioned packages `leanexe-core` and `proposal-4f56fd45fe24-d4f7d73b3648`.

I searched the selected packages' array, compiler-motif, proof-construction, loop, memory, and allocation indexes with three queries: `leanexe.array.length-dispatch.v1|wp_fixed_array_length_le_dispatch_from|leProgram_spec`; `leanexe.array.fold.v1|foldPrefix|FixedArrayFold|continuingProgram_spec|bodyProgram_spec`; and `singletonResult|singleton-result|singleton result|UInt64Array|represented`.  The first query selected `leanexe-core/fixed-array-length-dispatch`.  The other queries identified fold, traversal, result, and allocator entries for later residual goals; I have not inspected those entries yet.

I inspected `leanexe-core/fixed-array-length-dispatch`.  Its structured tactic matches the next goal after `Wasm.TerminatesWith.of_wp_entry_for`: the goal begins at the bounded length-dispatch annotation, `hArray` supplies the represented input, and the recipe fixes input local 7 and maximum size 8.  I will attempt the recipe command before its fallback theorem.  Progress means replacing the public execution goal with separate valid and invalid branch obligations.

The import check accepted the first candidate edit.  The Lean check showed that `wp_fixed_array_length_le_dispatch_from hArray at 7, 8` matched and produced the expected valid and invalid branch obligations, together with five closed frame and constant premises.  The command therefore worked; its fallback declaration `FixedArrayLengthDispatch.leProgram_spec` is unnecessary.

I inspected `leanexe-core/fixed-array-capacity` and `leanexe-core/fixed-array-allocation`, selected by the earlier annotation and allocator queries.  Each branch begins with the checked constant-capacity prefix at local 11, followed by `FixedArrayAllocatorWindow.region` at offset 2 with four trailing locals.  `constantCapacityRegion_spec_withTail` matches both residual programs and supplies the capacity getter and frame projections internally.  I will apply it with logical result length one in the valid branch and zero in the invalid branch.  Progress means reaching post-allocation continuations with the allocation store and root frame, while the runtime heap bounds discharge each capacity-specific fit premise.

The import check accepted the allocator edit.  The Lean check stopped before either allocator theorem because the length-dispatch local-bound premise remained open: `7 < 1 + func0Def.toLocals(...).locals.length`.  The following bullet was therefore applied to the wrong goal.  The length-dispatch entry names normalization with `func0Def`, `Function.toLocals`, `Function.numParams`, and `ValueType.zero` for this exact residual premise.  I will use that named normalization before retrying the unchanged allocator applications.

The import check accepted the local-bound normalization.  The Lean check discharged every dispatch and allocation premise and left only the two post-allocation continuations.  The valid continuation starts with a length-one store and the annotated fold.  The invalid continuation is the exact empty-result length store and root-transfer suffix.

I inspected `leanexe-core/fixed-array-fold-structure`, `leanexe-core/fixed-array-fold-body`, `leanexe-core/fixed-array-traversal-input`, `leanexe-core/array-fold-prefix`, `leanexe-core/annotated-fold-frame-accessors`, `leanexe-core/compact-loop-suffix-boundary`, `leanexe-core/fixed-array-result`, and `proposal-4f56fd45fe24-d4f7d73b3648/fixed-array-singleton-result-from-frame`.  The fold entries match the valid branch annotation and provide setup, generated getters, the loop rule, the continuing body composition, prefix equations, and the complete singleton suffix.  The result entry matches the invalid branch through `lengthStore_spec`, `emptyStore_at`, and `finishProgram_spec`.  The proposal theorem is a possible generic singleton-suffix adapter; the generated `function_0_array_fold_0_singleton_result_spec` has the stronger exact frame match, so I will try that generated adapter first.

Before reducing the invalid output frame, I searched the recipes and selected allocation, array, memory, and proof-construction indexes for `finishFrame_return_get|allocFrame_get_root|emptyStore_at|wordAddress_toNat|resultFrame_get`.  The search returned `FixedArrayAllocatorWindow.allocFrame_get_root`, `Allocation.bumpFacts`, and the fixed-array result entry.  I will use those declarations to prove the root getter and address bounds.  Progress means closing the invalid branch and retaining only the valid fold continuation.

The import check accepted the invalid-branch construction.  The Lean check reached the final empty suffix and exposed two local defects.  I had unfolded normalized capacity while proving its minimum bound, although `FixedArrayCapacity.normalizedCapacity_toNat_ge_eight` states that premise directly.  I also allowed simplification to expand `toUInt32` before rewriting the checked root address, producing an unwanted modulo form.  I will use the direct capacity theorem, rewrite the address before store simplification, and execute the final `localGet 6` with `finishFrame_return_get`.

The import check accepted those corrections.  The Lean check completed the allocator, empty length store, root transfer, and final `localGet 6`; only the conversion from the shared `UInt64Array.At` fact to the frozen `FormalSpec.UInt64ArrayAt` fact remained.  Broad simplification unfolded both predicates and changed the address presentation.  Their definitions are identical, so I will change the target to `UInt64Array.At`, rewrite `FormalSpec.expected` with the invalid-branch hypothesis, and apply `hEmpty` without reduction.

For the remaining valid branch, I searched the allocation, array, memory, and proof-construction indexes for `input_preserved_by_alloc|write64After|frameBefore` and inspected `leanexe-core/array-memory-framing`.  Its allocator theorem and `UInt64Array.At.write64After` match the two writes preceding the fold.  I will use them before applying the exact fold setup theorem.

The import check accepted the predicate conversion, and the Lean check left only the valid post-allocation continuation.  I will state a compact invariant with `ArrayFold.foldPrefix`, the compiler-generated continuing frame, the fixed post-length-store memory, and the natural traversal index.  I will apply `FixedArrayFold.forwardSetupProgram_spec`, followed by the recipe command `wp_block_loop`.  Progress means discharging invariant initialization and exposing one annotated loop-body transition plus its exit continuation.

The import check accepted the invariant and exact setup composition.  The Lean check reached `wp_block_loop`, then exhausted one million heartbeats while normalizing the initial setup frame at lines 189–190.  The command produced no loop-step diagnostic.  I will not repeat that elaboration boundary unchanged.  I will add a separate checked equality from this artifact's valid allocation frame through `forwardSetupFrame` to the generated continuing frame, then use that lemma at invariant initialization.  A generated setup-frame-to-continuing-frame equality in `AnnotationMatches` would remove this artifact-local normalization boundary.

The import check accepted the extracted initializer lemma.  The next Lean check produced the loop-step goal in 24 seconds, so the smaller boundary removed the public-theorem timeout.  The initializer lemma's `rfl` failed because the allocation, setup, and generated scalar-frame constructors are propositionally equal after concrete list-update normalization rather than definitionally equal.  I will prove that isolated equality with focused simplification.  For the loop exit, I will use `FixedArrayTraversalInput.continuingProgram_exit_spec` and the generated value, index, and stop accessors selected by the annotated-frame entry.  Progress means reaching the singleton result suffix from `Break 1`.

The focused simplifier exposed a semantic error in my frame model: setup stores the input pointer in combined local 16, the logical stop in local 14, and the effective stop in local 15.  Local 16 becomes the loop's done flag after the first body execution, so the invariant must quantify it.  I will correct those local roles.  The exit theorem matched and left the expected `Break 1` continuation; I will reduce that control wrapper to the compiler-matched singleton-result program and apply the generated arbitrary-postcondition adapter.

The import check accepted the corrected local roles.  The initializer lemma now closes.  The exit path reached `function_0_array_fold_0_singleton_result_spec`, which discharged all frame projections and left the public result fact.  The payload-bound proof retained an opaque page-count presentation, so `omega` could not connect it to the capacity bound.  I will state the output-store and fold-store page equalities explicitly, normalize capacity one to 16 once, and use `singletonStore_at` for the final represented array.  This also supplies the exact postcondition needed after the generated suffix adapter.

The import check accepted the page equalities and singleton representation.  The Lean check applied the exact generated singleton suffix and left three issues: the return-get parameter-length premise needs direct normalization rather than arithmetic search; the public `foldl` function is definitionally `sumStep` but the final simplifier retained the private name; and the continuing loop branch remains open.  The exact exit composition is useful, while the generic proposal singleton adapter remains unnecessary.

For the continuing branch I will apply `FixedArrayFoldBody.continuingGuardedProgram_spec`, selected from `leanexe-core/fixed-array-fold-body`, to locals 11, 13, 15, and 2 and scratch local 11.  Its premises match the generated continuing frame accessors, `hInputFold`, the strict index bound, the generated body and condition evaluators, and the generated continuing-step evaluator.  Progress means obtaining the invariant at index `index + 1` with `ArrayFold.foldPrefix_succ` and a strictly smaller natural measure.  No additional frame parameter, local-length, operand, or getter projection will be reduced before using the generated accessor declarations already selected from `annotated-fold-frame-accessors`.

The import check accepted the extracted one-iteration theorem and the repaired exit conversion.  The Lean check confirmed that `continuingGuardedProgram_spec` matches the exact concatenation.  Lean deferred the dependent item-validity and index premises, so positional bullets associated later proofs with the wrong premises; two generated getters also retained local abbreviations for the encoded index and stop.  I will name every frame, input, and index premise explicitly, unfold only those local abbreviations, and retain the generated loaded-frame, body, condition, and continuing evaluators.  The remaining public-loop goal after this helper is exactly the `Break 0` invariant and decreasing-measure obligation.

The import check accepted the named-premise revision.  The Lean check closed the encoded comparison and successor arithmetic and isolated five definitional boundaries.  Three frame getters require the exact generated getter theorems without unfolding `Locals.get`; the loaded-frame target requires changing the state reconstruction back to the generated continuing frame; and the generated scalar evaluators retain `ScalarTransition.U64Op.apply`, whose defining equation must be named.  Broad simplification of the final continued frame exceeded its step limit, so I will prove that frame equality by changing both sides to the same generated frame and rewriting only the prefix-sum and encoded-index equalities.

The import check accepted the focused frame conversions.  The Lean check now proves the complete extracted iteration theorem; a trailing `rfl` follows a rewrite that already closes its equality and must be removed.  The only substantive residual goal is the loop rule's `Break 0` callback.  I will reduce that constructor, use the invariant witnesses at `index + 1`, and compare the natural measures through the generated local-13 getter and `UInt64.toNat_ofNat_of_lt'`.  This residual introduces no new annotation kind or proof pattern, so another knowledge search would duplicate the selected frame-accessor and fold-prefix entries.

The import check accepted the invariant reconstruction.  The Lean check discharged the next-index witnesses and the old-frame measure.  The loop rule restores the next frame's empty operand stack with a record update, so the generated getter does not match syntactically until `Frame.withValues_get` removes that update.  This accessor was returned by the earlier frame-accessor search and applies exactly; after it, the generated local-13 getter and the checked `toNat` equality reduce the remaining inequality to natural arithmetic.

The final import check succeeded, and the final `ArtifactResult` build succeeded.  The accepted proof uses the shared length-dispatch tactic, constant-capacity allocator window, allocation and memory-framing facts, fold setup and body compositions, input-traversal exit theorem, generated annotation accessors and scalar evaluators, prefix-fold equations, loop boundary rule, and fixed-array result declarations.  The artifact-generated singleton suffix replaced the inspected proposal adapter.  No residual goal remains, and the checked candidate was not edited after acceptance.
