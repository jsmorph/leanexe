# Proposed proof knowledge

This package contains one run-derived proposal.  Its catalog entry records the proposed use and its evidence directory preserves the source run and learning journal.  The package remains experimental until an explicit promotion creates a new snapshot.
