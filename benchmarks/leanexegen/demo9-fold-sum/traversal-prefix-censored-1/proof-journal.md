# Proof Journal

## 2026-08-09: Initial check

The untouched candidate passed `PROOF_IMPORT_CHECK.js`.  The prescribed `ArtifactResult` build then failed at the deterministic starter goal.  After `RuntimeReady` was decomposed into globals, memory bounds, the represented input array, and common reads, the remaining goal was `TerminatesWith env module 0 initial [Value.i64 inputPtr]` with a postcondition requiring one result pointer and `FormalSpec.UInt64ArrayAt final outputPtr (FormalSpec.expected input)`.

The diagnostic confirms that the starter has already reduced `ArtifactSpec` to the execution theorem boundary.  The missing proof is a theorem connecting the translated function's execution to the expected conditional fold.  I will inspect the frozen definitions, task guidance, annotations, recipes, and the task-specific LTG indexes before changing `Behavior.lean`.

## LTG retrieval and recipe selection

I began LTG retrieval with `LTG/categories.json`, then searched the array, loop, compiler-motif, proof-construction, allocation, and memory indexes.  The first query used `leanexe.array.fold.v1`, `leanexe.array.length-dispatch.v1`, `foldPrefix`, `FixedArrayLengthDispatch`, `continuingProgram_spec`, and `wp_block_loop`.  The second query used `FixedArrayAllocator`, `singleton`, `resultSuffix`, `FreshFixedArrayAt`, `bumpFacts`, and `UInt64ArrayAt`, while later queries used `RuntimeReady`, `ArtifactSpec`, `artifact_behavior`, `TerminatesWith`, `array literal`, `result suffix`, `sum`, `wrapping`, and `accumulator`.

I inspected the canonical `array-fold-prefix`, `fixed-array-length-dispatch`, `fixed-array-traversal-input`, `fixed-array-allocation`, and `array-memory-framing` entries by reading only each `entry.json` and `README.md`.  The first three match the two checked annotation kinds and provide the fold invariant, bounded entry dispatch, and dynamic indexed loader.  The allocation and framing entries match the visible bump-allocation and later traversal structure, although the frozen annotations do not provide an allocator-region equality, so their exact theorem applicability still requires comparison with `func0`.

I will use `fixed-array-length-dispatch` first because `PROOF_RECIPES.json` gives the exact invocation `wp_fixed_array_length_le_dispatch 7, 8`.  I will use `array-fold-prefix` and `fixed-array-traversal-input` for the valid branch because the fold annotation records accumulator local 1, item local 2, input local 11, index local 13, effective-stop local 15, done local 16, staged value local 17, and result local 10.  I will use `fixed-array-allocation` and `array-memory-framing` only if the generated allocation interval matches their declarations, since the input must remain represented after allocation and before traversal.

The index also returned `fixed-array-singleton-wrapper`, `residual-specification-normalization`, the map and filter whole-function entries, scalar post-test entries, and Demo 4, 5, 7, and 8 worked examples.  I rejected these without opening their canonical files because their annotation kinds require a singleton-input direct-call wrapper, map, filter, or scalar post-test loop, none of which occurs in `PROGRAM_ANNOTATIONS.json`.  The worked-example index contains no Demo 9 entry, consistent with the task-specific exclusion of worked examples for this artifact.

## Direct recipe attempt

The frozen `func0` begins with the exact eight-instruction unsigned length dispatch recorded by the recipe.  Its valid branch allocates a one-element result, stores length one, traverses the input with a loop-carried sum in local 1, stores that sum, and returns the result pointer.  Its invalid branch allocates an empty result, stores length zero, and returns that pointer.

I will now apply `Wasm.TerminatesWith.of_wp_entry_for (f := func0Def) rfl` followed by the exact direct recipe `wp_fixed_array_length_le_dispatch 7, 8`.  This attempt should expose only the valid and invalid branch weakest-precondition obligations plus definitional frame premises.  I will preserve the annotated fold interval until the valid-branch loop proof requires its checked decomposition.

The import check passed, and the build showed that the exact recipe recognized the program prefix and produced the intended valid and invalid branch goals.  Lean left the represented pointer and array as metavariables, which also left five routine frame and numeric premises separate from the two branches.  This is an instruction-adapter gap covered by the LTG entry's `_from` form, so I will replace the invocation with `wp_fixed_array_length_le_dispatch_from hArray at 7, 8` before working on either branch.

The `_from` invocation fixed the pointer and input metavariables and retained the same exact branch decomposition.  The remaining non-branch goals state the entry frame's parameter and empty-value equalities, local 7 validity, and `8 < UInt64.size`; each reduces from `func0Def`.  I will discharge these five definitional premises explicitly, introduce the two size hypotheses, and use the next diagnostic to set the branch proof boundary.

The first two frame equalities and the closed numeric premises reduced, but `decide` did not unfold the generated local-frame length because its target retained the free `inputPtr`.  I will normalize that one premise with the generated function and `Function.toLocals` definitions.  The valid and invalid branches now stand alone, so I will also name the common emitted capacity prefix and test an exact `change` from the valid branch to `resultCapacityPrefix 1 ++ FixedArrayAllocatorWindow.region 2 1 ++ rest`.

The exact valid-branch decomposition succeeded, confirming offset 2, stride 1, and four unused trailing locals for `FixedArrayAllocatorWindow.region_spec_withTail`.  `wp_run` did not cross the still-associated concrete list append, so the diagnostic remained at the named capacity prefix rather than worsening the proof boundary.  I will normalize only `List.cons_append` and `List.nil_append` before rerunning the straight-line tactic, leaving the allocator definition folded.

After list normalization, generic `wp_run` stopped at local 11 because it retained the generated entry-frame definitions and therefore could not reduce the local set and get.  The residual also preserved `FixedArrayAllocatorWindow.region 2 1` as intended.  The length-dispatch entry recommends `wp_length_dispatch` with `func0Def`, `branchFrame`, `Function.toLocals`, `Function.numParams`, and `ValueType.zero` for this exact frame-normalization boundary, so I will use that focused tactic on the capacity prefix.

The focused tactic reduced the valid capacity to 16, normalized the complete generated frame, and stopped at the capacity minimum check with a zero condition.  The residual program starts with the exact allocator window and retains the result suffix.  I will select the false branch of that check and apply `FixedArrayAllocatorWindow.region_spec_withTail 2 4` with capacity 16 and stride 1, which is the checked theorem selected from the `fixed-array-allocation` entry.

The allocator theorem did not apply immediately because `wp_iff_cons` left the selected empty else program as `wp module []` with the allocator in its fallthrough continuation.  This residual is a control normalization step rather than an allocator mismatch.  I will reduce `wp_nil` and the zero-width block stack transfer, then retry the same exact allocator theorem without unfolding its instructions.

After the empty branch reduction, `region_spec_withTail` matched and replaced the complete allocator with its checked allocation store and frame.  Its premises are the expected concrete frame facts, capacity lower bound, existing-memory fit, page and module bounds, allocator globals, and the post-allocation suffix.  I will discharge the concrete and global premises directly, derive the 64-byte valid allocation bound from `heapReserveBytes` under `hSize`, and keep the suffix as the next semantic boundary.

All allocator premises now close, leaving the valid suffix in the checked allocation store and frame.  Before reducing the result-length store and fold initialization, I will derive `Allocation.bumpFacts`, preserve the input representation across the allocator with `FixedArrayPairResult.input_preserved_by_alloc`, and preserve it across the result-length store with `UInt64Array.At.write64After`.  These facts provide the exact root bound, represented length read, and input address equations needed by the focused `wp_alloc_window` simplifier.

The first suffix attempt found two presentation gaps.  `wordAddress_toNat 0` retained `+ UInt64.ofNat (8 * 0)`, while the goals used the reduced root address, and the focused simplifier retained concrete list head lookups as matches.  I will add the reduced root-address equality with `simpa` and use the documented `wp_alloc_window_lists` variant, which adds only concrete head and successor lookup normalization.

The list-aware tactic now reduces the suffix to three explicit memory-bound choices followed by the fold's effective-stop conditional.  Its store uses the generated `UInt32.ofNat ((heapTop.toNat + 48) % 2^32)` presentation, while the preserved-input fact uses `(heapTop + 48).toUInt32`.  I will add the directed root-address equality and the modulo root bound, select the three no-trap branches, and rewrite the two represented length loads before defining the loop invariant.

Those directed rewrites close all allocation, store, and input-load presentation gaps.  The valid proof now reaches the annotated fold region with both length locals equal to `UInt64.ofNat input.size`, accumulator and index zero, and a false effective-stop comparison.  I will select the branch that copies the represented size into local 15 and normalize its one-result stack transfer, stopping at the block-wrapped loop.

The normalization now stops exactly at the block-wrapped fold loop.  I will use `ArrayFold.foldPrefix input (fun sum element => sum + element) 0 index` as the accumulator coordinate, carry exact getters for the root, input pointer, index, stop, and accumulator, and use `input.size - encodedIndex.toNat` as the measure.  The fixed store is the checked allocation store after the length-one write, and the preserved input representation remains available outside the invariant because the invariant fixes that store.

The block and loop rules accepted this invariant and measure, producing only initialization and step obligations.  A separate arithmetic proof of `heapTop.toUInt32 + 48 = (heapTop + 48).toUInt32` used an over-general modulo rewrite and left metavariables that `omega` could not constrain.  The simplifier already uses the direct `toUInt32` addition theorem at this point, so I will replace that proof with `simp`, close invariant initialization at index zero, and expose the continuing and completed step cases.

The prescribed import check passed after that edit.  The build accepted the direct root-address simplification and the index-zero invariant, then failed at the deliberate holes in the continuing, completed, and oversized-input branches.  The continuing goal begins with the exact sixteen-instruction region certified by `function_0_array_fold_0_continuing_eq`, so I will apply `FixedArrayTraversalInput.continuingProgram_spec` before reducing the remaining accumulator and index updates.

I confirmed the selected theorem in `Project.ProofKit.FixedArrayTraversalInput`, as permitted by the inspected LTG entry.  Its premises match local 11 as the input pointer, local 13 as the encoded natural index, local 15 as the represented input size, and local 2 as the loaded item.  The theorem leaves the dynamic result frame at the unannotated arithmetic suffix, where `wp_traversal_input` and `ArrayFold.foldPrefix_succ` should establish the next invariant and strict measure decrease.

The import check passed, and the build accepted every premise of `continuingProgram_spec`.  The focused simplifier then exhausted the candidate's one-million-heartbeat setting while normalizing the arithmetic suffix and the loop rule's dependent postcondition, before producing a residual goal.  The checked traversal module assigns the same simplifier a one-million-heartbeat local allowance for a smaller postcondition, while the generated annotation module uses eight million for artifact-scale normalization, so I will adopt that existing artifact-support limit and retry the exact composition unchanged.

The import check passed with the artifact-scale limit.  The build then reduced the continuing branch to two operand-stack matches containing `frame.locals[0]` and `frame.locals[12]`; these are the accumulator and encoded index hidden behind `Locals.get`, while the completed and oversized branches remain open.  This diagnostic confirms the semantic composition and identifies local-frame presentation, fold-successor equality, and measure normalization as the remaining continuing obligations.

I searched the loops, arrays, proof-construction, compiler-motifs, and memory indexes with `frame.locals`, `getElem`, `foldPrefix_succ`, `loop measure`, `measure decrease`, `wp_traversal_input`, `local getter`, and `Locals.get`.  The only result was the already-inspected canonical `array-fold-prefix` entry, which I will continue to use for `foldPrefix_succ`; no catalog entry supplies a separate local-getter adapter.  I rejected no newly returned entry, and I will expose the accumulator and index as direct list getter equalities derived from the invariant before rerunning the focused traversal tactic.

The import check passed, and the build no longer left either operand-stack match, which confirms that the direct getters close the local-frame presentation gap.  Simplification then reached maximum recursion depth because I supplied `hIndexSucc` from encoded addition to `ofNat (index + 1)` while the library's simplifier expands `UInt64.ofNat_add` in the opposite direction.  I will remove that cyclic rewrite and let the standard expansion normalize the next invariant's encoded index; the completed and oversized branches are unchanged.

The next import check passed, but the focused simplifier again reached maximum recursion depth before exposing a residual.  The remaining opposing rule is `hRootAddressAdd`: the standard UInt64 simplifier expands `(heapTop + 48).toUInt32` to `heapTop.toUInt32 + 48`, while that supplied equality rewrites the expansion back to the original term.  I will remove this second cyclic rule; the fixed-store equality will then use the library's canonical expanded address on both sides.

The import check passed after removing the address rewrite.  The build completed symbolic execution of the continuing body and left a compact conjunction: choose the next invariant index, identify the accumulator with `foldPrefix_succ`, preserve three untouched locals, identify encoded successor addition, and prove the natural measure decreases.  I will discharge this residual directly with index `index + 1`, list getter equalities extracted from the existing option facts, the selected LTG fold theorem, `UInt64.ofNat_add`, and `omega` after removing the non-wrapping modulus.

The import check passed, and the build accepted the complete continuing transition, including the next-prefix witness and strict measure decrease.  I searched the loop, array, allocation, memory, and proof-construction indexes with `foldPrefix_size`, `completed prefix`, `singleton`, `payload store`, `result suffix`, `branchPost`, `UInt64Array.singleton`, `wordAddress`, and `read64_write64`; the results were the already-inspected `array-fold-prefix` entry and singleton-wrapper entries whose direct-call annotation does not match this artifact.  I will use `foldPrefix_size`, the already-inspected allocation and memory-framing guidance, `UInt64Array.singleton`, and disjoint-word reads; I again reject the singleton-wrapper and worked-example entries because their whole-program shapes and annotations do not apply.

The completed branch fixes the invariant index at `input.size`, so the accumulator is the required `Array.foldl`.  Allocation facts give the payload address at `heapTop + 56`, and the frozen result bounds give the sixteen bytes needed for the length and payload words.  I will construct the singleton representation after the payload write, then let the focused traversal simplifier execute the completed guard, result suffix, outer `localGet 6`, and public postcondition against that representation.
