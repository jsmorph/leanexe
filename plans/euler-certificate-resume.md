# Euler conservation-certificate resume record

Work paused at the user's request on 16 September 2026.  The sequential proof batch exited after export decoding failed.  The batch has no remaining scheduled jobs.  This checkpoint preserves the accepted proofs, unfinished proof modules, frozen binary, failure output, and preparation files.

The [certificate, completion, and convergence plan](euler-certificates-and-convergence.md) remains the authorized work order.  The next task is export decoding for the conservation observer.  Certificate production runs follow complete exact-byte verification and independent package checking.

## Checked results

The earlier reconstructed solver and its [192-grid and 800-grid data](../data/euler-reconstructed-v1/README.md) are complete.  Its exact-byte mathematical results cover accepted-state positivity, directional hyperbolicity, characteristic-speed bounds, CFL inequalities, reconstruction properties, and conservation with rounding residuals.  Its published data remain the numerical reference for the new observer.

The observer has complete source and generated-WASM proofs.  Its [complete specification](../proofs/talos/lean/Project/EulerCertificate/CompleteSpec.lean) proves terminating execution, exact output, and a 512 MiB linear-memory bound for runtime grid sizes 2 through 800 and every UInt64 reconstruction-trial count.  The output preserves the previous solver's words and appends twelve words: status, lower endpoint, and upper endpoint for mass, x-momentum, y-momentum, and energy.

The enclosure theorem connects each status-zero certificate to the accumulated physical conservation residual along the same accepted numerical trace.  For component i, the residual is the final exact decoded cell total minus the initial total and accepted physical boundary contributions.  The certificate bounds the absolute net residual by the maximum of the negative lower endpoint and the upper endpoint.  Its status premise remains explicit.  Guaranteed successful completion and continuum convergence remain later mathematical tasks.

The focused source-artifact command `tools/talos-proof.js check euler_certificate` passed before this checkpoint.  The source registry contains 48 completed cases.  The existing exact-artifact registry contains 42 entries.

| Exact-byte boundary | Checkpoint result |
|---------------------|-------------------|
| Embedded bytes, balanced lookup, and raw-cache data definition | Passed. |
| Function bodies 0–194 | All 195 passed. |
| Function signatures 0–194 | All 195 passed. |
| Function-type indices 0–194 | All 195 passed. |
| Type, function, memory, and global sections | Complete item and section proofs passed. |
| Export entries 0–10 | All eleven failed at the recursion-depth limit. |
| Export-section composition and code vector | Prepared, untested. |
| Whole-file decoding, validation, and translation | Prepared, untested. |
| Host reset and independent package checking | Pending. |

The [proof-status record](euler-certificate-resume/proof-status.json) binds each artifact proof source to its SHA-256 and records the observed result.  It contains 122 passed modules, one failed module, and fourteen untested modules.  HostInitial is an additional untested module.  All recorded successful artifact audits use propext or no axioms.  Complete execution and enclosure audits use only propext, Classical.choice, and Quot.sound.  The status record summarizes focused checks.  The independent package gate remains pending.

Earlier pushed checkpoints are `d1b84d7d` for complete output and entry proofs, `70fa944f` for byte identity and bodies 0–63, `2bb30026` for bodies 64–159, and `6aa9838b` for the remaining function bodies.

## Frozen subject and unfinished files

The observer binary has 45,644 bytes and SHA-256:

```text
22696951ce81106990843e19494430292bdde35183058a34ff30133cc47bd981
```

The [frozen package](../proofs/artifacts/euler_certificate/22696951ce81106990843e19494430292bdde35183058a34ff30133cc47bd981/manifest.json) contains the binary and prepared manifest.  The package is pending registration.  The [registration changes](euler-certificate-resume/registration.diff) preserve the exact-artifact registry entry and embedded-byte checker case outside the active registry until the proof targets pass.  This keeps the current registered inventory aligned with completed packages.

The proof workspace pins Lean 4.34.0-rc2 at `6a10ac8c22beadecabdbb0919c2b50214762f91d` and Talos at `87e3aa5e8f6e6f3b3eb5e7e4c5aba43071002d47`.  Program.lean and AnnotationMatches.lean are generated caches.  The current numerical source and generated bytes have remained unchanged during exact-byte development.

All unfinished modules remain at their intended paths under `proofs/talos/lean/Project/EulerCertificate`.  The failed export module and its dependent drafts are outside the completed source specification's import closure.  Their presence in this checkpoint records unfinished work.

The [preparation directory](euler-certificate-resume/) preserves section and nested-instruction offsets, the draft generator, its original target list, measured target times, a failed index-lookup draft, and an unexecuted profiling diagnostic.  The draft generator still refers to preparation files under tmp and predates the checked eight-chunk byte layout and index-field lemma.  The checked proof sources are the current implementation.  Running the generator over them would require review and updates first.

## First issue on resume

The final command was:

```sh
tools/leanrun --timeout 3m --lock-timeout 30 lake -q -d proofs/talos/lean build Project.EulerCertificate.ArtifactExportSectionItems
```

It returned status 1 after 148.632 seconds.  Each export theorem reached `maxRecDepth 131072`, at lines 17, 23, 29, 35, 41, 47, 53, 59, 65, 71, and 77.  The [failure output](euler-certificate-resume/export-decoding-failure.log) preserves the diagnostics.  Its sorryAx reports arise from failed elaboration.  No export theorem was accepted.

The next diagnostic should isolate export0_decoded, then its name and byte-slice evaluation.  The [name parser](../proofs/talos/lean/Project/Artifact/Binary/Primitives.lean) calls readBytes, which uses ByteArray.extract.  Slice extraction may expand the full byte array despite the existing balanced single-byte lookup.  That explanation remains a hypothesis.  The older accepted comparison is [the reconstructed solver's export proof](../proofs/talos/lean/Project/EulerReconstructed/ArtifactExportItems.lean), whose artifact has 30,726 bytes.  A checked slice equality or a reusable parser-composition lemma may reduce the failing evaluation boundary.  Diagnose that boundary before another export-module run.

Index decoding supplied a separate timing experiment.  Its first four sixteen-item groups took 87–94 seconds each.  [The index-field equality](../proofs/talos/lean/Project/EulerCertificate/ArtifactIndexLookup.lean) rewrites the raw field to a range mapped through UInt32.ofNat.  Its first draft incorrectly used List Nat.  The corrected lemma passed, but subsequent groups still took 84–90 seconds.  These measurements establish no speed improvement.  [The prepared profile](euler-certificate-resume/IndexProfile.lean) compares the same first index with and without that rewrite.  It has not run.

Every Lean and Lake invocation must use tools/leanrun.  Preserve the standard one-job lock, one thread, 4 GiB MemoryHigh, 6 GiB MemoryMax, 1 GiB swap maximum, and 100% CPU quota.  No local-mode opt-out or remote job is part of this checkpoint.

## Remaining proof and production sequence

After correcting export decoding, check the remaining modules in this dependency order.  Each name has the prefix Project.EulerCertificate:

```text
ArtifactExportSectionItems
ArtifactExportSection
ArtifactCodeVector
ArtifactMetadata
ArtifactParsedStates
ArtifactParsedCode
ArtifactParsedSections
ArtifactParsed
ArtifactDecoded
ArtifactRawCache
ArtifactDecode
ArtifactValidationExports
ArtifactValidationMetadata
ArtifactValidation
ArtifactTranslation
HostInitial
```

Use a separate bounded target for each diagnostic, for example:

```sh
tools/leanrun --timeout 3m --lock-timeout 30 lake -q -d proofs/talos/lean build Project.EulerCertificate.ArtifactTranslation
```

Review each successful target's trace and public axiom audit.  A timeout or elaboration failure requires an identified smaller boundary before another attempt.  The intended public artifact results are artifact_solve_exact and artifact_solve_enclosure.  HostInitial proves the reset call from module initialization.

After these targets pass, restore the saved registration changes and run the independent package gate:

```sh
git apply --unidiff-zero plans/euler-certificate-resume/registration.diff
tools/artifact-proof.js check proofs/artifacts/euler_certificate/22696951ce81106990843e19494430292bdde35183058a34ff30133cc47bd981/program.wasm Project.EulerCertificate.ArtifactTranslation
```

The final audits must use only the standard logical axioms.  The generic artifact tool permits additional decision axioms, so its acceptance also requires inspection against this task's stricter requirement.  Update the registered package count to 43 after the gate passes.  The older aggregate release checks remain deferred as documented in the development status.

The [run driver](../tools/euler-riemann-complete.js) already supports run-certificate, and its output tests passed.  It preserves raw words, requires solver success at the exact time word for 0.8, checks finite positive fields, and requires each certificate's status and endpoints to pass.  Certificate simulations and figures are pending.

After the proof gate, use fresh directories for these sequential runs:

```sh
node tools/euler-riemann-complete.js run-certificate 192 8 new-certificate-192
tools/leanrun --timeout 2m build/tools/riemann-figures-venv/bin/python tools/euler-riemann-plot.py new-certificate-192
node tools/euler-riemann-complete.js run-certificate 800 8 new-certificate-800
tools/leanrun --timeout 2m build/tools/riemann-figures-venv/bin/python tools/euler-riemann-plot.py new-certificate-800
```

Each simulation makes one solve call in one local Wasmtime process under the runner.  Compare the output prefix against the preserved reconstructed data, inspect each plot, and publish endpoint words, absolute bounds, approximate widths, runtimes, and proof references.  The earlier reconstructed runs took 176.7 seconds and 3 hours 58 minutes.  Observer runtimes remain unmeasured.

Successful-completion research follows publication of the certificate results.  Continuum-convergence research follows that investigation.  Changes to the numerical method or mathematical target require the user's agreement.

## Transformer review

The requested agent review is complete and recorded in the [development notes](../devnotes.md#2026-09-16-transformer-review).  It examined the published rational tensor model without building it.  The proposed first experiment is a scalar binary64 state scan whose tiled and ordinary forms preserve multiplication and addition order.  Required new proofs cover array representation, accumulated error, and exact-byte execution.  Attention and tensor-parallel identities need separate floating-point claims because they rearrange arithmetic.  Implementation awaits discussion.
