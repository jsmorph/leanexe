#!/bin/sh
# Compatibility entry: WGSL subcommands retain their meaning; plain flags use Wasm.
set -eu
root=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)
case "${1:-}" in
 build|run|serve|test) exec "$root/tools/gpt2-wgsl" "$@" ;;
 *) exec "$root/tools/gpt2-wasm" "$@" ;;
esac
